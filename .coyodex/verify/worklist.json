{
 "findings": [
  {
   "check": "recorded-exceptions",
   "severity": "WARNING",
   "location": "'Audit exceptions' extras heading",
   "where": "'Audit exceptions' extras heading",
   "message": "1 advisory/advisories suppressed by recorded exception(s): read-never-created HP4. Each was judged acceptable by an operator and is NOT re-reported above; re-read them by validating a copy with the line removed. A recorded line silences exactly one (check, id) pair — never a whole family."
  }
 ],
 "worklist": [
  {
   "claim": "Role 'Map owner' may do everything 'Map reader' may do",
   "anchor": null,
   "detail": "R1 includes R2 — the map anchors this to NO line, so nothing shows where it is granted",
   "why_risky": "a privilege claim — it says one role may do everything another may do, which a reader takes as a fact about who can reach what.",
   "theme": "security",
   "drift_eligible": false
  },
  {
   "claim": "Rule 'A served map is reachable only from the machine that runs the server.' is enforced at tools/coyodex/viewer/serve.py:885 — Opens the listening address on this machine alone, so a connection from another machine never arrives.",
   "anchor": "tools/coyodex/viewer/serve.py:885",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A served map is reachable only from the machine that runs the server.' is enforced at tools/coyodex/viewer/serve.py:634 — Refuses a page request whose address names any host other than this machine.",
   "anchor": "tools/coyodex/viewer/serve.py:634",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A served map is reachable only from the machine that runs the server.' is enforced at tools/coyodex/viewer/serve.py:655 — Refuses the same way for a request that changes the remembered project list.",
   "anchor": "tools/coyodex/viewer/serve.py:655",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project folder is served only when it was deliberately added, never because the server found it.' is enforced at tools/coyodex/viewer/serve.py:883 — Builds the served set from the remembered folder list alone, with no search of the disk.",
   "anchor": "tools/coyodex/viewer/serve.py:883",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project folder is served only when it was deliberately added, never because the server found it.' is enforced at tools/coyodex/viewer/serve.py:697 — Refuses to add a folder that holds no coyodex data folder inside it.",
   "anchor": "tools/coyodex/viewer/serve.py:697",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project folder is served only when it was deliberately added, never because the server found it.' is enforced at tools/coyodex/viewer/serve.py:878 — Applies the same check to a folder named when the server is started.",
   "anchor": "tools/coyodex/viewer/serve.py:878",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project folder is served only when it was deliberately added, never because the server found it.' is enforced at tools/coyodex/viewer/serve.py:658 — Refuses a change to the remembered list unless the request came from this server's own start page.",
   "anchor": "tools/coyodex/viewer/serve.py:658",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A request for a file can only ever reach inside the mapped project's own folder.' is enforced at tools/coyodex/viewer/serve.py:778 — Rejects a file request whose path is absolute or climbs above the project folder.",
   "anchor": "tools/coyodex/viewer/serve.py:778",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A request for a file can only ever reach inside the mapped project's own folder.' is enforced at tools/coyodex/viewer/serve.py:380 — Rejects a working copy read that escapes the project or names the version history folder.",
   "anchor": "tools/coyodex/viewer/serve.py:380",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A request for a file can only ever reach inside the mapped project's own folder.' is enforced at tools/coyodex/viewer/serve.py:387 — Refuses a file whose real location, after following a shortcut, sits outside the project folder.",
   "anchor": "tools/coyodex/viewer/serve.py:387",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A request for a file can only ever reach inside the mapped project's own folder.' is enforced at tools/coyodex/viewer/serve.py:849 — Hands back a shared page asset only when the requested name is one of the two allowed names.",
   "anchor": "tools/coyodex/viewer/serve.py:849",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A file that version history deliberately ignores is never handed back to the browser.' is enforced at tools/coyodex/viewer/serve.py:391 — Allows a working copy read when version history already tracks the file.",
   "anchor": "tools/coyodex/viewer/serve.py:391",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A file that version history deliberately ignores is never handed back to the browser.' is enforced at tools/coyodex/viewer/serve.py:393 — Allows an untracked file only when no ignore rule covers it.",
   "anchor": "tools/coyodex/viewer/serve.py:393",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A version name arriving in a web address is refused unless it reads as an ordinary commit name.' is enforced at tools/coyodex/viewer/serve.py:790 — Refuses a requested version that is not a plain commit identifier.",
   "anchor": "tools/coyodex/viewer/serve.py:790",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A version name arriving in a web address is refused unless it reads as an ordinary commit name.' is enforced at tools/coyodex/viewer/serve.py:366 — Refuses a branch or tag name that opens with a dash or carries unusual characters.",
   "anchor": "tools/coyodex/viewer/serve.py:366",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A version name arriving in a web address is refused unless it reads as an ordinary commit name.' is enforced at tools/coyodex/viewer/serve.py:368 — Marks where the options end, so the name after it can never be read as one.",
   "anchor": "tools/coyodex/viewer/serve.py:368",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A version name arriving in a web address is refused unless it reads as an ordinary commit name.' is enforced at tools/coyodex/viewer/serve.py:301 — Skips the history read entirely when the map's own recorded commit is not plain.",
   "anchor": "tools/coyodex/viewer/serve.py:301",
   "detail": "In: Map server (C83)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Code fetched from the public internet runs in the viewer only when its fingerprint matches the expected one.' is enforced at tools/coyodex/viewer/viewer.html:17 — Pins the expected fingerprint of the picture panning library.",
   "anchor": "tools/coyodex/viewer/viewer.html:17",
   "detail": "In: Viewer shell (C70)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Code fetched from the public internet runs in the viewer only when its fingerprint matches the expected one.' is enforced at tools/coyodex/viewer/viewer.html:20 — Pins the expected fingerprint of the diagram drawing library.",
   "anchor": "tools/coyodex/viewer/viewer.html:20",
   "detail": "In: Viewer shell (C70)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Code fetched from the public internet runs in the viewer only when its fingerprint matches the expected one.' is enforced at tools/coyodex/viewer/viewer.js:14094 — Pins the expected fingerprint of the styling that colours source code.",
   "anchor": "tools/coyodex/viewer/viewer.js:14094",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Code fetched from the public internet runs in the viewer only when its fingerprint matches the expected one.' is enforced at tools/coyodex/viewer/viewer.js:14097 — Pins the expected fingerprint of the code colouring library itself.",
   "anchor": "tools/coyodex/viewer/viewer.js:14097",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A source link may open only through an address belonging to a known code editor.' is enforced at tools/coyodex/viewer/viewer.js:14880 — Offers no link when the built address is not one a known editor answers.",
   "anchor": "tools/coyodex/viewer/viewer.js:14880",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A source link may open only through an address belonging to a known code editor.' is enforced at tools/coyodex/viewer/viewer.js:15011 — Refuses to save a hand-typed link pattern whose address is not a known editor one.",
   "anchor": "tools/coyodex/viewer/viewer.js:15011",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A run refuses to read or overwrite coyodex's own map unless the operator declares coyodex is the project being mapped.' is enforced at tools/coyodex/model.py:1443 — Refuses to read a map stored inside coyodex's own copy unless the run declared that coyodex is the project.",
   "anchor": "tools/coyodex/model.py:1443",
   "detail": "In: Map model (C1)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A run refuses to read or overwrite coyodex's own map unless the operator declares coyodex is the project being mapped.' is enforced at tools/coyodex/model.py:1408 — Refuses the same target for a write, so a stray build cannot overwrite the accepted map of coyodex.",
   "anchor": "tools/coyodex/model.py:1408",
   "detail": "In: Map model (C1)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A run refuses to read or overwrite coyodex's own map unless the operator declares coyodex is the project being mapped.' is enforced at method.md:1369 — Tells the build the refusal exists, and warns that a hand written script is not covered by the refusal.",
   "anchor": "method.md:1369",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link claiming an action must name the acting line, never a definition, an import or a comment.' is enforced at tools/coyodex/anchors.py:131 — Refuses every line shape that can never act.",
   "anchor": "tools/coyodex/anchors.py:131",
   "detail": "In: Shared map grammar (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link claiming an action must name the acting line, never a definition, an import or a comment.' is enforced at tools/coyodex/validate_model.py:5957 — Reports each stored link sitting on a line that cannot act.",
   "anchor": "tools/coyodex/validate_model.py:5957",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link claiming an action must name the acting line, never a definition, an import or a comment.' is enforced at method.md:1314 — Tells the map builder to link the write, call or enforce line itself.",
   "anchor": "method.md:1314",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A decision's evidence is exactly one numbered line, or a note saying no single line enforces the decision.' is enforced at tools/coyodex/validate_model.py:1686 — Refuses evidence that names no line and does not declare the absence.",
   "anchor": "tools/coyodex/validate_model.py:1686",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A decision's evidence is exactly one numbered line, or a note saying no single line enforces the decision.' is enforced at tools/coyodex/validate_model.py:1689 — Refuses evidence that names a line and also declares there is none.",
   "anchor": "tools/coyodex/validate_model.py:1689",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A decision's evidence is exactly one numbered line, or a note saying no single line enforces the decision.' is enforced at tools/coyodex/validate_model.py:1692 — Refuses evidence naming only a file, with no line number.",
   "anchor": "tools/coyodex/validate_model.py:1692",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A decision's evidence is exactly one numbered line, or a note saying no single line enforces the decision.' is enforced at tools/coyodex/validate_model.py:5579 — Repeats the same refusal when the whole map is checked, not one piece.",
   "anchor": "tools/coyodex/validate_model.py:5579",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A decision's evidence is exactly one numbered line, or a note saying no single line enforces the decision.' is enforced at method/model.md:570 — States that a decision's link is the one link whose line number is required.",
   "anchor": "method/model.md:570",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Only a link claiming an action fires there is checked against the line. A link showing where a thing is defined is not.' is enforced at tools/coyodex/validate_model.py:5907 — Skips a relation the definition heading itself declares, like one type extending another.",
   "anchor": "tools/coyodex/validate_model.py:5907",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Only a link claiming an action fires there is checked against the line. A link showing where a thing is defined is not.' is enforced at tools/coyodex/validate_model.py:5924 — Includes a decision's own line, the strongest claim in the map that a line acts.",
   "anchor": "tools/coyodex/validate_model.py:5924",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Only a link claiming an action fires there is checked against the line. A link showing where a thing is defined is not.' is enforced at tools/coyodex/validate_model.py:5945 — Skips a link into a written document, where no line acts.",
   "anchor": "tools/coyodex/validate_model.py:5945",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link on the wrong line is only a warning, because the claim it carries can still be true.' is enforced at tools/coyodex/validate_model.py:6269 — Files each wrongly placed link under warnings, never under failures.",
   "anchor": "tools/coyodex/validate_model.py:6269",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link on the wrong line is only a warning, because the claim it carries can still be true.' is enforced at tools/coyodex/lint_fragment.py:626 — Shows the same finding to the map builder as advice, so the check still passes.",
   "anchor": "tools/coyodex/lint_fragment.py:626",
   "detail": "In: Fragment self-check (C5)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A stored link is judged out of date only when most reviewers agreed the claim it carries is true.' is enforced at tools/coyodex/anchor_drift.py:90 — Skips a claim whose votes are tied or missing.",
   "anchor": "tools/coyodex/anchor_drift.py:90",
   "detail": "In: Map checks (C21)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A stored link is judged out of date only when most reviewers agreed the claim it carries is true.' is enforced at method.md:2383 — States that two reviewers cannot form a majority.",
   "anchor": "method.md:2383",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim whose code link deliberately points at a declaration is never moved onto the line a reviewer read.' is enforced at tools/coyodex/anchor_drift.py:86 — Skips the comparison for a claim marked as report only.",
   "anchor": "tools/coyodex/anchor_drift.py:86",
   "detail": "In: Map checks (C21)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim whose code link deliberately points at a declaration is never moved onto the line a reviewer read.' is enforced at tools/coyodex/audit_model.py:1213 — Marks a claim about where records are kept as report only.",
   "anchor": "tools/coyodex/audit_model.py:1213",
   "detail": "In: Map checks (C21)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim whose code link deliberately points at a declaration is never moved onto the line a reviewer read.' is enforced at tools/coyodex/audit_model.py:472 — Refuses to write a correction for a kind of claim that has no movable link.",
   "anchor": "tools/coyodex/audit_model.py:472",
   "detail": "In: Map checks (C21)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A corrected code link is refused when it lands in a file that neither end of the relation claims.' is enforced at tools/coyodex/audit_model.py:431 — Refuses the correction and names the two parts whose files were checked.",
   "anchor": "tools/coyodex/audit_model.py:431",
   "detail": "In: Map checks (C21)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A corrected code link is refused when it lands in a file that neither end of the relation claims.' is enforced at tools/coyodex/fix.py:251 — Runs the refusal before any correction is written into the map.",
   "anchor": "tools/coyodex/fix.py:251",
   "detail": "In: Map editor (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A corrected code link is refused when it lands in a file that neither end of the relation claims.' is enforced at tools/coyodex/reconcile.py:678 — Runs the same refusal when a recorded correction is replayed later.",
   "anchor": "tools/coyodex/reconcile.py:678",
   "detail": "In: Assignment rules (C47)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A person may excuse one code link a check called out. The note must quote the whole claim, then give the reason.' is enforced at tools/coyodex/anchor_drift.py:195 — Accepts a written excuse only in the form that quotes a claim and gives a reason.",
   "anchor": "tools/coyodex/anchor_drift.py:195",
   "detail": "In: Map checks (C21)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A person may excuse one code link a check called out. The note must quote the whole claim, then give the reason.' is enforced at tools/coyodex/anchor_drift.py:229 — Drops exactly the one finding whose claim the note quotes.",
   "anchor": "tools/coyodex/anchor_drift.py:229",
   "detail": "In: Map checks (C21)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Two maps are scored against each other only when both describe the same code. A run over changed code refuses.' is enforced at eval/method.md:176 — orders the run to stop as soon as any same-code check fails",
   "anchor": "eval/method.md:176",
   "detail": "In: Map quality eval instructions (C135)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Two maps are scored against each other only when both describe the same code. A run over changed code refuses.' is enforced at eval/method.md:48 — states that a score across two different states of the code is not a verdict on the instructions",
   "anchor": "eval/method.md:48",
   "detail": "In: Map quality eval instructions (C135)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Two maps are scored against each other only when both describe the same code. A run over changed code refuses.' is enforced at eval/tools/coyodex_eval/arrows.py:258 — blocks the dropped-relation check when source files moved between the two maps",
   "anchor": "eval/tools/coyodex_eval/arrows.py:258",
   "detail": "In: Lost arrows (C105)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map built over uncommitted code can never be scored, and nobody may waive that refusal.' is enforced at eval/method.md:90 — orders an outright refusal when a map's recorded commit says the code was uncommitted",
   "anchor": "eval/method.md:90",
   "detail": "In: Map quality eval instructions (C135)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map built over uncommitted code can never be scored, and nobody may waive that refusal.' is enforced at eval/method.md:97 — says this one refusal cannot be overridden, unlike every other same-code check",
   "anchor": "eval/method.md:97",
   "detail": "In: Map quality eval instructions (C135)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Two quality scores compare only when one judging setup produced both, so a changed setup forces a fresh judging.' is enforced at eval/tools/coyodex_eval/compare.py:485 — flags the comparison when the two sides were judged under different setups",
   "anchor": "eval/tools/coyodex_eval/compare.py:485",
   "detail": "In: Baseline comparison (C102)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Two quality scores compare only when one judging setup produced both, so a changed setup forces a fresh judging.' is enforced at eval/tools/coyodex_eval/run.py:455 — tells the caller to judge again when the stored scores came from another setup",
   "anchor": "eval/tools/coyodex_eval/run.py:455",
   "detail": "In: Eval run (C104)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Two quality scores compare only when one judging setup produced both, so a changed setup forces a fresh judging.' is enforced at eval/tools/coyodex_eval/run.py:451 — treats stored scores that record no setup at all as unusable",
   "anchor": "eval/tools/coyodex_eval/run.py:451",
   "detail": "In: Eval run (C104)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Two quality scores compare only when one judging setup produced both, so a changed setup forces a fresh judging.' is enforced at eval/method.md:283 — pins every doubter and every scorer to one named model",
   "anchor": "eval/method.md:283",
   "detail": "In: Map quality eval instructions (C135)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map that grew is never marked down for growing, because only lost content breaches an allowance.' is enforced at eval/tools/coyodex_eval/compare.py:201 — lets a count rise freely and breaches only on a fall past the allowance",
   "anchor": "eval/tools/coyodex_eval/compare.py:201",
   "detail": "In: Baseline comparison (C102)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map that grew is never marked down for growing, because only lost content breaches an allowance.' is enforced at eval/tools/coyodex_eval/compare.py:224 — scores a quality measure on its drop alone, never on its rise",
   "anchor": "eval/tools/coyodex_eval/compare.py:224",
   "detail": "In: Baseline comparison (C102)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Losing a check the accepted map passed blocks the run. A number that moved too far only asks for a human look.' is enforced at eval/tools/coyodex_eval/compare.py:511 — one failed check makes the whole verdict a block",
   "anchor": "eval/tools/coyodex_eval/compare.py:511",
   "detail": "In: Baseline comparison (C102)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Losing a check the accepted map passed blocks the run. A number that moved too far only asks for a human look.' is enforced at eval/tools/coyodex_eval/compare.py:513 — a number outside its allowance makes the verdict a look rather than a block",
   "anchor": "eval/tools/coyodex_eval/compare.py:513",
   "detail": "In: Baseline comparison (C102)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'How many boxes a map should hold is worked out from the code, never from the accepted map.' is enforced at eval/tools/coyodex_eval/compare.py:458 — checks the fresh map's box count, and only that one, against what the code expects",
   "anchor": "eval/tools/coyodex_eval/compare.py:458",
   "detail": "In: Baseline comparison (C102)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'How many boxes a map should hold is worked out from the code, never from the accepted map.' is enforced at eval/tools/coyodex_eval/profile.py:357 — works the expected box count out of the code tree at scoring time",
   "anchor": "eval/tools/coyodex_eval/profile.py:357",
   "detail": "In: Map profile (C101)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A run that cannot get one of the two sides refuses to report, instead of reporting that nothing got worse.' is enforced at eval/tools/coyodex_eval/run.py:242 — stops when the accepted map's folder is missing, rather than scoring one map alone",
   "anchor": "eval/tools/coyodex_eval/run.py:242",
   "detail": "In: Eval run (C104)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A run that cannot get one of the two sides refuses to report, instead of reporting that nothing got worse.' is enforced at eval/tools/coyodex_eval/run.py:247 — stops when that folder holds no stored numbers to compare against",
   "anchor": "eval/tools/coyodex_eval/run.py:247",
   "detail": "In: Eval run (C104)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A run that cannot get one of the two sides refuses to report, instead of reporting that nothing got worse.' is enforced at eval/tools/coyodex_eval/compare.py:491 — marks the comparison drifted when only one of the two maps carries quality scores",
   "anchor": "eval/tools/coyodex_eval/compare.py:491",
   "detail": "In: Baseline comparison (C102)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map edited after it was frozen cannot be scored, so the run stops on any later change to it.' is enforced at eval/tools/coyodex_eval/run.py:219 — stops the run when the map no longer matches the fingerprint taken at freeze time",
   "anchor": "eval/tools/coyodex_eval/run.py:219",
   "detail": "In: Eval run (C104)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map edited after it was frozen cannot be scored, so the run stops on any later change to it.' is enforced at eval/tools/coyodex_eval/run.py:212 — stops when the freeze fingerprint was asked for but arrived empty, rather than skipping the check",
   "anchor": "eval/tools/coyodex_eval/run.py:212",
   "detail": "In: Eval run (C104)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map edited after it was frozen cannot be scored, so the run stops on any later change to it.' is enforced at eval/method.md:41 — states that editing either map during a run voids the run",
   "anchor": "eval/method.md:41",
   "detail": "In: Map quality eval instructions (C135)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim is settled only when most reviewers gave the same answer. A split with no majority stays unsettled.' is enforced at tools/coyodex/grounding.py:105 — calls a claim confirmed only when more than half the votes on it say so",
   "anchor": "tools/coyodex/grounding.py:105",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim is settled only when most reviewers gave the same answer. A split with no majority stays unsettled.' is enforced at tools/coyodex/grounding.py:107 — calls a claim disproved only when more than half the votes on it say so",
   "anchor": "tools/coyodex/grounding.py:107",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim is settled only when most reviewers gave the same answer. A split with no majority stays unsettled.' is enforced at tools/coyodex/grounding.py:121 — sends a split with no majority to the unsettled outcome instead of crediting either side",
   "anchor": "tools/coyodex/grounding.py:121",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A reviewer answers holds up, disproved, or cannot tell. Any other word is refused rather than counted.' is enforced at tools/coyodex/grounding.py:422 — refuses to build the honesty record when a vote carries a word outside the three",
   "anchor": "tools/coyodex/grounding.py:422",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A reviewer answers holds up, disproved, or cannot tell. Any other word is refused rather than counted.' is enforced at tools/coyodex/grounding.py:1119 — fails the early check on the vote files for the same unknown word, while the reviewer is still nearby",
   "anchor": "tools/coyodex/grounding.py:1119",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A reviewer answers holds up, disproved, or cannot tell. Any other word is refused rather than counted.' is enforced at method.md:2496 — tells every reviewer to answer cannot tell when the code settles nothing, so the third answer stays reachable",
   "anchor": "method.md:2496",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim counts as challenged only when a reviewer voted on it.' is enforced at tools/coyodex/grounding.py:469 — counts the challenged claims as the captured list minus every claim that drew no vote",
   "anchor": "tools/coyodex/grounding.py:469",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim counts as challenged only when a reviewer voted on it.' is enforced at method.md:2432 — defines the challenged number as how many claims got a verdict, not how many were sent out",
   "anchor": "method.md:2432",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pass that leaves claims unvoted is refused unless someone declares that stopping early was deliberate. The declaration must say which claims were prioritized.' is enforced at tools/coyodex/grounding.py:446 — refuses the honesty record when a captured claim has no vote and nobody declared the stop",
   "anchor": "tools/coyodex/grounding.py:446",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pass that leaves claims unvoted is refused unless someone declares that stopping early was deliberate. The declaration must say which claims were prioritized.' is enforced at tools/coyodex/grounding.py:457 — refuses a declared partial pass that does not say what it prioritized",
   "anchor": "tools/coyodex/grounding.py:457",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pass that leaves claims unvoted is refused unless someone declares that stopping early was deliberate. The declaration must say which claims were prioritized.' is enforced at method.md:2435 — states the refusal, because a planned stop and a dead batch of reviewers look identical from inside the tool",
   "anchor": "method.md:2435",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pass that leaves claims unvoted is refused unless someone declares that stopping early was deliberate. The declaration must say which claims were prioritized.' is enforced at method.md:2468 — requires the written reason, because the counts say how many claims were checked and never why those ones",
   "anchor": "method.md:2468",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pass that checked only part of the map still records the size of the whole claim surface.' is enforced at tools/coyodex/grounding.py:468 — sets the total from the whole captured list, whatever share of it drew votes",
   "anchor": "tools/coyodex/grounding.py:468",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pass that checked only part of the map still records the size of the whole claim surface.' is enforced at method.md:2470 — forbids cutting the captured list down to what was checked as a way past the refusal",
   "anchor": "method.md:2470",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A vote about a claim missing from the list captured for the reviewers is refused.' is enforced at tools/coyodex/grounding.py:428 — refuses the record when a vote names a claim the captured list never held",
   "anchor": "tools/coyodex/grounding.py:428",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A vote about a claim missing from the list captured for the reviewers is refused.' is enforced at method.md:2548 — requires capturing the list before any correction lands, so the two sides still describe one moment",
   "anchor": "method.md:2548",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A build stops when the map still carries a claim its reviewers disproved, word for word.' is enforced at tools/coyodex/grounding.py:1591 — fails the check when a disproved claim still matches a row of the finished map",
   "anchor": "tools/coyodex/grounding.py:1591",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A build stops when the map still carries a claim its reviewers disproved, word for word.' is enforced at tools/coyodex/finalize.py:416 — turns each surviving disproved claim into a blocking finding of the closing report",
   "anchor": "tools/coyodex/finalize.py:416",
   "detail": "In: Build closer (C25)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A file cited as evidence that the reviewer's own record never names fails the check. A file it only saw printed by a search is flagged, not failed.' is enforced at tools/coyodex/grounding.py:1417 — fails the check for a cited file that appears nowhere in the reviewer's own turn record",
   "anchor": "tools/coyodex/grounding.py:1417",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A file cited as evidence that the reviewer's own record never names fails the check. A file it only saw printed by a search is flagged, not failed.' is enforced at tools/coyodex/grounding.py:1425 — reports a file the reviewer only saw printed as worth a second look, without failing it",
   "anchor": "tools/coyodex/grounding.py:1425",
   "detail": "In: Grounding record (C22)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every screen of the map should hold between three and nine boxes. Five is the number it aims for.' is enforced at method.md:1609 — states the target of five boxes a screen and the readable range of three to nine",
   "anchor": "method.md:1609",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every screen of the map should hold between three and nine boxes. Five is the number it aims for.' is enforced at tools/coyodex/balance_lib.py:613 — counts a screen as reading well only when its box count falls inside that range",
   "anchor": "tools/coyodex/balance_lib.py:613",
   "detail": "In: Diagram balance (C48)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every screen of the map should hold between three and nine boxes. Five is the number it aims for.' is enforced at tools/coyodex/balance.py:67 — marks a screen holding ten boxes or more as past the readable range",
   "anchor": "tools/coyodex/balance.py:67",
   "detail": "In: Diagram balance (C48)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen whose boxes are all the same kind reads as a list, so fifteen boxes are fine there.' is enforced at method.md:1614 — grants the one exemption, for a screen of same-kind neighbours sharing a folder or a name ending",
   "anchor": "method.md:1614",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen whose boxes are all the same kind reads as a list, so fifteen boxes are fine there.' is enforced at tools/coyodex/balance_lib.py:309 — holds back the crowded-screen advice for such a screen until it passes fifteen boxes",
   "anchor": "tools/coyodex/balance_lib.py:309",
   "detail": "In: Diagram balance (C48)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen whose boxes are all the same kind reads as a list, so fifteen boxes are fine there.' is enforced at tools/coyodex/balance.py:66 — labels the same screen exempt in the report instead of crowded",
   "anchor": "tools/coyodex/balance.py:66",
   "detail": "In: Diagram balance (C48)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Only the first screen is faulted for holding too few boxes. A screen deeper down may be small.' is enforced at method.md:1612 — says too few boxes is a fault at the first screen only, and normal deeper down",
   "anchor": "method.md:1612",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Only the first screen is faulted for holding too few boxes. A screen deeper down may be small.' is enforced at tools/coyodex/balance_lib.py:290 — raises the too-few-boxes advice for the first screen and for no other",
   "anchor": "tools/coyodex/balance_lib.py:290",
   "detail": "In: Diagram balance (C48)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Only the first screen is faulted for holding too few boxes. A screen deeper down may be small.' is enforced at tools/coyodex/balance.py:147 — names the thin deeper screens in the report while saying they are not a fault",
   "anchor": "tools/coyodex/balance.py:147",
   "detail": "In: Diagram balance (C48)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A group holding exactly one thing is reported, because that level costs a click and adds no meaning.' is enforced at method.md:1613 — names a group with one member as a level pulling no weight, to be removed or grown",
   "anchor": "method.md:1613",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A group holding exactly one thing is reported, because that level costs a click and adds no meaning.' is enforced at tools/coyodex/balance_lib.py:303 — reports a group whose single member is one box rather than another group",
   "anchor": "tools/coyodex/balance_lib.py:303",
   "detail": "In: Diagram balance (C48)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A group holding exactly one thing is reported, because that level costs a click and adds no meaning.' is enforced at tools/coyodex/validate_model.py:6317 — reports a group whose single member is another group of the same kind",
   "anchor": "tools/coyodex/validate_model.py:6317",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A crowded or thin screen is only ever advice. It never refuses a map or stops a build.' is enforced at method.md:1673 — states that screen density never refuses a map, because grouping is a free choice about the picture",
   "anchor": "method.md:1673",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A crowded or thin screen is only ever advice. It never refuses a map or stops a build.' is enforced at tools/coyodex/validate_model.py:6328 — files every screen-density finding as advice rather than as a fault that refuses the map",
   "anchor": "tools/coyodex/validate_model.py:6328",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A crowded or thin screen is only ever advice. It never refuses a map or stops a build.' is enforced at tools/coyodex/finalize.py:587 — records the density findings in the closing report without moving its verdict",
   "anchor": "tools/coyodex/finalize.py:587",
   "detail": "In: Build closer (C25)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Regrouping may only move boxes between groups. It may never merge or split the boxes themselves.' is enforced at method.md:1675 — forbids any density finding from merging or splitting boxes to reach a number",
   "anchor": "method.md:1675",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Regrouping may only move boxes between groups. It may never merge or split the boxes themselves.' is enforced at tools/coyodex/balance.py:107 — writes a split suggestion as a change of which group a box sits in, and nothing else",
   "anchor": "tools/coyodex/balance.py:107",
   "detail": "In: Diagram balance (C48)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Regrouping may only move boxes between groups. It may never merge or split the boxes themselves.' is enforced at tools/coyodex/balance.py:109 — tells the reader applying a suggestion to add no member the suggestion did not list",
   "anchor": "tools/coyodex/balance.py:109",
   "detail": "In: Diagram balance (C48)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A group is never one end of an arrow, so moving a box into another group moves no arrow.' is enforced at method.md:3134 — says dissolving a level moves no arrow, because a group is never an end of one",
   "anchor": "method.md:3134",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A group is never one end of an arrow, so moving a box into another group moves no arrow.' is enforced at tools/coyodex/validate_model.py:5283 — refuses a map holding any arrow that starts or ends at a group",
   "anchor": "tools/coyodex/validate_model.py:5283",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Grouping is worth doing only once a map passes about fifteen boxes. A smaller map stays flat.' is enforced at method.md:414 — makes groups optional, and recommends them only above about fifteen boxes",
   "anchor": "method.md:414",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Grouping is worth doing only once a map passes about fifteen boxes. A smaller map stays flat.' is enforced at tools/coyodex/balance_lib.py:280 — asks for groups only once an ungrouped map passes that count",
   "anchor": "tools/coyodex/balance_lib.py:280",
   "detail": "In: Diagram balance (C48)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map covers the files a project's version control accounts for, including new files it does not already skip.' is enforced at tools/coyodex/preindex_lib.py:198 — Asks version control for the files it already tracks.",
   "anchor": "tools/coyodex/preindex_lib.py:198",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map covers the files a project's version control accounts for, including new files it does not already skip.' is enforced at tools/coyodex/preindex_lib.py:201 — Adds files the author created but never registered, minus what version control skips.",
   "anchor": "tools/coyodex/preindex_lib.py:201",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map covers the files a project's version control accounts for, including new files it does not already skip.' is enforced at tools/coyodex/preindex_lib.py:222 — The file list for a project starts from version control, not from the folder tree.",
   "anchor": "tools/coyodex/preindex_lib.py:222",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Packaged output, copied-in libraries and lock files never count as a project's own code.' is enforced at tools/coyodex/preindex_lib.py:148 — Drops every file sitting under a known build, package or vendor folder.",
   "anchor": "tools/coyodex/preindex_lib.py:148",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Packaged output, copied-in libraries and lock files never count as a project's own code.' is enforced at tools/coyodex/preindex_lib.py:150 — Drops the lock files a package manager writes for itself.",
   "anchor": "tools/coyodex/preindex_lib.py:150",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Packaged output, copied-in libraries and lock files never count as a project's own code.' is enforced at tools/coyodex/preindex_lib.py:257 — Keeps the same folders out when the project has no version control at all.",
   "anchor": "tools/coyodex/preindex_lib.py:257",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project can list committed code that its map must leave out.' is enforced at tools/coyodex/ignorefile.py:88 — Keeps the last pattern that matched, so a later line can put a file back.",
   "anchor": "tools/coyodex/ignorefile.py:88",
   "detail": "In: Ignore list (C46)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project can list committed code that its map must leave out.' is enforced at tools/coyodex/ignorefile.py:95 — Answers whether a file is left out, from the pattern that decided it.",
   "anchor": "tools/coyodex/ignorefile.py:95",
   "detail": "In: Ignore list (C46)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project can list committed code that its map must leave out.' is enforced at tools/coyodex/preindex_lib.py:245 — Removes the file from the set a build will read.",
   "anchor": "tools/coyodex/preindex_lib.py:245",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every run reports what a project's own skip list removed, one pattern at a time.' is enforced at tools/coyodex/validate_model.py:6288 — Says the tree was narrowed on every check, not only the thorough one.",
   "anchor": "tools/coyodex/validate_model.py:6288",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every run reports what a project's own skip list removed, one pattern at a time.' is enforced at tools/coyodex/scope.py:133 — Prints one line per pattern in the briefing a person reads before a build.",
   "anchor": "tools/coyodex/scope.py:133",
   "detail": "In: Build briefing (C26)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every run reports what a project's own skip list removed, one pattern at a time.' is enforced at tools/coyodex/ignorefile.py:129 — Builds the line saying how many files each pattern removed.",
   "anchor": "tools/coyodex/ignorefile.py:129",
   "detail": "In: Ignore list (C46)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every run reports what a project's own skip list removed, one pattern at a time.' is enforced at tools/coyodex/validate_analysis.py:336 — Names the patterns that removed nothing, which read as coverage the author never got.",
   "anchor": "tools/coyodex/validate_analysis.py:336",
   "detail": "In: Shape checks (C6)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'One box covers about one folder of code, at most ten files or three thousand lines.' is enforced at tools/coyodex/preindex_lib.py:599 — A folder inside both limits becomes one box and is not opened further.",
   "anchor": "tools/coyodex/preindex_lib.py:599",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'One box covers about one folder of code, at most ten files or three thousand lines.' is enforced at tools/coyodex/preindex_lib.py:615 — One oversized folder holding no subfolders splits into several boxes.",
   "anchor": "tools/coyodex/preindex_lib.py:615",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'One box covers about one folder of code, at most ten files or three thousand lines.' is enforced at method.md:1591 — States the size one box should cover, in the instructions a build follows.",
   "anchor": "method.md:1591",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Findings about how coarsely a map is drawn are advice, because deliberate abstraction is allowed.' is enforced at tools/coyodex/validate_model.py:6299 — Files a folded-away module as advice rather than as a failure.",
   "anchor": "tools/coyodex/validate_model.py:6299",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Findings about how coarsely a map is drawn are advice, because deliberate abstraction is allowed.' is enforced at tools/coyodex/validate_model.py:6308 — Files the box-count nudge as advice rather than as a failure.",
   "anchor": "tools/coyodex/validate_model.py:6308",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Findings about how coarsely a map is drawn are advice, because deliberate abstraction is allowed.' is enforced at tools/coyodex/validate_model.py:6680 — The run fails on real errors only, so advice never stops it.",
   "anchor": "tools/coyodex/validate_model.py:6680",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map can record a deliberate coarse decision, and the advice about that decision then stops.' is enforced at tools/coyodex/validate_model.py:6293 — Reads the folders a map records as deliberately drawn coarsely.",
   "anchor": "tools/coyodex/validate_model.py:6293",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map can record a deliberate coarse decision, and the advice about that decision then stops.' is enforced at tools/coyodex/validate_analysis.py:171 — Skips a folder that sits at or under one the map recorded.",
   "anchor": "tools/coyodex/validate_analysis.py:171",
   "detail": "In: Shape checks (C6)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map can record a deliberate coarse decision, and the advice about that decision then stops.' is enforced at tools/coyodex/validate_model.py:6307 — Skips the box-count nudge when the map records that altitude decision.",
   "anchor": "tools/coyodex/validate_model.py:6307",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map a build replaces is never an input to the new one.' is enforced at method/dispatch.md:135 — Tells a build not to open the map it is replacing.",
   "anchor": "method/dispatch.md:135",
   "detail": "In: Mode dispatch instructions (C130)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map a build replaces is never an input to the new one.' is enforced at tools/coyodex/scope.py:116 — Warns before a build when a stray map sits among the files to be read.",
   "anchor": "tools/coyodex/scope.py:116",
   "detail": "In: Build briefing (C26)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map a build replaces is never an input to the new one.' is enforced at tools/coyodex/preindex_lib.py:108 — Keeps the folder holding a map out of the files a build reads.",
   "anchor": "tools/coyodex/preindex_lib.py:108",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at tools/coyodex/viewer/viewer.js:13472 — Picks, for each kind of thing, the single screen that draws it.",
   "anchor": "tools/coyodex/viewer/viewer.js:13472",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at tools/coyodex/viewer/viewer.js:7647 — Reads that same choice to decide which tab a thing's own page sits under.",
   "anchor": "tools/coyodex/viewer/viewer.js:7647",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at tools/coyodex/viewer/viewer.js:7651 — Sends a page about one thing to the tab that thing lives under.",
   "anchor": "tools/coyodex/viewer/viewer.js:7651",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at method/diagrams.md:54 — Tells the map builder that a link to a thing lands on that thing's home screen.",
   "anchor": "method/diagrams.md:54",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A label that would be true of nearly every card is left off.' is enforced at tools/coyodex/viewer/viewer.js:8219 — Leaves off the word saying who a feature serves when the answer is the ordinary one.",
   "anchor": "tools/coyodex/viewer/viewer.js:8219",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A label that would be true of nearly every card is left off.' is enforced at tools/coyodex/viewer/viewer.js:721 — Gives a person on the customer's side no word saying whose side they are on.",
   "anchor": "tools/coyodex/viewer/viewer.js:721",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen leaves off a fact it already shows beside the same thing.' is enforced at tools/coyodex/viewer/viewer.js:730 — Drops a card's sentence when the sentence only repeats the card's own name.",
   "anchor": "tools/coyodex/viewer/viewer.js:730",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen leaves off a fact it already shows beside the same thing.' is enforced at tools/coyodex/viewer/viewer.js:6915 — Drops the feature line when the path above the page already names that feature.",
   "anchor": "tools/coyodex/viewer/viewer.js:6915",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen leaves off a fact it already shows beside the same thing.' is enforced at tools/coyodex/viewer/viewer.js:8345 — Skips a group heading that would only repeat the section heading above it.",
   "anchor": "tools/coyodex/viewer/viewer.js:8345",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A box drops the type word wherever its colour and mark already say what kind of thing it is.' is enforced at tools/coyodex/viewer/viewer.js:987 — Prints the type word only for the box sizes that ask for it, and for every interface.",
   "anchor": "tools/coyodex/viewer/viewer.js:987",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A box drops the type word wherever its colour and mark already say what kind of thing it is.' is enforced at tools/coyodex/viewer/gen_viewer.py:3057 — Asks for the small box on a flow picture, which carries no type word.",
   "anchor": "tools/coyodex/viewer/gen_viewer.py:3057",
   "detail": "In: Diagram builder (C82)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A box drops the type word wherever its colour and mark already say what kind of thing it is.' is enforced at method/diagrams.md:56 — Tells the map builder to colour each box on a flow picture by its kind.",
   "anchor": "method/diagrams.md:56",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyodex/viewer/viewer.js:15584 — Hides the decisions tab on a map that states no decision.",
   "anchor": "tools/coyodex/viewer/viewer.js:15584",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyodex/viewer/viewer.js:15585 — Hides the interfaces tab on a map that records no interface.",
   "anchor": "tools/coyodex/viewer/viewer.js:15585",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyodex/viewer/viewer.js:15595 — Drops a whole group of tabs once every tab inside it has been hidden.",
   "anchor": "tools/coyodex/viewer/viewer.js:15595",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyodex/viewer/gen_viewer.py:3417 — Works out from the map whether there is any interface for a tab to show.",
   "anchor": "tools/coyodex/viewer/gen_viewer.py:3417",
   "detail": "In: Diagram builder (C82)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Where the map records nothing, the screen says so instead of leaving empty space.' is enforced at tools/coyodex/viewer/viewer.js:8541 — Says a feature reaches no outside service, rather than showing an empty block.",
   "anchor": "tools/coyodex/viewer/viewer.js:8541",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Where the map records nothing, the screen says so instead of leaving empty space.' is enforced at tools/coyodex/viewer/viewer.js:12874 — Says no flow step reaches this decision, rather than showing an empty block.",
   "anchor": "tools/coyodex/viewer/viewer.js:12874",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Where the map records nothing, the screen says so instead of leaving empty space.' is enforced at tools/coyodex/viewer/viewer.js:12757 — Says no flow comes through this interface, so no feature can be named here.",
   "anchor": "tools/coyodex/viewer/viewer.js:12757",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A line between two things offers no code link, because the place it names is only one example.' is enforced at tools/coyodex/viewer/viewer.js:3052 — Builds a relation row as plain text, with no link to a line of code.",
   "anchor": "tools/coyodex/viewer/viewer.js:3052",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A line between two things offers no code link, because the place it names is only one example.' is enforced at tools/coyodex/viewer/viewer.js:2957 — Clears the code viewer when a relation line is picked, rather than pointing it somewhere.",
   "anchor": "tools/coyodex/viewer/viewer.js:2957",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A line between two things offers no code link, because the place it names is only one example.' is enforced at tools/coyodex/viewer/viewer.js:14163 — Marks a line of code from a flow step's own place, never from a relation line.",
   "anchor": "tools/coyodex/viewer/viewer.js:14163",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A picture of one flow draws only the connections that flow itself makes.' is enforced at tools/coyodex/viewer/gen_viewer.py:3081 — Builds each arrow from the flow's own steps, grouped by the pair of things they join.",
   "anchor": "tools/coyodex/viewer/gen_viewer.py:3081",
   "detail": "In: Diagram builder (C82)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A picture of one flow draws only the connections that flow itself makes.' is enforced at method/diagrams.md:58 — Tells the map builder that the arrows are the flow's steps, never the general connection list.",
   "anchor": "method/diagrams.md:58",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Code shown beside the map is the version the map was built from, not whatever sits on disk now.' is enforced at tools/coyodex/viewer/serve.py:782 — Answers a file request at the map's own commit unless the address asks for another version.",
   "anchor": "tools/coyodex/viewer/serve.py:782",
   "detail": "In: Map server (C83)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Code shown beside the map is the version the map was built from, not whatever sits on disk now.' is enforced at tools/coyodex/viewer/serve.py:491 — Builds the file list from version history at the map's commit rather than from the folder on disk.",
   "anchor": "tools/coyodex/viewer/serve.py:491",
   "detail": "In: Map server (C83)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Code shown beside the map is the version the map was built from, not whatever sits on disk now.' is enforced at method.md:3108 — The instructions tell the reader that data and source both come from history at the map's commit.",
   "anchor": "method.md:3108",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Data the product writes only to read back crosses no interface, unless the map records an outside reader.' is enforced at method.md:511 — Keeps the product's own database, cache and queue off the outside edge.",
   "anchor": "method.md:511",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Data the product writes only to read back crosses no interface, unless the map records an outside reader.' is enforced at method.md:515 — Lets a reader the map records overturn that exclusion, so a store a person browses counts again.",
   "anchor": "method.md:515",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Data the product writes only to read back crosses no interface, unless the map records an outside reader.' is enforced at method/model.md:460 — Repeats the same exclusion on the page that fixes every field a map may hold.",
   "anchor": "method/model.md:460",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A step names the service on the far side, never the proxy or the library that carries the call.' is enforced at method.md:516 — Names a reverse proxy and a log shipper as carriers, and the log store as the real edge.",
   "anchor": "method.md:516",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A step names the service on the far side, never the proxy or the library that carries the call.' is enforced at tools/coyodex/validate_model.py:2840 — Flags a story step that stops at a carrier standing in front of an interface.",
   "anchor": "tools/coyodex/validate_model.py:2840",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A step names the service on the far side, never the proxy or the library that carries the call.' is enforced at tools/coyodex/validate_model.py:2851 — Flags the same shortcut inside shared steps, where one carrier is drawn in every story running them.",
   "anchor": "tools/coyodex/validate_model.py:2851",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Each outside system names the interfaces it stands on, or records why it is none, never both.' is enforced at tools/coyodex/validate_model.py:2744 — Stops a map where an outside system neither claims an interface nor gives a reason.",
   "anchor": "tools/coyodex/validate_model.py:2744",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Each outside system names the interfaces it stands on, or records why it is none, never both.' is enforced at tools/coyodex/validate_model.py:2531 — Stops a system that claims an interface and also gives a reason for having none.",
   "anchor": "tools/coyodex/validate_model.py:2531",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Each outside system names the interfaces it stands on, or records why it is none, never both.' is enforced at method/model.md:530 — Demands the reason on every outside system that names no interface.",
   "anchor": "method/model.md:530",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An interface is labelled by what it is, never by what it is for.' is enforced at tools/coyodex/validate_model.py:2630 — Flags a label that answers what the interface is for, such as payments or analytics.",
   "anchor": "tools/coyodex/validate_model.py:2630",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An interface is labelled by what it is, never by what it is for.' is enforced at tools/coyodex/lint_fragment.py:244 — Catches the same purpose label while one part of the map is still being written.",
   "anchor": "tools/coyodex/lint_fragment.py:244",
   "detail": "In: Fragment self-check (C5)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An interface is labelled by what it is, never by what it is for.' is enforced at method.md:564 — States that a payment service and a crash reporter are the same kind of thing.",
   "anchor": "method.md:564",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Who stands on the far side of an interface is read from the flows, never written by hand.' is enforced at tools/coyodex/validate_model.py:1248 — Takes each person standing at a step next to an interface as its far side.",
   "anchor": "tools/coyodex/validate_model.py:1248",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Who stands on the far side of an interface is read from the flows, never written by hand.' is enforced at method/model.md:487 — States that the far side has no field of its own.",
   "anchor": "method/model.md:487",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Who stands on the far side of an interface is read from the flows, never written by hand.' is enforced at method.md:625 — Forbids writing the far side by hand, and names the two ways it is worked out.",
   "anchor": "method.md:625",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A person meets the product through an interface at every step, not only the first and the last.' is enforced at tools/coyodex/validate_model.py:2947 — Calls a step a crossing when a person is at one end, code at the other, and no interface between.",
   "anchor": "tools/coyodex/validate_model.py:2947",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A person meets the product through an interface at every step, not only the first and the last.' is enforced at tools/coyodex/validate_model.py:2837 — Reports every such step of a story, so a crossing in the middle is heard about too.",
   "anchor": "tools/coyodex/validate_model.py:2837",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A person meets the product through an interface at every step, not only the first and the last.' is enforced at method.md:661 — States that a crossing in the middle of a story owes a door like the two ends.",
   "anchor": "method.md:661",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Work the product schedules for itself is never an outsider, so it crosses no interface.' is enforced at tools/coyodex/validate_model.py:918 — Drops a role that is both a machine and internal from the list of outsiders.",
   "anchor": "tools/coyodex/validate_model.py:918",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Work the product schedules for itself is never an outsider, so it crosses no interface.' is enforced at method.md:652 — States that the product's own scheduled work is not an actor for the door rule.",
   "anchor": "method.md:652",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A command is a way in only when it acts on the running product or its stored data.' is enforced at method.md:466 — States the only test that decides a command: what the command acts on, not its name and not its purpose.",
   "anchor": "method.md:466",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A command is a way in only when it acts on the running product or its stored data.' is enforced at method/templates/harvest-contract.md:95 — Hands the same test to the agent that mints the way-in rows. A table shows what counts on each side.",
   "anchor": "method/templates/harvest-contract.md:95",
   "detail": "In: Fan-out worker contracts (C132)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A command judged not to be a way in must still be recorded as a run or build command.' is enforced at method.md:482 — Says the decision is only half the work. The other half is writing the command into the run-and-build list.",
   "anchor": "method.md:482",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A command judged not to be a way in must still be recorded as a run or build command.' is enforced at method/templates/harvest-contract.md:125 — Makes the mover count and name every command it took out, so the second half becomes visible in the reply.",
   "anchor": "method/templates/harvest-contract.md:125",
   "detail": "In: Fan-out worker contracts (C132)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A way in is recorded once, at the thing that does the acting, and never again at whatever calls it.' is enforced at method/templates/harvest-contract.md:115 — Says a command calling one of the product's own addresses adds no new way in. The address already is one.",
   "anchor": "method/templates/harvest-contract.md:115",
   "detail": "In: Fan-out worker contracts (C132)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A way in is recorded once, at the thing that does the acting, and never again at whatever calls it.' is enforced at method/templates/harvest-contract.md:117 — Says a wrapper and the command it wraps are one way in, recorded at the command that acts.",
   "anchor": "method/templates/harvest-contract.md:117",
   "detail": "In: Fan-out worker contracts (C132)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A way in is recorded once, at the thing that does the acting, and never again at whatever calls it.' is enforced at method.md:477 — States both cuts in the method itself, so the rule survives outside the worker brief.",
   "anchor": "method.md:477",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A timer, a boot hook or a queue reader is work the product does to itself, not a way in.' is enforced at tools/coyodex/validate_model.py:2594 — Rejects an interface that claims a self-started way in, and says the work is the product acting on itself.",
   "anchor": "tools/coyodex/validate_model.py:2594",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A timer, a boot hook or a queue reader is work the product does to itself, not a way in.' is enforced at tools/coyodex/validate_model.py:770 — Keeps only the outside-driven ways in, so every outside-edge check reads the same shorter list.",
   "anchor": "tools/coyodex/validate_model.py:770",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A timer, a boot hook or a queue reader is work the product does to itself, not a way in.' is enforced at tools/coyodex/grammar.py:575 — Fixes, per kind of way in, which ones are self-started and which ones somebody outside asks for.",
   "anchor": "tools/coyodex/grammar.py:575",
   "detail": "In: Shared map grammar (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A way in of a kind nobody recognises is treated as one somebody outside asks for.' is enforced at tools/coyodex/grammar.py:599 — Answers outside-driven whenever no self-starting word matches the kind of the way in.",
   "anchor": "tools/coyodex/grammar.py:599",
   "detail": "In: Shared map grammar (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every way in that somebody outside asks for belongs to exactly one interface, or its absence is recorded.' is enforced at method.md:641 — States the one-interface rule and the recorded line that excuses a deliberate exception.",
   "anchor": "method.md:641",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every way in that somebody outside asks for belongs to exactly one interface, or its absence is recorded.' is enforced at tools/coyodex/validate_model.py:2598 — Blocks a way in that more than one interface claims, and blocks one interface listing it twice.",
   "anchor": "tools/coyodex/validate_model.py:2598",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every way in that somebody outside asks for belongs to exactly one interface, or its absence is recorded.' is enforced at tools/coyodex/validate_model.py:2764 — Collects the ways in that no interface claims and that no recorded line excuses.",
   "anchor": "tools/coyodex/validate_model.py:2764",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Work every request passes through on its way in is not itself a way in, so it joins no interface.' is enforced at tools/coyodex/validate_model.py:2486 — Names shared request handling as the one kind excused from belonging to an interface.",
   "anchor": "tools/coyodex/validate_model.py:2486",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Work every request passes through on its way in is not itself a way in, so it joins no interface.' is enforced at tools/coyodex/validate_model.py:2500 — Drops that kind before every check that asks which interface a way in belongs to.",
   "anchor": "tools/coyodex/validate_model.py:2500",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A way in that no story reaches is reported, including the ones the product starts for itself.' is enforced at method.md:212 — States that self-started ways in are not exempt, because a scheduled job is an actor with a goal.",
   "anchor": "method.md:212",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A way in that no story reaches is reported, including the ones the product starts for itself.' is enforced at tools/coyodex/validate_model.py:2142 — Reports the outside-driven ways in that no story reaches, grouped by the part of the product that owns them.",
   "anchor": "tools/coyodex/validate_model.py:2142",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A way in that no story reaches is reported, including the ones the product starts for itself.' is enforced at tools/coyodex/validate_model.py:2149 — Reports the self-started ways in that no story reaches, in the same grouped shape.",
   "anchor": "tools/coyodex/validate_model.py:2149",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An advisory never stops a build; only a gate does.' is enforced at tools/coyodex/validate_model.py:6657 — fails a build on gate findings alone, however many advisories were printed",
   "anchor": "tools/coyodex/validate_model.py:6657",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An advisory never stops a build; only a gate does.' is enforced at tools/coyodex/lint_fragment.py:654 — passes one fragment, counting its advisories, when no gate finding was found",
   "anchor": "tools/coyodex/lint_fragment.py:654",
   "detail": "In: Fragment self-check (C5)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An advisory never stops a build; only a gate does.' is enforced at method.md:1604 — states that the component count check advises, and that a justified exception stays a judgement call",
   "anchor": "method.md:1604",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A build is clean only when every gate ran and no advisory is left open.' is enforced at tools/coyodex/finalize.py:693 — calls a build incomplete when a gate that should have run did not",
   "anchor": "tools/coyodex/finalize.py:693",
   "detail": "In: Build closer (C25)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A build is clean only when every gate ran and no advisory is left open.' is enforced at tools/coyodex/finalize.py:695 — calls a build advisory when only advisories were found, which is not clean",
   "anchor": "tools/coyodex/finalize.py:695",
   "detail": "In: Build closer (C25)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A build is clean only when every gate ran and no advisory is left open.' is enforced at tools/coyodex/finalize.py:1286 — exits with failure on an incomplete build, so a skipped gate cannot read as a pass",
   "anchor": "tools/coyodex/finalize.py:1286",
   "detail": "In: Build closer (C25)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A build is clean only when every gate ran and no advisory is left open.' is enforced at tools/coyodex/ship.py:353 — stops the closing sequence at the first failing step, and names the steps that never ran",
   "anchor": "tools/coyodex/ship.py:353",
   "detail": "In: Build closer (C25)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An advisory the operator decides to live with is silenced only by a reason recorded in the map.' is enforced at method.md:1620 — says a durably justified exception is recorded in the map and silences its own advisory",
   "anchor": "method.md:1620",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An advisory the operator decides to live with is silenced only by a reason recorded in the map.' is enforced at method.md:1640 — forbids rewording the map to dodge an advisory, and asks for a recorded reason instead",
   "anchor": "method.md:1640",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An advisory the operator decides to live with is silenced only by a reason recorded in the map.' is enforced at tools/coyodex/records.py:324 — matches a finding against the recorded reasons, so only a recorded one is passed over",
   "anchor": "tools/coyodex/records.py:324",
   "detail": "In: Shared map grammar (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A recorded reason is refused unless it names a finding the checks really read.' is enforced at tools/coyodex/record.py:268 — refuses a reason filed under a heading no check ever reads",
   "anchor": "tools/coyodex/record.py:268",
   "detail": "In: Map editor (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A recorded reason is refused unless it names a finding the checks really read.' is enforced at tools/coyodex/record.py:309 — refuses a reason that answers no finding, and writes nothing at all",
   "anchor": "tools/coyodex/record.py:309",
   "detail": "In: Map editor (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A recorded reason is refused unless it names a finding the checks really read.' is enforced at tools/coyodex/records.py:243 — reads nothing from a reason listing several things when one of them is not a finding",
   "anchor": "tools/coyodex/records.py:243",
   "detail": "In: Shared map grammar (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every advisory a recorded reason silenced is still counted out loud in the build's report.' is enforced at tools/coyodex/validate_model.py:720 — adds a line saying how many advisories the recorded reasons silenced, and on which elements",
   "anchor": "tools/coyodex/validate_model.py:720",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every advisory a recorded reason silenced is still counted out loud in the build's report.' is enforced at tools/coyodex/validate_analysis.py:329 — names on every build the code a written exclusion removed from every check",
   "anchor": "tools/coyodex/validate_analysis.py:329",
   "detail": "In: Shape checks (C6)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every advisory a recorded reason silenced is still counted out loud in the build's report.' is enforced at tools/coyodex/records.py:286 — reports a reason that failed to parse instead of dropping it in silence",
   "anchor": "tools/coyodex/records.py:286",
   "detail": "In: Shared map grammar (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every advisory a recorded reason silenced is still counted out loud in the build's report.' is enforced at tools/coyodex/anchor_drift.py:199 — collects a line that tries to be a reason and answers nothing, so the build reports it",
   "anchor": "tools/coyodex/anchor_drift.py:199",
   "detail": "In: Map checks (C21)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every advisory a recorded reason silenced is still counted out loud in the build's report.' is enforced at tests/test_method_contract.py:671 — keeps the announcement of a silence out of reach of any recorded reason",
   "anchor": "tests/test_method_contract.py:671",
   "detail": "In: no component claims this file — the site is UNVERIFIED.",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A finding whose only honest answer is a fix cannot be silenced by a recorded reason.' is enforced at tests/test_method_contract.py:531 — holds the findings no recorded reason may silence, each beside the reason it cannot",
   "anchor": "tests/test_method_contract.py:531",
   "detail": "In: no component claims this file — the site is UNVERIFIED.",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A finding whose only honest answer is a fix cannot be silenced by a recorded reason.' is enforced at method.md:2093 — refuses any way to record a use case as deliberately left untold",
   "anchor": "method.md:2093",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A finding whose only honest answer is a fix cannot be silenced by a recorded reason.' is enforced at tools/coyodex/anchor_drift.py:437 — tells an operator that a recorded reason cannot answer a finding about a code link's shape",
   "anchor": "tools/coyodex/anchor_drift.py:437",
   "detail": "In: Map checks (C21)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An advisory must say where the operator records the reason for living with it.' is enforced at tests/test_method_contract.py:716 — fails when an advisory leaves the reader nowhere to record the decision",
   "anchor": "tests/test_method_contract.py:716",
   "detail": "In: no component claims this file — the site is UNVERIFIED.",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'An advisory must say where the operator records the reason for living with it.' is enforced at tools/coyodex/validate_model.py:6676 — prints beside the advisories the command that records a reason the checks will find",
   "anchor": "tools/coyodex/validate_model.py:6676",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Only files git accounts for are analysed, so ignored and generated trees stay out of the map.' is enforced at tools/coyodex/preindex_lib.py:198 — Takes the starting file list from everything git tracks in the project.",
   "anchor": "tools/coyodex/preindex_lib.py:198",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Only files git accounts for are analysed, so ignored and generated trees stay out of the map.' is enforced at tools/coyodex/preindex_lib.py:201 — Adds files someone created but never added yet, and leaves every ignored path to git.",
   "anchor": "tools/coyodex/preindex_lib.py:201",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Only files git accounts for are analysed, so ignored and generated trees stay out of the map.' is enforced at tools/coyodex/preindex_lib.py:148 — Drops dependency folders, build output and caches by name, whether or not git is there to ask.",
   "anchor": "tools/coyodex/preindex_lib.py:148",
   "detail": "In: Code survey (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project's own exclusion patterns are named on every run, each beside the number of files it removed.' is enforced at tools/coyodex/validate_model.py:6288 — Names the patterns on every check run, including the cheap pass a lead runs most often.",
   "anchor": "tools/coyodex/validate_model.py:6288",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project's own exclusion patterns are named on every run, each beside the number of files it removed.' is enforced at tools/coyodex/ignorefile.py:133 — Collects each pattern that removed nothing, so a typo cannot read as coverage the map never got.",
   "anchor": "tools/coyodex/ignorefile.py:133",
   "detail": "In: Ignore list (C46)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project's own exclusion patterns are named on every run, each beside the number of files it removed.' is enforced at tools/coyodex/scope.py:131 — Puts the same per pattern account in the briefing, printed before a build reads any code.",
   "anchor": "tools/coyodex/scope.py:131",
   "detail": "In: Build briefing (C26)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A build never opens the map it is replacing, so the new map cannot quietly copy the old one.' is enforced at method/dispatch.md:135 — Bans opening the map being replaced, including the one version history still holds and one already filed away.",
   "anchor": "method/dispatch.md:135",
   "detail": "In: Mode dispatch instructions (C130)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A build never opens the map it is replacing, so the new map cannot quietly copy the old one.' is enforced at tools/coyodex/scope.py:116 — Warns when a copy of a map sits among the files that are about to be read as code.",
   "anchor": "tools/coyodex/scope.py:116",
   "detail": "In: Build briefing (C26)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map is built again from scratch only when the person asks, because a rebuild discards the accepted map.' is enforced at method/dispatch.md:187 — Sends an existing map to a change report, and makes a fresh build need a spoken request and a confirmation.",
   "anchor": "method/dispatch.md:187",
   "detail": "In: Mode dispatch instructions (C130)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map is built again from scratch only when the person asks, because a rebuild discards the accepted map.' is enforced at method.md:2831 — Says a build writes a new map over any existing one, so nothing of the older map is kept.",
   "anchor": "method.md:2831",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at tools/coyodex/fix.py:136 — Warns that a correction made to the finished map disappears the next time the pieces are merged.",
   "anchor": "tools/coyodex/fix.py:136",
   "detail": "In: Map editor (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at tools/coyodex/fix.py:470 — Refuses a removal aimed at one piece, because the pieces beside that one cannot be seen from there.",
   "anchor": "tools/coyodex/fix.py:470",
   "detail": "In: Map editor (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at tools/coyodex/assemble.py:922 — Applies the recorded assignments after every merge, which is what makes those assignments last.",
   "anchor": "tools/coyodex/assemble.py:922",
   "detail": "In: Map assembly (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at method.md:2742 — States the split: what must survive a rebuild goes in the assignment file, never in the finished map.",
   "anchor": "method.md:2742",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at tools/coyodex/validate_model.py:6134 — Compares the committed readable copy with a freshly generated one, and reports any difference.",
   "anchor": "tools/coyodex/validate_model.py:6134",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at method/dispatch.md:194 — States that the readable copy is generated from the map and never edited by hand.",
   "anchor": "method/dispatch.md:194",
   "detail": "In: Mode dispatch instructions (C130)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A warning the person decides to accept is silenced only by a note written under a heading the checks read.' is enforced at tools/coyodex/record.py:85 — Refuses a heading no check reads, because a note filed under such a heading silences nothing.",
   "anchor": "tools/coyodex/record.py:85",
   "detail": "In: Map editor (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A warning the person decides to accept is silenced only by a note written under a heading the checks read.' is enforced at tools/coyodex/finalize.py:635 — Drops each file named by a recorded note out of the advisory the finish check would otherwise raise.",
   "anchor": "tools/coyodex/finalize.py:635",
   "detail": "In: Build closer (C25)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A warning the person decides to accept is silenced only by a note written under a heading the checks read.' is enforced at method/dispatch.md:110 — Names the heading the finish check reads, after twenty notes were filed where nothing could find them.",
   "anchor": "method/dispatch.md:110",
   "detail": "In: Mode dispatch instructions (C130)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map names the version of the code it describes. That name is marked when the code on disk was never committed.' is enforced at tools/coyodex/provenance.py:191 — appends the mark to the version name when anything is uncommitted",
   "anchor": "tools/coyodex/provenance.py:191",
   "detail": "In: Build log (C27)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map names the version of the code it describes. That name is marked when the code on disk was never committed.' is enforced at tools/coyodex/provenance.py:164 — asks version control what is uncommitted, ignoring the map's own folder",
   "anchor": "tools/coyodex/provenance.py:164",
   "detail": "In: Build log (C27)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map names the version of the code it describes. That name is marked when the code on disk was never committed.' is enforced at method/change-impact.md:129 — tells the agent to offer the choice, and to mark the pin only if the person declines to commit",
   "anchor": "method/change-impact.md:129",
   "detail": "In: Change impact instructions (C131)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "C27 queries D90",
   "anchor": "tools/coyodex/provenance.py:125",
   "detail": "From: C27 = Build log (tools/coyodex/provenance.py:327); entry points: An operator runs `coyodex provenance show` to list the sessions that built this map. (tools/coyodex/provenance.py:308); An operator runs `coyodex provenance stamp` to record which session built this map, and when. (tools/coyodex/provenance.py:327); An operator runs `coyodex timings record` to write down what each fan-out slice took. (tools/coyodex/timings.py:439); An operator runs `coyodex timings order` to see one phase's slices, longest first. (tools/coyodex/timings.py:449); An operator runs `coyodex timings show` to see every phase that has a timing record. (tools/coyodex/timings.py:456); To: D90 = Git (platform: version control program — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C27's code to confirm it reaches D90; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C27 reads D95",
   "anchor": "tools/coyodex/provenance.py:209",
   "detail": "From: C27 = Build log (tools/coyodex/provenance.py:327); entry points: An operator runs `coyodex provenance show` to list the sessions that built this map. (tools/coyodex/provenance.py:308); An operator runs `coyodex provenance stamp` to record which session built this map, and when. (tools/coyodex/provenance.py:327); An operator runs `coyodex timings record` to write down what each fan-out slice took. (tools/coyodex/timings.py:439); An operator runs `coyodex timings order` to see one phase's slices, longest first. (tools/coyodex/timings.py:449); An operator runs `coyodex timings show` to see every phase that has a timing record. (tools/coyodex/timings.py:456); To: D95 = Coding agent (platform: AI coding assistant — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C27's code to confirm it reaches D95; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C27 writes D91",
   "anchor": "tools/coyodex/provenance.py:232",
   "detail": "From: C27 = Build log (tools/coyodex/provenance.py:327); entry points: An operator runs `coyodex provenance show` to list the sessions that built this map. (tools/coyodex/provenance.py:308); An operator runs `coyodex provenance stamp` to record which session built this map, and when. (tools/coyodex/provenance.py:327); An operator runs `coyodex timings record` to write down what each fan-out slice took. (tools/coyodex/timings.py:439); An operator runs `coyodex timings order` to see one phase's slices, longest first. (tools/coyodex/timings.py:449); An operator runs `coyodex timings show` to see every phase that has a timing record. (tools/coyodex/timings.py:456); To: D91 = Local file system (platform: operating system storage — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C27's code to confirm it reaches D91; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C49 queries D90",
   "anchor": "tools/coyodex/impact_git.py:50",
   "detail": "From: C49 = Change impact (tools/coyodex/impact_git.py:285); To: D90 = Git (platform: version control program — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C49's code to confirm it reaches D90; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C50 writes D91",
   "anchor": "tools/map_backup.py:391",
   "detail": "From: C50 = Map and transcript backup (tools/map_backup.py:391); entry points: A build records which conversation produced this map. (tools/map_backup.py:452); An operator archives a project's map with the conversation that built it. (tools/map_backup.py:478); To: D91 = Local file system (platform: operating system storage — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C50's code to confirm it reaches D91; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C69 requests D161",
   "anchor": "tools/coyodex/viewer/viewer.js:14100",
   "detail": "From: C69 = Source column (tools/coyodex/viewer/viewer.js:13894); To: D161 = cdnjs (service: content delivery network — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C69's code to confirm it reaches D161; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C83 calls D92",
   "anchor": "tools/coyodex/viewer/serve.py:895",
   "detail": "From: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883); To: D92 = Web browser (platform: desktop browser — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C83's code to confirm it reaches D92; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C70 calls D93",
   "anchor": "tools/coyodex/viewer/viewer.js:14925",
   "detail": "From: C70 = Viewer shell (tools/coyodex/viewer/viewer.js:14609); entry points: Opening `#v=overview` shows the sentences saying what the product is for. (tools/coyodex/viewer/viewer.html:117); Opening `#v=features` lists everything the product does, one card per feature. (tools/coyodex/viewer/viewer.html:118); Opening `#v=hp` shows the product's one successful run, end to end. (tools/coyodex/viewer/viewer.html:119); Opening `#v=interfaces` shows every place the product meets the outside world. (tools/coyodex/viewer/viewer.html:120); Opening `#v=rules` shows the decisions the product makes, grouped by area. (tools/coyodex/viewer/viewer.html:126); Opening `#v=domain` shows the things the product knows about. (tools/coyodex/viewer/viewer.html:127); Opening `#v=glossary` shows the product's own words and what each one means. (tools/coyodex/viewer/viewer.html:128); Opening `#v=container` draws the subsystems the code is arranged into. (tools/coyodex/viewer/viewer.html:133); Opening `#v=data` shows where the product's data physically lives. (tools/coyodex/viewer/viewer.html:134); Opening `#v=context` draws what the product depends on outside itself. (tools/coyodex/viewer/viewer.html:135); Opening `#v=tests` shows how much of the product the tests reach. (tools/coyodex/viewer/viewer.html:136); Opening `#v=deployment` draws the processes that run and what each one holds. (tools/coyodex/viewer/viewer.html:138); Opening `#v=system` shows the operational tables: commands, settings and ways in. (tools/coyodex/viewer/viewer.html:139); To: D93 = Code editor (platform: desktop editor — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C70's code to confirm it reaches D93; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C70 requests D94",
   "anchor": "tools/coyodex/viewer/viewer.js:14928",
   "detail": "From: C70 = Viewer shell (tools/coyodex/viewer/viewer.js:14609); entry points: Opening `#v=overview` shows the sentences saying what the product is for. (tools/coyodex/viewer/viewer.html:117); Opening `#v=features` lists everything the product does, one card per feature. (tools/coyodex/viewer/viewer.html:118); Opening `#v=hp` shows the product's one successful run, end to end. (tools/coyodex/viewer/viewer.html:119); Opening `#v=interfaces` shows every place the product meets the outside world. (tools/coyodex/viewer/viewer.html:120); Opening `#v=rules` shows the decisions the product makes, grouped by area. (tools/coyodex/viewer/viewer.html:126); Opening `#v=domain` shows the things the product knows about. (tools/coyodex/viewer/viewer.html:127); Opening `#v=glossary` shows the product's own words and what each one means. (tools/coyodex/viewer/viewer.html:128); Opening `#v=container` draws the subsystems the code is arranged into. (tools/coyodex/viewer/viewer.html:133); Opening `#v=data` shows where the product's data physically lives. (tools/coyodex/viewer/viewer.html:134); Opening `#v=context` draws what the product depends on outside itself. (tools/coyodex/viewer/viewer.html:135); Opening `#v=tests` shows how much of the product the tests reach. (tools/coyodex/viewer/viewer.html:136); Opening `#v=deployment` draws the processes that run and what each one holds. (tools/coyodex/viewer/viewer.html:138); Opening `#v=system` shows the operational tables: commands, settings and ways in. (tools/coyodex/viewer/viewer.html:139); To: D94 = GitHub (service: code host — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C70's code to confirm it reaches D94; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C70 requests D96",
   "anchor": "tools/coyodex/viewer/viewer.html:19",
   "detail": "From: C70 = Viewer shell (tools/coyodex/viewer/viewer.js:14609); entry points: Opening `#v=overview` shows the sentences saying what the product is for. (tools/coyodex/viewer/viewer.html:117); Opening `#v=features` lists everything the product does, one card per feature. (tools/coyodex/viewer/viewer.html:118); Opening `#v=hp` shows the product's one successful run, end to end. (tools/coyodex/viewer/viewer.html:119); Opening `#v=interfaces` shows every place the product meets the outside world. (tools/coyodex/viewer/viewer.html:120); Opening `#v=rules` shows the decisions the product makes, grouped by area. (tools/coyodex/viewer/viewer.html:126); Opening `#v=domain` shows the things the product knows about. (tools/coyodex/viewer/viewer.html:127); Opening `#v=glossary` shows the product's own words and what each one means. (tools/coyodex/viewer/viewer.html:128); Opening `#v=container` draws the subsystems the code is arranged into. (tools/coyodex/viewer/viewer.html:133); Opening `#v=data` shows where the product's data physically lives. (tools/coyodex/viewer/viewer.html:134); Opening `#v=context` draws what the product depends on outside itself. (tools/coyodex/viewer/viewer.html:135); Opening `#v=tests` shows how much of the product the tests reach. (tools/coyodex/viewer/viewer.html:136); Opening `#v=deployment` draws the processes that run and what each one holds. (tools/coyodex/viewer/viewer.html:138); Opening `#v=system` shows the operational tables: commands, settings and ways in. (tools/coyodex/viewer/viewer.html:139); To: D96 = jsDelivr (service: content delivery network — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C70's code to confirm it reaches D96; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C105 queries D90",
   "anchor": "eval/tools/coyodex_eval/arrows.py:184",
   "detail": "From: C105 = Lost arrows (eval/tools/coyodex_eval/arrows.py:152); To: D90 = Git (platform: version control program — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C105's code to confirm it reaches D90; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C113 reads D91",
   "anchor": "eval/tools/coyodex_eval/mutate.py:112",
   "detail": "From: C113 = Skeptic recall test (eval/tools/coyodex_eval/mutate.py:236); To: D91 = Local file system (platform: operating system storage — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C113's code to confirm it reaches D91; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C107 reads D105",
   "anchor": "eval/tools/coyodex_eval/transcript.py:328",
   "detail": "From: C107 = Transcript reader (eval/tools/coyodex_eval/transcript.py:442); To: D105 = Coding agent chat logs (platform: agent harness — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C107's code to confirm it reaches D105; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C108 reads D105",
   "anchor": "eval/tools/coyodex_eval/cost.py:272",
   "detail": "From: C108 = Build cost (eval/tools/coyodex_eval/cost.py:620); To: D105 = Coding agent chat logs (platform: agent harness — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C108's code to confirm it reaches D105; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C110 reads D105",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:216",
   "detail": "From: C110 = Retro precheck (eval/tools/coyodex_eval/retro_precheck.py:198); To: D105 = Coding agent chat logs (platform: agent harness — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C110's code to confirm it reaches D105; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C111 queries D90",
   "anchor": "eval/tools/coyodex_eval/ledger.py:87",
   "detail": "From: C111 = Ledger check (eval/tools/coyodex_eval/ledger.py:110); To: D90 = Git (platform: version control program — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C111's code to confirm it reaches D90; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C85 persists D91",
   "anchor": "tools/coyodex/viewer/recents.py:44",
   "detail": "From: C85 = Remembered projects (tools/coyodex/viewer/recents.py:22); To: D91 = Local file system (platform: operating system storage — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C85's code to confirm it reaches D91; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C71 reads E39",
   "anchor": "tools/coyodex/viewer/viewer.js:15114",
   "detail": "From: C71 = Map search (tools/coyodex/viewer/viewer.js:15119); To: E39 = Graph (tools/coyodex/viewer/build_graph.py:116)",
   "why_risky": "domain-model ownership edge — verify C71's code actually 'reads' E39; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C72 reads E46",
   "anchor": "tools/coyodex/viewer/viewer.js:15650",
   "detail": "From: C72 = Impact explorer (tools/coyodex/viewer/viewer.js:15650); To: E46 = Change impact report (tools/coyodex/viewer/build_graph.py:198)",
   "why_risky": "domain-model ownership edge — verify C72's code actually 'reads' E46; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C84 reads E39",
   "anchor": "tools/coyodex/viewer/filetree.py:107",
   "detail": "From: C84 = File browser (tools/coyodex/viewer/filetree.py:214); To: E39 = Graph (tools/coyodex/viewer/build_graph.py:116)",
   "why_risky": "domain-model ownership edge — verify C84's code actually 'reads' E39; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C47 writes E56",
   "anchor": "tools/coyodex/reconcile_build.py:480",
   "detail": "From: C47 = Assignment rules (tools/coyodex/reconcile.py:692); To: E56 = Reconcile file (tools/coyodex/reconcile.py:215)",
   "why_risky": "domain-model ownership edge — verify C47's code actually 'writes' E56; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C4 writes E65",
   "anchor": "tools/coyodex/assemble.py:298",
   "detail": "From: C4 = Map assembly (tools/coyodex/assemble.py:408); To: E65 = Fragment load (tools/coyodex/assemble.py:226)",
   "why_risky": "domain-model ownership edge — verify C4's code actually 'writes' E65; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C4 persists E1",
   "anchor": "tools/coyodex/assemble.py:978",
   "detail": "From: C4 = Map assembly (tools/coyodex/assemble.py:408); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C4's code actually 'persists' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C22 persists E37",
   "anchor": "tools/coyodex/grounding.py:1749",
   "detail": "From: C22 = Grounding record (tools/coyodex/grounding.py:1704); entry points: An operator runs `coyodex grounding lint` to check the skeptics' verdict files are well formed. (tools/coyodex/grounding.py:1514); An operator runs `coyodex grounding refutations` to fail a map still holding a claim the skeptics refuted. (tools/coyodex/grounding.py:1563); An operator runs `coyodex grounding by-element` to see what the review pass decided about each element. (tools/coyodex/grounding.py:1682); An operator runs `coyodex grounding report` to see which claims were refuted, tied or unvoted. (tools/coyodex/grounding.py:1690); An operator runs `coyodex grounding write` to build the map's honesty record from the verdicts. (tools/coyodex/grounding.py:1704); To: E37 = Grounding record (tools/coyodex/model.py:680)",
   "why_risky": "domain-model ownership edge — verify C22's code actually 'persists' E37; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C6 reads E1",
   "anchor": "tools/coyodex/validate_model.py:6585",
   "detail": "From: C6 = Shape checks (tools/coyodex/validate_model.py:195); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C6's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C80 reads E1",
   "anchor": "tools/coyodex/viewer/render.py:48",
   "detail": "From: C80 = Map rendering (tools/coyodex/views.py:299); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C80's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C131 reads E1",
   "anchor": "method/change-impact.md:41",
   "detail": "From: C131 = Change impact instructions (method/change-impact.md:3); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C131's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C131 writes E46",
   "anchor": "method/change-impact.md:13",
   "detail": "From: C131 = Change impact instructions (method/change-impact.md:3); To: E46 = Change impact report (tools/coyodex/viewer/build_graph.py:198)",
   "why_risky": "domain-model ownership edge — verify C131's code actually 'writes' E46; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C131 reads E46",
   "anchor": "method/change-impact.md:80",
   "detail": "From: C131 = Change impact instructions (method/change-impact.md:3); To: E46 = Change impact report (tools/coyodex/viewer/build_graph.py:198)",
   "why_risky": "domain-model ownership edge — verify C131's code actually 'reads' E46; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C23 writes E1",
   "anchor": "tools/coyodex/fix.py:119",
   "detail": "From: C23 = Map editor (tools/coyodex/fix.py:119); entry points: An operator runs `coyodex fix apply-drift` to write each corrected code link back into the map. (tools/coyodex/fix.py:1820); An operator runs `coyodex fix dedup-relation` to drop one of two records of the same data link. (tools/coyodex/fix.py:1820); An operator runs `coyodex fix drop-edge` to remove an arrow the skeptics refuted. (tools/coyodex/fix.py:1820); An operator runs `coyodex fix dedup-edge` to keep one code link where an arrow was recorded twice. (tools/coyodex/fix.py:1821); An operator runs `coyodex fix rows` to apply many wording corrections in one all-or-nothing write. (tools/coyodex/fix.py:1821); An operator runs `coyodex fix security-row` to rewrite a refuted guard's wording and code link. (tools/coyodex/fix.py:1821); An operator runs `coyodex fix dedup-security` to drop a guard an older map recorded twice. (tools/coyodex/fix.py:1822); An operator runs `coyodex fix row` to rewrite one row's wording in the fragment that authored it. (tools/coyodex/fix.py:1822); An operator runs `coyodex record` to note one advisory they judged acceptable. (tools/coyodex/record.py:318); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C23's code actually 'writes' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C45 writes E50",
   "anchor": "tools/coyodex/preindex_lib.py:249",
   "detail": "From: C45 = Code survey (tools/coyodex/preindex.py:604); To: E50 = File walk result (tools/coyodex/preindex_lib.py:156)",
   "why_risky": "domain-model ownership edge — verify C45's code actually 'writes' E50; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C24 reads E1",
   "anchor": "tools/coyodex/dump.py:138",
   "detail": "From: C24 = Map reader (tools/coyodex/dump.py:303); entry points: A skeptic runs `coyodex context` to get one claims batch with its evidence already beside it. (tools/coyodex/context.py:200); An operator runs `coyodex dump` to read the map, or one element's stored record, as data. (tools/coyodex/dump.py:303); An operator runs `coyodex diff` to see which rows two maps of the same work differ on. (tools/coyodex/mapdiff.py:254); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C24's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C23 writes E36",
   "anchor": "tools/coyodex/record.py:101",
   "detail": "From: C23 = Map editor (tools/coyodex/fix.py:119); entry points: An operator runs `coyodex fix apply-drift` to write each corrected code link back into the map. (tools/coyodex/fix.py:1820); An operator runs `coyodex fix dedup-relation` to drop one of two records of the same data link. (tools/coyodex/fix.py:1820); An operator runs `coyodex fix drop-edge` to remove an arrow the skeptics refuted. (tools/coyodex/fix.py:1820); An operator runs `coyodex fix dedup-edge` to keep one code link where an arrow was recorded twice. (tools/coyodex/fix.py:1821); An operator runs `coyodex fix rows` to apply many wording corrections in one all-or-nothing write. (tools/coyodex/fix.py:1821); An operator runs `coyodex fix security-row` to rewrite a refuted guard's wording and code link. (tools/coyodex/fix.py:1821); An operator runs `coyodex fix dedup-security` to drop a guard an older map recorded twice. (tools/coyodex/fix.py:1822); An operator runs `coyodex fix row` to rewrite one row's wording in the fragment that authored it. (tools/coyodex/fix.py:1822); An operator runs `coyodex record` to note one advisory they judged acceptable. (tools/coyodex/record.py:318); To: E36 = Extra section (tools/coyodex/model.py:673)",
   "why_risky": "domain-model ownership edge — verify C23's code actually 'writes' E36; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C101 reads E1",
   "anchor": "eval/tools/coyodex_eval/profile.py:595",
   "detail": "From: C101 = Map profile (eval/tools/coyodex_eval/profile.py:430); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C101's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C103 reads E1",
   "anchor": "eval/tools/coyodex_eval/judge.py:234",
   "detail": "From: C103 = Judge report (eval/tools/coyodex_eval/judge.py:257); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C103's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C105 reads E1",
   "anchor": "eval/tools/coyodex_eval/arrows.py:251",
   "detail": "From: C105 = Lost arrows (eval/tools/coyodex_eval/arrows.py:152); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C105's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C112 reads E1",
   "anchor": "eval/tools/coyodex_eval/live_numbers.py:498",
   "detail": "From: C112 = Live number ledger (eval/tools/coyodex_eval/live_numbers.py:540); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C112's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C106 moves E1",
   "anchor": "eval/tools/coyodex_eval/archive.py:110",
   "detail": "From: C106 = Rebuild archive (eval/tools/coyodex_eval/archive.py:110); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C106's code actually 'moves' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C104 reads E55",
   "anchor": "eval/tools/coyodex_eval/run.py:78",
   "detail": "From: C104 = Eval run (eval/tools/coyodex_eval/run.py:69); To: E55 = Map profile (eval/tools/coyodex_eval/profile.py:39)",
   "why_risky": "domain-model ownership edge — verify C104's code actually 'reads' E55; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C104 writes E55",
   "anchor": "eval/tools/coyodex_eval/run.py:166",
   "detail": "From: C104 = Eval run (eval/tools/coyodex_eval/run.py:69); To: E55 = Map profile (eval/tools/coyodex_eval/profile.py:39)",
   "why_risky": "domain-model ownership edge — verify C104's code actually 'writes' E55; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C102 reads E55",
   "anchor": "eval/tools/coyodex_eval/compare.py:247",
   "detail": "From: C102 = Baseline comparison (eval/tools/coyodex_eval/compare.py:518); To: E55 = Map profile (eval/tools/coyodex_eval/profile.py:39)",
   "why_risky": "domain-model ownership edge — verify C102's code actually 'reads' E55; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C110 reads E1",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:185",
   "detail": "From: C110 = Retro precheck (eval/tools/coyodex_eval/retro_precheck.py:198); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C110's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C109 reads E1",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:1851",
   "detail": "From: C109 = Process scorecard (eval/tools/coyodex_eval/process_scorecard.py:3318); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C109's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C83 reads E1",
   "anchor": "tools/coyodex/viewer/serve.py:509",
   "detail": "From: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883); To: E1 = Project map (tools/coyodex/model.py:761)",
   "why_risky": "domain-model ownership edge — verify C83's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C80 writes E39",
   "anchor": "tools/coyodex/views.py:1282",
   "detail": "From: C80 = Map rendering (tools/coyodex/views.py:299); To: E39 = Graph (tools/coyodex/viewer/build_graph.py:116)",
   "why_risky": "domain-model ownership edge — verify C80's code actually 'writes' E39; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C61 reads E40",
   "anchor": "tools/coyodex/viewer/viewer.js:1265",
   "detail": "From: C61 = Element cards (tools/coyodex/viewer/viewer.js:1265); To: E40 = Graph box (tools/coyodex/viewer/build_graph.py:29)",
   "why_risky": "domain-model ownership edge — verify C61's code actually 'reads' E40; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C60 reads E40",
   "anchor": "tools/coyodex/viewer/viewer.js:2818",
   "detail": "From: C60 = Map loader (tools/coyodex/viewer/viewer.js:172); entry points: Opening an interface's address shows who is on the far side and what crosses. (tools/coyodex/viewer/viewer.js:12679); Opening an element's address shows everything the map holds about that one thing. (tools/coyodex/viewer/viewer.js:13011); Opening a feature's address shows that feature's page and its use cases. (tools/coyodex/viewer/viewer.js:13016); Opening an actor's address shows what that person or program does, in order. (tools/coyodex/viewer/viewer.js:13042); Opening a collection's address lists that collection's rows in full. (tools/coyodex/viewer/viewer.js:13059); Opening a rule's address shows that decision and where the code makes it. (tools/coyodex/viewer/viewer.js:13083); Opening a subsystem's address draws that subsystem and its neighbours. (tools/coyodex/viewer/viewer.js:6492); Opening a use case's address draws that use case's numbered steps. (tools/coyodex/viewer/viewer.js:6503); To: E40 = Graph box (tools/coyodex/viewer/build_graph.py:29)",
   "why_risky": "domain-model ownership edge — verify C60's code actually 'reads' E40; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "E1 (Project map) is stored in D91 container 'project-map.json' (collection)",
   "anchor": "tools/coyodex/model.py:761",
   "detail": null,
   "why_risky": "the persistence inventory hangs on this row — a wrong dep/container mis-answers 'what is persisted where?' for every reader.",
   "theme": "persistence",
   "drift_eligible": false
  },
  {
   "claim": "E55 (Map profile) is stored in D91 container 'profile.json' (collection)",
   "anchor": "eval/tools/coyodex_eval/profile.py:39",
   "detail": null,
   "why_risky": "the persistence inventory hangs on this row — a wrong dep/container mis-answers 'what is persisted where?' for every reader.",
   "theme": "persistence",
   "drift_eligible": false
  },
  {
   "claim": "E56 (Reconcile file) is stored in D91 container 'reconcile.json' (collection)",
   "anchor": "tools/coyodex/reconcile.py:215",
   "detail": null,
   "why_risky": "the persistence inventory hangs on this row — a wrong dep/container mis-answers 'what is persisted where?' for every reader.",
   "theme": "persistence",
   "drift_eligible": false
  },
  {
   "claim": "I8 'Project source' is an interface the product exchanges data through with Git",
   "anchor": "tools/coyodex/impact_git.py:50",
   "detail": "the walks carry in through it",
   "why_risky": "whose data crosses is not visible at the call site — read what this service actually holds or returns, and refute the row if the data is the product's own.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I9 'Code editor' is an interface the product exchanges data through with Code editor",
   "anchor": "tools/coyodex/viewer/viewer.js:14925",
   "detail": "the walks carry out through it",
   "why_risky": "whose data crosses is not visible at the call site — read what this service actually holds or returns, and refute the row if the data is the product's own.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I10 'Code host' is an interface the product exchanges data through with GitHub",
   "anchor": "tools/coyodex/viewer/viewer.js:14928",
   "detail": "the walks carry out through it",
   "why_risky": "whose data crosses is not visible at the call site — read what this service actually holds or returns, and refute the row if the data is the product's own.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I11 'Coding agent chat logs' is an interface the product exchanges data through with Coding agent chat logs",
   "anchor": "tools/map_backup.py:130",
   "detail": "the walks carry in through it",
   "why_risky": "whose data crosses is not visible at the call site — read what this service actually holds or returns, and refute the row if the data is the product's own.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I1 'Command line': R1 'Map owner' is on its far side",
   "anchor": "tools/coyodex/cli.py:168",
   "detail": "derived, brought by UC1, UC2, UC3, UC4, UC7, UC8, UC9; anchored at a way in their own use case drives",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I2 'Method quality command line': R3 'Method author' is on its far side",
   "anchor": "eval/tools/coyodex_eval/cli.py:75",
   "detail": "derived, brought by UC10; anchored at a way in their own use case drives",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I3 'Map viewer': R1 'Map owner' is on its far side",
   "anchor": null,
   "detail": "derived, brought by UC4; anchored at nothing anchorable",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I3 'Map viewer': R2 'Map reader' is on its far side",
   "anchor": "tools/coyodex/viewer/serve.py:640",
   "detail": "derived, brought by UC5, UC6; anchored at a way in their own use case drives",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I4 'Build skill': R1 'Map owner' is on its far side",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": "derived, brought by UC1, UC2, UC7, UC8, UC9; anchored at a way in their own use case drives",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I5 'Developer skills': R1 'Map owner' is on its far side",
   "anchor": "Makefile:17",
   "detail": "derived, brought by UC1; anchored at the dependency standing on this surface",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I5 'Developer skills': R3 'Method author' is on its far side",
   "anchor": "eval/SKILL.md:28",
   "detail": "derived, brought by UC10, UC11; anchored at a way in their own use case drives",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I6 'Map folder': R1 'Map owner' is on its far side",
   "anchor": "tools/coyodex/assemble.py:978",
   "detail": "derived, brought by UC7; anchored at the dependency standing on this surface",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I7 'Settings': R1 'Map owner' is on its far side",
   "anchor": "tools/coyodex/assemble.py:978",
   "detail": "derived, brought by UC3; anchored at the dependency standing on this surface",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I9 'Code editor': R2 'Map reader' is on its far side",
   "anchor": "tools/coyodex/viewer/viewer.js:14925",
   "detail": "derived, brought by UC6; anchored at the dependency standing on this surface",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I10 'Code host': R2 'Map reader' is on its far side",
   "anchor": "tools/coyodex/viewer/viewer.js:14928",
   "detail": "derived, brought by UC6; anchored at the dependency standing on this surface",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "I12 'Score and review reports': R3 'Method author' is on its far side",
   "anchor": null,
   "detail": "derived, brought by UC11; anchored at nothing anchorable",
   "why_risky": "DERIVED, so nothing else checks it: no field states this and the map will draw whoever the join produces. A wrong one puts a person at a surface only a program reaches, or hides that a surface hands something to somebody.",
   "theme": "interface",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 1: R1 → I4 — ask the coding agent for a map of this project",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 2: I4 → C134 [in] — point the agent at the coyodex clone and the one document to read",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 4: C130 → C126 — read the method end to end and plan the build as waves of helper agents",
   "anchor": "method/dispatch.md:83",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 5: C126 → C45 — size each helper's slice from a survey of the project's code",
   "anchor": "method.md:1769",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 6: C45 → I8 [in] — count the files, lines and symbols of the project's own code",
   "anchor": "tools/coyodex/preindex.py:557",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 7: C126 → C26 — fill each helper's brief from the shared contract",
   "anchor": "method.md:1750",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 8: C126 → C5 — make each helper self-check its own piece before handing it in",
   "anchor": "method.md:1784",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 9: C126 → C47 — turn the path rules into one assignment for every mapped thing",
   "anchor": "method.md:2049",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 10: C47 → E56 [out] — record where every mapped thing belongs before the merge",
   "anchor": "tools/coyodex/reconcile_build.py:480",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 11: C126 → C4 — merge every returned piece into one map",
   "anchor": "method.md:2033",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 12: C4 → E65 [out] — gather every piece the helpers wrote, and name the ones that failed",
   "anchor": "tools/coyodex/assemble.py:298",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 13: C4 → E1 [out] — write the merged map into the project's map folder",
   "anchor": "tools/coyodex/assemble.py:978",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 14: C126 → C127 — hand the finished map to fresh agents that try to disprove it",
   "anchor": "method.md:2149",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 15: C127 → C128 — hand the corrected map to the closing sequence",
   "anchor": "method.md:2650",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 16: C128 → C25 — run the closing steps in order and stop at the first failure",
   "anchor": "method.md:2656",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 17: C25 → C22 — turn the skeptics' votes into the map's honesty record",
   "anchor": "tools/coyodex/ship.py:320",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 18: C22 → E37 [out] — write how much of the map was challenged and how the votes fell",
   "anchor": "tools/coyodex/grounding.py:1749",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 19: C25 → C27 — stamp which session built the map, and when",
   "anchor": "tools/coyodex/ship.py:323",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 21: C128 → I4 [out] — name the finished map's files and the address that opens it",
   "anchor": "method.md:3104",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 step 22: I4 → R1 — hand back the finished map and the checks it passed",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 step 1: R1 → I4 — ask what the newer code did to the map",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 step 2: I4 → C134 [in] — point the coding agent at the method in the coyodex clone",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 step 4: C130 → C131 — read the change-impact instructions and follow them",
   "anchor": "method/dispatch.md:167",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 step 5: C131 → E1 [in] — read the commit the map is pinned to",
   "anchor": "method/change-impact.md:41",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 step 6: C131 → I8 [in] — diff the pinned commit against the working tree, untracked files included",
   "anchor": "method/change-impact.md:57",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 step 7: C131 → I8 [in] — read the changed code and classify each change as modified, added or deleted",
   "anchor": "method/change-impact.md:63",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 step 8: C131 → E1 [in] — follow the map's arrows out to every element the change reaches",
   "anchor": "method/change-impact.md:64",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 step 9: C131 → E46 [out] — write the report, carrying the exact new text of every element it touches",
   "anchor": "method/change-impact.md:84",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 step 10: C131 → I6 [out] — leave the report on disk beside the map",
   "anchor": "method/change-impact.md:13",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 step 11: I6 → R1 — name every part of the map the change touched, and what each should now say",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 1: R1 → I4 — agree the report is right and ask to fold it into the map",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 2: I4 → C134 [in] — point the coding agent at the method in the coyodex clone",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 4: C130 → C131 — read the accept steps and follow them",
   "anchor": "method/dispatch.md:170",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 5: C131 → E46 [in] — read back the new text the report already carries for every element it touches",
   "anchor": "method/change-impact.md:80",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 6: C131 → C23 — apply each was-to-now block to the stored map",
   "anchor": "method/change-impact.md:123",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 7: C23 → E1 [out] — write the corrected rows back into the stored map",
   "anchor": "tools/coyodex/fix.py:119",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 8: C131 → C27 — stamp this accept session and re-pin the map to the code commit",
   "anchor": "method/change-impact.md:125",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 9: C27 → I8 [in] — read the commit the code now stands at",
   "anchor": "tools/coyodex/provenance.py:217",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 10: C131 → I4 [out] — ask whether to commit the code first, or pin the map as dirty",
   "anchor": "method/change-impact.md:129",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 11: I4 → R1 — put the two choices in front of the person",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 12: R1 → I4 — commit the code, then say to go on",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 13: I4 → C131 [in] — carry the answer back to the accept steps",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 14: C27 → I6 [out] — write the build stamp beside the map",
   "anchor": "tools/coyodex/provenance.py:232",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 15: C131 → C45 — rebuild the code survey at the new pin",
   "anchor": "method/change-impact.md:133",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 16: C45 → I8 [in] — list the project's tracked and untracked files",
   "anchor": "tools/coyodex/preindex_lib.py:190",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 17: C45 → I6 [out] — write the code survey beside the map",
   "anchor": "tools/coyodex/preindex.py:604",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 19: C131 → I6 [out] — commit the map, its readable view, the code survey and the report together",
   "anchor": "method/change-impact.md:138",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 20: C131 → I4 [out] — hand back the address that opens this project's map",
   "anchor": "method/change-impact.md:140",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 step 21: I4 → R1 — give the person the address of the re-pinned map",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 1: R1 → I7 — write one ignore pattern per line beside the map",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 2: I7 → C46 [in] — hand over the pattern lines in the order they were written",
   "anchor": "tools/coyodex/ignorefile.py:195",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 3: R1 → I1 — ask for the briefing that says what will be read",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 4: I1 → C26 [in] — carry the request to describe the tree, and nothing else",
   "anchor": "tools/coyodex/scope.py:172",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 5: C26 → C45 — walk the project's files, the same walk the survey and the checks use",
   "anchor": "tools/coyodex/scope.py:74",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 6: C45 → C46 — ask which rule decides each file the walk found",
   "anchor": "tools/coyodex/preindex_lib.py:242",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 7: C45 → E50 [out] — record which files stayed and how many each rule took out",
   "anchor": "tools/coyodex/preindex_lib.py:249",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 8: C26 → C46 — ask what each pattern took out, for the briefing",
   "anchor": "tools/coyodex/scope.py:131",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 9: C26 → I1 [out] — print the file count and every pattern beside what it took out",
   "anchor": "tools/coyodex/scope.py:132",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 10: I1 → R1 — show the narrowed tree before any code is read",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 11: C45 → I6 [out] — write the per-pattern counts into the code survey beside the map",
   "anchor": "tools/coyodex/preindex.py:604",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 12: R1 → I1 — ask for the map's own checks to run",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 13: I1 → C6 [in] — carry the check request, whether the cheap pass or the full one",
   "anchor": "tools/coyodex/cli.py:156",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 14: C6 → C45 — walk the tree again instead of trusting the code survey",
   "anchor": "tools/coyodex/validate_analysis.py:324",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 15: C6 → C46 — ask what each pattern decided on that second walk",
   "anchor": "tools/coyodex/validate_analysis.py:328",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 16: C6 → I1 [out] — name every pattern that decided nothing on this tree",
   "anchor": "tools/coyodex/validate_analysis.py:337",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 step 17: I1 → R1 — show what the map was told to leave out, on every check run",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 1: R1 → I4 — ask in plain language to move, split or rename part of the map",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 2: I4 → C134 [in] — point the agent at the clone and at the one document that decides the mode",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 4: R1 → I1 — run the map commands the method names",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 5: I1 → C24 [in] — carry the request to read one element's stored record",
   "anchor": "tools/coyodex/dump.py:322",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 6: C24 → E1 [in] — read the stored record of the element the person named",
   "anchor": "tools/coyodex/dump.py:138",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 7: I1 → C48 [in] — carry the request for a proposed split of one crowded screen",
   "anchor": "tools/coyodex/cli.py:183",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 8: C48 → I1 [out] — print a proposed grouping for a screen that holds too many boxes",
   "anchor": "tools/coyodex/balance.py:281",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 9: I1 → R1 — show the split as a starting point, never as ready to apply",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 10: I1 → C23 [in] — carry one addressed row rewrite, refusing an address that matches none or many",
   "anchor": "tools/coyodex/fix.py:1961",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 11: C23 → C4 — merge the harvest pieces first, so an edit that would mint or retire an id is refused",
   "anchor": "tools/coyodex/fix.py:1436",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 12: C23 → E1 [out] — rewrite the named rows in the piece that authored them",
   "anchor": "tools/coyodex/fix.py:1668",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 13: C23 → C47 — repoint every use case step that rode an arrow the change removed",
   "anchor": "tools/coyodex/fix.py:480",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 14: C23 → E36 [out] — note under its own heading the warning the person judged acceptable",
   "anchor": "tools/coyodex/record.py:101",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 15: C23 → I1 [out] — name every field that changed, old value then new",
   "anchor": "tools/coyodex/fix.py:1674",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 step 16: I1 → R1 — show what changed before the checks run",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 1: R3 → I5 — ask for a score on the change to the instructions",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 2: I5 → C135 [in] — hand the agent the scoring recipe to follow",
   "anchor": "eval/SKILL.md:28",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 3: R3 → I2 — type the eval commands the recipe names, over the two maps",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 4: I2 → C100 [in] — route each typed sub-command to the checker that answers it",
   "anchor": "eval/tools/coyodex_eval/cli.py:65",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 5: C100 → C101 — reduce each map to the numbers two builds can be compared on",
   "anchor": "eval/tools/coyodex_eval/cli.py:72",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 6: C101 → E1 [in] — read the built map and count what it holds",
   "anchor": "eval/tools/coyodex_eval/profile.py:595",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 7: C100 → C104 — hand over the doubters' votes and the scorers' marks",
   "anchor": "eval/tools/coyodex_eval/cli.py:84",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 8: C104 → C103 — fold the votes and the marks into one quality report",
   "anchor": "eval/tools/coyodex_eval/run.py:394",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 9: C100 → C105 — list the relations the rebuild dropped while the code still makes them",
   "anchor": "eval/tools/coyodex_eval/cli.py:93",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 10: C100 → C104 — run one scoring pass over the frozen candidate map",
   "anchor": "eval/tools/coyodex_eval/cli.py:75",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 11: C104 → E55 [in] — read the accepted map's stored numbers",
   "anchor": "eval/tools/coyodex_eval/run.py:78",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 12: C104 → C102 — check the fresh map's numbers against the accepted one's",
   "anchor": "eval/tools/coyodex_eval/run.py:67",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 13: C102 → C104 — return the verdict: as good, drifted, or worse",
   "anchor": "eval/tools/coyodex_eval/compare.py:518",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 14: C104 → E55 [out] — file the fresh map's numbers with the run",
   "anchor": "eval/tools/coyodex_eval/run.py:166",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 15: C104 → I2 [out] — print the verdict with the gates and bands that moved",
   "anchor": "eval/tools/coyodex_eval/run.py:274",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 step 16: I2 → R3 — show the verdict and where the run was filed",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 1: R3 → I5 — ask for a review of the build that just finished",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 2: I5 → C136 [in] — point the reviewer at the review recipe and its preconditions",
   "anchor": "eval/retro/SKILL.md:28",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 3: C136 → C110 — refuse the review while anything is still writing the map",
   "anchor": "eval/retro/method.md:62",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 4: C110 → E1 [in] — parse the stored map to catch one a build is still writing",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:185",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 5: C110 → I11 [in] — look for another chat still being appended to",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:216",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 6: C136 → C111 — ask the code history whether the last review's finished rows really shipped",
   "anchor": "eval/retro/method.md:221",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 7: C136 → C107 — cut the build's chat into one turn per model answer",
   "anchor": "eval/retro/method.md:421",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 8: C107 → I11 [in] — read the build's own chat file",
   "anchor": "eval/tools/coyodex_eval/transcript.py:328",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 9: C136 → C108 — price the build in minutes and tokens per row of map",
   "anchor": "eval/retro/method.md:444",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 10: C108 → I11 [in] — read every helper's chat file beside the lead's",
   "anchor": "eval/tools/coyodex_eval/cost.py:272",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 11: C136 → C109 — score the run against the method's checked rules",
   "anchor": "eval/retro/method.md:419",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 12: C109 → C107 — take the run's turns from the reader that groups them",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:3335",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 13: C109 → E1 [in] — load the finished map for the checks whose subject is the map",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:1851",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 14: C109 → I12 [out] — write the run's score sheet into the reports folder",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:3592",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 15: I12 → R3 — leave the score sheet where the method author reads it",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 16: C136 → C107 — print one slice of the chat for each fresh reader",
   "anchor": "eval/retro/method.md:562",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 17: C136 → I5 [out] — hand over the findings, the proposals and what could not be checked",
   "anchor": "eval/retro/method.md:814",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 step 18: I5 → R3 — put the finished review in front of the method author",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 1: R1 → I1 — run `make install` in the cloned coyodex folder",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 2: I1 → C20 [in] — create the folder-local Python environment and install the map tool into it",
   "anchor": "Makefile:40",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 3: I1 → C134 [in] — copy the skill pointer into every coding agent's skills folder, with this clone's path written into it",
   "anchor": "Makefile:74",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 4: C134 → I4 [out] — point the coding agent at the method's entry document inside this clone",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 5: I4 → R1 — give the person's coding agent a `/coyodex` command it can run",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 6: R1 → I1 — run `make install-dev` to add the two developer skills as well",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 7: I1 → C135 [in] — copy the map-scoring instructions into every skills folder",
   "anchor": "Makefile:86",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 8: C135 → I5 [out] — point the coding agent at the scoring recipe inside this clone",
   "anchor": "eval/SKILL.md:28",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 9: I1 → C136 [in] — copy the build-review instructions into every skills folder",
   "anchor": "Makefile:98",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 10: C136 → I5 [out] — point the coding agent at the build-review recipe inside this clone",
   "anchor": "eval/retro/SKILL.md:28",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 step 11: I5 → R1 — give the coding agent the `/coyodex-eval` and `/coyodex-retro` commands",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 1: R1 → I1 — run `coyodex serve` to put every mapped project on one page",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 2: I1 → C20 [in] — hand the typed command, its port and its options to the tool",
   "anchor": "tools/coyodex/cli.py:139",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 3: C20 → C83 — start the local map server on the requested port",
   "anchor": "tools/coyodex/cli.py:168",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 4: C83 → C85 — read the project folders remembered from earlier sessions",
   "anchor": "tools/coyodex/viewer/serve.py:883",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 5: C83 → E1 [in] — read each remembered folder's map for its title, goal and pinned commit",
   "anchor": "tools/coyodex/viewer/serve.py:123",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 6: C83 → I1 [out] — report how many projects are being served and the address to open",
   "anchor": "tools/coyodex/viewer/serve.py:889",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 7: I1 → R1 — show the address the maps are served at, and how to stop the server",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 8: C83 → I3 [out] — open the landing page in the person's browser",
   "anchor": "tools/coyodex/viewer/serve.py:895",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 9: I3 → C83 [in] — carry the landing page's request for the remembered projects",
   "anchor": "tools/coyodex/viewer/serve.py:642",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 10: C83 → C85 — re-read the remembered folders, so a project mapped since startup appears without a restart",
   "anchor": "tools/coyodex/viewer/serve.py:675",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 11: C83 → I3 [out] — return one card per remembered project, saying which of their maps can be opened",
   "anchor": "tools/coyodex/viewer/serve.py:677",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 step 12: I3 → R1 — list every mapped project on one page, each one openable or removable",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 2: R2 → I3 — pick the Features tab",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 3: I3 → C70 [in] — pass the chosen tab to the page frame",
   "anchor": "tools/coyodex/viewer/viewer.html:118",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 4: C70 → C65 — record the new screen and write its address",
   "anchor": "tools/coyodex/viewer/viewer.js:5116",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 5: C65 → C66 — draw the product description and one card per feature",
   "anchor": "tools/coyodex/viewer/viewer.js:13006",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 6: C66 → C61 — build one card for each feature",
   "anchor": "tools/coyodex/viewer/viewer.js:10919",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 7: C61 → E40 [in] — read each box's name, its type and its one sentence",
   "anchor": "tools/coyodex/viewer/viewer.js:1265",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 8: C66 → I3 [out] — put the feature cards on the page",
   "anchor": "tools/coyodex/viewer/viewer.js:10934",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 9: I3 → R2 — show what the product does, feature by feature",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 10: R2 → I3 — click one feature's card",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 11: I3 → C61 [in] — pass the feature's address to the cards",
   "anchor": "tools/coyodex/viewer/viewer.js:13016",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 12: C61 → C65 — open that feature's own page",
   "anchor": "tools/coyodex/viewer/viewer.js:1401",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 13: C65 → C66 — list the feature's use cases and the people who drive them",
   "anchor": "tools/coyodex/viewer/viewer.js:8719",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 14: R2 → I3 — drill from one use case into its numbered steps",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 15: I3 → C62 [in] — pass the use case's address to the canvas",
   "anchor": "tools/coyodex/viewer/viewer.js:6503",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 16: C62 → I3 [out] — draw the use case's flow, one box per step",
   "anchor": "tools/coyodex/viewer/viewer.js:13104",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 17: I3 → R2 — show every step of that feature, in order",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 18: R2 → I3 — click one box on the picture",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 19: I3 → C63 [in] — pass the box click to the click rules",
   "anchor": "tools/coyodex/viewer/viewer.js:5895",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 20: C63 → C64 — show that box's card in the info pane",
   "anchor": "tools/coyodex/viewer/viewer.js:4158",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 21: C64 → I3 [out] — put the box's card beside the picture",
   "anchor": "tools/coyodex/viewer/viewer.js:2881",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 step 22: I3 → R2 — show one box's name, type, sentence and code link",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 2: R2 → I3 — open one box's own page",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 3: I3 → C60 [in] — pass the element's address to the page",
   "anchor": "tools/coyodex/viewer/viewer.js:13011",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 4: C60 → E40 [in] — read that box's name, file and line",
   "anchor": "tools/coyodex/viewer/viewer.js:2818",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 5: C60 → I3 [out] — render the box's page with its code link",
   "anchor": "tools/coyodex/viewer/viewer.js:2837",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 6: I3 → R2 — show everything the map holds about that one box",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 7: R2 → I3 — pick the code link on the box",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 8: I3 → C69 [in] — pass the picked code link to the source column",
   "anchor": "tools/coyodex/viewer/viewer.js:223",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 9: C69 → C83 — fetch that file's text at the map's own commit",
   "anchor": "tools/coyodex/viewer/viewer.js:14537",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 10: C83 → C69 — return the file exactly as the pinned commit holds it",
   "anchor": "tools/coyodex/viewer/serve.py:797",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 11: C69 → I3 [out] — show the file, scrolled to the box's line",
   "anchor": "tools/coyodex/viewer/viewer.js:14396",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 12: I3 → R2 — show the code behind the box, beside the map",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 13: R2 → I3 — press the open button on the file's header",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 14: I3 → C69 [in] — pass the open request to the source column",
   "anchor": "tools/coyodex/viewer/viewer.js:13930",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 15: C69 → I9 [out] — open the file at that line in the reader's own editor",
   "anchor": "tools/coyodex/viewer/viewer.js:14925",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 16: I9 → R2 — put the reader in their editor, at the mapped line",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 17: C69 → I10 [out] — open the file on the code host at the pinned commit",
   "anchor": "tools/coyodex/viewer/viewer.js:14928",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 step 18: I10 → R2 — show the file on the web, pinned to the map's commit",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF1 step 1: C134 → C130 — read the dispatcher end to end",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF1 step 2: C130 → C20 — run every coyodex command from the clone's own command line",
   "anchor": "method/dispatch.md:4",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF1 step 3: C20 → C26 — print the build briefing before anything else runs",
   "anchor": "tools/coyodex/cli.py:180",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF1 step 4: C26 → I8 [in] — read the project's own files to see what the build will cover",
   "anchor": "tools/coyodex/scope.py:74",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF1 step 5: C26 → C46 — drop every path the project's own ignore list names",
   "anchor": "tools/coyodex/scope.py:131",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF1 step 6: C26 → I1 [out] — print what will be read and what the map will be pinned to",
   "anchor": "tools/coyodex/scope.py:172",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF1 step 7: I1 → R1 — show the person the briefing before any work starts",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF1 step 8: C130 → I6 [in] — look for a map already in the project's map folder",
   "anchor": "method/dispatch.md:75",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF2 step 1: C20 → C6 — run every name, link, story and record check over the map",
   "anchor": "tools/coyodex/cli.py:156",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF2 step 2: C6 → E1 [in] — read the stored map back and refuse one whose shape is wrong",
   "anchor": "tools/coyodex/validate_model.py:6585",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF2 step 3: C6 → I8 [in] — open every cited file and line to prove the code links exist",
   "anchor": "tools/coyodex/validate_model.py:6265",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF2 step 4: C20 → C21 — make the map's stories and its arrows refute each other",
   "anchor": "tools/coyodex/cli.py:159",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF2 step 5: C20 → C80 — render the map's readable view",
   "anchor": "tools/coyodex/cli.py:165",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF2 step 6: C80 → I6 [out] — write the readable view beside the stored map",
   "anchor": "tools/coyodex/viewer/render.py:53",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 1: R2 → I3 — open the map server's home address",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 2: I3 → C83 [in] — pass the browser's request to the map server",
   "anchor": "tools/coyodex/viewer/serve.py:640",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 3: C83 → C85 — re-read the project folders this machine remembers",
   "anchor": "tools/coyodex/viewer/serve.py:675",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 4: C83 → I3 [out] — list every remembered project with its title and its commit",
   "anchor": "tools/coyodex/viewer/serve.py:677",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 5: I3 → R2 — show one card per remembered project",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 6: R2 → I3 — pick a project card",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 7: I3 → C83 [in] — pass the chosen project's address to the server",
   "anchor": "tools/coyodex/viewer/serve.py:730",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 8: C83 → C70 — send back the page frame every map is drawn with",
   "anchor": "tools/coyodex/viewer/serve.py:730",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 9: C70 → C60 — start the script that loads the map",
   "anchor": "tools/coyodex/viewer/viewer.html:359",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 10: C60 → C83 — ask the server for this map's whole drawing data",
   "anchor": "tools/coyodex/viewer/viewer.js:172",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 11: C83 → E1 [in] — read the stored project map",
   "anchor": "tools/coyodex/viewer/serve.py:509",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 12: C83 → C80 — turn the stored map into the graph the browser draws",
   "anchor": "tools/coyodex/viewer/serve.py:511",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 13: C80 → E39 [out] — assemble the graph, one box per element",
   "anchor": "tools/coyodex/views.py:1282",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 14: C83 → C82 — pre-render every diagram, timeline and flow of this map",
   "anchor": "tools/coyodex/viewer/serve.py:516",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 15: C83 → C60 — return the finished drawing data",
   "anchor": "tools/coyodex/viewer/serve.py:752",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 16: C60 → C62 — draw the screen the address names",
   "anchor": "tools/coyodex/viewer/viewer.js:15892",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 17: C62 → I3 [out] — put the drawn map on the page",
   "anchor": "tools/coyodex/viewer/viewer.js:13104",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "SF20 step 18: I3 → R2 — show the reader the product's map",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC1 Install coyodex into a coding agent: Someone who has cloned this project asks to install it. Their coding agent gains a new command, and the tools it drives are built and ready.",
   "anchor": "Makefile:40",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC2 Build a project's first map: A person asks their coding agent for a map of a project that has none. A map of that project exists, pinned to the commit it describes.",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC3 Leave code off the map: A person names committed code the map is not meant to describe. Later maps leave that code out, and every count the tools print says so.",
   "anchor": "tools/coyodex/ignorefile.py:195",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC4 Serve the maps on this machine: A person starts the local map server. Every project they have mapped is listed on one page in their browser.",
   "anchor": "tools/coyodex/cli.py:139",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC5 Explore a project's map: A person opens a project's map. They see what the product does and drill from one feature down to a single box.",
   "anchor": "tools/coyodex/viewer/viewer.html:118",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC6 Open the code behind a box: A reader picks the code link on a box. The file opens at that line, in their own editor or on the code host.",
   "anchor": "tools/coyodex/viewer/viewer.js:13011",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC7 See what a code change did to the map: The code has moved on since the map was pinned. A report names the parts of the map the change touched.",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC8 Fold a change report into the baseline: A person agrees the change report is right. The map is edited to match, re-pinned, and checked again.",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC9 Change the map by asking: A person asks in plain language for a part of the map to move, split or be renamed. The map is edited in place and passes its checks.",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC10 Score whether the method got better: Someone changed the instructions and wants to know the cost. Two maps of one project are scored and compared, with a verdict.",
   "anchor": "eval/SKILL.md:28",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "UC11 Review a finished build: A build has finished and its chat is on disk. A report names the friction, the bugs and the gaps that run revealed.",
   "anchor": "eval/retro/SKILL.md:28",
   "detail": null,
   "why_risky": "the headline sentence of the behavioural layer — a reader takes the trigger and the outcome away as what the product does.",
   "theme": "behaviour",
   "drift_eligible": false
  },
  {
   "claim": "Entry point [poller] A map page opened in developer mode asks the server every second whether the viewer's files changed. runs on cadence 'every 1s'",
   "anchor": "tools/coyodex/viewer/serve.py:222",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [startup-hook] Starting the server reads the remembered folders and loads every map still found there. runs on cadence 'on-boot'",
   "anchor": "tools/coyodex/viewer/serve.py:885",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [poller] The viewer's code changed on disk, so the map server restarts. runs on cadence 'every 1s'",
   "anchor": "tools/devserve.py:91",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Component C1 (Map model) is described as: Defines every kind of thing a map can hold. Reads a map file back and refuses one whose shape is wrong.",
   "anchor": "tools/coyodex/model.py:1153",
   "detail": "declared at tools/coyodex/model.py:1153; files: tools/coyodex/model.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C2 (Shared map grammar) is described as: Holds the shared words and formats every other part of the map reads.",
   "anchor": "tools/coyodex/grammar.py:21",
   "detail": "declared at tools/coyodex/grammar.py:21; files: tools/coyodex/grammar.py, tools/coyodex/anchors.py, tools/coyodex/records.py, tools/coyodex/pathmatch.py, tools/coyodex/pysrc.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C3 (Map schema document) is described as: Writes a machine-readable description of the map file, for editors and other tools to read.",
   "anchor": "tools/coyodex/json_schema.py:505",
   "detail": "declared at tools/coyodex/json_schema.py:505; files: tools/coyodex/json_schema.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C4 (Map assembly) is described as: Merges the pieces each build agent returned into one map. Refuses two pieces that name the same thing twice.",
   "anchor": "tools/coyodex/assemble.py:408",
   "detail": "declared at tools/coyodex/assemble.py:408; files: tools/coyodex/assemble.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C5 (Fragment self-check) is described as: Checks one agent's piece of the map before it is handed in. The agent that made a mistake is the one that fixes it.",
   "anchor": "tools/coyodex/lint_fragment.py:134",
   "detail": "declared at tools/coyodex/lint_fragment.py:134; files: tools/coyodex/lint_fragment.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C6 (Shape checks) is described as: Checks that every name in the map points at something real. Also checks that each code link is well formed and really exists.",
   "anchor": "tools/coyodex/validate_model.py:195",
   "detail": "declared at tools/coyodex/validate_model.py:195; files: tools/coyodex/validate_model.py, tools/coyodex/validate_analysis.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C7 (Story checks) is described as: Checks the use cases, their flows and the happy path against each other. Reports the parts of the code no story covers.",
   "anchor": "tools/coyodex/validate_model.py:405",
   "detail": "declared at tools/coyodex/validate_model.py:405; files: tools/coyodex/validate_model.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C8 (Rule checks) is described as: Checks that each business rule names real code lines. Also writes down which files the map says guard access.",
   "anchor": "tools/coyodex/validate_model.py:1748",
   "detail": "declared at tools/coyodex/validate_model.py:1748; files: tools/coyodex/validate_model.py, tools/coyodex/access_surface.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C9 (Wiring checks) is described as: Checks how the product connects: its interfaces, its ways in, the services it calls and the processes it runs in.",
   "anchor": "tools/coyodex/validate_model.py:2700",
   "detail": "declared at tools/coyodex/validate_model.py:2700; files: tools/coyodex/validate_model.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C10 (Data checks) is described as: Checks the saved records, their stores, who owns each data area, and how much of the map was verified.",
   "anchor": "tools/coyodex/validate_model.py:4205",
   "detail": "declared at tools/coyodex/validate_model.py:4205; files: tools/coyodex/validate_model.py, tools/coyodex/areas.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C100 (Eval command line) is described as: Routes each typed eval sub-command to the checker that answers it. Loads that checker only when it is asked for.",
   "anchor": "eval/tools/coyodex_eval/cli.py:69",
   "detail": "declared at eval/tools/coyodex_eval/cli.py:69; files: eval/tools/coyodex_eval/cli.py, eval/tools/coyodex_eval/__init__.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C101 (Map profile) is described as: Reduces one built map to the numbers two builds can be compared on. Two maps of one project never share wording, so counts are what survives.",
   "anchor": "eval/tools/coyodex_eval/profile.py:430",
   "detail": "declared at eval/tools/coyodex_eval/profile.py:430; files: eval/tools/coyodex_eval/profile.py, eval/tools/coyodex_eval/legacy_map.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C102 (Baseline comparison) is described as: Judges a fresh map against the accepted one. Returns one word: as good, drifted, or worse.",
   "anchor": "eval/tools/coyodex_eval/compare.py:518",
   "detail": "declared at eval/tools/coyodex_eval/compare.py:518; files: eval/tools/coyodex_eval/compare.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C103 (Judge report) is described as: Folds many doubters' votes and many scorers' marks into one quality report. Never talks to a model itself, so the arithmetic can be tested alone.",
   "anchor": "eval/tools/coyodex_eval/judge.py:257",
   "detail": "declared at eval/tools/coyodex_eval/judge.py:257; files: eval/tools/coyodex_eval/judge.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C104 (Eval run) is described as: Runs one scoring pass end to end and files everything it produced. Can then promote that run to be the accepted baseline.",
   "anchor": "eval/tools/coyodex_eval/run.py:69",
   "detail": "declared at eval/tools/coyodex_eval/run.py:69; files: eval/tools/coyodex_eval/run.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C105 (Lost arrows) is described as: Finds the relations a rebuild dropped. Re-reads each dropped relation's recorded line to say whether the code still does it.",
   "anchor": "eval/tools/coyodex_eval/arrows.py:152",
   "detail": "declared at eval/tools/coyodex_eval/arrows.py:152; files: eval/tools/coyodex_eval/arrows.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C106 (Rebuild archive) is described as: Moves a project's current map aside so the next build starts from nothing. Moves, never deletes: the old map is what the new one is compared against.",
   "anchor": "eval/tools/coyodex_eval/archive.py:110",
   "detail": "declared at eval/tools/coyodex_eval/archive.py:110; files: eval/tools/coyodex_eval/archive.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C107 (Transcript reader) is described as: Turns a build's raw chat log into one record per model answer. The log stores one answer as many rows, so counting rows gets every later number wrong.",
   "anchor": "eval/tools/coyodex_eval/transcript.py:442",
   "detail": "declared at eval/tools/coyodex_eval/transcript.py:442; files: eval/tools/coyodex_eval/transcript.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C108 (Build cost) is described as: Reports what one build spent in time and tokens. Divides both by how much map the build produced. Two builds of different sizes then read side by side.",
   "anchor": "eval/tools/coyodex_eval/cost.py:620",
   "detail": "declared at eval/tools/coyodex_eval/cost.py:620; files: eval/tools/coyodex_eval/cost.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C109 (Process scorecard) is described as: Scores whether the build agent behaved as the method says, one checked rule at a time. Reports numbers, never a pass or a fail.",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:3318",
   "detail": "declared at eval/tools/coyodex_eval/process_scorecard.py:3318; files: eval/tools/coyodex_eval/process_scorecard.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C110 (Retro precheck) is described as: Refuses a retrospective while the build it would read is still running. A build stamps who made it near the end, so mid-run the stamp still names the previous build.",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:198",
   "detail": "declared at eval/tools/coyodex_eval/retro_precheck.py:198; files: eval/tools/coyodex_eval/retro_precheck.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C111 (Ledger check) is described as: Asks the code history whether a retrospective's finished rows really shipped. A row saying it is still open while its named change is merged gets proposed again for nothing.",
   "anchor": "eval/tools/coyodex_eval/ledger.py:110",
   "detail": "declared at eval/tools/coyodex_eval/ledger.py:110; files: eval/tools/coyodex_eval/ledger.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C112 (Live number ledger) is described as: Re-measures every count the tools state about a map that keeps changing. A person writes the sentence twice, so nothing has to guess which sum produced the number.",
   "anchor": "eval/tools/coyodex_eval/live_numbers.py:540",
   "detail": "declared at eval/tools/coyodex_eval/live_numbers.py:540; files: eval/tools/coyodex_eval/live_numbers.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C113 (Skeptic recall test) is described as: Plants claims that are false by construction and keeps the answer key. Counts how many of them the doubters caught, which is a number that means something on its own.",
   "anchor": "eval/tools/coyodex_eval/mutate.py:236",
   "detail": "declared at eval/tools/coyodex_eval/mutate.py:236; files: eval/tools/coyodex_eval/mutate.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C45 (Code survey) is described as: Measures how big a project's code is before the map is drawn.",
   "anchor": "tools/coyodex/preindex.py:604",
   "detail": "declared at tools/coyodex/preindex.py:604; files: tools/coyodex/preindex.py, tools/coyodex/preindex_lib.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C46 (Ignore list) is described as: Lets a project declare which of its own code the map must leave out.",
   "anchor": "tools/coyodex/ignorefile.py:178",
   "detail": "declared at tools/coyodex/ignorefile.py:178; files: tools/coyodex/ignorefile.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C47 (Assignment rules) is described as: Records where every mapped thing belongs, so a rebuild keeps the decision.",
   "anchor": "tools/coyodex/reconcile.py:692",
   "detail": "declared at tools/coyodex/reconcile.py:692; files: tools/coyodex/reconcile.py, tools/coyodex/reconcile_build.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C48 (Diagram balance) is described as: Reports how crowded each screen of the map is.",
   "anchor": "tools/coyodex/balance.py:142",
   "detail": "declared at tools/coyodex/balance.py:142; files: tools/coyodex/balance.py, tools/coyodex/balance_lib.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C49 (Change impact) is described as: Works out which parts of the map a code change touches.",
   "anchor": "tools/coyodex/impact_git.py:285",
   "detail": "declared at tools/coyodex/impact_git.py:285; files: tools/coyodex/impact_lib.py, tools/coyodex/impact_ripple.py, tools/coyodex/impact_git.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C50 (Map and transcript backup) is described as: Bundles a finished map with the conversation that produced it.",
   "anchor": "tools/map_backup.py:391",
   "detail": "declared at tools/map_backup.py:391; files: tools/map_backup.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C51 (Live reload supervisor) is described as: Restarts the map server when someone editing the viewer changes its code.",
   "anchor": "tools/devserve.py:103",
   "detail": "declared at tools/devserve.py:103; files: tools/devserve.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C120 (Product description instructions) is described as: Tells the mapping agent to describe the product before reading code. It orders the goal, the shared words, the people and their goals. The one successful run through those goals comes last.",
   "anchor": "method.md:28",
   "detail": "declared at method.md:28; files: method.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C121 (Code inventory instructions) is described as: Says how to list what the product is made of, area by area. Every box gets a plain sentence, a code link and a confidence label. Outside services and the ways in are listed the same way.",
   "anchor": "method.md:414",
   "detail": "declared at method.md:414; files: method.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C122 (Outside edge instructions) is described as: Says how to name every place the product meets the outside world. Each place gets one word for what it is, and a side saying whose design it is. It also says how a story crosses one, in both directions.",
   "anchor": "method.md:506",
   "detail": "declared at method.md:506; files: method.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C123 (Data and step instructions) is described as: Says how to write the records the product keeps and the steps of each goal. A step names who acts, on what, and which way the data moves. Machinery shared by several goals is written once and referenced.",
   "anchor": "method.md:879",
   "detail": "declared at method.md:879; files: method.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C124 (Operations and decision instructions) is described as: Says how to record what runs, what is watched, what is configured and what the product decides. A decision is only worth recording when a product person could have chosen otherwise. Test coverage is measured against the map, never against line counts.",
   "anchor": "method.md:1068",
   "detail": "declared at method.md:1068; files: method.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C125 (Cross-cutting build instructions) is described as: Holds the rules that apply to every part of a build. It says how to write text a reader meets alone, with no page around it. It also sets the order of work, the tools to reach for, and how big one box may be.",
   "anchor": "method.md:1332",
   "detail": "declared at method.md:1332; files: method.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C126 (Fan-out phase instructions) is described as: Plans the parallel work of a build in three phases. Many agents gather the pieces, one agent merges them, then many agents trace the goals. It says how to size each slice, brief it and wait for the batch.",
   "anchor": "method.md:1728",
   "detail": "declared at method.md:1728; files: method.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C127 (Verification phase instructions) is described as: Plans what happens once every goal has been traced. Fresh agents try to disprove each claim the map makes, against the code. Two more passes write what the product decides and measure what the tests cover.",
   "anchor": "method.md:2152",
   "detail": "declared at method.md:2152; files: method.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C128 (Closing sequence instructions) is described as: Runs the closing steps that turn a finished map into a committed one. Corrections land, the record of what was checked is written, then the gates run. It ends by telling the reader where to open the map.",
   "anchor": "method.md:2651",
   "detail": "declared at method.md:2651; files: method.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C129 (Map model spec) is described as: States every field a map may hold and the exact shape of each. Names, code links and closed word lists are all fixed here. It also says how each part of the map is drawn on screen.",
   "anchor": "method/model.md:45",
   "detail": "declared at method/model.md:45; files: method/model.md, method/domain-cards.md, method/diagrams.md, method/project-map.schema.json",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C130 (Mode dispatch instructions) is described as: Decides which job to do when someone calls coyodex on a project. Build a first map, report a code change, or fold a report in. It briefs the person on what will be read before anything starts.",
   "anchor": "method/dispatch.md:3",
   "detail": "declared at method/dispatch.md:3; files: method/dispatch.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C131 (Change impact instructions) is described as: Says how to report what a code change does to an existing map. The report carries the exact new text for every box it touches. Folding the report in is then mechanical, with no fresh reading of code.",
   "anchor": "method/change-impact.md:3",
   "detail": "declared at method/change-impact.md:3; files: method/change-impact.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C132 (Fan-out worker contracts) is described as: Holds the briefs handed word for word to the agents a build fans out to. One brief per job, from gathering the pieces to disproving the finished claims. A shared file carries the writing rules every brief appends.",
   "anchor": null,
   "detail": "declared at method/templates/; files: method/templates/harvest-contract.md, method/templates/t5-addendum.md, method/templates/trace-contract.md, method/templates/doors-contract.md, method/templates/rules-contract.md, method/templates/skeptic-contract.md …",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C133 (Method regression checks) is described as: Holds one written promise per change to the method or the tools. Each file says what the next build should do differently, and what a backfire would look like. A review of a finished build runs every check the change range added.",
   "anchor": null,
   "detail": "declared at method/retro-checks/; files: method/retro-checks/README.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C134 (Build skill pointer) is described as: Points a coding agent at this repository so the map commands work. Kept deliberately thin, because a copy is installed into the agent and goes stale. It names the clone path and the one document to read.",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": "declared at skill/coyodex/SKILL.md:27; files: skill/coyodex/SKILL.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C135 (Map quality eval instructions) is described as: Says how to score two maps of the same code and tell which one is better. A map is scored on counts, well-formedness, coverage and a model's judgement. Both maps must describe the same code, or the comparison means nothing.",
   "anchor": "eval/method.md:8",
   "detail": "declared at eval/method.md:8; files: eval/SKILL.md, eval/method.md, eval/rubric.md, eval/README.md, eval/blind-build.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C136 (Build retrospective instructions) is described as: Says how to review one finished build from its map and its chat. The review reports what the run revealed about the tools and the method. It changes nothing, and a standing backlog carries every proposal forward.",
   "anchor": "eval/retro/method.md:4",
   "detail": "declared at eval/retro/method.md:4; files: eval/retro/SKILL.md, eval/retro/method.md, eval/retro/backlog.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C137 (Public documentation) is described as: Explains to a newcomer what coyodex is, why it exists and how to install it. A longer page walks through how a map gets built, phase by phase. Both are read on the web by a person, never by a mapping agent.",
   "anchor": "README.md:13",
   "detail": "declared at README.md:13; files: README.md, docs/how-coyodex-works.html",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C138 (Contributor guide) is described as: Says how to work on coyodex itself, for a person and for an agent alike. It names what each folder is for and which tests a given change needs. It also holds the design philosophy and the words this project uses.",
   "anchor": "CONTRIBUTING.md:48",
   "detail": "declared at CONTRIBUTING.md:48; files: CONTRIBUTING.md, CLAUDE.md, .github/PULL_REQUEST_TEMPLATE.md, .github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/idea.yml, .github/ISSUE_TEMPLATE/config.yml",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C139 (Method rationale record) is described as: Keeps the story of the build that made each method rule necessary. Storing those stories here keeps them out of the text every build agent reads. A test checks that each entry still quotes a rule that exists.",
   "anchor": "internal/docs/method-rationale.md:3",
   "detail": "declared at internal/docs/method-rationale.md:3; files: internal/docs/method-rationale.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C20 (Command line) is described as: Routes each typed coyodex command to the tool that runs it. The same layer decides how much of a long finding list a reader sees.",
   "anchor": "tools/coyodex/cli.py:139",
   "detail": "declared at tools/coyodex/cli.py:139; files: tools/coyodex/cli.py, tools/coyodex/subverb_help.py, tools/coyodex/reporting.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C21 (Map checks) is described as: Reads a finished map and reports where it contradicts itself. The same family flags a code link pointing at a line that cannot act.",
   "anchor": "tools/coyodex/audit_model.py:1695",
   "detail": "declared at tools/coyodex/audit_model.py:1695; files: tools/coyodex/audit_model.py, tools/coyodex/anchor_drift.py, tools/coyodex/prose.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C22 (Grounding record) is described as: Turns the skeptics' verdict files into the map's honesty record. The four counts a gate blocks on are derived, never hand-tallied.",
   "anchor": "tools/coyodex/grounding.py:1704",
   "detail": "declared at tools/coyodex/grounding.py:1704; files: tools/coyodex/grounding.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C23 (Map editor) is described as: Applies one mechanical correction to a stored map in place. An edit matching no row, or more than one, is refused rather than guessed.",
   "anchor": "tools/coyodex/fix.py:119",
   "detail": "declared at tools/coyodex/fix.py:119; files: tools/coyodex/fix.py, tools/coyodex/record.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C24 (Map reader) is described as: Reads a map and never changes it. Three answers: one element's stored record, what two maps differ on, and the evidence behind a claim.",
   "anchor": "tools/coyodex/dump.py:303",
   "detail": "declared at tools/coyodex/dump.py:303; files: tools/coyodex/dump.py, tools/coyodex/mapdiff.py, tools/coyodex/context.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C25 (Build closer) is described as: Runs a build's closing steps in order and stops at the first failure. The last step writes one pre-commit verdict to a file.",
   "anchor": "tools/coyodex/ship.py:439",
   "detail": "declared at tools/coyodex/ship.py:439; files: tools/coyodex/ship.py, tools/coyodex/finalize.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C26 (Build briefing) is described as: Says which files a build will read and which commit its map will name. A second command prints the exact brief each helper agent is sent.",
   "anchor": "tools/coyodex/scope.py:172",
   "detail": "declared at tools/coyodex/scope.py:172; files: tools/coyodex/scope.py, tools/coyodex/contract.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C27 (Build log) is described as: Records which session built a map, and when. Beside that it records how long each slice of the build took.",
   "anchor": "tools/coyodex/provenance.py:327",
   "detail": "declared at tools/coyodex/provenance.py:327; files: tools/coyodex/provenance.py, tools/coyodex/timings.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C80 (Map rendering) is described as: Turns a stored map into the readable file kept beside the code. Also builds the boxes and arrows the browser draws.",
   "anchor": "tools/coyodex/views.py:299",
   "detail": "declared at tools/coyodex/views.py:299; files: tools/coyodex/views.py, tools/coyodex/viewer/render.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C81 (Feature facts) is described as: Works out what each feature owns: its people, its decisions, its records and the doors reaching it.",
   "anchor": "tools/coyodex/features.py:410",
   "detail": "declared at tools/coyodex/features.py:410; files: tools/coyodex/features.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C82 (Diagram builder) is described as: Draws every diagram, timeline and flow of one map, ready for the browser page to show.",
   "anchor": "tools/coyodex/viewer/gen_viewer.py:3322",
   "detail": "declared at tools/coyodex/viewer/gen_viewer.py:3322; files: tools/coyodex/viewer/gen_viewer.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C83 (Map server) is described as: Serves every map on this machine at a local web address. Reads a project's own code from its version history.",
   "anchor": "tools/coyodex/viewer/serve.py:869",
   "detail": "declared at tools/coyodex/viewer/serve.py:869; files: tools/coyodex/viewer/serve.py, tools/coyodex/viewer/diffmap.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C84 (File browser) is described as: Lists a project's folders and files, marking each one by how far the map covers it.",
   "anchor": "tools/coyodex/viewer/filetree.py:214",
   "detail": "declared at tools/coyodex/viewer/filetree.py:214; files: tools/coyodex/viewer/filetree.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C85 (Remembered projects) is described as: Keeps the list of project folders a person has opened, so each map is one click away next time.",
   "anchor": "tools/coyodex/viewer/recents.py:22",
   "detail": "declared at tools/coyodex/viewer/recents.py:22; files: tools/coyodex/viewer/recents.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C60 (Map loader) is described as: Loads the whole map from the server when the page opens, before any screen is drawn. Glossary words in the text then become definitions you can read in place.",
   "anchor": "tools/coyodex/viewer/viewer.js:172",
   "detail": "declared at tools/coyodex/viewer/viewer.js:172",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C61 (Element cards) is described as: Draws one card for anything on the map: its name, a tag saying its type, one sentence. Clicking a card opens that thing.",
   "anchor": "tools/coyodex/viewer/viewer.js:1265",
   "detail": "declared at tools/coyodex/viewer/viewer.js:1265",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C62 (Diagram canvas) is described as: Draws a view's picture from the drawing the map recorded. Selecting a box lights it and dims everything else.",
   "anchor": "tools/coyodex/viewer/viewer.js:13096",
   "detail": "declared at tools/coyodex/viewer/viewer.js:13096",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C63 (Diagram clicks) is described as: Decides what a click does on each drawn box and arrow, view by view. Holding a key drills into a box instead of selecting it.",
   "anchor": "tools/coyodex/viewer/viewer.js:5346",
   "detail": "declared at tools/coyodex/viewer/viewer.js:5346",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C64 (Info pane) is described as: Shows what you selected: a card floating over the picture, or a drawer along the bottom. What the pane holds changes with the kind of thing selected.",
   "anchor": "tools/coyodex/viewer/viewer.js:2881",
   "detail": "declared at tools/coyodex/viewer/viewer.js:2881; files: tools/coyodex/viewer/viewer.js, tools/coyodex/viewer/info-pane.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C65 (Trail and screen address) is described as: Keeps the tabs, the trail and the browser address in step with the screen on show. Copying that address gives someone else the same screen.",
   "anchor": "tools/coyodex/viewer/viewer.js:7896",
   "detail": "declared at tools/coyodex/viewer/viewer.js:7896",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C66 (Product pages) is described as: Draws the pages that say what the product is for and what it does. Covers the overview, the features, the happy path, actor pages and the glossary.",
   "anchor": "tools/coyodex/viewer/viewer.js:10089",
   "detail": "declared at tools/coyodex/viewer/viewer.js:10089",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C67 (Storage, tests and system pages) is described as: Draws the tables for where data is kept, how far the tests reach, and the operational lists.",
   "anchor": "tools/coyodex/viewer/viewer.js:11400",
   "detail": "declared at tools/coyodex/viewer/viewer.js:11400",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C68 (Rules and interfaces pages) is described as: Draws the decisions the product makes and the places it meets the outside world. Each rule and each interface has a page of its own.",
   "anchor": "tools/coyodex/viewer/viewer.js:12798",
   "detail": "declared at tools/coyodex/viewer/viewer.js:12798",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C69 (Source column) is described as: Shows the mapped project's files, and the source of the one you open, at the map's commit. Selecting a box on a picture opens its file here.",
   "anchor": "tools/coyodex/viewer/viewer.js:13894",
   "detail": "declared at tools/coyodex/viewer/viewer.js:13894",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C70 (Viewer shell) is described as: Holds the page frame: the title bar, the tab rows, the panes and the settings dialog. Also wires the keyboard and the first-run guide.",
   "anchor": "tools/coyodex/viewer/viewer.js:14609",
   "detail": "declared at tools/coyodex/viewer/viewer.js:14609; files: tools/coyodex/viewer/viewer.html, tools/coyodex/viewer/viewer.js",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C71 (Map search) is described as: Finds anything in the map by name as you type: elements, files, folders, glossary words. Picking a result opens it where it lives.",
   "anchor": "tools/coyodex/viewer/viewer.js:15119",
   "detail": "declared at tools/coyodex/viewer/viewer.js:15119",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C72 (Impact explorer) is described as: Projects a code change onto the map and marks what that change touches, plus what ripples out from it. Hidden from the title bar for now.",
   "anchor": "tools/coyodex/viewer/viewer.js:15650",
   "detail": "declared at tools/coyodex/viewer/viewer.js:15650",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C73 (Viewer styles) is described as: Holds every colour, size and layout rule the viewer page draws itself with. One stylesheet covers every view.",
   "anchor": "tools/coyodex/viewer/viewer.css:6",
   "detail": "declared at tools/coyodex/viewer/viewer.css:6; files: tools/coyodex/viewer/viewer.css",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "C3 reads C1",
   "anchor": "tools/coyodex/json_schema.py:505",
   "detail": "From: C3 = Map schema document (tools/coyodex/json_schema.py:505); To: C1 = Map model (tools/coyodex/model.py:1153)",
   "why_risky": "backbone edge — no deterministic gate confirms C3's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C3 reads C2",
   "anchor": "tools/coyodex/json_schema.py:206",
   "detail": "From: C3 = Map schema document (tools/coyodex/json_schema.py:505); To: C2 = Shared map grammar (tools/coyodex/grammar.py:21)",
   "why_risky": "backbone edge — no deterministic gate confirms C3's code 'reads' C2; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C20 calls C24",
   "anchor": "tools/coyodex/cli.py:177",
   "detail": "From: C20 = Command line (tools/coyodex/cli.py:139); entry points: An operator runs `coyodex preindex` to build the structural pre-index a map build starts from. (tools/coyodex/cli.py:153); An operator runs `coyodex validate` to check that a map is well formed. (tools/coyodex/cli.py:156); An operator runs `coyodex audit` to find where a map contradicts itself. (tools/coyodex/cli.py:159); An operator runs `coyodex render` to write the map's committed markdown view. (tools/coyodex/cli.py:165); An operator runs `coyodex serve` to open the viewer for a map in a browser. (tools/coyodex/cli.py:168); An operator runs `coyodex assemble` to merge the build agents' fragments into one map. (tools/coyodex/cli.py:171); An operator runs `coyodex balance` to see which diagrams hold too many boxes. (tools/coyodex/cli.py:183); An operator runs `coyodex reconcile` to expand the path rules into an explicit assignment file. (tools/coyodex/cli.py:186); A build agent runs `coyodex lint-fragment` to self-check one fragment before returning it. (tools/coyodex/cli.py:189); An operator runs `coyodex finalize` to get one pre-commit verdict over every check. (tools/coyodex/cli.py:195); To: C24 = Map reader (tools/coyodex/dump.py:303); entry points: A skeptic runs `coyodex context` to get one claims batch with its evidence already beside it. (tools/coyodex/context.py:200); An operator runs `coyodex dump` to read the map, or one element's stored record, as data. (tools/coyodex/dump.py:303); An operator runs `coyodex diff` to see which rows two maps of the same work differ on. (tools/coyodex/mapdiff.py:254)",
   "why_risky": "backbone edge — no deterministic gate confirms C20's code 'calls' C24; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C24 calls C4",
   "anchor": "tools/coyodex/dump.py:298",
   "detail": "From: C24 = Map reader (tools/coyodex/dump.py:303); entry points: A skeptic runs `coyodex context` to get one claims batch with its evidence already beside it. (tools/coyodex/context.py:200); An operator runs `coyodex dump` to read the map, or one element's stored record, as data. (tools/coyodex/dump.py:303); An operator runs `coyodex diff` to see which rows two maps of the same work differ on. (tools/coyodex/mapdiff.py:254); To: C4 = Map assembly (tools/coyodex/assemble.py:408)",
   "why_risky": "backbone edge — no deterministic gate confirms C24's code 'calls' C4; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C24 reads C1",
   "anchor": "tools/coyodex/context.py:196",
   "detail": "From: C24 = Map reader (tools/coyodex/dump.py:303); entry points: A skeptic runs `coyodex context` to get one claims batch with its evidence already beside it. (tools/coyodex/context.py:200); An operator runs `coyodex dump` to read the map, or one element's stored record, as data. (tools/coyodex/dump.py:303); An operator runs `coyodex diff` to see which rows two maps of the same work differ on. (tools/coyodex/mapdiff.py:254); To: C1 = Map model (tools/coyodex/model.py:1153)",
   "why_risky": "backbone edge — no deterministic gate confirms C24's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C46 calls C2",
   "anchor": "tools/coyodex/ignorefile.py:87",
   "detail": "From: C46 = Ignore list (tools/coyodex/ignorefile.py:178); To: C2 = Shared map grammar (tools/coyodex/grammar.py:21)",
   "why_risky": "backbone edge — no deterministic gate confirms C46's code 'calls' C2; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C45 calls C46",
   "anchor": "tools/coyodex/preindex_lib.py:227",
   "detail": "From: C45 = Code survey (tools/coyodex/preindex.py:604); To: C46 = Ignore list (tools/coyodex/ignorefile.py:178)",
   "why_risky": "backbone edge — no deterministic gate confirms C45's code 'calls' C46; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C26 calls C46",
   "anchor": "tools/coyodex/scope.py:131",
   "detail": "From: C26 = Build briefing (tools/coyodex/scope.py:172); entry points: An operator runs `coyodex contract` to print the exact brief one helper agent should receive. (tools/coyodex/contract.py:405); An operator runs `coyodex scope` to see which files a build will read before it starts. (tools/coyodex/scope.py:172); To: C46 = Ignore list (tools/coyodex/ignorefile.py:178)",
   "why_risky": "backbone edge — no deterministic gate confirms C26's code 'calls' C46; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C84 calls C46",
   "anchor": "tools/coyodex/viewer/filetree.py:260",
   "detail": "From: C84 = File browser (tools/coyodex/viewer/filetree.py:214); To: C46 = Ignore list (tools/coyodex/ignorefile.py:178)",
   "why_risky": "backbone edge — no deterministic gate confirms C84's code 'calls' C46; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C6 calls C46",
   "anchor": "tools/coyodex/validate_analysis.py:321",
   "detail": "From: C6 = Shape checks (tools/coyodex/validate_model.py:195); To: C46 = Ignore list (tools/coyodex/ignorefile.py:178)",
   "why_risky": "backbone edge — no deterministic gate confirms C6's code 'calls' C46; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C48 reads C1",
   "anchor": "tools/coyodex/balance_lib.py:156",
   "detail": "From: C48 = Diagram balance (tools/coyodex/balance.py:142); To: C1 = Map model (tools/coyodex/model.py:1153)",
   "why_risky": "backbone edge — no deterministic gate confirms C48's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C48 calls C2",
   "anchor": "tools/coyodex/balance_lib.py:238",
   "detail": "From: C48 = Diagram balance (tools/coyodex/balance.py:142); To: C2 = Shared map grammar (tools/coyodex/grammar.py:21)",
   "why_risky": "backbone edge — no deterministic gate confirms C48's code 'calls' C2; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C20 calls C48",
   "anchor": "tools/coyodex/cli.py:183",
   "detail": "From: C20 = Command line (tools/coyodex/cli.py:139); entry points: An operator runs `coyodex preindex` to build the structural pre-index a map build starts from. (tools/coyodex/cli.py:153); An operator runs `coyodex validate` to check that a map is well formed. (tools/coyodex/cli.py:156); An operator runs `coyodex audit` to find where a map contradicts itself. (tools/coyodex/cli.py:159); An operator runs `coyodex render` to write the map's committed markdown view. (tools/coyodex/cli.py:165); An operator runs `coyodex serve` to open the viewer for a map in a browser. (tools/coyodex/cli.py:168); An operator runs `coyodex assemble` to merge the build agents' fragments into one map. (tools/coyodex/cli.py:171); An operator runs `coyodex balance` to see which diagrams hold too many boxes. (tools/coyodex/cli.py:183); An operator runs `coyodex reconcile` to expand the path rules into an explicit assignment file. (tools/coyodex/cli.py:186); A build agent runs `coyodex lint-fragment` to self-check one fragment before returning it. (tools/coyodex/cli.py:189); An operator runs `coyodex finalize` to get one pre-commit verdict over every check. (tools/coyodex/cli.py:195); To: C48 = Diagram balance (tools/coyodex/balance.py:142)",
   "why_risky": "backbone edge — no deterministic gate confirms C20's code 'calls' C48; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C25 calls C48",
   "anchor": "tools/coyodex/finalize.py:125",
   "detail": "From: C25 = Build closer (tools/coyodex/ship.py:439); entry points: An operator runs `coyodex ship` to run a build's whole closing sequence as one step. (tools/coyodex/ship.py:439); To: C48 = Diagram balance (tools/coyodex/balance.py:142)",
   "why_risky": "backbone edge — no deterministic gate confirms C25's code 'calls' C48; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C101 calls C48",
   "anchor": "eval/tools/coyodex_eval/profile.py:383",
   "detail": "From: C101 = Map profile (eval/tools/coyodex_eval/profile.py:430); To: C48 = Diagram balance (tools/coyodex/balance.py:142)",
   "why_risky": "backbone edge — no deterministic gate confirms C101's code 'calls' C48; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C71 calls C62",
   "anchor": "tools/coyodex/viewer/viewer.js:15120",
   "detail": "From: C71 = Map search (tools/coyodex/viewer/viewer.js:15119); To: C62 = Diagram canvas (tools/coyodex/viewer/viewer.js:13096)",
   "why_risky": "backbone edge — no deterministic gate confirms C71's code 'calls' C62; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C71 calls C83",
   "anchor": "tools/coyodex/viewer/viewer.js:15224",
   "detail": "From: C71 = Map search (tools/coyodex/viewer/viewer.js:15119); To: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883)",
   "why_risky": "backbone edge — no deterministic gate confirms C71's code 'calls' C83; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C72 calls C83",
   "anchor": "tools/coyodex/viewer/viewer.js:15794",
   "detail": "From: C72 = Impact explorer (tools/coyodex/viewer/viewer.js:15650); To: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883)",
   "why_risky": "backbone edge — no deterministic gate confirms C72's code 'calls' C83; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C72 calls C62",
   "anchor": "tools/coyodex/viewer/viewer.js:15716",
   "detail": "From: C72 = Impact explorer (tools/coyodex/viewer/viewer.js:15650); To: C62 = Diagram canvas (tools/coyodex/viewer/viewer.js:13096)",
   "why_risky": "backbone edge — no deterministic gate confirms C72's code 'calls' C62; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C70 loads C73",
   "anchor": "tools/coyodex/viewer/viewer.html:12",
   "detail": "From: C70 = Viewer shell (tools/coyodex/viewer/viewer.js:14609); entry points: Opening `#v=overview` shows the sentences saying what the product is for. (tools/coyodex/viewer/viewer.html:117); Opening `#v=features` lists everything the product does, one card per feature. (tools/coyodex/viewer/viewer.html:118); Opening `#v=hp` shows the product's one successful run, end to end. (tools/coyodex/viewer/viewer.html:119); Opening `#v=interfaces` shows every place the product meets the outside world. (tools/coyodex/viewer/viewer.html:120); Opening `#v=rules` shows the decisions the product makes, grouped by area. (tools/coyodex/viewer/viewer.html:126); Opening `#v=domain` shows the things the product knows about. (tools/coyodex/viewer/viewer.html:127); Opening `#v=glossary` shows the product's own words and what each one means. (tools/coyodex/viewer/viewer.html:128); Opening `#v=container` draws the subsystems the code is arranged into. (tools/coyodex/viewer/viewer.html:133); Opening `#v=data` shows where the product's data physically lives. (tools/coyodex/viewer/viewer.html:134); Opening `#v=context` draws what the product depends on outside itself. (tools/coyodex/viewer/viewer.html:135); Opening `#v=tests` shows how much of the product the tests reach. (tools/coyodex/viewer/viewer.html:136); Opening `#v=deployment` draws the processes that run and what each one holds. (tools/coyodex/viewer/viewer.html:138); Opening `#v=system` shows the operational tables: commands, settings and ways in. (tools/coyodex/viewer/viewer.html:139); To: C73 = Viewer styles (tools/coyodex/viewer/viewer.css:6)",
   "why_risky": "backbone edge — no deterministic gate confirms C70's code 'loads' C73; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C83 serves C73",
   "anchor": "tools/coyodex/viewer/serve.py:851",
   "detail": "From: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883); To: C73 = Viewer styles (tools/coyodex/viewer/viewer.css:6)",
   "why_risky": "backbone edge — no deterministic gate confirms C83's code 'serves' C73; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C83 calls C84",
   "anchor": "tools/coyodex/viewer/serve.py:492",
   "detail": "From: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883); To: C84 = File browser (tools/coyodex/viewer/filetree.py:214)",
   "why_risky": "backbone edge — no deterministic gate confirms C83's code 'calls' C84; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C84 calls C45",
   "anchor": "tools/coyodex/viewer/filetree.py:253",
   "detail": "From: C84 = File browser (tools/coyodex/viewer/filetree.py:214); To: C45 = Code survey (tools/coyodex/preindex.py:604)",
   "why_risky": "backbone edge — no deterministic gate confirms C84's code 'calls' C45; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C121 cites C122",
   "anchor": "method.md:489",
   "detail": "From: C121 = Code inventory instructions (method.md:414); To: C122 = Outside edge instructions (method.md:506)",
   "why_risky": "backbone edge — no deterministic gate confirms C121's code 'cites' C122; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C125 cites C122",
   "anchor": "method.md:1465",
   "detail": "From: C125 = Cross-cutting build instructions (method.md:1332); To: C122 = Outside edge instructions (method.md:506)",
   "why_risky": "backbone edge — no deterministic gate confirms C125's code 'cites' C122; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C125 cites C123",
   "anchor": "method.md:1467",
   "detail": "From: C125 = Cross-cutting build instructions (method.md:1332); To: C123 = Data and step instructions (method.md:879)",
   "why_risky": "backbone edge — no deterministic gate confirms C125's code 'cites' C123; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C125 cites C132",
   "anchor": "method.md:1333",
   "detail": "From: C125 = Cross-cutting build instructions (method.md:1332); To: C132 = Fan-out worker contracts (method/templates/)",
   "why_risky": "backbone edge — no deterministic gate confirms C125's code 'cites' C132; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C123 cites C129",
   "anchor": "method.md:884",
   "detail": "From: C123 = Data and step instructions (method.md:879); To: C129 = Map model spec (method/model.md:45)",
   "why_risky": "backbone edge — no deterministic gate confirms C123's code 'cites' C129; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C124 cites C129",
   "anchor": "method.md:1321",
   "detail": "From: C124 = Operations and decision instructions (method.md:1068); To: C129 = Map model spec (method/model.md:45)",
   "why_risky": "backbone edge — no deterministic gate confirms C124's code 'cites' C129; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C121 cites C129",
   "anchor": "method.md:505",
   "detail": "From: C121 = Code inventory instructions (method.md:414); To: C129 = Map model spec (method/model.md:45)",
   "why_risky": "backbone edge — no deterministic gate confirms C121's code 'cites' C129; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C127 cites C129",
   "anchor": "method.md:2205",
   "detail": "From: C127 = Verification phase instructions (method.md:2152); To: C129 = Map model spec (method/model.md:45)",
   "why_risky": "backbone edge — no deterministic gate confirms C127's code 'cites' C129; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C128 cites C129",
   "anchor": "method.md:2834",
   "detail": "From: C128 = Closing sequence instructions (method.md:2651); To: C129 = Map model spec (method/model.md:45)",
   "why_risky": "backbone edge — no deterministic gate confirms C128's code 'cites' C129; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C126 cites C132",
   "anchor": "method.md:2113",
   "detail": "From: C126 = Fan-out phase instructions (method.md:1728); To: C132 = Fan-out worker contracts (method/templates/)",
   "why_risky": "backbone edge — no deterministic gate confirms C126's code 'cites' C132; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C128 cites C132",
   "anchor": "method.md:2781",
   "detail": "From: C128 = Closing sequence instructions (method.md:2651); To: C132 = Fan-out worker contracts (method/templates/)",
   "why_risky": "backbone edge — no deterministic gate confirms C128's code 'cites' C132; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C138 cites C132",
   "anchor": "CONTRIBUTING.md:48",
   "detail": "From: C138 = Contributor guide (CONTRIBUTING.md:48); To: C132 = Fan-out worker contracts (method/templates/)",
   "why_risky": "backbone edge — no deterministic gate confirms C138's code 'cites' C132; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C136 cites C133",
   "anchor": "eval/retro/method.md:281",
   "detail": "From: C136 = Build retrospective instructions (eval/retro/method.md:4); entry points: A person types `/coyodex-retro`, and their agent reads this file to find the review recipe. (eval/retro/SKILL.md:28); To: C133 = Method regression checks (method/retro-checks/)",
   "why_risky": "backbone edge — no deterministic gate confirms C136's code 'cites' C133; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C135 cites C100",
   "anchor": "eval/method.md:215",
   "detail": "From: C135 = Map quality eval instructions (eval/method.md:8); entry points: A person types `/coyodex-eval`, and their agent reads this file to find the scoring recipe. (eval/SKILL.md:28); To: C100 = Eval command line (eval/tools/coyodex_eval/cli.py:69); entry points: A developer moves a project's map aside so the next build starts from nothing. (eval/tools/coyodex_eval/cli.py:102); A developer checks a retrospective's finished rows against the code history. (eval/tools/coyodex_eval/cli.py:105); A developer re-measures the counts the tools state about a live map. (eval/tools/coyodex_eval/cli.py:108); A developer asks whether the build a retrospective would read has finished. (eval/tools/coyodex_eval/cli.py:111); A developer reads a build's chat log in slices. (eval/tools/coyodex_eval/cli.py:114); A developer asks what one build spent in time and tokens. (eval/tools/coyodex_eval/cli.py:117); A developer plants false claims, then scores how many the doubters caught. (eval/tools/coyodex_eval/cli.py:120); A developer asks for one built map's quality numbers. (eval/tools/coyodex_eval/cli.py:72); A developer scores a fresh map, compares it with the accepted one, and files the result. (eval/tools/coyodex_eval/cli.py:75); A developer freezes a map file's fingerprint before scoring it. (eval/tools/coyodex_eval/cli.py:78); A developer lists the risky statements a doubter should check against the code. (eval/tools/coyodex_eval/cli.py:81); A developer folds the doubters' votes and the scorers' marks into one report. (eval/tools/coyodex_eval/cli.py:84); A developer checks whether stored scores were made under today's judging rules. (eval/tools/coyodex_eval/cli.py:87); A developer promotes one scored run to be the new accepted baseline. (eval/tools/coyodex_eval/cli.py:90); A developer asks which relations a rebuild dropped while the code still makes them. (eval/tools/coyodex_eval/cli.py:93); A developer applies the regression checks to two maps' number sheets. (eval/tools/coyodex_eval/cli.py:96); A developer scores how the build agent behaved during one build. (eval/tools/coyodex_eval/cli.py:99)",
   "why_risky": "backbone edge — no deterministic gate confirms C135's code 'cites' C100; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C138 cites C137",
   "anchor": "CONTRIBUTING.md:40",
   "detail": "From: C138 = Contributor guide (CONTRIBUTING.md:48); To: C137 = Public documentation (README.md:13)",
   "why_risky": "backbone edge — no deterministic gate confirms C138's code 'cites' C137; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C137 cites C120",
   "anchor": "README.md:117",
   "detail": "From: C137 = Public documentation (README.md:13); To: C120 = Product description instructions (method.md:28)",
   "why_risky": "backbone edge — no deterministic gate confirms C137's code 'cites' C120; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C138 cites C139",
   "anchor": "CONTRIBUTING.md:59",
   "detail": "From: C138 = Contributor guide (CONTRIBUTING.md:48); To: C139 = Method rationale record (internal/docs/method-rationale.md:3)",
   "why_risky": "backbone edge — no deterministic gate confirms C138's code 'cites' C139; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C139 cites C120",
   "anchor": "internal/docs/method-rationale.md:32",
   "detail": "From: C139 = Method rationale record (internal/docs/method-rationale.md:3); To: C120 = Product description instructions (method.md:28)",
   "why_risky": "backbone edge — no deterministic gate confirms C139's code 'cites' C120; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C20 calls C5",
   "anchor": "tools/coyodex/cli.py:189",
   "detail": "From: C20 = Command line (tools/coyodex/cli.py:139); entry points: An operator runs `coyodex preindex` to build the structural pre-index a map build starts from. (tools/coyodex/cli.py:153); An operator runs `coyodex validate` to check that a map is well formed. (tools/coyodex/cli.py:156); An operator runs `coyodex audit` to find where a map contradicts itself. (tools/coyodex/cli.py:159); An operator runs `coyodex render` to write the map's committed markdown view. (tools/coyodex/cli.py:165); An operator runs `coyodex serve` to open the viewer for a map in a browser. (tools/coyodex/cli.py:168); An operator runs `coyodex assemble` to merge the build agents' fragments into one map. (tools/coyodex/cli.py:171); An operator runs `coyodex balance` to see which diagrams hold too many boxes. (tools/coyodex/cli.py:183); An operator runs `coyodex reconcile` to expand the path rules into an explicit assignment file. (tools/coyodex/cli.py:186); A build agent runs `coyodex lint-fragment` to self-check one fragment before returning it. (tools/coyodex/cli.py:189); An operator runs `coyodex finalize` to get one pre-commit verdict over every check. (tools/coyodex/cli.py:195); To: C5 = Fragment self-check (tools/coyodex/lint_fragment.py:134)",
   "why_risky": "backbone edge — no deterministic gate confirms C20's code 'calls' C5; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C25 calls C5",
   "anchor": "tools/coyodex/ship.py:326",
   "detail": "From: C25 = Build closer (tools/coyodex/ship.py:439); entry points: An operator runs `coyodex ship` to run a build's whole closing sequence as one step. (tools/coyodex/ship.py:439); To: C5 = Fragment self-check (tools/coyodex/lint_fragment.py:134)",
   "why_risky": "backbone edge — no deterministic gate confirms C25's code 'calls' C5; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C5 calls C4",
   "anchor": "tools/coyodex/lint_fragment.py:590",
   "detail": "From: C5 = Fragment self-check (tools/coyodex/lint_fragment.py:134); To: C4 = Map assembly (tools/coyodex/assemble.py:408)",
   "why_risky": "backbone edge — no deterministic gate confirms C5's code 'calls' C4; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C5 calls C6",
   "anchor": "tools/coyodex/lint_fragment.py:198",
   "detail": "From: C5 = Fragment self-check (tools/coyodex/lint_fragment.py:134); To: C6 = Shape checks (tools/coyodex/validate_model.py:195)",
   "why_risky": "backbone edge — no deterministic gate confirms C5's code 'calls' C6; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C5 calls C7",
   "anchor": "tools/coyodex/lint_fragment.py:161",
   "detail": "From: C5 = Fragment self-check (tools/coyodex/lint_fragment.py:134); To: C7 = Story checks (tools/coyodex/validate_model.py:405)",
   "why_risky": "backbone edge — no deterministic gate confirms C5's code 'calls' C7; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C5 calls C8",
   "anchor": "tools/coyodex/lint_fragment.py:180",
   "detail": "From: C5 = Fragment self-check (tools/coyodex/lint_fragment.py:134); To: C8 = Rule checks (tools/coyodex/validate_model.py:1748)",
   "why_risky": "backbone edge — no deterministic gate confirms C5's code 'calls' C8; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C5 calls C9",
   "anchor": "tools/coyodex/lint_fragment.py:153",
   "detail": "From: C5 = Fragment self-check (tools/coyodex/lint_fragment.py:134); To: C9 = Wiring checks (tools/coyodex/validate_model.py:2700)",
   "why_risky": "backbone edge — no deterministic gate confirms C5's code 'calls' C9; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C5 calls C10",
   "anchor": "tools/coyodex/lint_fragment.py:149",
   "detail": "From: C5 = Fragment self-check (tools/coyodex/lint_fragment.py:134); To: C10 = Data checks (tools/coyodex/validate_model.py:4205)",
   "why_risky": "backbone edge — no deterministic gate confirms C5's code 'calls' C10; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C5 calls C27",
   "anchor": "tools/coyodex/lint_fragment.py:280",
   "detail": "From: C5 = Fragment self-check (tools/coyodex/lint_fragment.py:134); To: C27 = Build log (tools/coyodex/provenance.py:327); entry points: An operator runs `coyodex provenance show` to list the sessions that built this map. (tools/coyodex/provenance.py:308); An operator runs `coyodex provenance stamp` to record which session built this map, and when. (tools/coyodex/provenance.py:327); An operator runs `coyodex timings record` to write down what each fan-out slice took. (tools/coyodex/timings.py:439); An operator runs `coyodex timings order` to see one phase's slices, longest first. (tools/coyodex/timings.py:449); An operator runs `coyodex timings show` to see every phase that has a timing record. (tools/coyodex/timings.py:456)",
   "why_risky": "backbone edge — no deterministic gate confirms C5's code 'calls' C27; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C80 calls C7",
   "anchor": "tools/coyodex/views.py:1003",
   "detail": "From: C80 = Map rendering (tools/coyodex/views.py:299); To: C7 = Story checks (tools/coyodex/validate_model.py:405)",
   "why_risky": "backbone edge — no deterministic gate confirms C80's code 'calls' C7; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C81 calls C7",
   "anchor": "tools/coyodex/features.py:467",
   "detail": "From: C81 = Feature facts (tools/coyodex/features.py:410); To: C7 = Story checks (tools/coyodex/validate_model.py:405)",
   "why_risky": "backbone edge — no deterministic gate confirms C81's code 'calls' C7; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C112 calls C7",
   "anchor": "eval/tools/coyodex_eval/live_numbers.py:192",
   "detail": "From: C112 = Live number ledger (eval/tools/coyodex_eval/live_numbers.py:540); To: C7 = Story checks (tools/coyodex/validate_model.py:405)",
   "why_risky": "backbone edge — no deterministic gate confirms C112's code 'calls' C7; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C7 reads C1",
   "anchor": "tools/coyodex/validate_model.py:404",
   "detail": "From: C7 = Story checks (tools/coyodex/validate_model.py:405); To: C1 = Map model (tools/coyodex/model.py:1153)",
   "why_risky": "backbone edge — no deterministic gate confirms C7's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C80 calls C8",
   "anchor": "tools/coyodex/views.py:1024",
   "detail": "From: C80 = Map rendering (tools/coyodex/views.py:299); To: C8 = Rule checks (tools/coyodex/validate_model.py:1748)",
   "why_risky": "backbone edge — no deterministic gate confirms C80's code 'calls' C8; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C81 calls C8",
   "anchor": "tools/coyodex/features.py:469",
   "detail": "From: C81 = Feature facts (tools/coyodex/features.py:410); To: C8 = Rule checks (tools/coyodex/validate_model.py:1748)",
   "why_risky": "backbone edge — no deterministic gate confirms C81's code 'calls' C8; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C21 calls C8",
   "anchor": "tools/coyodex/audit_model.py:1055",
   "detail": "From: C21 = Map checks (tools/coyodex/audit_model.py:1695); entry points: An operator runs `coyodex anchor-drift` to find code links that point at the wrong line. (tools/coyodex/anchor_drift.py:409); To: C8 = Rule checks (tools/coyodex/validate_model.py:1748)",
   "why_risky": "backbone edge — no deterministic gate confirms C21's code 'calls' C8; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C8 calls C49",
   "anchor": "tools/coyodex/validate_model.py:1453",
   "detail": "From: C8 = Rule checks (tools/coyodex/validate_model.py:1748); To: C49 = Change impact (tools/coyodex/impact_git.py:285)",
   "why_risky": "backbone edge — no deterministic gate confirms C8's code 'calls' C49; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C80 calls C9",
   "anchor": "tools/coyodex/views.py:1175",
   "detail": "From: C80 = Map rendering (tools/coyodex/views.py:299); To: C9 = Wiring checks (tools/coyodex/validate_model.py:2700)",
   "why_risky": "backbone edge — no deterministic gate confirms C80's code 'calls' C9; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C81 calls C9",
   "anchor": "tools/coyodex/features.py:554",
   "detail": "From: C81 = Feature facts (tools/coyodex/features.py:410); To: C9 = Wiring checks (tools/coyodex/validate_model.py:2700)",
   "why_risky": "backbone edge — no deterministic gate confirms C81's code 'calls' C9; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C21 calls C9",
   "anchor": "tools/coyodex/audit_model.py:1243",
   "detail": "From: C21 = Map checks (tools/coyodex/audit_model.py:1695); entry points: An operator runs `coyodex anchor-drift` to find code links that point at the wrong line. (tools/coyodex/anchor_drift.py:409); To: C9 = Wiring checks (tools/coyodex/validate_model.py:2700)",
   "why_risky": "backbone edge — no deterministic gate confirms C21's code 'calls' C9; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C9 calls C48",
   "anchor": "tools/coyodex/validate_model.py:3640",
   "detail": "From: C9 = Wiring checks (tools/coyodex/validate_model.py:2700); To: C48 = Diagram balance (tools/coyodex/balance.py:142)",
   "why_risky": "backbone edge — no deterministic gate confirms C9's code 'calls' C48; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C80 calls C10",
   "anchor": "tools/coyodex/views.py:952",
   "detail": "From: C80 = Map rendering (tools/coyodex/views.py:299); To: C10 = Data checks (tools/coyodex/validate_model.py:4205)",
   "why_risky": "backbone edge — no deterministic gate confirms C80's code 'calls' C10; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C10 calls C21",
   "anchor": "tools/coyodex/validate_model.py:3928",
   "detail": "From: C10 = Data checks (tools/coyodex/validate_model.py:4205); To: C21 = Map checks (tools/coyodex/audit_model.py:1695); entry points: An operator runs `coyodex anchor-drift` to find code links that point at the wrong line. (tools/coyodex/anchor_drift.py:409)",
   "why_risky": "backbone edge — no deterministic gate confirms C10's code 'calls' C21; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C20 calls C21",
   "anchor": "tools/coyodex/cli.py:159",
   "detail": "From: C20 = Command line (tools/coyodex/cli.py:139); entry points: An operator runs `coyodex preindex` to build the structural pre-index a map build starts from. (tools/coyodex/cli.py:153); An operator runs `coyodex validate` to check that a map is well formed. (tools/coyodex/cli.py:156); An operator runs `coyodex audit` to find where a map contradicts itself. (tools/coyodex/cli.py:159); An operator runs `coyodex render` to write the map's committed markdown view. (tools/coyodex/cli.py:165); An operator runs `coyodex serve` to open the viewer for a map in a browser. (tools/coyodex/cli.py:168); An operator runs `coyodex assemble` to merge the build agents' fragments into one map. (tools/coyodex/cli.py:171); An operator runs `coyodex balance` to see which diagrams hold too many boxes. (tools/coyodex/cli.py:183); An operator runs `coyodex reconcile` to expand the path rules into an explicit assignment file. (tools/coyodex/cli.py:186); A build agent runs `coyodex lint-fragment` to self-check one fragment before returning it. (tools/coyodex/cli.py:189); An operator runs `coyodex finalize` to get one pre-commit verdict over every check. (tools/coyodex/cli.py:195); To: C21 = Map checks (tools/coyodex/audit_model.py:1695); entry points: An operator runs `coyodex anchor-drift` to find code links that point at the wrong line. (tools/coyodex/anchor_drift.py:409)",
   "why_risky": "backbone edge — no deterministic gate confirms C20's code 'calls' C21; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C22 calls C21",
   "anchor": "tools/coyodex/grounding.py:1585",
   "detail": "From: C22 = Grounding record (tools/coyodex/grounding.py:1704); entry points: An operator runs `coyodex grounding lint` to check the skeptics' verdict files are well formed. (tools/coyodex/grounding.py:1514); An operator runs `coyodex grounding refutations` to fail a map still holding a claim the skeptics refuted. (tools/coyodex/grounding.py:1563); An operator runs `coyodex grounding by-element` to see what the review pass decided about each element. (tools/coyodex/grounding.py:1682); An operator runs `coyodex grounding report` to see which claims were refuted, tied or unvoted. (tools/coyodex/grounding.py:1690); An operator runs `coyodex grounding write` to build the map's honesty record from the verdicts. (tools/coyodex/grounding.py:1704); To: C21 = Map checks (tools/coyodex/audit_model.py:1695); entry points: An operator runs `coyodex anchor-drift` to find code links that point at the wrong line. (tools/coyodex/anchor_drift.py:409)",
   "why_risky": "backbone edge — no deterministic gate confirms C22's code 'calls' C21; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C47 calls C21",
   "anchor": "tools/coyodex/reconcile.py:680",
   "detail": "From: C47 = Assignment rules (tools/coyodex/reconcile.py:692); To: C21 = Map checks (tools/coyodex/audit_model.py:1695); entry points: An operator runs `coyodex anchor-drift` to find code links that point at the wrong line. (tools/coyodex/anchor_drift.py:409)",
   "why_risky": "backbone edge — no deterministic gate confirms C47's code 'calls' C21; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C103 calls C21",
   "anchor": "eval/tools/coyodex_eval/judge.py:234",
   "detail": "From: C103 = Judge report (eval/tools/coyodex_eval/judge.py:257); To: C21 = Map checks (tools/coyodex/audit_model.py:1695); entry points: An operator runs `coyodex anchor-drift` to find code links that point at the wrong line. (tools/coyodex/anchor_drift.py:409)",
   "why_risky": "backbone edge — no deterministic gate confirms C103's code 'calls' C21; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C20 calls C27",
   "anchor": "tools/coyodex/cli.py:213",
   "detail": "From: C20 = Command line (tools/coyodex/cli.py:139); entry points: An operator runs `coyodex preindex` to build the structural pre-index a map build starts from. (tools/coyodex/cli.py:153); An operator runs `coyodex validate` to check that a map is well formed. (tools/coyodex/cli.py:156); An operator runs `coyodex audit` to find where a map contradicts itself. (tools/coyodex/cli.py:159); An operator runs `coyodex render` to write the map's committed markdown view. (tools/coyodex/cli.py:165); An operator runs `coyodex serve` to open the viewer for a map in a browser. (tools/coyodex/cli.py:168); An operator runs `coyodex assemble` to merge the build agents' fragments into one map. (tools/coyodex/cli.py:171); An operator runs `coyodex balance` to see which diagrams hold too many boxes. (tools/coyodex/cli.py:183); An operator runs `coyodex reconcile` to expand the path rules into an explicit assignment file. (tools/coyodex/cli.py:186); A build agent runs `coyodex lint-fragment` to self-check one fragment before returning it. (tools/coyodex/cli.py:189); An operator runs `coyodex finalize` to get one pre-commit verdict over every check. (tools/coyodex/cli.py:195); To: C27 = Build log (tools/coyodex/provenance.py:327); entry points: An operator runs `coyodex provenance show` to list the sessions that built this map. (tools/coyodex/provenance.py:308); An operator runs `coyodex provenance stamp` to record which session built this map, and when. (tools/coyodex/provenance.py:327); An operator runs `coyodex timings record` to write down what each fan-out slice took. (tools/coyodex/timings.py:439); An operator runs `coyodex timings order` to see one phase's slices, longest first. (tools/coyodex/timings.py:449); An operator runs `coyodex timings show` to see every phase that has a timing record. (tools/coyodex/timings.py:456)",
   "why_risky": "backbone edge — no deterministic gate confirms C20's code 'calls' C27; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C26 calls C27",
   "anchor": "tools/coyodex/scope.py:68",
   "detail": "From: C26 = Build briefing (tools/coyodex/scope.py:172); entry points: An operator runs `coyodex contract` to print the exact brief one helper agent should receive. (tools/coyodex/contract.py:405); An operator runs `coyodex scope` to see which files a build will read before it starts. (tools/coyodex/scope.py:172); To: C27 = Build log (tools/coyodex/provenance.py:327); entry points: An operator runs `coyodex provenance show` to list the sessions that built this map. (tools/coyodex/provenance.py:308); An operator runs `coyodex provenance stamp` to record which session built this map, and when. (tools/coyodex/provenance.py:327); An operator runs `coyodex timings record` to write down what each fan-out slice took. (tools/coyodex/timings.py:439); An operator runs `coyodex timings order` to see one phase's slices, longest first. (tools/coyodex/timings.py:449); An operator runs `coyodex timings show` to see every phase that has a timing record. (tools/coyodex/timings.py:456)",
   "why_risky": "backbone edge — no deterministic gate confirms C26's code 'calls' C27; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C50 calls C27",
   "anchor": "tools/map_backup.py:293",
   "detail": "From: C50 = Map and transcript backup (tools/map_backup.py:391); entry points: A build records which conversation produced this map. (tools/map_backup.py:452); An operator archives a project's map with the conversation that built it. (tools/map_backup.py:478); To: C27 = Build log (tools/coyodex/provenance.py:327); entry points: An operator runs `coyodex provenance show` to list the sessions that built this map. (tools/coyodex/provenance.py:308); An operator runs `coyodex provenance stamp` to record which session built this map, and when. (tools/coyodex/provenance.py:327); An operator runs `coyodex timings record` to write down what each fan-out slice took. (tools/coyodex/timings.py:439); An operator runs `coyodex timings order` to see one phase's slices, longest first. (tools/coyodex/timings.py:449); An operator runs `coyodex timings show` to see every phase that has a timing record. (tools/coyodex/timings.py:456)",
   "why_risky": "backbone edge — no deterministic gate confirms C50's code 'calls' C27; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C110 reads C27",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:179",
   "detail": "From: C110 = Retro precheck (eval/tools/coyodex_eval/retro_precheck.py:198); To: C27 = Build log (tools/coyodex/provenance.py:327); entry points: An operator runs `coyodex provenance show` to list the sessions that built this map. (tools/coyodex/provenance.py:308); An operator runs `coyodex provenance stamp` to record which session built this map, and when. (tools/coyodex/provenance.py:327); An operator runs `coyodex timings record` to write down what each fan-out slice took. (tools/coyodex/timings.py:439); An operator runs `coyodex timings order` to see one phase's slices, longest first. (tools/coyodex/timings.py:449); An operator runs `coyodex timings show` to see every phase that has a timing record. (tools/coyodex/timings.py:456)",
   "why_risky": "backbone edge — no deterministic gate confirms C110's code 'reads' C27; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C83 calls C49",
   "anchor": "tools/coyodex/viewer/serve.py:476",
   "detail": "From: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883); To: C49 = Change impact (tools/coyodex/impact_git.py:285)",
   "why_risky": "backbone edge — no deterministic gate confirms C83's code 'calls' C49; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C82 calls C49",
   "anchor": "tools/coyodex/viewer/gen_viewer.py:3345",
   "detail": "From: C82 = Diagram builder (tools/coyodex/viewer/gen_viewer.py:3322); To: C49 = Change impact (tools/coyodex/impact_git.py:285)",
   "why_risky": "backbone edge — no deterministic gate confirms C82's code 'calls' C49; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C62 calls C63",
   "anchor": "tools/coyodex/viewer/viewer.js:13113",
   "detail": "From: C62 = Diagram canvas (tools/coyodex/viewer/viewer.js:13096); To: C63 = Diagram clicks (tools/coyodex/viewer/viewer.js:5346)",
   "why_risky": "backbone edge — no deterministic gate confirms C62's code 'calls' C63; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C63 calls C64",
   "anchor": "tools/coyodex/viewer/viewer.js:5349",
   "detail": "From: C63 = Diagram clicks (tools/coyodex/viewer/viewer.js:5346); To: C64 = Info pane (tools/coyodex/viewer/viewer.js:2881)",
   "why_risky": "backbone edge — no deterministic gate confirms C63's code 'calls' C64; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C63 calls C65",
   "anchor": "tools/coyodex/viewer/viewer.js:5348",
   "detail": "From: C63 = Diagram clicks (tools/coyodex/viewer/viewer.js:5346); To: C65 = Trail and screen address (tools/coyodex/viewer/viewer.js:7896)",
   "why_risky": "backbone edge — no deterministic gate confirms C63's code 'calls' C65; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C64 calls C61",
   "anchor": "tools/coyodex/viewer/viewer.js:2881",
   "detail": "From: C64 = Info pane (tools/coyodex/viewer/viewer.js:2881); To: C61 = Element cards (tools/coyodex/viewer/viewer.js:1265)",
   "why_risky": "backbone edge — no deterministic gate confirms C64's code 'calls' C61; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C64 calls C69",
   "anchor": "tools/coyodex/viewer/viewer.js:2891",
   "detail": "From: C64 = Info pane (tools/coyodex/viewer/viewer.js:2881); To: C69 = Source column (tools/coyodex/viewer/viewer.js:13894)",
   "why_risky": "backbone edge — no deterministic gate confirms C64's code 'calls' C69; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C62 calls C65",
   "anchor": "tools/coyodex/viewer/viewer.js:13242",
   "detail": "From: C62 = Diagram canvas (tools/coyodex/viewer/viewer.js:13096); To: C65 = Trail and screen address (tools/coyodex/viewer/viewer.js:7896)",
   "why_risky": "backbone edge — no deterministic gate confirms C62's code 'calls' C65; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C62 calls C66",
   "anchor": "tools/coyodex/viewer/viewer.js:13024",
   "detail": "From: C62 = Diagram canvas (tools/coyodex/viewer/viewer.js:13096); To: C66 = Product pages (tools/coyodex/viewer/viewer.js:10089)",
   "why_risky": "backbone edge — no deterministic gate confirms C62's code 'calls' C66; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C62 calls C67",
   "anchor": "tools/coyodex/viewer/viewer.js:13068",
   "detail": "From: C62 = Diagram canvas (tools/coyodex/viewer/viewer.js:13096); To: C67 = Storage, tests and system pages (tools/coyodex/viewer/viewer.js:11400)",
   "why_risky": "backbone edge — no deterministic gate confirms C62's code 'calls' C67; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C62 calls C68",
   "anchor": "tools/coyodex/viewer/viewer.js:13077",
   "detail": "From: C62 = Diagram canvas (tools/coyodex/viewer/viewer.js:13096); To: C68 = Rules and interfaces pages (tools/coyodex/viewer/viewer.js:12798)",
   "why_risky": "backbone edge — no deterministic gate confirms C62's code 'calls' C68; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C65 calls C62",
   "anchor": "tools/coyodex/viewer/viewer.js:5307",
   "detail": "From: C65 = Trail and screen address (tools/coyodex/viewer/viewer.js:7896); To: C62 = Diagram canvas (tools/coyodex/viewer/viewer.js:13096)",
   "why_risky": "backbone edge — no deterministic gate confirms C65's code 'calls' C62; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C66 calls C61",
   "anchor": "tools/coyodex/viewer/viewer.js:10919",
   "detail": "From: C66 = Product pages (tools/coyodex/viewer/viewer.js:10089); To: C61 = Element cards (tools/coyodex/viewer/viewer.js:1265)",
   "why_risky": "backbone edge — no deterministic gate confirms C66's code 'calls' C61; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C67 calls C61",
   "anchor": "tools/coyodex/viewer/viewer.js:11720",
   "detail": "From: C67 = Storage, tests and system pages (tools/coyodex/viewer/viewer.js:11400); To: C61 = Element cards (tools/coyodex/viewer/viewer.js:1265)",
   "why_risky": "backbone edge — no deterministic gate confirms C67's code 'calls' C61; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C67 calls C65",
   "anchor": "tools/coyodex/viewer/viewer.js:11535",
   "detail": "From: C67 = Storage, tests and system pages (tools/coyodex/viewer/viewer.js:11400); To: C65 = Trail and screen address (tools/coyodex/viewer/viewer.js:7896)",
   "why_risky": "backbone edge — no deterministic gate confirms C67's code 'calls' C65; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C68 calls C61",
   "anchor": "tools/coyodex/viewer/viewer.js:12810",
   "detail": "From: C68 = Rules and interfaces pages (tools/coyodex/viewer/viewer.js:12798); To: C61 = Element cards (tools/coyodex/viewer/viewer.js:1265)",
   "why_risky": "backbone edge — no deterministic gate confirms C68's code 'calls' C61; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C68 calls C65",
   "anchor": "tools/coyodex/viewer/viewer.js:12894",
   "detail": "From: C68 = Rules and interfaces pages (tools/coyodex/viewer/viewer.js:12798); To: C65 = Trail and screen address (tools/coyodex/viewer/viewer.js:7896)",
   "why_risky": "backbone edge — no deterministic gate confirms C68's code 'calls' C65; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C69 calls C83",
   "anchor": "tools/coyodex/viewer/viewer.js:13892",
   "detail": "From: C69 = Source column (tools/coyodex/viewer/viewer.js:13894); To: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883)",
   "why_risky": "backbone edge — no deterministic gate confirms C69's code 'calls' C83; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C82 calls C81",
   "anchor": "tools/coyodex/viewer/gen_viewer.py:3348",
   "detail": "From: C82 = Diagram builder (tools/coyodex/viewer/gen_viewer.py:3322); To: C81 = Feature facts (tools/coyodex/features.py:410)",
   "why_risky": "backbone edge — no deterministic gate confirms C82's code 'calls' C81; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C83 calls C82",
   "anchor": "tools/coyodex/viewer/serve.py:516",
   "detail": "From: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883); To: C82 = Diagram builder (tools/coyodex/viewer/gen_viewer.py:3322)",
   "why_risky": "backbone edge — no deterministic gate confirms C83's code 'calls' C82; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C112 calls C82",
   "anchor": "eval/tools/coyodex_eval/live_numbers.py:293",
   "detail": "From: C112 = Live number ledger (eval/tools/coyodex_eval/live_numbers.py:540); To: C82 = Diagram builder (tools/coyodex/viewer/gen_viewer.py:3322)",
   "why_risky": "backbone edge — no deterministic gate confirms C112's code 'calls' C82; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C4 calls C47",
   "anchor": "tools/coyodex/assemble.py:939",
   "detail": "From: C4 = Map assembly (tools/coyodex/assemble.py:408); To: C47 = Assignment rules (tools/coyodex/reconcile.py:692)",
   "why_risky": "backbone edge — no deterministic gate confirms C4's code 'calls' C47; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C23 calls C4",
   "anchor": "tools/coyodex/fix.py:119",
   "detail": "From: C23 = Map editor (tools/coyodex/fix.py:119); entry points: An operator runs `coyodex fix apply-drift` to write each corrected code link back into the map. (tools/coyodex/fix.py:1820); An operator runs `coyodex fix dedup-relation` to drop one of two records of the same data link. (tools/coyodex/fix.py:1820); An operator runs `coyodex fix drop-edge` to remove an arrow the skeptics refuted. (tools/coyodex/fix.py:1820); An operator runs `coyodex fix dedup-edge` to keep one code link where an arrow was recorded twice. (tools/coyodex/fix.py:1821); An operator runs `coyodex fix rows` to apply many wording corrections in one all-or-nothing write. (tools/coyodex/fix.py:1821); An operator runs `coyodex fix security-row` to rewrite a refuted guard's wording and code link. (tools/coyodex/fix.py:1821); An operator runs `coyodex fix dedup-security` to drop a guard an older map recorded twice. (tools/coyodex/fix.py:1822); An operator runs `coyodex fix row` to rewrite one row's wording in the fragment that authored it. (tools/coyodex/fix.py:1822); An operator runs `coyodex record` to note one advisory they judged acceptable. (tools/coyodex/record.py:318); To: C4 = Map assembly (tools/coyodex/assemble.py:408)",
   "why_risky": "backbone edge — no deterministic gate confirms C23's code 'calls' C4; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C100 calls C101",
   "anchor": "eval/tools/coyodex_eval/cli.py:72",
   "detail": "From: C100 = Eval command line (eval/tools/coyodex_eval/cli.py:69); entry points: A developer moves a project's map aside so the next build starts from nothing. (eval/tools/coyodex_eval/cli.py:102); A developer checks a retrospective's finished rows against the code history. (eval/tools/coyodex_eval/cli.py:105); A developer re-measures the counts the tools state about a live map. (eval/tools/coyodex_eval/cli.py:108); A developer asks whether the build a retrospective would read has finished. (eval/tools/coyodex_eval/cli.py:111); A developer reads a build's chat log in slices. (eval/tools/coyodex_eval/cli.py:114); A developer asks what one build spent in time and tokens. (eval/tools/coyodex_eval/cli.py:117); A developer plants false claims, then scores how many the doubters caught. (eval/tools/coyodex_eval/cli.py:120); A developer asks for one built map's quality numbers. (eval/tools/coyodex_eval/cli.py:72); A developer scores a fresh map, compares it with the accepted one, and files the result. (eval/tools/coyodex_eval/cli.py:75); A developer freezes a map file's fingerprint before scoring it. (eval/tools/coyodex_eval/cli.py:78); A developer lists the risky statements a doubter should check against the code. (eval/tools/coyodex_eval/cli.py:81); A developer folds the doubters' votes and the scorers' marks into one report. (eval/tools/coyodex_eval/cli.py:84); A developer checks whether stored scores were made under today's judging rules. (eval/tools/coyodex_eval/cli.py:87); A developer promotes one scored run to be the new accepted baseline. (eval/tools/coyodex_eval/cli.py:90); A developer asks which relations a rebuild dropped while the code still makes them. (eval/tools/coyodex_eval/cli.py:93); A developer applies the regression checks to two maps' number sheets. (eval/tools/coyodex_eval/cli.py:96); A developer scores how the build agent behaved during one build. (eval/tools/coyodex_eval/cli.py:99); To: C101 = Map profile (eval/tools/coyodex_eval/profile.py:430)",
   "why_risky": "backbone edge — no deterministic gate confirms C100's code 'calls' C101; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C100 calls C104",
   "anchor": "eval/tools/coyodex_eval/cli.py:75",
   "detail": "From: C100 = Eval command line (eval/tools/coyodex_eval/cli.py:69); entry points: A developer moves a project's map aside so the next build starts from nothing. (eval/tools/coyodex_eval/cli.py:102); A developer checks a retrospective's finished rows against the code history. (eval/tools/coyodex_eval/cli.py:105); A developer re-measures the counts the tools state about a live map. (eval/tools/coyodex_eval/cli.py:108); A developer asks whether the build a retrospective would read has finished. (eval/tools/coyodex_eval/cli.py:111); A developer reads a build's chat log in slices. (eval/tools/coyodex_eval/cli.py:114); A developer asks what one build spent in time and tokens. (eval/tools/coyodex_eval/cli.py:117); A developer plants false claims, then scores how many the doubters caught. (eval/tools/coyodex_eval/cli.py:120); A developer asks for one built map's quality numbers. (eval/tools/coyodex_eval/cli.py:72); A developer scores a fresh map, compares it with the accepted one, and files the result. (eval/tools/coyodex_eval/cli.py:75); A developer freezes a map file's fingerprint before scoring it. (eval/tools/coyodex_eval/cli.py:78); A developer lists the risky statements a doubter should check against the code. (eval/tools/coyodex_eval/cli.py:81); A developer folds the doubters' votes and the scorers' marks into one report. (eval/tools/coyodex_eval/cli.py:84); A developer checks whether stored scores were made under today's judging rules. (eval/tools/coyodex_eval/cli.py:87); A developer promotes one scored run to be the new accepted baseline. (eval/tools/coyodex_eval/cli.py:90); A developer asks which relations a rebuild dropped while the code still makes them. (eval/tools/coyodex_eval/cli.py:93); A developer applies the regression checks to two maps' number sheets. (eval/tools/coyodex_eval/cli.py:96); A developer scores how the build agent behaved during one build. (eval/tools/coyodex_eval/cli.py:99); To: C104 = Eval run (eval/tools/coyodex_eval/run.py:69)",
   "why_risky": "backbone edge — no deterministic gate confirms C100's code 'calls' C104; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C100 calls C102",
   "anchor": "eval/tools/coyodex_eval/cli.py:96",
   "detail": "From: C100 = Eval command line (eval/tools/coyodex_eval/cli.py:69); entry points: A developer moves a project's map aside so the next build starts from nothing. (eval/tools/coyodex_eval/cli.py:102); A developer checks a retrospective's finished rows against the code history. (eval/tools/coyodex_eval/cli.py:105); A developer re-measures the counts the tools state about a live map. (eval/tools/coyodex_eval/cli.py:108); A developer asks whether the build a retrospective would read has finished. (eval/tools/coyodex_eval/cli.py:111); A developer reads a build's chat log in slices. (eval/tools/coyodex_eval/cli.py:114); A developer asks what one build spent in time and tokens. (eval/tools/coyodex_eval/cli.py:117); A developer plants false claims, then scores how many the doubters caught. (eval/tools/coyodex_eval/cli.py:120); A developer asks for one built map's quality numbers. (eval/tools/coyodex_eval/cli.py:72); A developer scores a fresh map, compares it with the accepted one, and files the result. (eval/tools/coyodex_eval/cli.py:75); A developer freezes a map file's fingerprint before scoring it. (eval/tools/coyodex_eval/cli.py:78); A developer lists the risky statements a doubter should check against the code. (eval/tools/coyodex_eval/cli.py:81); A developer folds the doubters' votes and the scorers' marks into one report. (eval/tools/coyodex_eval/cli.py:84); A developer checks whether stored scores were made under today's judging rules. (eval/tools/coyodex_eval/cli.py:87); A developer promotes one scored run to be the new accepted baseline. (eval/tools/coyodex_eval/cli.py:90); A developer asks which relations a rebuild dropped while the code still makes them. (eval/tools/coyodex_eval/cli.py:93); A developer applies the regression checks to two maps' number sheets. (eval/tools/coyodex_eval/cli.py:96); A developer scores how the build agent behaved during one build. (eval/tools/coyodex_eval/cli.py:99); To: C102 = Baseline comparison (eval/tools/coyodex_eval/compare.py:518)",
   "why_risky": "backbone edge — no deterministic gate confirms C100's code 'calls' C102; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C100 calls C105",
   "anchor": "eval/tools/coyodex_eval/cli.py:93",
   "detail": "From: C100 = Eval command line (eval/tools/coyodex_eval/cli.py:69); entry points: A developer moves a project's map aside so the next build starts from nothing. (eval/tools/coyodex_eval/cli.py:102); A developer checks a retrospective's finished rows against the code history. (eval/tools/coyodex_eval/cli.py:105); A developer re-measures the counts the tools state about a live map. (eval/tools/coyodex_eval/cli.py:108); A developer asks whether the build a retrospective would read has finished. (eval/tools/coyodex_eval/cli.py:111); A developer reads a build's chat log in slices. (eval/tools/coyodex_eval/cli.py:114); A developer asks what one build spent in time and tokens. (eval/tools/coyodex_eval/cli.py:117); A developer plants false claims, then scores how many the doubters caught. (eval/tools/coyodex_eval/cli.py:120); A developer asks for one built map's quality numbers. (eval/tools/coyodex_eval/cli.py:72); A developer scores a fresh map, compares it with the accepted one, and files the result. (eval/tools/coyodex_eval/cli.py:75); A developer freezes a map file's fingerprint before scoring it. (eval/tools/coyodex_eval/cli.py:78); A developer lists the risky statements a doubter should check against the code. (eval/tools/coyodex_eval/cli.py:81); A developer folds the doubters' votes and the scorers' marks into one report. (eval/tools/coyodex_eval/cli.py:84); A developer checks whether stored scores were made under today's judging rules. (eval/tools/coyodex_eval/cli.py:87); A developer promotes one scored run to be the new accepted baseline. (eval/tools/coyodex_eval/cli.py:90); A developer asks which relations a rebuild dropped while the code still makes them. (eval/tools/coyodex_eval/cli.py:93); A developer applies the regression checks to two maps' number sheets. (eval/tools/coyodex_eval/cli.py:96); A developer scores how the build agent behaved during one build. (eval/tools/coyodex_eval/cli.py:99); To: C105 = Lost arrows (eval/tools/coyodex_eval/arrows.py:152)",
   "why_risky": "backbone edge — no deterministic gate confirms C100's code 'calls' C105; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C100 calls C106",
   "anchor": "eval/tools/coyodex_eval/cli.py:102",
   "detail": "From: C100 = Eval command line (eval/tools/coyodex_eval/cli.py:69); entry points: A developer moves a project's map aside so the next build starts from nothing. (eval/tools/coyodex_eval/cli.py:102); A developer checks a retrospective's finished rows against the code history. (eval/tools/coyodex_eval/cli.py:105); A developer re-measures the counts the tools state about a live map. (eval/tools/coyodex_eval/cli.py:108); A developer asks whether the build a retrospective would read has finished. (eval/tools/coyodex_eval/cli.py:111); A developer reads a build's chat log in slices. (eval/tools/coyodex_eval/cli.py:114); A developer asks what one build spent in time and tokens. (eval/tools/coyodex_eval/cli.py:117); A developer plants false claims, then scores how many the doubters caught. (eval/tools/coyodex_eval/cli.py:120); A developer asks for one built map's quality numbers. (eval/tools/coyodex_eval/cli.py:72); A developer scores a fresh map, compares it with the accepted one, and files the result. (eval/tools/coyodex_eval/cli.py:75); A developer freezes a map file's fingerprint before scoring it. (eval/tools/coyodex_eval/cli.py:78); A developer lists the risky statements a doubter should check against the code. (eval/tools/coyodex_eval/cli.py:81); A developer folds the doubters' votes and the scorers' marks into one report. (eval/tools/coyodex_eval/cli.py:84); A developer checks whether stored scores were made under today's judging rules. (eval/tools/coyodex_eval/cli.py:87); A developer promotes one scored run to be the new accepted baseline. (eval/tools/coyodex_eval/cli.py:90); A developer asks which relations a rebuild dropped while the code still makes them. (eval/tools/coyodex_eval/cli.py:93); A developer applies the regression checks to two maps' number sheets. (eval/tools/coyodex_eval/cli.py:96); A developer scores how the build agent behaved during one build. (eval/tools/coyodex_eval/cli.py:99); To: C106 = Rebuild archive (eval/tools/coyodex_eval/archive.py:110)",
   "why_risky": "backbone edge — no deterministic gate confirms C100's code 'calls' C106; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C100 calls C112",
   "anchor": "eval/tools/coyodex_eval/cli.py:108",
   "detail": "From: C100 = Eval command line (eval/tools/coyodex_eval/cli.py:69); entry points: A developer moves a project's map aside so the next build starts from nothing. (eval/tools/coyodex_eval/cli.py:102); A developer checks a retrospective's finished rows against the code history. (eval/tools/coyodex_eval/cli.py:105); A developer re-measures the counts the tools state about a live map. (eval/tools/coyodex_eval/cli.py:108); A developer asks whether the build a retrospective would read has finished. (eval/tools/coyodex_eval/cli.py:111); A developer reads a build's chat log in slices. (eval/tools/coyodex_eval/cli.py:114); A developer asks what one build spent in time and tokens. (eval/tools/coyodex_eval/cli.py:117); A developer plants false claims, then scores how many the doubters caught. (eval/tools/coyodex_eval/cli.py:120); A developer asks for one built map's quality numbers. (eval/tools/coyodex_eval/cli.py:72); A developer scores a fresh map, compares it with the accepted one, and files the result. (eval/tools/coyodex_eval/cli.py:75); A developer freezes a map file's fingerprint before scoring it. (eval/tools/coyodex_eval/cli.py:78); A developer lists the risky statements a doubter should check against the code. (eval/tools/coyodex_eval/cli.py:81); A developer folds the doubters' votes and the scorers' marks into one report. (eval/tools/coyodex_eval/cli.py:84); A developer checks whether stored scores were made under today's judging rules. (eval/tools/coyodex_eval/cli.py:87); A developer promotes one scored run to be the new accepted baseline. (eval/tools/coyodex_eval/cli.py:90); A developer asks which relations a rebuild dropped while the code still makes them. (eval/tools/coyodex_eval/cli.py:93); A developer applies the regression checks to two maps' number sheets. (eval/tools/coyodex_eval/cli.py:96); A developer scores how the build agent behaved during one build. (eval/tools/coyodex_eval/cli.py:99); To: C112 = Live number ledger (eval/tools/coyodex_eval/live_numbers.py:540)",
   "why_risky": "backbone edge — no deterministic gate confirms C100's code 'calls' C112; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C100 calls C113",
   "anchor": "eval/tools/coyodex_eval/cli.py:120",
   "detail": "From: C100 = Eval command line (eval/tools/coyodex_eval/cli.py:69); entry points: A developer moves a project's map aside so the next build starts from nothing. (eval/tools/coyodex_eval/cli.py:102); A developer checks a retrospective's finished rows against the code history. (eval/tools/coyodex_eval/cli.py:105); A developer re-measures the counts the tools state about a live map. (eval/tools/coyodex_eval/cli.py:108); A developer asks whether the build a retrospective would read has finished. (eval/tools/coyodex_eval/cli.py:111); A developer reads a build's chat log in slices. (eval/tools/coyodex_eval/cli.py:114); A developer asks what one build spent in time and tokens. (eval/tools/coyodex_eval/cli.py:117); A developer plants false claims, then scores how many the doubters caught. (eval/tools/coyodex_eval/cli.py:120); A developer asks for one built map's quality numbers. (eval/tools/coyodex_eval/cli.py:72); A developer scores a fresh map, compares it with the accepted one, and files the result. (eval/tools/coyodex_eval/cli.py:75); A developer freezes a map file's fingerprint before scoring it. (eval/tools/coyodex_eval/cli.py:78); A developer lists the risky statements a doubter should check against the code. (eval/tools/coyodex_eval/cli.py:81); A developer folds the doubters' votes and the scorers' marks into one report. (eval/tools/coyodex_eval/cli.py:84); A developer checks whether stored scores were made under today's judging rules. (eval/tools/coyodex_eval/cli.py:87); A developer promotes one scored run to be the new accepted baseline. (eval/tools/coyodex_eval/cli.py:90); A developer asks which relations a rebuild dropped while the code still makes them. (eval/tools/coyodex_eval/cli.py:93); A developer applies the regression checks to two maps' number sheets. (eval/tools/coyodex_eval/cli.py:96); A developer scores how the build agent behaved during one build. (eval/tools/coyodex_eval/cli.py:99); To: C113 = Skeptic recall test (eval/tools/coyodex_eval/mutate.py:236)",
   "why_risky": "backbone edge — no deterministic gate confirms C100's code 'calls' C113; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C104 calls C101",
   "anchor": "eval/tools/coyodex_eval/run.py:60",
   "detail": "From: C104 = Eval run (eval/tools/coyodex_eval/run.py:69); To: C101 = Map profile (eval/tools/coyodex_eval/profile.py:430)",
   "why_risky": "backbone edge — no deterministic gate confirms C104's code 'calls' C101; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C104 calls C102",
   "anchor": "eval/tools/coyodex_eval/run.py:67",
   "detail": "From: C104 = Eval run (eval/tools/coyodex_eval/run.py:69); To: C102 = Baseline comparison (eval/tools/coyodex_eval/compare.py:518)",
   "why_risky": "backbone edge — no deterministic gate confirms C104's code 'calls' C102; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C104 calls C103",
   "anchor": "eval/tools/coyodex_eval/run.py:394",
   "detail": "From: C104 = Eval run (eval/tools/coyodex_eval/run.py:69); To: C103 = Judge report (eval/tools/coyodex_eval/judge.py:257)",
   "why_risky": "backbone edge — no deterministic gate confirms C104's code 'calls' C103; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C109 calls C107",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:3335",
   "detail": "From: C109 = Process scorecard (eval/tools/coyodex_eval/process_scorecard.py:3318); To: C107 = Transcript reader (eval/tools/coyodex_eval/transcript.py:442)",
   "why_risky": "backbone edge — no deterministic gate confirms C109's code 'calls' C107; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C108 calls C107",
   "anchor": "eval/tools/coyodex_eval/cost.py:283",
   "detail": "From: C108 = Build cost (eval/tools/coyodex_eval/cost.py:620); To: C107 = Transcript reader (eval/tools/coyodex_eval/transcript.py:442)",
   "why_risky": "backbone edge — no deterministic gate confirms C108's code 'calls' C107; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C20 starts C83",
   "anchor": "tools/coyodex/cli.py:168",
   "detail": "From: C20 = Command line (tools/coyodex/cli.py:139); entry points: An operator runs `coyodex preindex` to build the structural pre-index a map build starts from. (tools/coyodex/cli.py:153); An operator runs `coyodex validate` to check that a map is well formed. (tools/coyodex/cli.py:156); An operator runs `coyodex audit` to find where a map contradicts itself. (tools/coyodex/cli.py:159); An operator runs `coyodex render` to write the map's committed markdown view. (tools/coyodex/cli.py:165); An operator runs `coyodex serve` to open the viewer for a map in a browser. (tools/coyodex/cli.py:168); An operator runs `coyodex assemble` to merge the build agents' fragments into one map. (tools/coyodex/cli.py:171); An operator runs `coyodex balance` to see which diagrams hold too many boxes. (tools/coyodex/cli.py:183); An operator runs `coyodex reconcile` to expand the path rules into an explicit assignment file. (tools/coyodex/cli.py:186); A build agent runs `coyodex lint-fragment` to self-check one fragment before returning it. (tools/coyodex/cli.py:189); An operator runs `coyodex finalize` to get one pre-commit verdict over every check. (tools/coyodex/cli.py:195); To: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883)",
   "why_risky": "backbone edge — no deterministic gate confirms C20's code 'starts' C83; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C83 reads C85",
   "anchor": "tools/coyodex/viewer/serve.py:883",
   "detail": "From: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883); To: C85 = Remembered projects (tools/coyodex/viewer/recents.py:22)",
   "why_risky": "backbone edge — no deterministic gate confirms C83's code 'reads' C85; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C83 updates C85",
   "anchor": "tools/coyodex/viewer/serve.py:700",
   "detail": "From: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883); To: C85 = Remembered projects (tools/coyodex/viewer/recents.py:22)",
   "why_risky": "backbone edge — no deterministic gate confirms C83's code 'updates' C85; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C51 starts C83",
   "anchor": "tools/devserve.py:55",
   "detail": "From: C51 = Live reload supervisor (tools/devserve.py:103); entry points: The viewer's code changed on disk, so the map server restarts. (tools/devserve.py:97); To: C83 = Map server (tools/coyodex/viewer/serve.py:869); entry points: A map page opened in developer mode asks the server every second whether the viewer's files changed. (tools/coyodex/viewer/serve.py:225); Opening the server's home address lists every project folder opened before, each one openable or removable. (tools/coyodex/viewer/serve.py:640); Asking `/api/recents` re-reads the remembered folders and says which of their maps can be opened. (tools/coyodex/viewer/serve.py:677); Asking `/api/browse` lists the folders inside one folder, marking each folder that holds a map. (tools/coyodex/viewer/serve.py:688); Posting to `/api/open` remembers a folder and starts serving the map inside it. (tools/coyodex/viewer/serve.py:700); Posting to `/api/forget` drops a folder from the remembered list and stops serving its map. (tools/coyodex/viewer/serve.py:708); Posting to `/api/reorder` saves a new order for the remembered project folders. (tools/coyodex/viewer/serve.py:717); Opening a project's own address returns the browser page that draws that project's map. (tools/coyodex/viewer/serve.py:730); Asking `/api/dev-reload` says whether the viewer's own files changed, so a map page can reload itself. (tools/coyodex/viewer/serve.py:744); Asking `/api/health` says the server is answering and names the commit this map is pinned to. (tools/coyodex/viewer/serve.py:746); Asking `/api/view` returns every diagram, flow and colour the page needs for one map. (tools/coyodex/viewer/serve.py:752); Asking `/api/rawmap` returns the stored map exactly as its file holds it, byte for byte. (tools/coyodex/viewer/serve.py:763); Asking `/api/tree` returns the project's folders and files, each marked by how far the map covers it. (tools/coyodex/viewer/serve.py:769); Asking `/api/symbols` returns every function and class name found in the project at the map's commit. (tools/coyodex/viewer/serve.py:775); Asking `/api/src` returns one file's text, read at the map's commit or at another one named in the request. (tools/coyodex/viewer/serve.py:797); Asking `/api/impact` says what a code change between two commits does to the map. (tools/coyodex/viewer/serve.py:807); Asking `/api/impactcommits` lists the commits before and after the one this map is pinned to. (tools/coyodex/viewer/serve.py:815); Asking `/api/impactsrcdiff` returns one file's line-by-line changes between two commits. (tools/coyodex/viewer/serve.py:822); Asking `/static` returns the shared script or stylesheet every map page is drawn with. (tools/coyodex/viewer/serve.py:851); Starting the server reads the remembered folders and loads every map still found there. (tools/coyodex/viewer/serve.py:883)",
   "why_risky": "backbone edge — no deterministic gate confirms C51's code 'starts' C83; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C134 points at C130",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": "From: C134 = Build skill pointer (skill/coyodex/SKILL.md:27); entry points: A person types `/coyodex`, and their coding agent reads this file to find the method. (skill/coyodex/SKILL.md:27); To: C130 = Mode dispatch instructions (method/dispatch.md:3)",
   "why_risky": "backbone edge — no deterministic gate confirms C134's code 'points at' C130; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  }
 ],
 "themes": [
  "security",
  "rule",
  "dep-usage",
  "ownership",
  "persistence",
  "messaging",
  "interface",
  "lifecycle",
  "cadence",
  "description",
  "backbone",
  "behaviour"
 ],
 "theme_counts": {
  "security": 27,
  "rule": 222,
  "dep-usage": 17,
  "ownership": 30,
  "persistence": 3,
  "interface": 16,
  "cadence": 3,
  "description": 79,
  "backbone": 112,
  "behaviour": 219
 }
}
