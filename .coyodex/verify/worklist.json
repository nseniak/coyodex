{
 "findings": [
  {
   "check": "recorded-exceptions",
   "severity": "WARNING",
   "location": "'Audit exceptions' extras heading",
   "where": "'Audit exceptions' extras heading",
   "message": "7 advisory/advisories suppressed by recorded exception(s): read-before-create HP2, read-before-create HP5, read-never-created HP17, read-never-created HP17, read-never-created HP6, read-never-created HP8, why-less-step HP15. Each was judged acceptable by an operator and is NOT re-reported above; re-read them by validating a copy with the line removed. A recorded line silences exactly one (check, id) pair — never a whole family."
  }
 ],
 "worklist": [
  {
   "claim": "Rule 'Nothing the user owns is committed, stashed or overwritten unless the user asks for it.' is enforced at method/dispatch.md:46 — forbids committing or stashing the person's code, even to make the pin clean",
   "anchor": "method/dispatch.md:46",
   "detail": "In: The build method (C75)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Nothing the user owns is committed, stashed or overwritten unless the user asks for it.' is enforced at method.md:2040 — repeats the same ban at the moment the pin is recorded",
   "anchor": "method.md:2040",
   "detail": "In: The build method (C75)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Nothing the user owns is committed, stashed or overwritten unless the user asks for it.' is enforced at method/dispatch.md:171 — regenerates a map from scratch only on an explicit request, after a warning and a confirmation",
   "anchor": "method/dispatch.md:171",
   "detail": "In: The build method (C75)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map server answers only the person sitting at this machine. It listens on the loopback address and nowhere else. A request that names any other site is refused. A request that changes the remembered project list must carry a private marker header.' is enforced at tools/coyodex/viewer/serve.py:800 — The server binds to the loopback address, so nothing off this machine can connect.",
   "anchor": "tools/coyodex/viewer/serve.py:800",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map server answers only the person sitting at this machine. It listens on the loopback address and nowhere else. A request that names any other site is refused. A request that changes the remembered project list must carry a private marker header.' is enforced at tools/coyodex/viewer/serve.py:577 — Every read request whose claimed site is not loopback is refused.",
   "anchor": "tools/coyodex/viewer/serve.py:577",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map server answers only the person sitting at this machine. It listens on the loopback address and nowhere else. A request that names any other site is refused. A request that changes the remembered project list must carry a private marker header.' is enforced at tools/coyodex/viewer/serve.py:599 — The same site check refuses a write request.",
   "anchor": "tools/coyodex/viewer/serve.py:599",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map server answers only the person sitting at this machine. It listens on the loopback address and nowhere else. A request that names any other site is refused. A request that changes the remembered project list must carry a private marker header.' is enforced at tools/coyodex/viewer/serve.py:601 — A write request without the private marker header is refused.",
   "anchor": "tools/coyodex/viewer/serve.py:601",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The server shows only project folders the user has opened before. It never searches the disk for maps. An address naming any other project is refused. A folder can be added only when it already holds a coyodex folder.' is enforced at tools/coyodex/viewer/serve.py:799 — The served set is built from the remembered list alone, with no disk search.",
   "anchor": "tools/coyodex/viewer/serve.py:799",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The server shows only project folders the user has opened before. It never searches the disk for maps. An address naming any other project is refused. A folder can be added only when it already holds a coyodex folder.' is enforced at tools/coyodex/viewer/serve.py:590 — An address that does not name a served project is refused.",
   "anchor": "tools/coyodex/viewer/serve.py:590",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The server shows only project folders the user has opened before. It never searches the disk for maps. An address naming any other project is refused. A folder can be added only when it already holds a coyodex folder.' is enforced at tools/coyodex/viewer/serve.py:640 — Adding a folder is refused unless it holds a coyodex folder.",
   "anchor": "tools/coyodex/viewer/serve.py:640",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The server shows only project folders the user has opened before. It never searches the disk for maps. An address naming any other project is refused. A folder can be added only when it already holds a coyodex folder.' is enforced at tools/coyodex/viewer/serve.py:794 — A folder named at startup is skipped under the same condition.",
   "anchor": "tools/coyodex/viewer/serve.py:794",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The code viewer opens a file only when it sits inside the project being shown. A path that climbs out of the project is refused. A link that points outside the project is refused too. From the working tree only files version control accounts for are served.' is enforced at tools/coyodex/viewer/serve.py:285 — An absolute or otherwise malformed path is rejected before any read.",
   "anchor": "tools/coyodex/viewer/serve.py:285",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The code viewer opens a file only when it sits inside the project being shown. A path that climbs out of the project is refused. A link that points outside the project is refused too. From the working tree only files version control accounts for are served.' is enforced at tools/coyodex/viewer/serve.py:287 — A path containing a step up to the parent folder is rejected.",
   "anchor": "tools/coyodex/viewer/serve.py:287",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The code viewer opens a file only when it sits inside the project being shown. A path that climbs out of the project is refused. A link that points outside the project is refused too. From the working tree only files version control accounts for are served.' is enforced at tools/coyodex/viewer/serve.py:697 — The code request refuses a bad path before touching disk or version control.",
   "anchor": "tools/coyodex/viewer/serve.py:697",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The code viewer opens a file only when it sits inside the project being shown. A path that climbs out of the project is refused. A link that points outside the project is refused too. From the working tree only files version control accounts for are served.' is enforced at tools/coyodex/viewer/serve.py:324 — A working-tree read refuses anything inside the version-control folder.",
   "anchor": "tools/coyodex/viewer/serve.py:324",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The code viewer opens a file only when it sits inside the project being shown. A path that climbs out of the project is refused. A link that points outside the project is refused too. From the working tree only files version control accounts for are served.' is enforced at tools/coyodex/viewer/serve.py:331 — The real resolved location must still sit inside the project, so a link cannot escape.",
   "anchor": "tools/coyodex/viewer/serve.py:331",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The code viewer opens a file only when it sits inside the project being shown. A path that climbs out of the project is refused. A link that points outside the project is refused too. From the working tree only files version control accounts for are served.' is enforced at tools/coyodex/viewer/serve.py:338 — A file version control ignores is refused, so an ignored secrets file never leaks.",
   "anchor": "tools/coyodex/viewer/serve.py:338",
   "detail": "In: Map server (C43)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A link that leaves the page may open only a known code editor. The address must start with one of a fixed list of editor schemes. A hand-typed link template using any other scheme is refused.' is enforced at tools/coyodex/viewer/viewer.js:10684 — An address outside the allowed editor list never becomes a clickable link.",
   "anchor": "tools/coyodex/viewer/viewer.js:10684",
   "detail": "In: Map canvas (C55), Map reading pages (C56), Trail and history (C57), Source column (C58)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A link that leaves the page may open only a known code editor. The address must start with one of a fixed list of editor schemes. A hand-typed link template using any other scheme is refused.' is enforced at tools/coyodex/viewer/viewer.js:10815 — Saving a custom link template is refused when its scheme is not on the list.",
   "anchor": "tools/coyodex/viewer/viewer.js:10815",
   "detail": "In: Map canvas (C55), Map reading pages (C56), Trail and history (C57), Source column (C58)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The viewer page loads its two diagram libraries from the internet at exact pinned versions. Each one carries a fingerprint of the expected file. The browser refuses to run a file whose content does not match that fingerprint.' is enforced at tools/coyodex/viewer/viewer.html:17 — The pan and zoom library is pinned by fingerprint.",
   "anchor": "tools/coyodex/viewer/viewer.html:17",
   "detail": "In: Map canvas (C55), Trail and history (C57), Source column (C58)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The viewer page loads its two diagram libraries from the internet at exact pinned versions. Each one carries a fingerprint of the expected file. The browser refuses to run a file whose content does not match that fingerprint.' is enforced at tools/coyodex/viewer/viewer.html:20 — The diagram drawing library is pinned by fingerprint.",
   "anchor": "tools/coyodex/viewer/viewer.html:20",
   "detail": "In: Map canvas (C55), Trail and history (C57), Source column (C58)",
   "why_risky": "an ACCESS decision and the line that enforces it — a false claim here is an access-control hole.",
   "theme": "security",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Version control decides which files a map may describe. A project with no version control falls back to a walk of the disk.' is enforced at tools/coyodex/preindex_lib.py:198 — asks version control for the files it tracks, instead of walking the disk",
   "anchor": "tools/coyodex/preindex_lib.py:198",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Version control decides which files a map may describe. A project with no version control falls back to a walk of the disk.' is enforced at tools/coyodex/preindex_lib.py:201 — adds files a person created but never added, and drops everything version control is told to ignore",
   "anchor": "tools/coyodex/preindex_lib.py:201",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Version control decides which files a map may describe. A project with no version control falls back to a walk of the disk.' is enforced at tools/coyodex/preindex_lib.py:224 — falls back to reading the disk when the folder is not under version control",
   "anchor": "tools/coyodex/preindex_lib.py:224",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Installed packages, build output, lock files and images are never analysed, so the map measures code people wrote.' is enforced at tools/coyodex/preindex_lib.py:148 — drops any file sitting under a folder on the built-in list of generated and vendored trees",
   "anchor": "tools/coyodex/preindex_lib.py:148",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Installed packages, build output, lock files and images are never analysed, so the map measures code people wrote.' is enforced at tools/coyodex/preindex_lib.py:236 — applies that built-in list before anything the project itself declares",
   "anchor": "tools/coyodex/preindex_lib.py:236",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Installed packages, build output, lock files and images are never analysed, so the map measures code people wrote.' is enforced at tools/coyodex/preindex_lib.py:257 — prunes the same folders in the disk walk, so both ways of listing files give the same shape",
   "anchor": "tools/coyodex/preindex_lib.py:257",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project can name committed folders and files that the map is not meant to describe.' is enforced at tools/coyodex/preindex_lib.py:242 — tests each surviving file against the project's own exclusion list, kept separate from the built-in list",
   "anchor": "tools/coyodex/preindex_lib.py:242",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project can name committed folders and files that the map is not meant to describe.' is enforced at tools/coyodex/preindex_lib.py:245 — takes the file out of the analysed tree when the deciding pattern is an exclusion",
   "anchor": "tools/coyodex/preindex_lib.py:245",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A project can name committed folders and files that the map is not meant to describe.' is enforced at tools/coyodex/pathmatch.py:52 — a pattern naming a folder and no wildcard also covers everything beneath that folder",
   "anchor": "tools/coyodex/pathmatch.py:52",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'When several exclusion patterns match one file, the last pattern written decides. So a later line can put one file back.' is enforced at tools/coyodex/ignorefile.py:88 — keeps scanning after a match, so the answer is the last matching pattern and not the first",
   "anchor": "tools/coyodex/ignorefile.py:88",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'When several exclusion patterns match one file, the last pattern written decides. So a later line can put one file back.' is enforced at tools/coyodex/ignorefile.py:95 — reads the file as excluded only when that deciding pattern is an exclusion, not a put-back",
   "anchor": "tools/coyodex/ignorefile.py:95",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'When several exclusion patterns match one file, the last pattern written decides. So a later line can put one file back.' is enforced at tools/coyodex/preindex_lib.py:245 — keeps the file when the deciding pattern is a put-back line",
   "anchor": "tools/coyodex/preindex_lib.py:245",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every check and every screen reading a narrowed tree reports which patterns narrowed it and how many files went.' is enforced at tools/coyodex/validate_model.py:4464 — the disclosure runs on every validate, before the coverage checks, not only in the expensive pass",
   "anchor": "tools/coyodex/validate_model.py:4464",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every check and every screen reading a narrowed tree reports which patterns narrowed it and how many files went.' is enforced at tools/coyodex/scope.py:132 — the briefing shown before a build states how many files the project's own list removed",
   "anchor": "tools/coyodex/scope.py:132",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every check and every screen reading a narrowed tree reports which patterns narrowed it and how many files went.' is enforced at tools/coyodex/preindex.py:431 — the pre-index report names the removed count and every pattern behind it",
   "anchor": "tools/coyodex/preindex.py:431",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every check and every screen reading a narrowed tree reports which patterns narrowed it and how many files went.' is enforced at tools/coyodex/viewer/filetree.py:261 — puts the note on the root of the file browser, so a narrowed tree is never shown as the whole project",
   "anchor": "tools/coyodex/viewer/filetree.py:261",
   "detail": "In: File browser tree (C46)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pattern that removed no file is named on its own, including a line whose spelling can never match anything.' is enforced at tools/coyodex/ignorefile.py:133 — collects the patterns that decided nothing, so no report can offer only a total",
   "anchor": "tools/coyodex/ignorefile.py:133",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pattern that removed no file is named on its own, including a line whose spelling can never match anything.' is enforced at tools/coyodex/validate_analysis.py:336 — validate names those patterns in a warning of their own",
   "anchor": "tools/coyodex/validate_analysis.py:336",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pattern that removed no file is named on its own, including a line whose spelling can never match anything.' is enforced at tools/coyodex/scope.py:135 — the briefing before a build warns about them too",
   "anchor": "tools/coyodex/scope.py:135",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pattern that removed no file is named on its own, including a line whose spelling can never match anything.' is enforced at tools/coyodex/ignorefile.py:170 — a line carrying a comment after the pattern is dropped and reported, because a comment only opens at the start of a line",
   "anchor": "tools/coyodex/ignorefile.py:170",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pattern that removed no file is named on its own, including a line whose spelling can never match anything.' is enforced at tools/coyodex/ignorefile.py:176 — a line made only of slashes is dropped and reported instead of being stored as a pattern that can never fire",
   "anchor": "tools/coyodex/ignorefile.py:176",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A measurement that moved too far asks for a human look, while a failed check stops the work.' is enforced at eval/tools/coyodex_eval/compare.py:503 — any failed hard check makes the answer the blocking one, before anything else is considered",
   "anchor": "eval/tools/coyodex_eval/compare.py:503",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A measurement that moved too far asks for a human look, while a failed check stops the work.' is enforced at eval/tools/coyodex_eval/compare.py:505 — a breached allowance only reaches the softer answer, which asks for a person rather than blocking",
   "anchor": "eval/tools/coyodex_eval/compare.py:505",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A measurement that moved too far asks for a human look, while a failed check stops the work.' is enforced at eval/tools/coyodex_eval/compare.py:670 — the three answers leave three different exit codes, so an unattended run can tell them apart",
   "anchor": "eval/tools/coyodex_eval/compare.py:670",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A measurement that moved too far asks for a human look, while a failed check stops the work.' is enforced at eval/tools/coyodex_eval/run.py:275 — the full run returns the same three codes, keeping the meaning identical wherever the comparison is invoked",
   "anchor": "eval/tools/coyodex_eval/run.py:275",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A count that grew is never called worse, while a count that fell by thirty percent is.' is enforced at eval/tools/coyodex_eval/compare.py:195 — a shrink-only limit passes any growth and only breaches on a fall past the allowance",
   "anchor": "eval/tools/coyodex_eval/compare.py:195",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A count that grew is never called worse, while a count that fell by thirty percent is.' is enforced at eval/tools/coyodex_eval/compare.py:454 — the name of each limit decides whether it is shrink-only or reacts to movement in both directions",
   "anchor": "eval/tools/coyodex_eval/compare.py:454",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A count that grew is never called worse, while a count that fell by thirty percent is.' is enforced at eval/tools/coyodex_eval/compare.py:57 — sets the allowed fall for a raw count at thirty percent of the accepted map's count",
   "anchor": "eval/tools/coyodex_eval/compare.py:57",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The number of boxes is measured against a count derived from the code, not against the accepted map.' is enforced at eval/tools/coyodex_eval/profile.py:271 — the expected number of boxes is computed fresh from the source tree at scoring time",
   "anchor": "eval/tools/coyodex_eval/profile.py:271",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The number of boxes is measured against a count derived from the code, not against the accepted map.' is enforced at eval/tools/coyodex_eval/compare.py:444 — the new map's distance is measured from that code-derived expectation, not from the accepted map",
   "anchor": "eval/tools/coyodex_eval/compare.py:444",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The number of boxes is measured against a count derived from the code, not against the accepted map.' is enforced at eval/tools/coyodex_eval/compare.py:450 — only the new map's distance decides the check; the accepted map's distance is shown but never decides",
   "anchor": "eval/tools/coyodex_eval/compare.py:450",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Judging that did not happen never becomes a score, for the map or against it.' is enforced at eval/tools/coyodex_eval/judge.py:241 — claims nobody could read are removed from the divisor, so they never lower the pass rate",
   "anchor": "eval/tools/coyodex_eval/judge.py:241",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Judging that did not happen never becomes a score, for the map or against it.' is enforced at eval/tools/coyodex_eval/judge.py:303 — a reader who says it could not reach the code is recorded as a failure, never as a disproof",
   "anchor": "eval/tools/coyodex_eval/judge.py:303",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Judging that did not happen never becomes a score, for the map or against it.' is enforced at eval/tools/coyodex_eval/judge.py:336 — a scoring stage that produced nothing yields no dimension scores instead of zeros",
   "anchor": "eval/tools/coyodex_eval/judge.py:336",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Judging that did not happen never becomes a score, for the map or against it.' is enforced at eval/tools/coyodex_eval/compare.py:212 — caps how much of the reading may fail before the whole reading is called untrustworthy",
   "anchor": "eval/tools/coyodex_eval/compare.py:212",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Judging that did not happen never becomes a score, for the map or against it.' is enforced at eval/tools/coyodex_eval/compare.py:493 — either side missing its reading breaches, so a skipped reading can never end in a clean pass",
   "anchor": "eval/tools/coyodex_eval/compare.py:493",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check that cannot run is reported as skipped, because silence would read as a check that passed.' is enforced at eval/tools/coyodex_eval/compare.py:282 — the source-coverage check names itself as skipped when a map was scored without the code",
   "anchor": "eval/tools/coyodex_eval/compare.py:282",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check that cannot run is reported as skipped, because silence would read as a check that passed.' is enforced at eval/tools/coyodex_eval/compare.py:385 — a check that is always true against an older accepted map is announced as empty rather than reported as passing",
   "anchor": "eval/tools/coyodex_eval/compare.py:385",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check that cannot run is reported as skipped, because silence would read as a check that passed.' is enforced at eval/tools/coyodex_eval/compare.py:437 — the zoom check says it was skipped when the new map carries no code-derived expectation",
   "anchor": "eval/tools/coyodex_eval/compare.py:437",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check that cannot run is reported as skipped, because silence would read as a check that passed.' is enforced at eval/tools/coyodex_eval/compare.py:534 — the comparison of enforcement locations says it was skipped rather than reading absence as agreement",
   "anchor": "eval/tools/coyodex_eval/compare.py:534",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check that cannot run is reported as skipped, because silence would read as a check that passed.' is enforced at eval/tools/coyodex_eval/process_scorecard.py:3182 — the scorecard refuses to score at all when the map cannot be read, rather than letting three map-reading rules score zero while the run still reports success",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:3182",
   "detail": "In: Build behaviour scorecard (C65)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The run refuses a verdict when the map changed after freezing, or when the accepted map is missing.' is enforced at eval/tools/coyodex_eval/run.py:212 — an empty freeze value is refused, so the guard is never switched off by an unnoticed typing mistake",
   "anchor": "eval/tools/coyodex_eval/run.py:212",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The run refuses a verdict when the map changed after freezing, or when the accepted map is missing.' is enforced at eval/tools/coyodex_eval/run.py:219 — refuses to score a map whose bytes no longer match the ones frozen when the run was picked",
   "anchor": "eval/tools/coyodex_eval/run.py:219",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The run refuses a verdict when the map changed after freezing, or when the accepted map is missing.' is enforced at eval/tools/coyodex_eval/run.py:242 — a named accepted map that is not there stops the run instead of quietly comparing nothing",
   "anchor": "eval/tools/coyodex_eval/run.py:242",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The run refuses a verdict when the map changed after freezing, or when the accepted map is missing.' is enforced at eval/tools/coyodex_eval/run.py:247 — an accepted map holding no scores stops the run, because an empty standard cannot produce a verdict",
   "anchor": "eval/tools/coyodex_eval/run.py:247",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Judgements kept from an earlier run are thrown away when the judging method has changed since.' is enforced at eval/tools/coyodex_eval/run.py:451 — a stored reading that does not record which judging method made it is treated as unusable",
   "anchor": "eval/tools/coyodex_eval/run.py:451",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Judgements kept from an earlier run are thrown away when the judging method has changed since.' is enforced at eval/tools/coyodex_eval/run.py:455 — a stored reading made under a different judging method is refused, and the map must be judged again",
   "anchor": "eval/tools/coyodex_eval/run.py:455",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Judgements kept from an earlier run are thrown away when the judging method has changed since.' is enforced at eval/tools/coyodex_eval/compare.py:477 — two sides judged under different methods breach, so the comparison cannot end in a clean pass",
   "anchor": "eval/tools/coyodex_eval/compare.py:477",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The checkers are scored on how many deliberately false claims they refuse to confirm.' is enforced at eval/tools/coyodex_eval/mutate.py:228 — confirming a claim that was made false on purpose is the only outcome scored as a miss",
   "anchor": "eval/tools/coyodex_eval/mutate.py:228",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The checkers are scored on how many deliberately false claims they refuse to confirm.' is enforced at eval/tools/coyodex_eval/mutate.py:216 — a moved code link counts as caught only when the checker reports a different line than the planted one",
   "anchor": "eval/tools/coyodex_eval/mutate.py:216",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The checkers are scored on how many deliberately false claims they refuse to confirm.' is enforced at eval/tools/coyodex_eval/mutate.py:161 — the four kinds of falsehood are planted in turn, so no single easy kind can dominate the score",
   "anchor": "eval/tools/coyodex_eval/mutate.py:161",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The checkers are scored on how many deliberately false claims they refuse to confirm.' is enforced at eval/tools/coyodex_eval/mutate.py:113 — a planted moved link must land on a real line of code, so the hard case is not quietly replaced by the easy one",
   "anchor": "eval/tools/coyodex_eval/mutate.py:113",
   "detail": "In: Map quality score and verdict (C63)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A relationship, a step or a decision must name the code line that proves it, or the map is refused.' is enforced at tools/coyodex/validate_model.py:3540 — refuses a relationship that gives no line and does not declare that none exists",
   "anchor": "tools/coyodex/validate_model.py:3540",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A relationship, a step or a decision must name the code line that proves it, or the map is refused.' is enforced at tools/coyodex/validate_model.py:1133 — refuses a decision that lists no place where it is enforced",
   "anchor": "tools/coyodex/validate_model.py:1133",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A relationship, a step or a decision must name the code line that proves it, or the map is refused.' is enforced at tools/coyodex/validate_model.py:1140 — refuses one enforcement place carrying neither a line nor a declared absence",
   "anchor": "tools/coyodex/validate_model.py:1140",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A relationship, a step or a decision must name the code line that proves it, or the map is refused.' is enforced at tools/coyodex/validate_model.py:401 — refuses a step between two parts of the product that gives no line of its own",
   "anchor": "tools/coyodex/validate_model.py:401",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim may declare that no single line enforces it. Declaring no line and naming a line at once is refused.' is enforced at tools/coyodex/validate_model.py:1143 — refuses an enforcement place that both declares no line and gives a line",
   "anchor": "tools/coyodex/validate_model.py:1143",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim may declare that no single line enforces it. Declaring no line and naming a line at once is refused.' is enforced at tools/coyodex/validate_model.py:3546 — warns when a relationship declares no line and still carries one",
   "anchor": "tools/coyodex/validate_model.py:3546",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim may declare that no single line enforces it. Declaring no line and naming a line at once is refused.' is enforced at tools/coyodex/validate_model.py:407 — warns when a step declares no line and still carries one",
   "anchor": "tools/coyodex/validate_model.py:407",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A claim may declare that no single line enforces it. Declaring no line and naming a line at once is refused.' is enforced at tools/coyodex/audit_model.py:966 — leaves a declared absence out of the claim list a checker is asked to read",
   "anchor": "tools/coyodex/audit_model.py:966",
   "detail": "In: Map auditor (C14)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link must name one line, never a whole file. The line must exist in the file. A comment, a blank line or the opening line of a function cannot be the line that acts.' is enforced at tools/coyodex/validate_model.py:3840 — refuses an enforcement place that names a file without a line",
   "anchor": "tools/coyodex/validate_model.py:3840",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link must name one line, never a whole file. The line must exist in the file. A comment, a blank line or the opening line of a function cannot be the line that acts.' is enforced at tools/coyodex/validate_model.py:1146 — refuses the same file-only place while a worker is still writing its own rows",
   "anchor": "tools/coyodex/validate_model.py:1146",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link must name one line, never a whole file. The line must exist in the file. A comment, a blank line or the opening line of a function cannot be the line that acts.' is enforced at tools/coyodex/validate_model.py:4060 — blocks a link whose file is not in the project",
   "anchor": "tools/coyodex/validate_model.py:4060",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link must name one line, never a whole file. The line must exist in the file. A comment, a blank line or the opening line of a function cannot be the line that acts.' is enforced at tools/coyodex/validate_model.py:4078 — blocks a link naming a line past the end of the file",
   "anchor": "tools/coyodex/validate_model.py:4078",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link must name one line, never a whole file. The line must exist in the file. A comment, a blank line or the opening line of a function cannot be the line that acts.' is enforced at tools/coyodex/anchors.py:128 — counts a blank line as a line that cannot act",
   "anchor": "tools/coyodex/anchors.py:128",
   "detail": "In: Map document (C1)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link must name one line, never a whole file. The line must exist in the file. A comment, a blank line or the opening line of a function cannot be the line that acts.' is enforced at tools/coyodex/anchors.py:131 — matches the named line against shapes that never act, such as a comment or a function opening",
   "anchor": "tools/coyodex/anchors.py:131",
   "detail": "In: Map document (C1)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A code link must name one line, never a whole file. The line must exist in the file. A comment, a blank line or the opening line of a function cannot be the line that acts.' is enforced at tools/coyodex/validate_model.py:4148 — runs that shape test over every link claiming an action fires there",
   "anchor": "tools/coyodex/validate_model.py:4148",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A relationship shows one example line where it happens. A step shows the exact line, which the reader can open. A change to that line hits the step it belongs to.' is enforced at tools/coyodex/views.py:649 — shows exactly one line for each relationship, as a witness rather than a list of every call",
   "anchor": "tools/coyodex/views.py:649",
   "detail": "In: Graph builder (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A relationship shows one example line where it happens. A step shows the exact line, which the reader can open. A change to that line hits the step it belongs to.' is enforced at tools/coyodex/views.py:490 — renders a step's own line as a link the reader can open",
   "anchor": "tools/coyodex/views.py:490",
   "detail": "In: Graph builder (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A relationship shows one example line where it happens. A step shows the exact line, which the reader can open. A change to that line hits the step it belongs to.' is enforced at tools/coyodex/impact_lib.py:236 — registers a step's line, so a code change on it points back at that step",
   "anchor": "tools/coyodex/impact_lib.py:236",
   "detail": "In: Change impact (C34)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Two workers reporting the same relationship at the same line are folded into one claim. Two different lines are both kept, so a person decides which one is right.' is enforced at tools/coyodex/assemble.py:713 — makes the line part of what identifies a relationship, so only an exact match folds",
   "anchor": "tools/coyodex/assemble.py:713",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Two workers reporting the same relationship at the same line are folded into one claim. Two different lines are both kept, so a person decides which one is right.' is enforced at tools/coyodex/assemble.py:710 — keeps a relationship that names no line, because nothing tells two of them apart",
   "anchor": "tools/coyodex/assemble.py:710",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Two workers reporting the same relationship at the same line are folded into one claim. Two different lines are both kept, so a person decides which one is right.' is enforced at tools/coyodex/validate_model.py:3570 — flags one relationship declared twice at two different lines, for a person to settle",
   "anchor": "tools/coyodex/validate_model.py:3570",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A link found at the wrong line is reported, never taken as proof the claim is false. A reader may silence one report by writing down why the stored line is right.' is enforced at tools/coyodex/lint_fragment.py:479 — sends every moved link out on the advisory channel, beside the other nudges",
   "anchor": "tools/coyodex/lint_fragment.py:479",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A link found at the wrong line is reported, never taken as proof the claim is false. A reader may silence one report by writing down why the stored line is right.' is enforced at tools/coyodex/lint_fragment.py:491 — prints the moved-link count on a passing verdict, so a clean check still says how many moved",
   "anchor": "tools/coyodex/lint_fragment.py:491",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A link found at the wrong line is reported, never taken as proof the claim is false. A reader may silence one report by writing down why the stored line is right.' is enforced at tools/coyodex/anchor_drift.py:229 — drops one report the reader has recorded as a false alarm",
   "anchor": "tools/coyodex/anchor_drift.py:229",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A link found at the wrong line is reported, never taken as proof the claim is false. A reader may silence one report by writing down why the stored line is right.' is enforced at tools/coyodex/anchor_drift.py:169 — requires the whole claim and a non-empty reason, so one record answers one report",
   "anchor": "tools/coyodex/anchor_drift.py:169",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A link found at the wrong line is reported, never taken as proof the claim is false. A reader may silence one report by writing down why the stored line is right.' is enforced at tools/coyodex/anchor_drift.py:246 — reports back a record that silences nothing, because the claim changed or the line was fixed",
   "anchor": "tools/coyodex/anchor_drift.py:246",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Where a unit runs, how it is reached, and where a signal is emitted stay plain words. No single code line is that place.' is enforced at tools/coyodex/views.py:619 — writes what a unit runs on and how it is reached as plain cells, with no link",
   "anchor": "tools/coyodex/views.py:619",
   "detail": "In: Graph builder (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Where a unit runs, how it is reached, and where a signal is emitted stay plain words. No single code line is that place.' is enforced at tools/coyodex/views.py:623 — writes where a signal is emitted and where it is watched as plain cells, with no link",
   "anchor": "tools/coyodex/views.py:623",
   "detail": "In: Graph builder (C45)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Where a unit runs, how it is reached, and where a signal is emitted stay plain words. No single code line is that place.' is enforced at tools/coyodex/viewer/viewer.js:8178 — shows the emit place in the browser as text, unlike the fields the viewer turns into links",
   "anchor": "tools/coyodex/viewer/viewer.js:8178",
   "detail": "In: Map canvas (C55), Map reading pages (C56), Trail and history (C57), Source column (C58)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A finding stops the work only when whoever receives it can still fix it. A judgement call only advises.' is enforced at tools/coyodex/finalize.py:345 — A disproved claim still standing in the map stops the run, because correcting or dropping it is always possible.",
   "anchor": "tools/coyodex/finalize.py:345",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A finding stops the work only when whoever receives it can still fix it. A judgement call only advises.' is enforced at tools/coyodex/finalize.py:316 — A code link pointing at the wrong line is only advice, because the repair tool cannot apply every one.",
   "anchor": "tools/coyodex/finalize.py:316",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A finding stops the work only when whoever receives it can still fix it. A judgement call only advises.' is enforced at tools/coyodex/finalize.py:420 — How boxes are grouped on a screen is recorded as information, and never allowed to move the verdict.",
   "anchor": "tools/coyodex/finalize.py:420",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A finding stops the work only when whoever receives it can still fix it. A judgement call only advises.' is enforced at tools/coyodex/lint_fragment.py:174 — An access rule that names no stake stops the worker writing it, the one person able to answer.",
   "anchor": "tools/coyodex/lint_fragment.py:174",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A finding stops the work only when whoever receives it can still fix it. A judgement call only advises.' is enforced at tools/coyodex/lint_fragment.py:480 — A judgement-shaped nudge is printed for the worker and never fails the worker's own check.",
   "anchor": "tools/coyodex/lint_fragment.py:480",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check that did not run never counts as a check that passed. The run names each check that was missing.' is enforced at tools/coyodex/finalize.py:490 — One check that should have run and did not makes the whole verdict incomplete.",
   "anchor": "tools/coyodex/finalize.py:490",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check that did not run never counts as a check that passed. The run names each check that was missing.' is enforced at tools/coyodex/finalize.py:934 — An incomplete read exits as a failure, exactly like a finding that blocks.",
   "anchor": "tools/coyodex/finalize.py:934",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check that did not run never counts as a check that passed. The run names each check that was missing.' is enforced at tools/coyodex/lint_fragment.py:487 — A fragment that could not be read gives a did-not-run verdict instead of a clean one.",
   "anchor": "tools/coyodex/lint_fragment.py:487",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check that did not run never counts as a check that passed. The run names each check that was missing.' is enforced at tools/coyodex/finalize.py:393 — A check this run could have done, and was not asked to do, is reported as its own line.",
   "anchor": "tools/coyodex/finalize.py:393",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check that did not run never counts as a check that passed. The run names each check that was missing.' is enforced at tools/coyodex/finalize.py:511 — The written report lists every check that did not run, and says their silence is not a pass.",
   "anchor": "tools/coyodex/finalize.py:511",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Closing a finding for good takes a written line that names the finding and gives the reason. A line that misses the name, or misses the reason, is refused.' is enforced at tools/coyodex/record.py:264 — A line that names a finding and gives no reason is refused as a dismissal.",
   "anchor": "tools/coyodex/record.py:264",
   "detail": "In: Map lookups (C3)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Closing a finding for good takes a written line that names the finding and gives the reason. A line that misses the name, or misses the reason, is refused.' is enforced at tools/coyodex/records.py:151 — The one reader of these lines needs text after the separator before it reads a line as a record.",
   "anchor": "tools/coyodex/records.py:151",
   "detail": "In: Map lookups (C3)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Closing a finding for good takes a written line that names the finding and gives the reason. A line that misses the name, or misses the reason, is refused.' is enforced at tools/coyodex/record.py:269 — A heading no check reads is refused, because a line under it would silence nothing.",
   "anchor": "tools/coyodex/record.py:269",
   "detail": "In: Map lookups (C3)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Closing a finding for good takes a written line that names the finding and gives the reason. A line that misses the name, or misses the reason, is refused.' is enforced at tools/coyodex/record.py:309 — The batch is re-read after writing, and refused whole when one line answers no finding.",
   "anchor": "tools/coyodex/record.py:309",
   "detail": "In: Map lookups (C3)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Closing a finding for good takes a written line that names the finding and gives the reason. A line that misses the name, or misses the reason, is refused.' is enforced at tools/coyodex/validate_model.py:3489 — A stored line that tries to be a record and answers nothing is reported on every run.",
   "anchor": "tools/coyodex/validate_model.py:3489",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A written reason answers only the findings it names. A line that would silence a whole family of findings silences none of them.' is enforced at tools/coyodex/validate_model.py:3439 — An unscoped deployment exception is reported as silencing nothing, because it once switched off every deployment finding at once.",
   "anchor": "tools/coyodex/validate_model.py:3439",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A written reason answers only the findings it names. A line that would silence a whole family of findings silences none of them.' is enforced at tools/coyodex/records.py:271 — A free-text key answers a line only when the line starts with that key, never when it merely contains it.",
   "anchor": "tools/coyodex/records.py:271",
   "detail": "In: Map lookups (C3)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A written reason answers only the findings it names. A line that would silence a whole family of findings silences none of them.' is enforced at tools/coyodex/validate_model.py:1487 — A recorded folder covers itself and what sits under it, never a neighbour whose name starts the same.",
   "anchor": "tools/coyodex/validate_model.py:1487",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A written reason answers only the findings it names. A line that would silence a whole family of findings silences none of them.' is enforced at tools/coyodex/records.py:191 — A key list holding one token that is not a key records nothing, rather than the part it could read.",
   "anchor": "tools/coyodex/records.py:191",
   "detail": "In: Map lookups (C3)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A run always says what each written reason did. The count of findings that reason silenced is shown, or the fact that it silenced none.' is enforced at tools/coyodex/validate_model.py:3395 — Names how many deployment findings the written reasons swallowed, and which groups they covered.",
   "anchor": "tools/coyodex/validate_model.py:3395",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A run always says what each written reason did. The count of findings that reason silenced is shown, or the fact that it silenced none.' is enforced at tools/coyodex/validate_model.py:1262 — Names the decision-sounding steps a written reason silenced, and says they were counted as done because of it.",
   "anchor": "tools/coyodex/validate_model.py:1262",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A run always says what each written reason did. The count of findings that reason silenced is shown, or the fact that it silenced none.' is enforced at tools/coyodex/validate_model.py:3418 — Names a written reason silencing nothing today, so an inert line and a mistyped one are told apart.",
   "anchor": "tools/coyodex/validate_model.py:3418",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A run always says what each written reason did. The count of findings that reason silenced is shown, or the fact that it silenced none.' is enforced at tools/coyodex/validate_model.py:4744 — Drops every written reason for one read, so a person sees the findings they hide without editing the map.",
   "anchor": "tools/coyodex/validate_model.py:4744",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map with open advice is never reported as clean. A run marks one piece of advice answered only where it can name the written line that answers it.' is enforced at tools/coyodex/finalize.py:492 — Open advice gives the run its own verdict, which is not the clean one.",
   "anchor": "tools/coyodex/finalize.py:492",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map with open advice is never reported as clean. A run marks one piece of advice answered only where it can name the written line that answers it.' is enforced at tools/coyodex/finalize.py:929 — Says on screen that advice is not a pass, and that each piece is fixed or recorded.",
   "anchor": "tools/coyodex/finalize.py:929",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map with open advice is never reported as clean. A run marks one piece of advice answered only where it can name the written line that answers it.' is enforced at tools/coyodex/finalize.py:719 — Marks a piece of advice unanswered when the finding it names carries no recorded line.",
   "anchor": "tools/coyodex/finalize.py:719",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map with open advice is never reported as clean. A run marks one piece of advice answered only where it can name the written line that answers it.' is enforced at tools/coyodex/finalize.py:715 — Says the pairing cannot be decided, rather than guessing that a piece of advice was answered.",
   "anchor": "tools/coyodex/finalize.py:715",
   "detail": "In: Pre-commit gate run (C16)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'When two workers state the same thing differently, the merge stops and writes nothing. Neither version is quietly chosen over the other.' is enforced at tools/coyodex/assemble.py:394 — Two workers claiming one identifier is a conflict, never one silently overwriting the other.",
   "anchor": "tools/coyodex/assemble.py:394",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'When two workers state the same thing differently, the merge stops and writes nothing. Neither version is quietly chosen over the other.' is enforced at tools/coyodex/assemble.py:382 — One fact stated twice with different values stops the merge, and both workers are named.",
   "anchor": "tools/coyodex/assemble.py:382",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'When two workers state the same thing differently, the merge stops and writes nothing. Neither version is quietly chosen over the other.' is enforced at tools/coyodex/assemble.py:558 — Two rows for one channel disagreeing on a value is reported, because the merge will not choose between them.",
   "anchor": "tools/coyodex/assemble.py:558",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'When two workers state the same thing differently, the merge stops and writes nothing. Neither version is quietly chosen over the other.' is enforced at tools/coyodex/assemble.py:891 — A merge conflict leaves no map written at all.",
   "anchor": "tools/coyodex/assemble.py:891",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A folder of at most ten code files and three thousand lines counts as one box. A bigger folder is expected to hold several boxes instead.' is enforced at tools/coyodex/preindex_lib.py:599 — stops at a folder that fits both size limits and counts it as exactly one box",
   "anchor": "tools/coyodex/preindex_lib.py:599",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A folder of at most ten code files and three thousand lines counts as one box. A bigger folder is expected to hold several boxes instead.' is enforced at tools/coyodex/preindex_lib.py:616 — goes inside a folder that is over the limits, so its parts are counted one by one",
   "anchor": "tools/coyodex/preindex_lib.py:616",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A folder of at most ten code files and three thousand lines counts as one box. A bigger folder is expected to hold several boxes instead.' is enforced at tools/coyodex/preindex_lib.py:615 — counts an oversized folder with no sub-folders as several boxes, never as one",
   "anchor": "tools/coyodex/preindex_lib.py:615",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The expected number of boxes counts product code only. Documentation, configuration, tests, asset folders and nearly empty folders add no box to that number.' is enforced at tools/coyodex/preindex_lib.py:577 — drops documentation, settings and other text files before counting anything",
   "anchor": "tools/coyodex/preindex_lib.py:577",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The expected number of boxes counts product code only. Documentation, configuration, tests, asset folders and nearly empty folders add no box to that number.' is enforced at tools/coyodex/preindex_lib.py:579 — drops test folders, internal folders and asset folders such as icons, images and translations",
   "anchor": "tools/coyodex/preindex_lib.py:579",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The expected number of boxes counts product code only. Documentation, configuration, tests, asset folders and nearly empty folders add no box to that number.' is enforced at tools/coyodex/preindex_lib.py:609 — folds a nearly empty sub-folder into its parent rather than counting a box for it",
   "anchor": "tools/coyodex/preindex_lib.py:609",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The expected number of boxes counts product code only. Documentation, configuration, tests, asset folders and nearly empty folders add no box to that number.' is enforced at tools/coyodex/preindex_lib.py:619 — counts the loose files of a folder as a box only when they hold real code",
   "anchor": "tools/coyodex/preindex_lib.py:619",
   "detail": "In: Pre-index (C33)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'How coarse a map is can never fail a build, because every coarseness check only leaves a note. The note also stays quiet while the map sits within forty percent of the expected number of boxes.' is enforced at tools/coyodex/validate_model.py:4481 — files the box-count comparison under notes, never under failures",
   "anchor": "tools/coyodex/validate_model.py:4481",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'How coarse a map is can never fail a build, because every coarseness check only leaves a note. The note also stays quiet while the map sits within forty percent of the expected number of boxes.' is enforced at tools/coyodex/validate_model.py:4501 — files the crowded-screen and thin-screen findings under notes, never under failures",
   "anchor": "tools/coyodex/validate_model.py:4501",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'How coarse a map is can never fail a build, because every coarseness check only leaves a note. The note also stays quiet while the map sits within forty percent of the expected number of boxes.' is enforced at tools/coyodex/validate_analysis.py:382 — says nothing at all while the number of boxes stays inside the forty percent margin",
   "anchor": "tools/coyodex/validate_analysis.py:382",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'One screen reads well with three to nine boxes. Once a screen passes twelve boxes the map is told to group them.' is enforced at tools/coyodex/balance_lib.py:613 — counts a screen as reading well while it carries three to nine boxes",
   "anchor": "tools/coyodex/balance_lib.py:613",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'One screen reads well with three to nine boxes. Once a screen passes twelve boxes the map is told to group them.' is enforced at tools/coyodex/balance_lib.py:294 — raises the crowding note when the first screen carries more than twelve boxes",
   "anchor": "tools/coyodex/balance_lib.py:294",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'One screen reads well with three to nine boxes. Once a screen passes twelve boxes the map is told to group them.' is enforced at tools/coyodex/balance_lib.py:307 — raises the same crowding note when a group carries more than twelve boxes",
   "anchor": "tools/coyodex/balance_lib.py:307",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen may stay crowded when its boxes are same-kind siblings sharing one folder or one name ending. Such a family is only called crowded above fifteen boxes.' is enforced at tools/coyodex/balance_lib.py:194 — reads the children as one family when two thirds of them sit in the same folder",
   "anchor": "tools/coyodex/balance_lib.py:194",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen may stay crowded when its boxes are same-kind siblings sharing one folder or one name ending. Such a family is only called crowded above fifteen boxes.' is enforced at tools/coyodex/balance_lib.py:198 — reads the children as one family when two thirds of their names end in the same word",
   "anchor": "tools/coyodex/balance_lib.py:198",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A screen may stay crowded when its boxes are same-kind siblings sharing one folder or one name ending. Such a family is only called crowded above fifteen boxes.' is enforced at tools/coyodex/balance_lib.py:309 — leaves such a family alone until it passes fifteen boxes",
   "anchor": "tools/coyodex/balance_lib.py:309",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A thin screen is a fault on the first screen of a large map. Deeper in the map only a group holding exactly one box is called out.' is enforced at tools/coyodex/balance_lib.py:290 — raises the thin note when the first screen shows under three boxes and the map holds many",
   "anchor": "tools/coyodex/balance_lib.py:290",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A thin screen is a fault on the first screen of a large map. Deeper in the map only a group holding exactly one box is called out.' is enforced at tools/coyodex/balance_lib.py:303 — raises a note when a group holds exactly one box, so the level earns nothing",
   "anchor": "tools/coyodex/balance_lib.py:303",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A thin screen is a fault on the first screen of a large map. Deeper in the map only a group holding exactly one box is called out.' is enforced at tools/coyodex/balance.py:147 — names the thin screens deeper in the map and states plainly that none of them is a fault",
   "anchor": "tools/coyodex/balance.py:147",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A re-grouping suggestion may only move a box into another group. Merging two boxes into one, or cutting one box in two, is never suggested.' is enforced at tools/coyodex/balance_lib.py:524 — starts from exactly the boxes already on the screen, so none is created and none is dropped",
   "anchor": "tools/coyodex/balance_lib.py:524",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A re-grouping suggestion may only move a box into another group. Merging two boxes into one, or cutting one box in two, is never suggested.' is enforced at tools/coyodex/balance.py:103 — the only thing a suggestion adds to the map is a group row",
   "anchor": "tools/coyodex/balance.py:103",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A re-grouping suggestion may only move a box into another group. Merging two boxes into one, or cutting one box in two, is never suggested.' is enforced at tools/coyodex/balance.py:107 — the only change asked of an existing box is which group it belongs to",
   "anchor": "tools/coyodex/balance.py:107",
   "detail": "In: Diagram balance report (C15)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Each feature states by hand whether the product story must reach it. The map reports every feature whose claim the story contradicts. A feature that states nothing is reported too.' is enforced at tools/coyodex/validate_model.py:1693 — reports a feature claiming the story must reach it, when no story step does",
   "anchor": "tools/coyodex/validate_model.py:1693",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Each feature states by hand whether the product story must reach it. The map reports every feature whose claim the story contradicts. A feature that states nothing is reported too.' is enforced at tools/coyodex/validate_model.py:1726 — reports a story step whose work sits in a feature claiming the story skips it",
   "anchor": "tools/coyodex/validate_model.py:1726",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Each feature states by hand whether the product story must reach it. The map reports every feature whose claim the story contradicts. A feature that states nothing is reported too.' is enforced at tools/coyodex/validate_model.py:1701 — separates a feature that answered nothing from a feature claiming the story skips it",
   "anchor": "tools/coyodex/validate_model.py:1701",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Each feature states by hand whether the product story must reach it. The map reports every feature whose claim the story contradicts. A feature that states nothing is reported too.' is enforced at tools/coyodex/validate_model.py:2883 — rejects any answer outside the two allowed words, so an unanswered feature stays visible",
   "anchor": "tools/coyodex/validate_model.py:2883",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The party driving a use case is the one whose goal it serves. Machinery that only carries somebody's action inward is not a driver.' is enforced at tools/coyodex/validate_model.py:1803 — blocks a use case that names no driving party at all",
   "anchor": "tools/coyodex/validate_model.py:1803",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The party driving a use case is the one whose goal it serves. Machinery that only carries somebody's action inward is not a driver.' is enforced at tools/coyodex/validate_model.py:1820 — reports a use case naming a person and a program together, because the program is usually delivery machinery",
   "anchor": "tools/coyodex/validate_model.py:1820",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The party driving a use case is the one whose goal it serves. Machinery that only carries somebody's action inward is not a driver.' is enforced at tools/coyodex/audit_model.py:692 — reports a traced scenario whose opening party is not one of the declared drivers",
   "anchor": "tools/coyodex/audit_model.py:692",
   "detail": "In: Map auditor (C14)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Who a feature is for is never written on the feature itself. The people who drive it decide, and a program votes only when no person does. A feature that customers and the company's own staff both drive is flagged for a re-read.' is enforced at tools/coyodex/validate_model.py:725 — sorts each driving party's vote by whether that party is a person or a program",
   "anchor": "tools/coyodex/validate_model.py:725",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Who a feature is for is never written on the feature itself. The people who drive it decide, and a program votes only when no person does. A feature that customers and the company's own staff both drive is flagged for a re-read.' is enforced at tools/coyodex/validate_model.py:726 — counts the people's votes, and falls back to the programs only when no person voted",
   "anchor": "tools/coyodex/validate_model.py:726",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Who a feature is for is never written on the feature itself. The people who drive it decide, and a program votes only when no person does. A feature that customers and the company's own staff both drive is flagged for a re-read.' is enforced at tools/coyodex/features.py:353 — the feature's record takes the derived answer, so nothing on the feature can contradict its parties",
   "anchor": "tools/coyodex/features.py:353",
   "detail": "In: Feature index (C17)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Who a feature is for is never written on the feature itself. The people who drive it decide, and a program votes only when no person does. A feature that customers and the company's own staff both drive is flagged for a re-read.' is enforced at tools/coyodex/validate_model.py:3057 — reports a feature whose people pull to opposite sides, unless the map records why",
   "anchor": "tools/coyodex/validate_model.py:3057",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A feature the walk never reaches still holds a place in the one story column. Its author names the feature it reads beside. Without that name the map guesses from the feature's own parties, and otherwise puts it last.' is enforced at tools/coyodex/features.py:193 — uses the author's named neighbour when the feature carries one",
   "anchor": "tools/coyodex/features.py:193",
   "detail": "In: Feature index (C17)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A feature the walk never reaches still holds a place in the one story column. Its author names the feature it reads beside. Without that name the map guesses from the feature's own parties, and otherwise puts it last.' is enforced at tools/coyodex/features.py:203 — falls back to the last walk step the feature's own parties drive",
   "anchor": "tools/coyodex/features.py:203",
   "detail": "In: Feature index (C17)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A feature the walk never reaches still holds a place in the one story column. Its author names the feature it reads beside. Without that name the map guesses from the feature's own parties, and otherwise puts it last.' is enforced at tools/coyodex/features.py:207 — puts a feature with nothing to hang on at the end of the column",
   "anchor": "tools/coyodex/features.py:207",
   "detail": "In: Feature index (C17)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A feature the walk never reaches still holds a place in the one story column. Its author names the feature it reads beside. Without that name the map guesses from the feature's own parties, and otherwise puts it last.' is enforced at tools/coyodex/validate_model.py:2915 — rejects a named neighbour that is not a defined feature",
   "anchor": "tools/coyodex/validate_model.py:2915",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A feature the walk never reaches still holds a place in the one story column. Its author names the feature it reads beside. Without that name the map guesses from the feature's own parties, and otherwise puts it last.' is enforced at tools/coyodex/validate_model.py:2899 — blocks a story place on a code area or a data area, because only a feature sits in the column",
   "anchor": "tools/coyodex/validate_model.py:2899",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A front door is accounted for when a traced scenario reaches it, or a use case names it. A door nothing claims is reported, including one that starts itself with no caller. Writing a door off silences the report, and the map still counts it as a gap.' is enforced at tools/coyodex/validate_model.py:1313 — lists every outside-facing door that neither a traced scenario nor a use case reaches",
   "anchor": "tools/coyodex/validate_model.py:1313",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A front door is accounted for when a traced scenario reaches it, or a use case names it. A door nothing claims is reported, including one that starts itself with no caller. Writing a door off silences the report, and the map still counts it as a gap.' is enforced at tools/coyodex/validate_model.py:1335 — keeps a self-starting job in the same test, so having no caller is not an excuse",
   "anchor": "tools/coyodex/validate_model.py:1335",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A front door is accounted for when a traced scenario reaches it, or a use case names it. A door nothing claims is reported, including one that starts itself with no caller. Writing a door off silences the report, and the map still counts it as a gap.' is enforced at tools/coyodex/validate_model.py:1365 — drops a door the map has written off, so the report stays quiet about it",
   "anchor": "tools/coyodex/validate_model.py:1365",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A front door is accounted for when a traced scenario reaches it, or a use case names it. A door nothing claims is reported, including one that starts itself with no caller. Writing a door off silences the report, and the map still counts it as a gap.' is enforced at tools/coyodex/validate_model.py:1530 — counts the written-off doors out loud, so a silenced gap is still visible",
   "anchor": "tools/coyodex/validate_model.py:1530",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every party that drives a feature states in its own words what it comes there to do. A blank line does not count as an answer. A line written for a party that drives nothing is reported too.' is enforced at tools/coyodex/validate_model.py:2971 — reports a driving party that has no line of its own",
   "anchor": "tools/coyodex/validate_model.py:2971",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every party that drives a feature states in its own words what it comes there to do. A blank line does not count as an answer. A line written for a party that drives nothing is reported too.' is enforced at tools/coyodex/validate_model.py:2944 — blocks an empty line, which would count the party as covered while the arrow still falls back",
   "anchor": "tools/coyodex/validate_model.py:2944",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every party that drives a feature states in its own words what it comes there to do. A blank line does not count as an answer. A line written for a party that drives nothing is reported too.' is enforced at tools/coyodex/validate_model.py:2981 — reports a line written for a party that drives none of this feature's work",
   "anchor": "tools/coyodex/validate_model.py:2981",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Every party that drives a feature states in its own words what it comes there to do. A blank line does not count as an answer. A line written for a party that drives nothing is reported too.' is enforced at tools/coyodex/validate_model.py:2930 — blocks these lines on a code area or a data area, because only a feature has parties coming to it",
   "anchor": "tools/coyodex/validate_model.py:2930",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'When one person wears several roles, the map records the link between those roles. A role that turns into another names the action where the change happens.' is enforced at tools/coyodex/validate_model.py:3017 — blocks a role change that names no action where the change happens",
   "anchor": "tools/coyodex/validate_model.py:3017",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'When one person wears several roles, the map records the link between those roles. A role that turns into another names the action where the change happens.' is enforced at tools/coyodex/validate_model.py:3014 — allows only two kinds of link, so a misspelled one cannot pass unseen",
   "anchor": "tools/coyodex/validate_model.py:3014",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map draws a kind of data only when the code defines it as a named type.' is enforced at tools/coyodex/validate_model.py:3754 — rejects a data card that points at no definition in the code",
   "anchor": "tools/coyodex/validate_model.py:3754",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map draws a kind of data only when the code defines it as a named type.' is enforced at tools/coyodex/validate_model.py:3832 — requires that pointer to be a real file location, so a reader can open the type",
   "anchor": "tools/coyodex/validate_model.py:3832",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map credits a part of the project with owning data only when that part's own code writes it.' is enforced at tools/coyodex/validate_model.py:4533 — counts only save and write links when deciding which part owns a kind of data",
   "anchor": "tools/coyodex/validate_model.py:4533",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map credits a part of the project with owning data only when that part's own code writes it.' is enforced at tools/coyodex/assemble.py:89 — turns an unclear touch into a read, so a filled-in link never grants ownership",
   "anchor": "tools/coyodex/assemble.py:89",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The map credits a part of the project with owning data only when that part's own code writes it.' is enforced at tools/coyodex/assemble.py:128 — raises a filled-in link to ownership only when some step shows a real write",
   "anchor": "tools/coyodex/assemble.py:128",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Data that lives inside another record, in the source, or only during a call needs no owner.' is enforced at tools/coyodex/grammar.py:295 — names the five ways of being stored that mean nothing in the project writes the data",
   "anchor": "tools/coyodex/grammar.py:295",
   "detail": "In: Map document (C1)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Data that lives inside another record, in the source, or only during a call needs no owner.' is enforced at tools/coyodex/validate_model.py:4551 — excuses data stored in one of those ways from the missing-owner report",
   "anchor": "tools/coyodex/validate_model.py:4551",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A save into a store must be explained by a named type that records the same store.' is enforced at tools/coyodex/validate_model.py:2747 — accepts the save when the saving part writes a named type kept in that store",
   "anchor": "tools/coyodex/validate_model.py:2747",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A save into a store must be explained by a named type that records the same store.' is enforced at tools/coyodex/validate_model.py:2748 — also accepts it when the part saves on behalf of the type's owner, one step away",
   "anchor": "tools/coyodex/validate_model.py:2748",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Where data lives must be recorded exactly enough for a reader to find it in the running system.' is enforced at tools/coyodex/validate_model.py:2795 — flags a compartment named with no store linked to it",
   "anchor": "tools/coyodex/validate_model.py:2795",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Where data lives must be recorded exactly enough for a reader to find it in the running system.' is enforced at tools/coyodex/validate_model.py:2817 — flags a compartment name that reads as a description, spotted by the space inside it",
   "anchor": "tools/coyodex/validate_model.py:2817",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A relation between two kinds of data is recorded on one side only, never twice.' is enforced at tools/coyodex/validate_model.py:3780 — rejects a pair where both kinds of data declare the same link",
   "anchor": "tools/coyodex/validate_model.py:3780",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A relation between two kinds of data is recorded on one side only, never twice.' is enforced at tools/coyodex/validate_model.py:3680 — rejects the same link written twice on one card",
   "anchor": "tools/coyodex/validate_model.py:3680",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A lifecycle whose states no line declares is kept as a guess rather than thrown away.' is enforced at tools/coyodex/validate_model.py:2658 — marks a lifecycle citing no declaring line as guessed instead of rejecting it",
   "anchor": "tools/coyodex/validate_model.py:2658",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A lifecycle whose states no line declares is kept as a guess rather than thrown away.' is enforced at tools/coyodex/model.py:322 — makes the declaring line optional, with nothing written meaning guessed",
   "anchor": "tools/coyodex/model.py:322",
   "detail": "In: Map document (C1)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:2548 — Blocks a record whose held-up, disproved and unsettled tallies do not sum to the number of statements checked.",
   "anchor": "tools/coyodex/validate_model.py:2548",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:2555 — Blocks a record claiming more statements were checked than the list ever held.",
   "anchor": "tools/coyodex/validate_model.py:2555",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:2530 — Blocks negative tallies, which can make a wrong record add up.",
   "anchor": "tools/coyodex/validate_model.py:2530",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:4427 — Sends those arithmetic failures to the blocking list, so a map cannot ship carrying them.",
   "anchor": "tools/coyodex/validate_model.py:4427",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:4423 — Sends every judgement about how much was checked to the warning list instead, so effort never blocks a map.",
   "anchor": "tools/coyodex/validate_model.py:4423",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:2413 — Warns when a map carries no testing record at all, so silence cannot read as a clean pass.",
   "anchor": "tools/coyodex/validate_model.py:2413",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:2435 — Warns when under three statements in five held up, and asks which statements were checked first.",
   "anchor": "tools/coyodex/validate_model.py:2435",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check on one statement comes back held up, disproved, or impossible to settle from the code. A fourth answer is refused, never rounded into one of the three.' is enforced at tools/coyodex/grounding.py:157 — Refuses to write the record when any check carries a word outside the three answers.",
   "anchor": "tools/coyodex/grounding.py:157",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check on one statement comes back held up, disproved, or impossible to settle from the code. A fourth answer is refused, never rounded into one of the three.' is enforced at tools/coyodex/grounding.py:767 — Runs the same refusal the moment a checker returns, where the fix is still cheap.",
   "anchor": "tools/coyodex/grounding.py:767",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A check on one statement comes back held up, disproved, or impossible to settle from the code. A fourth answer is refused, never rounded into one of the three.' is enforced at tools/coyodex/grounding.py:103 — Reaches the third answer only when a checker actually said the code cannot settle the statement.",
   "anchor": "tools/coyodex/grounding.py:103",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A statement counts as held up only when more than half of its checks agree. An even split is filed as unsettled and credited to neither side.' is enforced at tools/coyodex/grounding.py:99 — Requires a strict majority of the checks before a statement counts as held up.",
   "anchor": "tools/coyodex/grounding.py:99",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A statement counts as held up only when more than half of its checks agree. An even split is filed as unsettled and credited to neither side.' is enforced at tools/coyodex/grounding.py:107 — Files an even split as unsettled rather than giving it to the larger-looking side.",
   "anchor": "tools/coyodex/grounding.py:107",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A statement counts as held up only when more than half of its checks agree. An even split is filed as unsettled and credited to neither side.' is enforced at tools/coyodex/anchor_drift.py:90 — Applies the same majority before the code-link check will touch a statement at all.",
   "anchor": "tools/coyodex/anchor_drift.py:90",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A statement counts as held up only when more than half of its checks agree. An even split is filed as unsettled and credited to neither side.' is enforced at tools/coyodex/grounding.py:281 — Lists a split apart from a checker's own cannot-settle answer, so a person adjudicates it.",
   "anchor": "tools/coyodex/grounding.py:281",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pass that left statements unchecked is refused until the operator declares that stopping early was deliberate. The declaration must name what was checked first.' is enforced at tools/coyodex/grounding.py:181 — Refuses the record while any statement has no verdict, unless the pass is declared partial.",
   "anchor": "tools/coyodex/grounding.py:181",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pass that left statements unchecked is refused until the operator declares that stopping early was deliberate. The declaration must name what was checked first.' is enforced at tools/coyodex/grounding.py:187 — Refuses the partial declaration when every statement did get a verdict, so a finished pass cannot understate itself.",
   "anchor": "tools/coyodex/grounding.py:187",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A pass that left statements unchecked is refused until the operator declares that stopping early was deliberate. The declaration must name what was checked first.' is enforced at tools/coyodex/grounding.py:192 — Refuses a partial pass that carries no note saying which statements were checked first.",
   "anchor": "tools/coyodex/grounding.py:192",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'How much of a map was checked is counted against the statement list as it stood before any fixes. Statements nobody voted on stay in that list.' is enforced at tools/coyodex/grounding.py:163 — Refuses a verdict for a statement missing from the pinned list, which proves the wrong snapshot was used.",
   "anchor": "tools/coyodex/grounding.py:163",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'How much of a map was checked is counted against the statement list as it stood before any fixes. Statements nobody voted on stay in that list.' is enforced at tools/coyodex/grounding.py:203 — Keeps the whole pinned list as the total, so a short pass cannot shrink what it is measured against.",
   "anchor": "tools/coyodex/grounding.py:203",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'How much of a map was checked is counted against the statement list as it stood before any fixes. Statements nobody voted on stay in that list.' is enforced at tools/coyodex/grounding.py:204 — Subtracts an unvoted statement from the number checked, never from the total.",
   "anchor": "tools/coyodex/grounding.py:204",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'How much of a map was checked is counted against the statement list as it stood before any fixes. Statements nobody voted on stay in that list.' is enforced at tools/coyodex/grounding.py:225 — Counts separately how many statements the shipped map carries that a checker actually saw.",
   "anchor": "tools/coyodex/grounding.py:225",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A statement the checkers disproved may not remain in the map word for word. A run that finds one fails.' is enforced at tools/coyodex/grounding.py:992 — Fails the run when any disproved statement still matches the shipped map.",
   "anchor": "tools/coyodex/grounding.py:992",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A statement the checkers disproved may not remain in the map word for word. A run that finds one fails.' is enforced at tools/coyodex/grounding.py:298 — Names each disproved statement the map still carries word for word, which the counts alone cannot show.",
   "anchor": "tools/coyodex/grounding.py:298",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Correcting the code link under a statement never counts against the statement itself. The check that finds a moved link reports it and never fails a run.' is enforced at tools/coyodex/anchor_drift.py:435 — Ends the code-link check as a report, so a moved link cannot block a map.",
   "anchor": "tools/coyodex/anchor_drift.py:435",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Correcting the code link under a statement never counts against the statement itself. The check that finds a moved link reports it and never fails a run.' is enforced at tools/coyodex/anchor_drift.py:86 — Skips statements whose link deliberately points at a definition rather than the acting line, so a different line is not a move.",
   "anchor": "tools/coyodex/anchor_drift.py:86",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Correcting the code link under a statement never counts against the statement itself. The check that finds a moved link reports it and never fails a run.' is enforced at tools/coyodex/anchor_drift.py:229 — Lets a person record for good that the stored link is the right one, so the finding stops repeating.",
   "anchor": "tools/coyodex/anchor_drift.py:229",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'What coyodex does next is decided by the working tree, never by what the project's history still holds.' is enforced at method/dispatch.md:75 — looks for an existing map only in the working tree, not in the project's history",
   "anchor": "method/dispatch.md:75",
   "detail": "In: The build method (C75)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'What coyodex does next is decided by the working tree, never by what the project's history still holds.' is enforced at method/dispatch.md:77 — refuses to restore a deleted map from history, so deleting it is how a person asks for a fresh build",
   "anchor": "method/dispatch.md:77",
   "detail": "In: The build method (C75)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'What coyodex does next is decided by the working tree, never by what the project's history still holds.' is enforced at method/dispatch.md:147 — stops and says the baseline is up to date when the code matches the pin, instead of writing an empty report",
   "anchor": "method/dispatch.md:147",
   "detail": "In: The build method (C75)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'What coyodex does next is decided by the working tree, never by what the project's history still holds.' is enforced at method/dispatch.md:150 — routes to change analysis when the code differs from the pin, uncommitted edits included",
   "anchor": "method/dispatch.md:150",
   "detail": "In: The build method (C75)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Before any work starts, the person is told which files will be read and what the pin means.' is enforced at method/dispatch.md:22 — requires the briefing to be the first message, shown word for word",
   "anchor": "method/dispatch.md:22",
   "detail": "In: The build method (C75)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Before any work starts, the person is told which files will be read and what the pin means.' is enforced at tools/coyodex/scope.py:172 — prints the briefing: how many files will be read and what the map's commit will claim",
   "anchor": "tools/coyodex/scope.py:172",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Before any work starts, the person is told which files will be read and what the pin means.' is enforced at tools/coyodex/scope.py:135 — names every ignore pattern that removed nothing, so a narrowing of the file set cannot pass unseen",
   "anchor": "tools/coyodex/scope.py:135",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A rebuild never opens the map it is replacing, so the new map cannot copy the old one.' is enforced at method/dispatch.md:119 — forbids opening any earlier map during a build, filed archives included",
   "anchor": "method/dispatch.md:119",
   "detail": "In: The build method (C75)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A rebuild never opens the map it is replacing, so the new map cannot copy the old one.' is enforced at tools/coyodex/scope.py:116 — warns when a map sits inside the set of files to be read, so a hand-made copy is not analyzed as source",
   "anchor": "tools/coyodex/scope.py:116",
   "detail": "In: Command shell and build setup (C4)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map built on uncommitted code records a commit marked as not containing that code.' is enforced at method.md:2043 — requires the marked pin whenever the person chooses to go on without committing",
   "anchor": "method.md:2043",
   "detail": "In: The build method (C75)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map built on uncommitted code records a commit marked as not containing that code.' is enforced at method/dispatch.md:49 — states, at the moment of the choice, that the marked pin means the code is in no commit",
   "anchor": "method/dispatch.md:49",
   "detail": "In: The build method (C75)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map built on uncommitted code records a commit marked as not containing that code.' is enforced at tools/coyodex/provenance.py:177 — computes the pin and marks it whenever the tree holds uncommitted code",
   "anchor": "tools/coyodex/provenance.py:177",
   "detail": "In: Assignment pass (C25)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A map built on uncommitted code records a commit marked as not containing that code.' is enforced at tools/coyodex/provenance.py:360 — writes that same pin into the map's header, so the header cannot claim a clean commit",
   "anchor": "tools/coyodex/provenance.py:360",
   "detail": "In: Assignment pass (C25)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Clearing the way for a rebuild files the old map away instead of deleting it.' is enforced at eval/tools/coyodex_eval/archive.py:107 — moves each part of the map into the archive folder rather than removing it",
   "anchor": "eval/tools/coyodex_eval/archive.py:107",
   "detail": "In: Map archive and build guard (C66)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'Clearing the way for a rebuild files the old map away instead of deleting it.' is enforced at eval/tools/coyodex_eval/archive.py:113 — puts back everything already moved when a move fails, so no half-filed map is left behind",
   "anchor": "eval/tools/coyodex_eval/archive.py:113",
   "detail": "In: Map archive and build guard (C66)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A review of a build is refused while anything is still writing that build's map.' is enforced at eval/tools/coyodex_eval/retro_precheck.py:201 — refuses when any map file was written moments ago",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:201",
   "detail": "In: Map archive and build guard (C66)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A review of a build is refused while anything is still writing that build's map.' is enforced at eval/tools/coyodex_eval/retro_precheck.py:235 — refuses when another session has been active moments ago",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:235",
   "detail": "In: Map archive and build guard (C66)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'A review of a build is refused while anything is still writing that build's map.' is enforced at eval/tools/coyodex_eval/retro_precheck.py:191 — refuses when the review would be reading the map its own session just wrote",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:191",
   "detail": "In: Map archive and build guard (C66)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'What a reader sees always matches the map. Each file is read from version control at the commit the map was pinned to, not from the working copy. A map edited while the server runs is picked up on the next page load.' is enforced at tools/coyodex/viewer/serve.py:701 — A code request defaults to the map's pinned commit; another version is asked for explicitly.",
   "anchor": "tools/coyodex/viewer/serve.py:701",
   "detail": "In: Map server (C43)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'What a reader sees always matches the map. Each file is read from version control at the commit the map was pinned to, not from the working copy. A map edited while the server runs is picked up on the next page load.' is enforced at tools/coyodex/viewer/serve.py:435 — The file browser lists the files present at the map's pinned commit.",
   "anchor": "tools/coyodex/viewer/serve.py:435",
   "detail": "In: Map server (C43)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'What a reader sees always matches the map. Each file is read from version control at the commit the map was pinned to, not from the working copy. A map edited while the server runs is picked up on the next page load.' is enforced at tools/coyodex/viewer/serve.py:592 — Every project request first checks whether the map changed on disk.",
   "anchor": "tools/coyodex/viewer/serve.py:592",
   "detail": "In: Map server (C43)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'What a reader sees always matches the map. Each file is read from version control at the commit the map was pinned to, not from the working copy. A map edited while the server runs is picked up on the next page load.' is enforced at tools/coyodex/viewer/serve.py:210 — A changed map drops the stored copies of the browser tree, the views and the code symbols.",
   "anchor": "tools/coyodex/viewer/serve.py:210",
   "detail": "In: Map server (C43)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "Rule 'The file browser never presents a shortened tree as the whole project. When the skip list hid files, the tree carries a note. The note says how many files went, which pattern removed each one, and which pattern removed nothing.' is enforced at tools/coyodex/viewer/filetree.py:261 — The note about what the skip list removed is attached to the top of the tree.",
   "anchor": "tools/coyodex/viewer/filetree.py:261",
   "detail": "In: File browser tree (C46)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most.",
   "theme": "rule",
   "drift_eligible": true
  },
  {
   "claim": "C4 invokes D2",
   "anchor": "Makefile:33",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: D2 = Python (platform: language runtime, version 3.10 or newer — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C4's code to confirm it reaches D2; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C4 writes D12",
   "anchor": "Makefile:58",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: D12 = Claude Code (platform: AI coding agent that hosts the coyodex skill — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C4's code to confirm it reaches D12; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C4 writes D13",
   "anchor": "Makefile:58",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: D13 = Codex (platform: AI coding agent that hosts the coyodex skill — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C4's code to confirm it reaches D13; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C4 writes D14",
   "anchor": "Makefile:17",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: D14 = Cursor (platform: AI coding agent, and a code editor — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C4's code to confirm it reaches D14; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C43 calls D11",
   "anchor": "tools/coyodex/viewer/serve.py:806",
   "detail": "From: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852); To: D11 = web browser (platform: desktop application the reader already has — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C43's code to confirm it reaches D11; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C64 reads D12",
   "anchor": "eval/tools/coyodex_eval/transcript.py:305",
   "detail": "From: C64 = Build transcript reader and spend report (eval/tools/coyodex_eval/transcript.py:305); entry points: coyodex-eval cost (eval/tools/coyodex_eval/cost.py:674); coyodex-eval transcript (eval/tools/coyodex_eval/transcript.py:1067); To: D12 = Claude Code (platform: AI coding agent that hosts the coyodex skill — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C64's code to confirm it reaches D12; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C66 reads D12",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:216",
   "detail": "From: C66 = Map archive and build guard (eval/tools/coyodex_eval/archive.py:107); entry points: coyodex-eval archive (eval/tools/coyodex_eval/archive.py:159); coyodex-eval retro-precheck (eval/tools/coyodex_eval/retro_precheck.py:249); python tools/map_backup.py stamp (tools/map_backup.py:452); python tools/map_backup.py backup (tools/map_backup.py:478); To: D12 = Claude Code (platform: AI coding agent that hosts the coyodex skill — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C66's code to confirm it reaches D12; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C75 queries D1",
   "anchor": "method/change-impact.md:57",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: D1 = git (platform: version-control program, run as a separate process — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C75's code to confirm it reaches D1; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C25 queries D1",
   "anchor": "tools/coyodex/provenance.py:159",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: D1 = git (platform: version-control program, run as a separate process — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C25's code to confirm it reaches D1; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C43 calls D1",
   "anchor": "tools/coyodex/viewer/serve.py:236",
   "detail": "From: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852); To: D1 = git (platform: version-control program, run as a separate process — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C43's code to confirm it reaches D1; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C44 calls D1",
   "anchor": "tools/coyodex/viewer/gen_viewer.py:60",
   "detail": "From: C44 = Diagram builder (tools/coyodex/viewer/gen_viewer.py:3303); entry points: python -m coyodex.viewer.gen_viewer <graph file> <output file> (tools/coyodex/viewer/gen_viewer.py:3366); To: D1 = git (platform: version-control program, run as a separate process — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C44's code to confirm it reaches D1; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C58 calls D16",
   "anchor": "tools/coyodex/viewer/viewer.js:10729",
   "detail": "From: C58 = Source column (tools/coyodex/viewer/viewer.js:10343); entry points: The map's page loads, so the viewer checks the server is alive and loads the project's file tree (tools/coyodex/viewer/viewer.js:11366); To: D16 = code editor (platform: desktop editor, opened by a link scheme — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C58's code to confirm it reaches D16; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C58 calls D15",
   "anchor": "tools/coyodex/viewer/viewer.js:10732",
   "detail": "From: C58 = Source column (tools/coyodex/viewer/viewer.js:10343); entry points: The map's page loads, so the viewer checks the server is alive and loads the project's file tree (tools/coyodex/viewer/viewer.js:11366); To: D15 = GitHub (service: code-hosting website, reached as a plain link — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C58's code to confirm it reaches D15; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C33 calls D1",
   "anchor": "tools/coyodex/preindex_lib.py:289",
   "detail": "From: C33 = Pre-index (tools/coyodex/preindex.py:604); entry points: A coding agent reads an already measured codebase back as a readable summary. (tools/coyodex/preindex.py:518); A coding agent sizes a codebase before choosing how coarse the map should be. (tools/coyodex/preindex.py:557); To: D1 = git (platform: version-control program, run as a separate process — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C33's code to confirm it reaches D1; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C4 calls D1",
   "anchor": "tools/coyodex/scope.py:48",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: D1 = git (platform: version-control program, run as a separate process — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C4's code to confirm it reaches D1; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C25 calls D1",
   "anchor": "tools/coyodex/provenance.py:120",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: D1 = git (platform: version-control program, run as a separate process — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C25's code to confirm it reaches D1; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C34 queries D1",
   "anchor": "tools/coyodex/impact_git.py:50",
   "detail": "From: C34 = Change impact (tools/coyodex/impact_git.py:285); To: D1 = git (platform: version-control program, run as a separate process — an external system, not a code module)",
   "why_risky": "external-dependency data-flow edge — no deterministic gate reads C34's code to confirm it reaches D1; ground the call site against the code (the audit→Elastic false-edge class).",
   "theme": "dep-usage",
   "drift_eligible": true
  },
  {
   "claim": "C1 loads E1",
   "anchor": "tools/coyodex/model.py:1080",
   "detail": "From: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C1's code actually 'loads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C43 reads E1",
   "anchor": "tools/coyodex/viewer/serve.py:123",
   "detail": "From: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C43's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C45 creates E51",
   "anchor": "tools/coyodex/views.py:730",
   "detail": "From: C45 = Graph builder (tools/coyodex/views.py:1242); entry points: coyodex render <map file> <output text file> (tools/coyodex/viewer/render.py:53); To: E51 = Graph node (tools/coyodex/viewer/build_graph.py:28)",
   "why_risky": "domain-model ownership edge — verify C45's code actually 'creates' E51; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C45 creates E52",
   "anchor": "tools/coyodex/views.py:1183",
   "detail": "From: C45 = Graph builder (tools/coyodex/views.py:1242); entry points: coyodex render <map file> <output text file> (tools/coyodex/viewer/render.py:53); To: E52 = Graph edge (tools/coyodex/viewer/build_graph.py:72)",
   "why_risky": "domain-model ownership edge — verify C45's code actually 'creates' E52; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C43 persists E56",
   "anchor": "tools/coyodex/viewer/recents.py:44",
   "detail": "From: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852); To: E56 = RecentsStore (tools/coyodex/viewer/recents.py:22)",
   "why_risky": "domain-model ownership edge — verify C43's code actually 'persists' E56; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C75 reads E1",
   "anchor": "method/dispatch.md:132",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C75's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C3 reads E1",
   "anchor": "tools/coyodex/dump.py:139",
   "detail": "From: C3 = Map lookups (tools/coyodex/dump.py:316); entry points: `coyodex dump` prints the whole map, or one named part of it, as data. (tools/coyodex/dump.py:246); `coyodex diff` reports what changed between two versions of a map, row by row. (tools/coyodex/mapdiff.py:198); `coyodex record` writes one accepted-advisory line under a named heading. (tools/coyodex/record.py:194); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C3's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C45 reads E1",
   "anchor": "tools/coyodex/views.py:1030",
   "detail": "From: C45 = Graph builder (tools/coyodex/views.py:1242); entry points: coyodex render <map file> <output text file> (tools/coyodex/viewer/render.py:53); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C45's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C45 reads E13",
   "anchor": "tools/coyodex/views.py:1122",
   "detail": "From: C45 = Graph builder (tools/coyodex/views.py:1242); entry points: coyodex render <map file> <output text file> (tools/coyodex/viewer/render.py:53); To: E13 = Component (tools/coyodex/model.py:187)",
   "why_risky": "domain-model ownership edge — verify C45's code actually 'reads' E13; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C17 reads E5",
   "anchor": "tools/coyodex/features.py:222",
   "detail": "From: C17 = Feature index (tools/coyodex/features.py:331); To: E5 = Group (tools/coyodex/model.py:145)",
   "why_risky": "domain-model ownership edge — verify C17's code actually 'reads' E5; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C17 reads E6",
   "anchor": "tools/coyodex/features.py:295",
   "detail": "From: C17 = Feature index (tools/coyodex/features.py:331); To: E6 = UseCase (tools/coyodex/model.py:92)",
   "why_risky": "domain-model ownership edge — verify C17's code actually 'reads' E6; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C17 reads E9",
   "anchor": "tools/coyodex/features.py:230",
   "detail": "From: C17 = Feature index (tools/coyodex/features.py:331); To: E9 = HappyStep (tools/coyodex/model.py:113)",
   "why_risky": "domain-model ownership edge — verify C17's code actually 'reads' E9; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C33 writes E37",
   "anchor": "tools/coyodex/preindex.py:148",
   "detail": "From: C33 = Pre-index (tools/coyodex/preindex.py:604); entry points: A coding agent reads an already measured codebase back as a readable summary. (tools/coyodex/preindex.py:518); A coding agent sizes a codebase before choosing how coarse the map should be. (tools/coyodex/preindex.py:557); To: E37 = Symbol (tools/coyodex/preindex_lib.py:312)",
   "why_risky": "domain-model ownership edge — verify C33's code actually 'writes' E37; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C33 writes E38",
   "anchor": "tools/coyodex/preindex_lib.py:625",
   "detail": "From: C33 = Pre-index (tools/coyodex/preindex.py:604); entry points: A coding agent reads an already measured codebase back as a readable summary. (tools/coyodex/preindex.py:518); A coding agent sizes a codebase before choosing how coarse the map should be. (tools/coyodex/preindex.py:557); To: E38 = DirExpectation (tools/coyodex/preindex_lib.py:533)",
   "why_risky": "domain-model ownership edge — verify C33's code actually 'writes' E38; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C2 reads E1",
   "anchor": "tools/coyodex/lint_fragment.py:452",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C2's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C1 writes E1",
   "anchor": "tools/coyodex/model.py:892",
   "detail": "From: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C1's code actually 'writes' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C2 writes E39",
   "anchor": "tools/coyodex/assemble.py:294",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: E39 = FragmentLoad (tools/coyodex/assemble.py:222)",
   "why_risky": "domain-model ownership edge — verify C2's code actually 'writes' E39; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C2 writes E16",
   "anchor": "tools/coyodex/assemble.py:688",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: E16 = EntryPoint (tools/coyodex/model.py:238)",
   "why_risky": "domain-model ownership edge — verify C2's code actually 'writes' E16; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C2 writes E15",
   "anchor": "tools/coyodex/assemble.py:131",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: E15 = Edge (tools/coyodex/model.py:412)",
   "why_risky": "domain-model ownership edge — verify C2's code actually 'writes' E15; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C2 reads E40",
   "anchor": "tools/coyodex/assemble.py:916",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: E40 = Reconcile (tools/coyodex/reconcile.py:159)",
   "why_risky": "domain-model ownership edge — verify C2's code actually 'reads' E40; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C25 writes E40",
   "anchor": "tools/coyodex/reconcile_build.py:439",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: E40 = Reconcile (tools/coyodex/reconcile.py:159)",
   "why_risky": "domain-model ownership edge — verify C25's code actually 'writes' E40; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C25 reads E16",
   "anchor": "tools/coyodex/reconcile_build.py:213",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: E16 = EntryPoint (tools/coyodex/model.py:238)",
   "why_risky": "domain-model ownership edge — verify C25's code actually 'reads' E16; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C3 reads E16",
   "anchor": "tools/coyodex/dump.py:99",
   "detail": "From: C3 = Map lookups (tools/coyodex/dump.py:316); entry points: `coyodex dump` prints the whole map, or one named part of it, as data. (tools/coyodex/dump.py:246); `coyodex diff` reports what changed between two versions of a map, row by row. (tools/coyodex/mapdiff.py:198); `coyodex record` writes one accepted-advisory line under a named heading. (tools/coyodex/record.py:194); To: E16 = EntryPoint (tools/coyodex/model.py:238)",
   "why_risky": "domain-model ownership edge — verify C3's code actually 'reads' E16; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C13 reads E1",
   "anchor": "tools/coyodex/validate_model.py:4359",
   "detail": "From: C13 = Map validator (tools/coyodex/validate_model.py:4361); entry points: coyodex validate (tools/coyodex/validate_model.py:4663); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C13's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C14 writes E42",
   "anchor": "tools/coyodex/audit_model.py:575",
   "detail": "From: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307); To: E42 = Audit finding (tools/coyodex/audit_model.py:413)",
   "why_risky": "domain-model ownership edge — verify C14's code actually 'writes' E42; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C3 reads E36",
   "anchor": "tools/coyodex/records.py:165",
   "detail": "From: C3 = Map lookups (tools/coyodex/dump.py:316); entry points: `coyodex dump` prints the whole map, or one named part of it, as data. (tools/coyodex/dump.py:246); `coyodex diff` reports what changed between two versions of a map, row by row. (tools/coyodex/mapdiff.py:198); `coyodex record` writes one accepted-advisory line under a named heading. (tools/coyodex/record.py:194); To: E36 = ExtraSection (tools/coyodex/model.py:558)",
   "why_risky": "domain-model ownership edge — verify C3's code actually 'reads' E36; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C3 writes E36",
   "anchor": "tools/coyodex/record.py:116",
   "detail": "From: C3 = Map lookups (tools/coyodex/dump.py:316); entry points: `coyodex dump` prints the whole map, or one named part of it, as data. (tools/coyodex/dump.py:246); `coyodex diff` reports what changed between two versions of a map, row by row. (tools/coyodex/mapdiff.py:198); `coyodex record` writes one accepted-advisory line under a named heading. (tools/coyodex/record.py:194); To: E36 = ExtraSection (tools/coyodex/model.py:558)",
   "why_risky": "domain-model ownership edge — verify C3's code actually 'writes' E36; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C15 reads E5",
   "anchor": "tools/coyodex/balance_lib.py:107",
   "detail": "From: C15 = Diagram balance report (tools/coyodex/balance.py:119); entry points: coyodex balance (tools/coyodex/balance.py:231); To: E5 = Group (tools/coyodex/model.py:145)",
   "why_risky": "domain-model ownership edge — verify C15's code actually 'reads' E5; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C15 reads E13",
   "anchor": "tools/coyodex/balance_lib.py:110",
   "detail": "From: C15 = Diagram balance report (tools/coyodex/balance.py:119); entry points: coyodex balance (tools/coyodex/balance.py:231); To: E13 = Component (tools/coyodex/model.py:187)",
   "why_risky": "domain-model ownership edge — verify C15's code actually 'reads' E13; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C14 writes E41",
   "anchor": "tools/coyodex/audit_model.py:1269",
   "detail": "From: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307); To: E41 = WorkItem (tools/coyodex/audit_model.py:421)",
   "why_risky": "domain-model ownership edge — verify C14's code actually 'writes' E41; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C14 writes E15",
   "anchor": "tools/coyodex/audit_model.py:382",
   "detail": "From: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307); To: E15 = Edge (tools/coyodex/model.py:412)",
   "why_risky": "domain-model ownership edge — verify C14's code actually 'writes' E15; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C23 writes E35",
   "anchor": "tools/coyodex/grounding.py:1086",
   "detail": "From: C23 = Grounding record (tools/coyodex/grounding.py:201); entry points: Running `coyodex anchor-drift` lists every code link pointing at a line that cannot be doing the work. (tools/coyodex/anchor_drift.py:378); Running `coyodex grounding write` derives the four counts and writes them into a build fragment. (tools/coyodex/grounding.py:1003); Running `coyodex grounding by-element` shows, per map element, what a fresh reader proved against the label its author typed. (tools/coyodex/grounding.py:1049); Running `coyodex grounding report` lists which claims were disproved, tied, unsettled, or never voted on. (tools/coyodex/grounding.py:1073); Running `coyodex grounding lint` checks the verdict files for shape while the reader that wrote them is still reachable. (tools/coyodex/grounding.py:924); Running `coyodex grounding refutations` fails the build when the map still carries a claim a fresh reader disproved. (tools/coyodex/grounding.py:974); To: E35 = Grounding (tools/coyodex/model.py:565)",
   "why_risky": "domain-model ownership edge — verify C23's code actually 'writes' E35; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C23 derives E43",
   "anchor": "tools/coyodex/grounding.py:1068",
   "detail": "From: C23 = Grounding record (tools/coyodex/grounding.py:201); entry points: Running `coyodex anchor-drift` lists every code link pointing at a line that cannot be doing the work. (tools/coyodex/anchor_drift.py:378); Running `coyodex grounding write` derives the four counts and writes them into a build fragment. (tools/coyodex/grounding.py:1003); Running `coyodex grounding by-element` shows, per map element, what a fresh reader proved against the label its author typed. (tools/coyodex/grounding.py:1049); Running `coyodex grounding report` lists which claims were disproved, tied, unsettled, or never voted on. (tools/coyodex/grounding.py:1073); Running `coyodex grounding lint` checks the verdict files for shape while the reader that wrote them is still reachable. (tools/coyodex/grounding.py:924); Running `coyodex grounding refutations` fails the build when the map still carries a claim a fresh reader disproved. (tools/coyodex/grounding.py:974); To: E43 = ElementCheck (tools/coyodex/grounding.py:412)",
   "why_risky": "domain-model ownership edge — verify C23's code actually 'derives' E43; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C13 reads E35",
   "anchor": "tools/coyodex/validate_model.py:2549",
   "detail": "From: C13 = Map validator (tools/coyodex/validate_model.py:4361); entry points: coyodex validate (tools/coyodex/validate_model.py:4663); To: E35 = Grounding (tools/coyodex/model.py:565)",
   "why_risky": "domain-model ownership edge — verify C13's code actually 'reads' E35; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C24 writes E40",
   "anchor": "tools/coyodex/fix.py:331",
   "detail": "From: C24 = Map repairs (tools/coyodex/fix.py:1724); entry points: Running `coyodex fix dedup-security` drops an access surface that two harvest fragments both recorded. (tools/coyodex/fix.py:1128); Running `coyodex fix row` rewrites one row's own text inside the fragment that authored the row. (tools/coyodex/fix.py:1416); Running `coyodex fix apply-drift` writes the corrected line into every code link a fresh reader found had moved. (tools/coyodex/fix.py:149); Running `coyodex fix drop-edge` removes a disproved relation and heals the happy-path steps that rode it. (tools/coyodex/fix.py:399); Running `coyodex fix dedup-relation` removes one chosen copy of a relation two data cards both declare. (tools/coyodex/fix.py:508); Running `coyodex fix dedup-edge` keeps one code link for a relation declared at several different lines. (tools/coyodex/fix.py:739); Running `coyodex fix security-row` rewrites one access surface's text, and refuses when the selector matches more than one row. (tools/coyodex/fix.py:976); To: E40 = Reconcile (tools/coyodex/reconcile.py:159)",
   "why_risky": "domain-model ownership edge — verify C24's code actually 'writes' E40; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C25 removes E15",
   "anchor": "tools/coyodex/reconcile.py:576",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: E15 = Edge (tools/coyodex/model.py:412)",
   "why_risky": "domain-model ownership edge — verify C25's code actually 'removes' E15; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C25 writes E11",
   "anchor": "tools/coyodex/reconcile.py:584",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: E11 = FlowStep (tools/coyodex/model.py:368)",
   "why_risky": "domain-model ownership edge — verify C25's code actually 'writes' E11; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C16 writes E45",
   "anchor": "tools/coyodex/finalize.py:472",
   "detail": "From: C16 = Pre-commit gate run (tools/coyodex/finalize.py:472); entry points: coyodex finalize (tools/coyodex/finalize.py:778); To: E45 = Leg (tools/coyodex/finalize.py:70)",
   "why_risky": "domain-model ownership edge — verify C16's code actually 'writes' E45; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C16 writes E46",
   "anchor": "tools/coyodex/finalize.py:495",
   "detail": "From: C16 = Pre-commit gate run (tools/coyodex/finalize.py:472); entry points: coyodex finalize (tools/coyodex/finalize.py:778); To: E46 = FinalizeReport (tools/coyodex/finalize.py:86)",
   "why_risky": "domain-model ownership edge — verify C16's code actually 'writes' E46; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C25 persists E47",
   "anchor": "tools/coyodex/provenance.py:217",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: E47 = Provenance (tools/coyodex/provenance.py:60)",
   "why_risky": "domain-model ownership edge — verify C25's code actually 'persists' E47; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C25 writes E48",
   "anchor": "tools/coyodex/provenance.py:199",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: E48 = SessionEntry (tools/coyodex/provenance.py:33)",
   "why_risky": "domain-model ownership edge — verify C25's code actually 'writes' E48; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C34 writes E49",
   "anchor": "tools/coyodex/impact_lib.py:196",
   "detail": "From: C34 = Change impact (tools/coyodex/impact_git.py:285); To: E49 = AnchorRef (tools/coyodex/impact_lib.py:170)",
   "why_risky": "domain-model ownership edge — verify C34's code actually 'writes' E49; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C34 writes E50",
   "anchor": "tools/coyodex/impact_lib.py:367",
   "detail": "From: C34 = Change impact (tools/coyodex/impact_git.py:285); To: E50 = DirectHit (tools/coyodex/impact_lib.py:289)",
   "why_risky": "domain-model ownership edge — verify C34's code actually 'writes' E50; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C75 writes E1",
   "anchor": "method/change-impact.md:123",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C75's code actually 'writes' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C25 writes E47",
   "anchor": "tools/coyodex/provenance.py:217",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: E47 = Provenance (tools/coyodex/provenance.py:60)",
   "why_risky": "domain-model ownership edge — verify C25's code actually 'writes' E47; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C63 persists E53",
   "anchor": "eval/tools/coyodex_eval/run.py:166",
   "detail": "From: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463); To: E53 = MapProfile (eval/tools/coyodex_eval/profile.py:39)",
   "why_risky": "domain-model ownership edge — verify C63's code actually 'persists' E53; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C63 persists E54",
   "anchor": "eval/tools/coyodex_eval/run.py:403",
   "detail": "From: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463); To: E54 = JudgeReport (eval/tools/coyodex_eval/judge.py:101)",
   "why_risky": "domain-model ownership edge — verify C63's code actually 'persists' E54; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C63 writes E55",
   "anchor": "eval/tools/coyodex_eval/compare.py:510",
   "detail": "From: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463); To: E55 = DeltaReport (eval/tools/coyodex_eval/compare.py:176)",
   "why_risky": "domain-model ownership edge — verify C63's code actually 'writes' E55; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C63 reads E41",
   "anchor": "eval/tools/coyodex_eval/run.py:328",
   "detail": "From: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463); To: E41 = WorkItem (tools/coyodex/audit_model.py:421)",
   "why_risky": "domain-model ownership edge — verify C63's code actually 'reads' E41; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C66 reads E47",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:179",
   "detail": "From: C66 = Map archive and build guard (eval/tools/coyodex_eval/archive.py:107); entry points: coyodex-eval archive (eval/tools/coyodex_eval/archive.py:159); coyodex-eval retro-precheck (eval/tools/coyodex_eval/retro_precheck.py:249); python tools/map_backup.py stamp (tools/map_backup.py:452); python tools/map_backup.py backup (tools/map_backup.py:478); To: E47 = Provenance (tools/coyodex/provenance.py:60)",
   "why_risky": "domain-model ownership edge — verify C66's code actually 'reads' E47; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C66 reads E48",
   "anchor": "eval/tools/coyodex_eval/retro_precheck.py:180",
   "detail": "From: C66 = Map archive and build guard (eval/tools/coyodex_eval/archive.py:107); entry points: coyodex-eval archive (eval/tools/coyodex_eval/archive.py:159); coyodex-eval retro-precheck (eval/tools/coyodex_eval/retro_precheck.py:249); python tools/map_backup.py stamp (tools/map_backup.py:452); python tools/map_backup.py backup (tools/map_backup.py:478); To: E48 = SessionEntry (tools/coyodex/provenance.py:33)",
   "why_risky": "domain-model ownership edge — verify C66's code actually 'reads' E48; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C66 persists E1",
   "anchor": "eval/tools/coyodex_eval/archive.py:107",
   "detail": "From: C66 = Map archive and build guard (eval/tools/coyodex_eval/archive.py:107); entry points: coyodex-eval archive (eval/tools/coyodex_eval/archive.py:159); coyodex-eval retro-precheck (eval/tools/coyodex_eval/retro_precheck.py:249); python tools/map_backup.py stamp (tools/map_backup.py:452); python tools/map_backup.py backup (tools/map_backup.py:478); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C66's code actually 'persists' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C25 reads E47",
   "anchor": "tools/coyodex/provenance.py:296",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: E47 = Provenance (tools/coyodex/provenance.py:60)",
   "why_risky": "domain-model ownership edge — verify C25's code actually 'reads' E47; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C25 reads E48",
   "anchor": "tools/coyodex/provenance.py:304",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: E48 = SessionEntry (tools/coyodex/provenance.py:33)",
   "why_risky": "domain-model ownership edge — verify C25's code actually 'reads' E48; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C65 reads E1",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:1615",
   "detail": "From: C65 = Build behaviour scorecard (eval/tools/coyodex_eval/process_scorecard.py:2924); entry points: coyodex-eval process (eval/tools/coyodex_eval/process_scorecard.py:3130); coyodex-eval process --diff (eval/tools/coyodex_eval/process_scorecard.py:3134); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C65's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "C64 reads E1",
   "anchor": "eval/tools/coyodex_eval/cost.py:392",
   "detail": "From: C64 = Build transcript reader and spend report (eval/tools/coyodex_eval/transcript.py:305); entry points: coyodex-eval cost (eval/tools/coyodex_eval/cost.py:674); coyodex-eval transcript (eval/tools/coyodex_eval/transcript.py:1067); To: E1 = ProjectModel (tools/coyodex/model.py:646)",
   "why_risky": "domain-model ownership edge — verify C64's code actually 'reads' E1; a wrong persists/writes/reads mis-wires the subsystem→subdomain bridge.",
   "theme": "ownership",
   "drift_eligible": true
  },
  {
   "claim": "E45 (Leg) has states [ran, failed]",
   "anchor": "tools/coyodex/finalize.py:65",
   "detail": null,
   "why_risky": "lifecycles rot first — verify the declaring enum/constants still list exactly these states and transitions.",
   "theme": "lifecycle",
   "drift_eligible": true
  },
  {
   "claim": "E46 (FinalizeReport) has states [BLOCKED, INCOMPLETE, ADVISORIES, CLEAN]",
   "anchor": "tools/coyodex/finalize.py:488",
   "detail": null,
   "why_risky": "lifecycles rot first — verify the declaring enum/constants still list exactly these states and transitions.",
   "theme": "lifecycle",
   "drift_eligible": true
  },
  {
   "claim": "E55 (DeltaReport) has states [PASS, DRIFT, REGRESSED]",
   "anchor": "eval/tools/coyodex_eval/compare.py:38",
   "detail": null,
   "why_risky": "lifecycles rot first — verify the declaring enum/constants still list exactly these states and transitions.",
   "theme": "lifecycle",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [poller] The server checks whether its own code changed since it started, and warns once runs on cadence 'every 2s'",
   "anchor": "tools/coyodex/viewer/serve.py:145",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [poller] The server checks whether a map changed on disk, and drops what it cached runs on cadence 'on every project request'",
   "anchor": "tools/coyodex/viewer/serve.py:592",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [worker-thread] Each browser request gets its own worker thread runs on cadence 'one per request'",
   "anchor": "tools/coyodex/viewer/serve.py:800",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [startup-hook] Starting with the open option pops the landing page up in a browser runs on cadence 'on-boot'",
   "anchor": "tools/coyodex/viewer/serve.py:805",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [server-loop] The server takes requests until someone stops it runs on cadence 'continuous'",
   "anchor": "tools/coyodex/viewer/serve.py:808",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [signal-handler] Pressing the stop key closes the listening port runs on cadence 'on stop'",
   "anchor": "tools/coyodex/viewer/serve.py:810",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [event-consumer] The browser window changes size, so the drawing refits itself to the new space runs on cadence 'continuous'",
   "anchor": "tools/coyodex/viewer/viewer.js:10593",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [startup-hook] A reader opens a map for the first time in this browser, so the guide to getting around opens by itself runs on cadence 'on-boot'",
   "anchor": "tools/coyodex/viewer/viewer.js:10853",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [startup-hook] The map's page loads, so the viewer checks the server is alive and loads the project's file tree runs on cadence 'on-boot'",
   "anchor": "tools/coyodex/viewer/viewer.js:11366",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [startup-hook] The map's page loads, so the viewer fetches the whole map from the server before drawing anything runs on cadence 'on-boot'",
   "anchor": "tools/coyodex/viewer/viewer.js:154",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Entry point [event-consumer] Any screen writes new text, so every glossary word inside that text becomes a link with no reader action runs on cadence 'continuous'",
   "anchor": "tools/coyodex/viewer/viewer.js:479",
   "detail": null,
   "why_risky": "a schedule is config-tuned and drifts silently — verify the declaring line still says this cadence.",
   "theme": "cadence",
   "drift_eligible": true
  },
  {
   "claim": "Component C78 (The eval recipe) is described as: Written instructions for scoring two maps of the same code. The scoring says whether a change to the coyodex method made maps better or worse. The same instructions fix the quality rubric a reviewer scores against, and the pass marks a map must clear. A run ends in one word: as good, drifted, or worse.",
   "anchor": "eval/method.md:8",
   "detail": "declared at eval/method.md:8; files: eval/method.md, eval/rubric.md, eval/thresholds.json, eval/README.md, eval/blind-build.md, eval/experiments/2026-08-16-skeptic-recall.md …",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C79 (The build retrospective recipe) is described as: Written instructions for reviewing one finished build. The review reads the map the build made and the chat that made it. The review reports the friction, bugs and gaps the build revealed. A review changes nothing on disk, so every finding is a proposal for the developer. Each past finding is re-checked with a command that returns a number.",
   "anchor": "eval/retro/method.md:4",
   "detail": "declared at eval/retro/method.md:4; files: eval/retro/method.md, eval/retro/backlog.md",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C1 (Map document) is described as: Holds the shape of a map: what a box, an arrow and a code link may each contain. Writes the map file the same way every time, so a rebuild shows only real changes. Also holds the word lists the whole tool shares.",
   "anchor": "tools/coyodex/model.py:1080",
   "detail": "declared at tools/coyodex/model.py:1080; files: tools/coyodex/model.py, tools/coyodex/grammar.py, tools/coyodex/anchors.py, tools/coyodex/json_schema.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C2 (Fragment merge) is described as: Merges every worker's part of the map into one document. A box claimed by two workers stops the build, instead of one part quietly winning. Gives a worker the self-check it runs before handing its part back.",
   "anchor": "tools/coyodex/assemble.py:886",
   "detail": "declared at tools/coyodex/assemble.py:886; files: tools/coyodex/assemble.py, tools/coyodex/lint_fragment.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C3 (Map lookups) is described as: Reads one part of a map, so nobody opens the file and picks it apart by hand. Compares two versions of a map row by row, saying what was added, dropped or changed. Writes the note an operator leaves when accepting an advisory.",
   "anchor": "tools/coyodex/dump.py:316",
   "detail": "declared at tools/coyodex/dump.py:316; files: tools/coyodex/dump.py, tools/coyodex/mapdiff.py, tools/coyodex/record.py, tools/coyodex/records.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C4 (Command shell and build setup) is described as: The single command every coyodex action runs through. Before a build starts, the command says which files will be read and which commit the map will name. The same command prints the brief one worker receives, and reads the list of code the map should skip.",
   "anchor": "tools/coyodex/cli.py:123",
   "detail": "declared at tools/coyodex/cli.py:123; files: tools/coyodex/cli.py, tools/coyodex/scope.py, tools/coyodex/ignorefile.py, tools/coyodex/pathmatch.py, tools/coyodex/contract.py, tools/coyodex/reporting.py …",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C13 (Map validator) is described as: Checks a finished map is well formed before anyone trusts it. Every reference must resolve and every code link must point at a real file. Softer findings are advice, and an operator can silence one by writing down a reason.",
   "anchor": "tools/coyodex/validate_model.py:4361",
   "detail": "declared at tools/coyodex/validate_model.py:4361; files: tools/coyodex/validate_model.py, tools/coyodex/validate_analysis.py, tools/coyodex/prose.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C14 (Map auditor) is described as: Reads a map against itself and reports where the story and the mechanism disagree. Also ranks every claim the map makes about the code. Fresh readers then try to disprove the riskiest claims first.",
   "anchor": "tools/coyodex/audit_model.py:811",
   "detail": "declared at tools/coyodex/audit_model.py:811; files: tools/coyodex/audit_model.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C15 (Diagram balance report) is described as: Counts the boxes on every diagram and says which screens are too crowded to read. Proposes a regrouping for each crowded screen, as a starting point for judgment. Never fails a build, because grouping is a view choice.",
   "anchor": "tools/coyodex/balance.py:119",
   "detail": "declared at tools/coyodex/balance.py:119; files: tools/coyodex/balance.py, tools/coyodex/balance_lib.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C16 (Pre-commit gate run) is described as: Runs the other gates in one command and writes every finding to a report file. Says for each gate whether it really ran, so silence never reads as a pass. Can also name files that held an access check in an earlier map and hold none now.",
   "anchor": "tools/coyodex/finalize.py:472",
   "detail": "declared at tools/coyodex/finalize.py:472; files: tools/coyodex/finalize.py, tools/coyodex/access_surface.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C17 (Feature index) is described as: Joins every part of the map onto the feature it belongs to, and stores nothing. Also derives the one story order every feature screen draws. Counts what fails to join, so a partial list is never shown as a whole one.",
   "anchor": "tools/coyodex/features.py:331",
   "detail": "declared at tools/coyodex/features.py:331; files: tools/coyodex/features.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C23 (Grounding record) is described as: Turns the fresh readers' verdicts into the four counts a map must carry before anyone trusts it. Those counts refuse a verdict for a claim nobody pinned, and a pinned claim nobody voted on. A second check compares a proven claim's code link against the line the fresh readers actually found.",
   "anchor": "tools/coyodex/grounding.py:201",
   "detail": "declared at tools/coyodex/grounding.py:201; files: tools/coyodex/grounding.py, tools/coyodex/anchor_drift.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C24 (Map repairs) is described as: Applies the mechanical corrections a map needs after a fresh reader reports, so none of them is hand-typed. A moved code link is rewritten, and a disproved relation is dropped. The happy-path steps that rode the dropped relation are healed too.",
   "anchor": "tools/coyodex/fix.py:1724",
   "detail": "declared at tools/coyodex/fix.py:1724; files: tools/coyodex/fix.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C25 (Assignment pass) is described as: Turns path rules into an explicit assignment file, so nobody lists hundreds of elements by hand. Every rule that matched nothing is named, and so is an assignment aimed at a group nobody declared. The build stamp is also written here, naming which conversation built the map and the code commit.",
   "anchor": "tools/coyodex/reconcile_build.py:358",
   "detail": "declared at tools/coyodex/reconcile_build.py:358; files: tools/coyodex/reconcile_build.py, tools/coyodex/reconcile.py, tools/coyodex/provenance.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C33 (Pre-index) is described as: Sizes a codebase before the map is drawn. The result gives the size and change rate of every folder. A symbol list says where every class and function is defined. The result also says how many parts a map of this size should hold.",
   "anchor": "tools/coyodex/preindex.py:604",
   "detail": "declared at tools/coyodex/preindex.py:604; files: tools/coyodex/preindex.py, tools/coyodex/preindex_lib.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C34 (Change impact) is described as: Tells what a code change did to a finished map. The map is pinned to one commit, so the code has moved on. Two comparisons against that commit say which map boxes the changed lines belong to. The answer then spreads along the map's own arrows, so a reader sees everything the change touches.",
   "anchor": "tools/coyodex/impact_git.py:285",
   "detail": "declared at tools/coyodex/impact_git.py:285; files: tools/coyodex/impact_lib.py, tools/coyodex/impact_git.py, tools/coyodex/impact_ripple.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C43 (Map server) is described as: Serves every remembered map to a browser on this machine. Each file shown comes from the project's history at the commit the map names.",
   "anchor": "tools/coyodex/viewer/serve.py:800",
   "detail": "declared at tools/coyodex/viewer/serve.py:800; files: tools/coyodex/viewer/serve.py, tools/coyodex/viewer/recents.py, tools/coyodex/viewer/__init__.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C44 (Diagram builder) is described as: Builds every diagram a map's screens draw, straight from the map. The work happens on each request, so no diagram file is ever saved.",
   "anchor": "tools/coyodex/viewer/gen_viewer.py:3303",
   "detail": "declared at tools/coyodex/viewer/gen_viewer.py:3303; files: tools/coyodex/viewer/gen_viewer.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C45 (Graph builder) is described as: Turns the stored map into the boxes and arrows every screen draws. The same code writes the committed text version of the map.",
   "anchor": "tools/coyodex/views.py:1242",
   "detail": "declared at tools/coyodex/views.py:1242; files: tools/coyodex/views.py, tools/coyodex/viewer/build_graph.py, tools/coyodex/viewer/render.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C46 (File browser tree) is described as: Builds the folder tree the file browser shows, marking which files the map covers. Also turns one file's change into the rows the code viewer paints.",
   "anchor": "tools/coyodex/viewer/filetree.py:231",
   "detail": "declared at tools/coyodex/viewer/filetree.py:231; files: tools/coyodex/viewer/filetree.py, tools/coyodex/viewer/diffmap.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C55 (Map canvas) is described as: Draws a map view as a diagram the reader can pan, zoom and click. Selecting a box or an arrow opens the card describing that box or arrow.",
   "anchor": "tools/coyodex/viewer/viewer.js:928",
   "detail": "declared at tools/coyodex/viewer/viewer.js:928; files: tools/coyodex/viewer/viewer.js, tools/coyodex/viewer/viewer.css, tools/coyodex/viewer/viewer.html",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C56 (Map reading pages) is described as: Draws the map screens that are pages of cards instead of drawings. The features, the rules, the storage, the tests and the glossary all live here.",
   "anchor": "tools/coyodex/viewer/viewer.js:7970",
   "detail": "declared at tools/coyodex/viewer/viewer.js:7970; files: tools/coyodex/viewer/viewer.js, tools/coyodex/viewer/viewer.css",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C57 (Trail and history) is described as: Decides which map screen is open, and names it in the tabs and the breadcrumb. Back and forward reopen a screen exactly as the reader left it.",
   "anchor": "tools/coyodex/viewer/viewer.js:4329",
   "detail": "declared at tools/coyodex/viewer/viewer.js:4329; files: tools/coyodex/viewer/viewer.js, tools/coyodex/viewer/viewer.css, tools/coyodex/viewer/viewer.html",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C58 (Source column) is described as: Shows the mapped project's files and their code, read at the commit the map names. A file can also be opened in the reader's own editor.",
   "anchor": "tools/coyodex/viewer/viewer.js:10343",
   "detail": "declared at tools/coyodex/viewer/viewer.js:10343; files: tools/coyodex/viewer/viewer.js, tools/coyodex/viewer/viewer.css, tools/coyodex/viewer/viewer.html",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C63 (Map quality score and verdict) is described as: Reduces a finished map to numbers that two runs can be compared on. Says whether the new map is as good as the accepted one, giving pass, drift or regressed. Also plants false claims about the map and counts how many a reviewer catches.",
   "anchor": "eval/tools/coyodex_eval/profile.py:337",
   "detail": "declared at eval/tools/coyodex_eval/profile.py:337; files: eval/tools/coyodex_eval/cli.py, eval/tools/coyodex_eval/profile.py, eval/tools/coyodex_eval/compare.py, eval/tools/coyodex_eval/run.py, eval/tools/coyodex_eval/judge.py, eval/tools/coyodex_eval/legacy_map.py …",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C64 (Build transcript reader and spend report) is described as: Reads a finished build's chat log in slices, because the whole log is too big to open. Also reports the time and the money that build spent, per row of map produced.",
   "anchor": "eval/tools/coyodex_eval/transcript.py:305",
   "detail": "declared at eval/tools/coyodex_eval/transcript.py:305; files: eval/tools/coyodex_eval/transcript.py, eval/tools/coyodex_eval/cost.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C65 (Build behaviour scorecard) is described as: Scores what a build agent actually did against the rules the method sets. Reports a share of the chances the build had, never a pass or a fail. Also compares two scorecards to show which way each number moved.",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:2924",
   "detail": "declared at eval/tools/coyodex_eval/process_scorecard.py:2924; files: eval/tools/coyodex_eval/process_scorecard.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C66 (Map archive and build guard) is described as: Moves a project's current map aside so the next run builds from scratch, keeping it as the baseline. Refuses a review while a build is still writing. Also stamps which chat built a map and bundles the two together.",
   "anchor": "eval/tools/coyodex_eval/archive.py:107",
   "detail": "declared at eval/tools/coyodex_eval/archive.py:107; files: eval/tools/coyodex_eval/archive.py, eval/tools/coyodex_eval/retro_precheck.py, tools/map_backup.py",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C75 (The build method) is described as: The written instructions a coding agent follows to build a map. The same instructions specify every field a map holds.",
   "anchor": "method.md:3",
   "detail": "declared at method.md:3; files: method.md, method/dispatch.md, method/model.md, method/domain-cards.md, method/diagrams.md, method/change-impact.md …",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C76 (The worker briefings) is described as: The briefings a build hands to each worker agent it fans out to. A briefing states what that worker must return.",
   "anchor": null,
   "detail": "declared at method/templates/; files: method/templates/harvest-contract.md, method/templates/trace-contract.md, method/templates/rules-contract.md, method/templates/skeptic-contract.md, method/templates/gapfill-contract.md, method/templates/writing-rules.md …",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "Component C77 (The installed skill and the project's own writing) is described as: The pointer a person installs into their coding agent, so the coyodex command reaches the method. The project's own writing for readers and contributors lives beside that pointer.",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": "declared at skill/coyodex/SKILL.md:27; files: skill/coyodex/SKILL.md, eval/SKILL.md, eval/retro/SKILL.md, README.md, CONTRIBUTING.md, CLAUDE.md …",
   "why_risky": "prose is read by no other gate: `validate` counts sentence length and `audit` compares records, so a sentence that is simply false about the code reaches the reader unchallenged. Read the WHOLE description against the files and name the clause that fails, if one does.",
   "theme": "description",
   "drift_eligible": false
  },
  {
   "claim": "C77 routes-to C78",
   "anchor": "eval/SKILL.md:28",
   "detail": "From: C77 = The installed skill and the project's own writing (skill/coyodex/SKILL.md:27); entry points: A coyodex developer runs the retro skill to review a build that has already finished. (eval/retro/SKILL.md:28); A coyodex developer runs the eval skill to score one project's map against an earlier map of the same code. (eval/SKILL.md:28); To: C78 = The eval recipe (eval/method.md:8)",
   "why_risky": "backbone edge — no deterministic gate confirms C77's code 'routes-to' C78; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C77 routes-to C79",
   "anchor": "eval/retro/SKILL.md:28",
   "detail": "From: C77 = The installed skill and the project's own writing (skill/coyodex/SKILL.md:27); entry points: A coyodex developer runs the retro skill to review a build that has already finished. (eval/retro/SKILL.md:28); A coyodex developer runs the eval skill to score one project's map against an earlier map of the same code. (eval/SKILL.md:28); To: C79 = The build retrospective recipe (eval/retro/method.md:4)",
   "why_risky": "backbone edge — no deterministic gate confirms C77's code 'routes-to' C79; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C63 reads C78",
   "anchor": "eval/tools/coyodex_eval/compare.py:601",
   "detail": "From: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463); To: C78 = The eval recipe (eval/method.md:8)",
   "why_risky": "backbone edge — no deterministic gate confirms C63's code 'reads' C78; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C78 prescribes C63",
   "anchor": "eval/method.md:352",
   "detail": "From: C78 = The eval recipe (eval/method.md:8); To: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463)",
   "why_risky": "backbone edge — no deterministic gate confirms C78's code 'prescribes' C63; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C79 prescribes C63",
   "anchor": "eval/retro/method.md:298",
   "detail": "From: C79 = The build retrospective recipe (eval/retro/method.md:4); To: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463)",
   "why_risky": "backbone edge — no deterministic gate confirms C79's code 'prescribes' C63; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C79 prescribes C64",
   "anchor": "eval/retro/method.md:384",
   "detail": "From: C79 = The build retrospective recipe (eval/retro/method.md:4); To: C64 = Build transcript reader and spend report (eval/tools/coyodex_eval/transcript.py:305); entry points: coyodex-eval cost (eval/tools/coyodex_eval/cost.py:674); coyodex-eval transcript (eval/tools/coyodex_eval/transcript.py:1067)",
   "why_risky": "backbone edge — no deterministic gate confirms C79's code 'prescribes' C64; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C79 prescribes C65",
   "anchor": "eval/retro/method.md:359",
   "detail": "From: C79 = The build retrospective recipe (eval/retro/method.md:4); To: C65 = Build behaviour scorecard (eval/tools/coyodex_eval/process_scorecard.py:2924); entry points: coyodex-eval process (eval/tools/coyodex_eval/process_scorecard.py:3130); coyodex-eval process --diff (eval/tools/coyodex_eval/process_scorecard.py:3134)",
   "why_risky": "backbone edge — no deterministic gate confirms C79's code 'prescribes' C65; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C79 prescribes C66",
   "anchor": "eval/retro/method.md:62",
   "detail": "From: C79 = The build retrospective recipe (eval/retro/method.md:4); To: C66 = Map archive and build guard (eval/tools/coyodex_eval/archive.py:107); entry points: coyodex-eval archive (eval/tools/coyodex_eval/archive.py:159); coyodex-eval retro-precheck (eval/tools/coyodex_eval/retro_precheck.py:249); python tools/map_backup.py stamp (tools/map_backup.py:452); python tools/map_backup.py backup (tools/map_backup.py:478)",
   "why_risky": "backbone edge — no deterministic gate confirms C79's code 'prescribes' C66; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C4 reads C77",
   "anchor": "Makefile:58",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: C77 = The installed skill and the project's own writing (skill/coyodex/SKILL.md:27); entry points: A coyodex developer runs the retro skill to review a build that has already finished. (eval/retro/SKILL.md:28); A coyodex developer runs the eval skill to score one project's map against an earlier map of the same code. (eval/SKILL.md:28)",
   "why_risky": "backbone edge — no deterministic gate confirms C4's code 'reads' C77; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C4 starts C43",
   "anchor": "tools/coyodex/cli.py:141",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852)",
   "why_risky": "backbone edge — no deterministic gate confirms C4's code 'starts' C43; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C43 calls C45",
   "anchor": "tools/coyodex/viewer/serve.py:455",
   "detail": "From: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852); To: C45 = Graph builder (tools/coyodex/views.py:1242); entry points: coyodex render <map file> <output text file> (tools/coyodex/viewer/render.py:53)",
   "why_risky": "backbone edge — no deterministic gate confirms C43's code 'calls' C45; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C43 calls C44",
   "anchor": "tools/coyodex/viewer/serve.py:460",
   "detail": "From: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852); To: C44 = Diagram builder (tools/coyodex/viewer/gen_viewer.py:3303); entry points: python -m coyodex.viewer.gen_viewer <graph file> <output file> (tools/coyodex/viewer/gen_viewer.py:3366)",
   "why_risky": "backbone edge — no deterministic gate confirms C43's code 'calls' C44; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C55 fetches C43",
   "anchor": "tools/coyodex/viewer/viewer.js:154",
   "detail": "From: C55 = Map canvas (tools/coyodex/viewer/viewer.js:928); entry points: Opens the Happy Path tab, under Product (tools/coyodex/viewer/viewer.html:121); Opens the Entities tab, under Data (tools/coyodex/viewer/viewer.html:130); Opens the Subsystems tab, under Under the hood (tools/coyodex/viewer/viewer.html:136); Opens the Dependencies tab, under Under the hood (tools/coyodex/viewer/viewer.html:137); Opens the Deployment tab, under Under the hood (tools/coyodex/viewer/viewer.html:140); The browser window changes size, so the drawing refits itself to the new space (tools/coyodex/viewer/viewer.js:10593); To: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852)",
   "why_risky": "backbone edge — no deterministic gate confirms C55's code 'fetches' C43; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C55 calls C57",
   "anchor": "tools/coyodex/viewer/viewer.js:11694",
   "detail": "From: C55 = Map canvas (tools/coyodex/viewer/viewer.js:928); entry points: Opens the Happy Path tab, under Product (tools/coyodex/viewer/viewer.html:121); Opens the Entities tab, under Data (tools/coyodex/viewer/viewer.html:130); Opens the Subsystems tab, under Under the hood (tools/coyodex/viewer/viewer.html:136); Opens the Dependencies tab, under Under the hood (tools/coyodex/viewer/viewer.html:137); Opens the Deployment tab, under Under the hood (tools/coyodex/viewer/viewer.html:140); The browser window changes size, so the drawing refits itself to the new space (tools/coyodex/viewer/viewer.js:10593); To: C57 = Trail and history (tools/coyodex/viewer/viewer.js:4329); entry points: A reader opens a map for the first time in this browser, so the guide to getting around opens by itself (tools/coyodex/viewer/viewer.js:10853); Opens a project's map in a browser, landing on the first view the map offers (tools/coyodex/viewer/viewer.js:11694); The map's page loads, so the viewer fetches the whole map from the server before drawing anything (tools/coyodex/viewer/viewer.js:154)",
   "why_risky": "backbone edge — no deterministic gate confirms C55's code 'calls' C57; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C77 routes-to C75",
   "anchor": "skill/coyodex/SKILL.md:27",
   "detail": "From: C77 = The installed skill and the project's own writing (skill/coyodex/SKILL.md:27); entry points: A coyodex developer runs the retro skill to review a build that has already finished. (eval/retro/SKILL.md:28); A coyodex developer runs the eval skill to score one project's map against an earlier map of the same code. (eval/SKILL.md:28); To: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83)",
   "why_risky": "backbone edge — no deterministic gate confirms C77's code 'routes-to' C75; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 routes-to C76",
   "anchor": "method.md:1960",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C76 = The worker briefings (method/templates/)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'routes-to' C76; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C4 reads C76",
   "anchor": "tools/coyodex/contract.py:90",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: C76 = The worker briefings (method/templates/)",
   "why_risky": "backbone edge — no deterministic gate confirms C4's code 'reads' C76; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C33",
   "anchor": "method.md:860",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C33 = Pre-index (tools/coyodex/preindex.py:604); entry points: A coding agent reads an already measured codebase back as a readable summary. (tools/coyodex/preindex.py:518); A coding agent sizes a codebase before choosing how coarse the map should be. (tools/coyodex/preindex.py:557)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C33; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C2",
   "anchor": "method.md:2100",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C2; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C25",
   "anchor": "method.md:1298",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C25; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C13",
   "anchor": "method.md:2112",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C13 = Map validator (tools/coyodex/validate_model.py:4361); entry points: coyodex validate (tools/coyodex/validate_model.py:4663)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C13; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C14",
   "anchor": "method.md:2127",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C14; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C23",
   "anchor": "method.md:1655",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C23 = Grounding record (tools/coyodex/grounding.py:201); entry points: Running `coyodex anchor-drift` lists every code link pointing at a line that cannot be doing the work. (tools/coyodex/anchor_drift.py:378); Running `coyodex grounding write` derives the four counts and writes them into a build fragment. (tools/coyodex/grounding.py:1003); Running `coyodex grounding by-element` shows, per map element, what a fresh reader proved against the label its author typed. (tools/coyodex/grounding.py:1049); Running `coyodex grounding report` lists which claims were disproved, tied, unsettled, or never voted on. (tools/coyodex/grounding.py:1073); Running `coyodex grounding lint` checks the verdict files for shape while the reader that wrote them is still reachable. (tools/coyodex/grounding.py:924); Running `coyodex grounding refutations` fails the build when the map still carries a claim a fresh reader disproved. (tools/coyodex/grounding.py:974)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C23; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C24",
   "anchor": "method.md:1943",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C24 = Map repairs (tools/coyodex/fix.py:1724); entry points: Running `coyodex fix dedup-security` drops an access surface that two harvest fragments both recorded. (tools/coyodex/fix.py:1128); Running `coyodex fix row` rewrites one row's own text inside the fragment that authored the row. (tools/coyodex/fix.py:1416); Running `coyodex fix apply-drift` writes the corrected line into every code link a fresh reader found had moved. (tools/coyodex/fix.py:149); Running `coyodex fix drop-edge` removes a disproved relation and heals the happy-path steps that rode it. (tools/coyodex/fix.py:399); Running `coyodex fix dedup-relation` removes one chosen copy of a relation two data cards both declare. (tools/coyodex/fix.py:508); Running `coyodex fix dedup-edge` keeps one code link for a relation declared at several different lines. (tools/coyodex/fix.py:739); Running `coyodex fix security-row` rewrites one access surface's text, and refuses when the selector matches more than one row. (tools/coyodex/fix.py:976)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C24; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C15",
   "anchor": "method.md:1421",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C15 = Diagram balance report (tools/coyodex/balance.py:119); entry points: coyodex balance (tools/coyodex/balance.py:231)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C15; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C3",
   "anchor": "method.md:1580",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C3 = Map lookups (tools/coyodex/dump.py:316); entry points: `coyodex dump` prints the whole map, or one named part of it, as data. (tools/coyodex/dump.py:246); `coyodex diff` reports what changed between two versions of a map, row by row. (tools/coyodex/mapdiff.py:198); `coyodex record` writes one accepted-advisory line under a named heading. (tools/coyodex/record.py:194)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C3; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C16",
   "anchor": "method.md:2164",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C16 = Pre-commit gate run (tools/coyodex/finalize.py:472); entry points: coyodex finalize (tools/coyodex/finalize.py:778)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C16; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C43",
   "anchor": "method/change-impact.md:142",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C43; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C16 reads C1",
   "anchor": "tools/coyodex/finalize.py:570",
   "detail": "From: C16 = Pre-commit gate run (tools/coyodex/finalize.py:472); entry points: coyodex finalize (tools/coyodex/finalize.py:778); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C16's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C17 reads C1",
   "anchor": "tools/coyodex/features.py:312",
   "detail": "From: C17 = Feature index (tools/coyodex/features.py:331); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C17's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C23 reads C1",
   "anchor": "tools/coyodex/grounding.py:979",
   "detail": "From: C23 = Grounding record (tools/coyodex/grounding.py:201); entry points: Running `coyodex anchor-drift` lists every code link pointing at a line that cannot be doing the work. (tools/coyodex/anchor_drift.py:378); Running `coyodex grounding write` derives the four counts and writes them into a build fragment. (tools/coyodex/grounding.py:1003); Running `coyodex grounding by-element` shows, per map element, what a fresh reader proved against the label its author typed. (tools/coyodex/grounding.py:1049); Running `coyodex grounding report` lists which claims were disproved, tied, unsettled, or never voted on. (tools/coyodex/grounding.py:1073); Running `coyodex grounding lint` checks the verdict files for shape while the reader that wrote them is still reachable. (tools/coyodex/grounding.py:924); Running `coyodex grounding refutations` fails the build when the map still carries a claim a fresh reader disproved. (tools/coyodex/grounding.py:974); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C23's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C24 reads C1",
   "anchor": "tools/coyodex/fix.py:1007",
   "detail": "From: C24 = Map repairs (tools/coyodex/fix.py:1724); entry points: Running `coyodex fix dedup-security` drops an access surface that two harvest fragments both recorded. (tools/coyodex/fix.py:1128); Running `coyodex fix row` rewrites one row's own text inside the fragment that authored the row. (tools/coyodex/fix.py:1416); Running `coyodex fix apply-drift` writes the corrected line into every code link a fresh reader found had moved. (tools/coyodex/fix.py:149); Running `coyodex fix drop-edge` removes a disproved relation and heals the happy-path steps that rode it. (tools/coyodex/fix.py:399); Running `coyodex fix dedup-relation` removes one chosen copy of a relation two data cards both declare. (tools/coyodex/fix.py:508); Running `coyodex fix dedup-edge` keeps one code link for a relation declared at several different lines. (tools/coyodex/fix.py:739); Running `coyodex fix security-row` rewrites one access surface's text, and refuses when the selector matches more than one row. (tools/coyodex/fix.py:976); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C24's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C34 reads C1",
   "anchor": "tools/coyodex/impact_lib.py:251",
   "detail": "From: C34 = Change impact (tools/coyodex/impact_git.py:285); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C34's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C44 reads C1",
   "anchor": "tools/coyodex/viewer/gen_viewer.py:3257",
   "detail": "From: C44 = Diagram builder (tools/coyodex/viewer/gen_viewer.py:3303); entry points: python -m coyodex.viewer.gen_viewer <graph file> <output file> (tools/coyodex/viewer/gen_viewer.py:3366); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C44's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C45 reads C1",
   "anchor": "tools/coyodex/views.py:661",
   "detail": "From: C45 = Graph builder (tools/coyodex/views.py:1242); entry points: coyodex render <map file> <output text file> (tools/coyodex/viewer/render.py:53); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C45's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C65 reads C1",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:2851",
   "detail": "From: C65 = Build behaviour scorecard (eval/tools/coyodex_eval/process_scorecard.py:2924); entry points: coyodex-eval process (eval/tools/coyodex_eval/process_scorecard.py:3130); coyodex-eval process --diff (eval/tools/coyodex_eval/process_scorecard.py:3134); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C65's code 'reads' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C43 calls C46",
   "anchor": "tools/coyodex/viewer/serve.py:436",
   "detail": "From: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852); To: C46 = File browser tree (tools/coyodex/viewer/filetree.py:231)",
   "why_risky": "backbone edge — no deterministic gate confirms C43's code 'calls' C46; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C44 calls C17",
   "anchor": "tools/coyodex/viewer/gen_viewer.py:3262",
   "detail": "From: C44 = Diagram builder (tools/coyodex/viewer/gen_viewer.py:3303); entry points: python -m coyodex.viewer.gen_viewer <graph file> <output file> (tools/coyodex/viewer/gen_viewer.py:3366); To: C17 = Feature index (tools/coyodex/features.py:331)",
   "why_risky": "backbone edge — no deterministic gate confirms C44's code 'calls' C17; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C56 calls C58",
   "anchor": "tools/coyodex/viewer/viewer.js:202",
   "detail": "From: C56 = Map reading pages (tools/coyodex/viewer/viewer.js:7970); entry points: Opens the Features tab, under Product (tools/coyodex/viewer/viewer.html:120); Opens the Rules tab, under Product (tools/coyodex/viewer/viewer.html:127); Opens the Storage tab, under Data (tools/coyodex/viewer/viewer.html:131); Opens the Tests tab, under Under the hood (tools/coyodex/viewer/viewer.html:138); Opens the System tab, under Under the hood (tools/coyodex/viewer/viewer.html:141); Opens the Glossary tab, its own group (tools/coyodex/viewer/viewer.html:145); Any screen writes new text, so every glossary word inside that text becomes a link with no reader action (tools/coyodex/viewer/viewer.js:479); To: C58 = Source column (tools/coyodex/viewer/viewer.js:10343); entry points: The map's page loads, so the viewer checks the server is alive and loads the project's file tree (tools/coyodex/viewer/viewer.js:11366)",
   "why_risky": "backbone edge — no deterministic gate confirms C56's code 'calls' C58; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C2 calls C1",
   "anchor": "tools/coyodex/assemble.py:155",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C2's code 'calls' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C2 calls C13",
   "anchor": "tools/coyodex/lint_fragment.py:125",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: C13 = Map validator (tools/coyodex/validate_model.py:4361); entry points: coyodex validate (tools/coyodex/validate_model.py:4663)",
   "why_risky": "backbone edge — no deterministic gate confirms C2's code 'calls' C13; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C2 calls C25",
   "anchor": "tools/coyodex/assemble.py:928",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329)",
   "why_risky": "backbone edge — no deterministic gate confirms C2's code 'calls' C25; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C2 calls C3",
   "anchor": "tools/coyodex/assemble.py:440",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: C3 = Map lookups (tools/coyodex/dump.py:316); entry points: `coyodex dump` prints the whole map, or one named part of it, as data. (tools/coyodex/dump.py:246); `coyodex diff` reports what changed between two versions of a map, row by row. (tools/coyodex/mapdiff.py:198); `coyodex record` writes one accepted-advisory line under a named heading. (tools/coyodex/record.py:194)",
   "why_risky": "backbone edge — no deterministic gate confirms C2's code 'calls' C3; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C2 calls C45",
   "anchor": "tools/coyodex/assemble.py:964",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: C45 = Graph builder (tools/coyodex/views.py:1242); entry points: coyodex render <map file> <output text file> (tools/coyodex/viewer/render.py:53)",
   "why_risky": "backbone edge — no deterministic gate confirms C2's code 'calls' C45; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C2 calls C43",
   "anchor": "tools/coyodex/assemble.py:968",
   "detail": "From: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353); To: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852)",
   "why_risky": "backbone edge — no deterministic gate confirms C2's code 'calls' C43; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C25 calls C2",
   "anchor": "tools/coyodex/reconcile_build.py:302",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353)",
   "why_risky": "backbone edge — no deterministic gate confirms C25's code 'calls' C2; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C25 calls C1",
   "anchor": "tools/coyodex/reconcile_build.py:105",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C25's code 'calls' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C25 calls C4",
   "anchor": "tools/coyodex/reconcile_build.py:150",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167)",
   "why_risky": "backbone edge — no deterministic gate confirms C25's code 'calls' C4; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C3 calls C2",
   "anchor": "tools/coyodex/dump.py:292",
   "detail": "From: C3 = Map lookups (tools/coyodex/dump.py:316); entry points: `coyodex dump` prints the whole map, or one named part of it, as data. (tools/coyodex/dump.py:246); `coyodex diff` reports what changed between two versions of a map, row by row. (tools/coyodex/mapdiff.py:198); `coyodex record` writes one accepted-advisory line under a named heading. (tools/coyodex/record.py:194); To: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353)",
   "why_risky": "backbone edge — no deterministic gate confirms C3's code 'calls' C2; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C3 calls C1",
   "anchor": "tools/coyodex/dump.py:103",
   "detail": "From: C3 = Map lookups (tools/coyodex/dump.py:316); entry points: `coyodex dump` prints the whole map, or one named part of it, as data. (tools/coyodex/dump.py:246); `coyodex diff` reports what changed between two versions of a map, row by row. (tools/coyodex/mapdiff.py:198); `coyodex record` writes one accepted-advisory line under a named heading. (tools/coyodex/record.py:194); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C3's code 'calls' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C13 calls C1",
   "anchor": "tools/coyodex/validate_model.py:4727",
   "detail": "From: C13 = Map validator (tools/coyodex/validate_model.py:4361); entry points: coyodex validate (tools/coyodex/validate_model.py:4663); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C13's code 'calls' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C13 calls C3",
   "anchor": "tools/coyodex/validate_model.py:3468",
   "detail": "From: C13 = Map validator (tools/coyodex/validate_model.py:4361); entry points: coyodex validate (tools/coyodex/validate_model.py:4663); To: C3 = Map lookups (tools/coyodex/dump.py:316); entry points: `coyodex dump` prints the whole map, or one named part of it, as data. (tools/coyodex/dump.py:246); `coyodex diff` reports what changed between two versions of a map, row by row. (tools/coyodex/mapdiff.py:198); `coyodex record` writes one accepted-advisory line under a named heading. (tools/coyodex/record.py:194)",
   "why_risky": "backbone edge — no deterministic gate confirms C13's code 'calls' C3; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C13 calls C15",
   "anchor": "tools/coyodex/validate_model.py:4501",
   "detail": "From: C13 = Map validator (tools/coyodex/validate_model.py:4361); entry points: coyodex validate (tools/coyodex/validate_model.py:4663); To: C15 = Diagram balance report (tools/coyodex/balance.py:119); entry points: coyodex balance (tools/coyodex/balance.py:231)",
   "why_risky": "backbone edge — no deterministic gate confirms C13's code 'calls' C15; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C14 calls C1",
   "anchor": "tools/coyodex/audit_model.py:1361",
   "detail": "From: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C14's code 'calls' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C14 calls C3",
   "anchor": "tools/coyodex/audit_model.py:782",
   "detail": "From: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307); To: C3 = Map lookups (tools/coyodex/dump.py:316); entry points: `coyodex dump` prints the whole map, or one named part of it, as data. (tools/coyodex/dump.py:246); `coyodex diff` reports what changed between two versions of a map, row by row. (tools/coyodex/mapdiff.py:198); `coyodex record` writes one accepted-advisory line under a named heading. (tools/coyodex/record.py:194)",
   "why_risky": "backbone edge — no deterministic gate confirms C14's code 'calls' C3; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C14 calls C13",
   "anchor": "tools/coyodex/audit_model.py:962",
   "detail": "From: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307); To: C13 = Map validator (tools/coyodex/validate_model.py:4361); entry points: coyodex validate (tools/coyodex/validate_model.py:4663)",
   "why_risky": "backbone edge — no deterministic gate confirms C14's code 'calls' C13; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C15 calls C1",
   "anchor": "tools/coyodex/balance.py:271",
   "detail": "From: C15 = Diagram balance report (tools/coyodex/balance.py:119); entry points: coyodex balance (tools/coyodex/balance.py:231); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C15's code 'calls' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C4 calls C14",
   "anchor": "tools/coyodex/cli.py:132",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307)",
   "why_risky": "backbone edge — no deterministic gate confirms C4's code 'calls' C14; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C4 calls C23",
   "anchor": "tools/coyodex/cli.py:171",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: C23 = Grounding record (tools/coyodex/grounding.py:201); entry points: Running `coyodex anchor-drift` lists every code link pointing at a line that cannot be doing the work. (tools/coyodex/anchor_drift.py:378); Running `coyodex grounding write` derives the four counts and writes them into a build fragment. (tools/coyodex/grounding.py:1003); Running `coyodex grounding by-element` shows, per map element, what a fresh reader proved against the label its author typed. (tools/coyodex/grounding.py:1049); Running `coyodex grounding report` lists which claims were disproved, tied, unsettled, or never voted on. (tools/coyodex/grounding.py:1073); Running `coyodex grounding lint` checks the verdict files for shape while the reader that wrote them is still reachable. (tools/coyodex/grounding.py:924); Running `coyodex grounding refutations` fails the build when the map still carries a claim a fresh reader disproved. (tools/coyodex/grounding.py:974)",
   "why_risky": "backbone edge — no deterministic gate confirms C4's code 'calls' C23; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C4 calls C24",
   "anchor": "tools/coyodex/cli.py:174",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: C24 = Map repairs (tools/coyodex/fix.py:1724); entry points: Running `coyodex fix dedup-security` drops an access surface that two harvest fragments both recorded. (tools/coyodex/fix.py:1128); Running `coyodex fix row` rewrites one row's own text inside the fragment that authored the row. (tools/coyodex/fix.py:1416); Running `coyodex fix apply-drift` writes the corrected line into every code link a fresh reader found had moved. (tools/coyodex/fix.py:149); Running `coyodex fix drop-edge` removes a disproved relation and heals the happy-path steps that rode it. (tools/coyodex/fix.py:399); Running `coyodex fix dedup-relation` removes one chosen copy of a relation two data cards both declare. (tools/coyodex/fix.py:508); Running `coyodex fix dedup-edge` keeps one code link for a relation declared at several different lines. (tools/coyodex/fix.py:739); Running `coyodex fix security-row` rewrites one access surface's text, and refuses when the selector matches more than one row. (tools/coyodex/fix.py:976)",
   "why_risky": "backbone edge — no deterministic gate confirms C4's code 'calls' C24; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C4 calls C16",
   "anchor": "tools/coyodex/cli.py:168",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: C16 = Pre-commit gate run (tools/coyodex/finalize.py:472); entry points: coyodex finalize (tools/coyodex/finalize.py:778)",
   "why_risky": "backbone edge — no deterministic gate confirms C4's code 'calls' C16; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C4 calls C25",
   "anchor": "tools/coyodex/cli.py:180",
   "detail": "From: C4 = Command shell and build setup (tools/coyodex/cli.py:123); entry points: Running `coyodex` with no command, or asking for help or the version, prints a summary. (tools/coyodex/cli.py:115); `coyodex contract` prints the brief one fan-out worker should receive. (tools/coyodex/contract.py:109); `coyodex scope` says which files will be analysed and which commit the map will name. (tools/coyodex/scope.py:167); To: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329)",
   "why_risky": "backbone edge — no deterministic gate confirms C4's code 'calls' C25; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C23 calls C14",
   "anchor": "tools/coyodex/grounding.py:1062",
   "detail": "From: C23 = Grounding record (tools/coyodex/grounding.py:201); entry points: Running `coyodex anchor-drift` lists every code link pointing at a line that cannot be doing the work. (tools/coyodex/anchor_drift.py:378); Running `coyodex grounding write` derives the four counts and writes them into a build fragment. (tools/coyodex/grounding.py:1003); Running `coyodex grounding by-element` shows, per map element, what a fresh reader proved against the label its author typed. (tools/coyodex/grounding.py:1049); Running `coyodex grounding report` lists which claims were disproved, tied, unsettled, or never voted on. (tools/coyodex/grounding.py:1073); Running `coyodex grounding lint` checks the verdict files for shape while the reader that wrote them is still reachable. (tools/coyodex/grounding.py:924); Running `coyodex grounding refutations` fails the build when the map still carries a claim a fresh reader disproved. (tools/coyodex/grounding.py:974); To: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307)",
   "why_risky": "backbone edge — no deterministic gate confirms C23's code 'calls' C14; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C24 calls C23",
   "anchor": "tools/coyodex/fix.py:192",
   "detail": "From: C24 = Map repairs (tools/coyodex/fix.py:1724); entry points: Running `coyodex fix dedup-security` drops an access surface that two harvest fragments both recorded. (tools/coyodex/fix.py:1128); Running `coyodex fix row` rewrites one row's own text inside the fragment that authored the row. (tools/coyodex/fix.py:1416); Running `coyodex fix apply-drift` writes the corrected line into every code link a fresh reader found had moved. (tools/coyodex/fix.py:149); Running `coyodex fix drop-edge` removes a disproved relation and heals the happy-path steps that rode it. (tools/coyodex/fix.py:399); Running `coyodex fix dedup-relation` removes one chosen copy of a relation two data cards both declare. (tools/coyodex/fix.py:508); Running `coyodex fix dedup-edge` keeps one code link for a relation declared at several different lines. (tools/coyodex/fix.py:739); Running `coyodex fix security-row` rewrites one access surface's text, and refuses when the selector matches more than one row. (tools/coyodex/fix.py:976); To: C23 = Grounding record (tools/coyodex/grounding.py:201); entry points: Running `coyodex anchor-drift` lists every code link pointing at a line that cannot be doing the work. (tools/coyodex/anchor_drift.py:378); Running `coyodex grounding write` derives the four counts and writes them into a build fragment. (tools/coyodex/grounding.py:1003); Running `coyodex grounding by-element` shows, per map element, what a fresh reader proved against the label its author typed. (tools/coyodex/grounding.py:1049); Running `coyodex grounding report` lists which claims were disproved, tied, unsettled, or never voted on. (tools/coyodex/grounding.py:1073); Running `coyodex grounding lint` checks the verdict files for shape while the reader that wrote them is still reachable. (tools/coyodex/grounding.py:924); Running `coyodex grounding refutations` fails the build when the map still carries a claim a fresh reader disproved. (tools/coyodex/grounding.py:974)",
   "why_risky": "backbone edge — no deterministic gate confirms C24's code 'calls' C23; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C25 calls C14",
   "anchor": "tools/coyodex/reconcile.py:526",
   "detail": "From: C25 = Assignment pass (tools/coyodex/reconcile_build.py:358); entry points: Running `coyodex provenance show` prints the conversations that built this map, newest last. (tools/coyodex/provenance.py:293); Running `coyodex provenance stamp` records this conversation's id, the build minute and the code commit. (tools/coyodex/provenance.py:308); Running `coyodex reconcile` expands path rules into an assignment file and names every rule that matched nothing. (tools/coyodex/reconcile_build.py:329); To: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307)",
   "why_risky": "backbone edge — no deterministic gate confirms C25's code 'calls' C14; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C16 calls C13",
   "anchor": "tools/coyodex/finalize.py:108",
   "detail": "From: C16 = Pre-commit gate run (tools/coyodex/finalize.py:472); entry points: coyodex finalize (tools/coyodex/finalize.py:778); To: C13 = Map validator (tools/coyodex/validate_model.py:4361); entry points: coyodex validate (tools/coyodex/validate_model.py:4663)",
   "why_risky": "backbone edge — no deterministic gate confirms C16's code 'calls' C13; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C16 calls C14",
   "anchor": "tools/coyodex/finalize.py:111",
   "detail": "From: C16 = Pre-commit gate run (tools/coyodex/finalize.py:472); entry points: coyodex finalize (tools/coyodex/finalize.py:778); To: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307)",
   "why_risky": "backbone edge — no deterministic gate confirms C16's code 'calls' C14; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C16 calls C23",
   "anchor": "tools/coyodex/finalize.py:114",
   "detail": "From: C16 = Pre-commit gate run (tools/coyodex/finalize.py:472); entry points: coyodex finalize (tools/coyodex/finalize.py:778); To: C23 = Grounding record (tools/coyodex/grounding.py:201); entry points: Running `coyodex anchor-drift` lists every code link pointing at a line that cannot be doing the work. (tools/coyodex/anchor_drift.py:378); Running `coyodex grounding write` derives the four counts and writes them into a build fragment. (tools/coyodex/grounding.py:1003); Running `coyodex grounding by-element` shows, per map element, what a fresh reader proved against the label its author typed. (tools/coyodex/grounding.py:1049); Running `coyodex grounding report` lists which claims were disproved, tied, unsettled, or never voted on. (tools/coyodex/grounding.py:1073); Running `coyodex grounding lint` checks the verdict files for shape while the reader that wrote them is still reachable. (tools/coyodex/grounding.py:924); Running `coyodex grounding refutations` fails the build when the map still carries a claim a fresh reader disproved. (tools/coyodex/grounding.py:974)",
   "why_risky": "backbone edge — no deterministic gate confirms C16's code 'calls' C23; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C43 calls C34",
   "anchor": "tools/coyodex/viewer/serve.py:420",
   "detail": "From: C43 = Map server (tools/coyodex/viewer/serve.py:800); entry points: The server checks whether its own code changed since it started, and warns once (tools/coyodex/viewer/serve.py:180); The server checks whether a map changed on disk, and drops what it cached (tools/coyodex/viewer/serve.py:210); GET / (tools/coyodex/viewer/serve.py:583); GET /static/<file> (tools/coyodex/viewer/serve.py:586); GET /api/recents (tools/coyodex/viewer/serve.py:614); GET /api/browse?path= (tools/coyodex/viewer/serve.py:621); POST /api/open (tools/coyodex/viewer/serve.py:643); POST /api/forget (tools/coyodex/viewer/serve.py:650); POST /api/reorder (tools/coyodex/viewer/serve.py:660); GET /p/<project>/ (tools/coyodex/viewer/serve.py:671); GET /p/<project>/api/health (tools/coyodex/viewer/serve.py:675); GET /p/<project>/api/view (tools/coyodex/viewer/serve.py:677); GET /p/<project>/api/tree (tools/coyodex/viewer/serve.py:686); GET /p/<project>/api/symbols (tools/coyodex/viewer/serve.py:691); GET /p/<project>/api/src?path=&at= (tools/coyodex/viewer/serve.py:695); GET /p/<project>/api/impact?base=&target= (tools/coyodex/viewer/serve.py:721); GET /p/<project>/api/impactcommits (tools/coyodex/viewer/serve.py:732); GET /p/<project>/api/impactsrcdiff?path=&base=&target= (tools/coyodex/viewer/serve.py:735); Each browser request gets its own worker thread (tools/coyodex/viewer/serve.py:800); Starting with the open option pops the landing page up in a browser (tools/coyodex/viewer/serve.py:806); The server takes requests until someone stops it (tools/coyodex/viewer/serve.py:808); Pressing the stop key closes the listening port (tools/coyodex/viewer/serve.py:812); coyodex serve [folder] [--port N] [--open] (tools/coyodex/viewer/serve.py:852); To: C34 = Change impact (tools/coyodex/impact_git.py:285)",
   "why_risky": "backbone edge — no deterministic gate confirms C43's code 'calls' C34; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C24 calls C2",
   "anchor": "tools/coyodex/fix.py:1399",
   "detail": "From: C24 = Map repairs (tools/coyodex/fix.py:1724); entry points: Running `coyodex fix dedup-security` drops an access surface that two harvest fragments both recorded. (tools/coyodex/fix.py:1128); Running `coyodex fix row` rewrites one row's own text inside the fragment that authored the row. (tools/coyodex/fix.py:1416); Running `coyodex fix apply-drift` writes the corrected line into every code link a fresh reader found had moved. (tools/coyodex/fix.py:149); Running `coyodex fix drop-edge` removes a disproved relation and heals the happy-path steps that rode it. (tools/coyodex/fix.py:399); Running `coyodex fix dedup-relation` removes one chosen copy of a relation two data cards both declare. (tools/coyodex/fix.py:508); Running `coyodex fix dedup-edge` keeps one code link for a relation declared at several different lines. (tools/coyodex/fix.py:739); Running `coyodex fix security-row` rewrites one access surface's text, and refuses when the selector matches more than one row. (tools/coyodex/fix.py:976); To: C2 = Fragment merge (tools/coyodex/assemble.py:886); entry points: `coyodex assemble` merges the build's fragments into one map and writes it. (tools/coyodex/assemble.py:812); `coyodex lint-fragment` checks one worker's fragment before the worker returns it. (tools/coyodex/lint_fragment.py:353)",
   "why_risky": "backbone edge — no deterministic gate confirms C24's code 'calls' C2; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C75 prescribes C45",
   "anchor": "method/dispatch.md:187",
   "detail": "From: C75 = The build method (method.md:3); entry points: A person asks coyodex to analyze, when a map exists and the code has moved on since it was pinned. (method/dispatch.md:151); A person says the change report looks right, so coyodex folds the report into the map. (method/dispatch.md:154); A person asks in plain words to move, rename, split or drill deeper into a part of the map. (method/dispatch.md:156); A person explicitly asks to regenerate the whole map from scratch, and confirms the warning. (method/dispatch.md:171); A person types the coyodex command in a project that has no map yet, so a first map is built. (method/dispatch.md:83); To: C45 = Graph builder (tools/coyodex/views.py:1242); entry points: coyodex render <map file> <output text file> (tools/coyodex/viewer/render.py:53)",
   "why_risky": "backbone edge — no deterministic gate confirms C75's code 'prescribes' C45; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C63 calls C1",
   "anchor": "eval/tools/coyodex_eval/profile.py:250",
   "detail": "From: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463); To: C1 = Map document (tools/coyodex/model.py:1080); entry points: `python -m coyodex.json_schema` prints the published schema for the map file. (tools/coyodex/json_schema.py:432)",
   "why_risky": "backbone edge — no deterministic gate confirms C63's code 'calls' C1; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C63 calls C13",
   "anchor": "eval/tools/coyodex_eval/profile.py:257",
   "detail": "From: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463); To: C13 = Map validator (tools/coyodex/validate_model.py:4361); entry points: coyodex validate (tools/coyodex/validate_model.py:4663)",
   "why_risky": "backbone edge — no deterministic gate confirms C63's code 'calls' C13; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C63 calls C14",
   "anchor": "eval/tools/coyodex_eval/profile.py:259",
   "detail": "From: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463); To: C14 = Map auditor (tools/coyodex/audit_model.py:811); entry points: coyodex audit (tools/coyodex/audit_model.py:1307)",
   "why_risky": "backbone edge — no deterministic gate confirms C63's code 'calls' C14; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C63 calls C44",
   "anchor": "eval/tools/coyodex_eval/run.py:136",
   "detail": "From: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463); To: C44 = Diagram builder (tools/coyodex/viewer/gen_viewer.py:3303); entry points: python -m coyodex.viewer.gen_viewer <graph file> <output file> (tools/coyodex/viewer/gen_viewer.py:3366)",
   "why_risky": "backbone edge — no deterministic gate confirms C63's code 'calls' C44; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C63 calls C45",
   "anchor": "eval/tools/coyodex_eval/run.py:163",
   "detail": "From: C63 = Map quality score and verdict (eval/tools/coyodex_eval/profile.py:337); entry points: coyodex-eval (eval/tools/coyodex_eval/cli.py:56); coyodex-eval compare (eval/tools/coyodex_eval/compare.py:606); coyodex-eval mutate plant (eval/tools/coyodex_eval/mutate.py:244); coyodex-eval mutate score (eval/tools/coyodex_eval/mutate.py:249); coyodex-eval score (eval/tools/coyodex_eval/profile.py:443); coyodex-eval run (eval/tools/coyodex_eval/run.py:192); coyodex-eval claims (eval/tools/coyodex_eval/run.py:279); coyodex-eval hash (eval/tools/coyodex_eval/run.py:338); coyodex-eval judge (eval/tools/coyodex_eval/run.py:353); coyodex-eval protocol (eval/tools/coyodex_eval/run.py:418); coyodex-eval bless (eval/tools/coyodex_eval/run.py:463); To: C45 = Graph builder (tools/coyodex/views.py:1242); entry points: coyodex render <map file> <output text file> (tools/coyodex/viewer/render.py:53)",
   "why_risky": "backbone edge — no deterministic gate confirms C63's code 'calls' C45; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  },
  {
   "claim": "C65 calls C64",
   "anchor": "eval/tools/coyodex_eval/process_scorecard.py:2941",
   "detail": "From: C65 = Build behaviour scorecard (eval/tools/coyodex_eval/process_scorecard.py:2924); entry points: coyodex-eval process (eval/tools/coyodex_eval/process_scorecard.py:3130); coyodex-eval process --diff (eval/tools/coyodex_eval/process_scorecard.py:3134); To: C64 = Build transcript reader and spend report (eval/tools/coyodex_eval/transcript.py:305); entry points: coyodex-eval cost (eval/tools/coyodex_eval/cost.py:674); coyodex-eval transcript (eval/tools/coyodex_eval/transcript.py:1067)",
   "why_risky": "backbone edge — no deterministic gate confirms C65's code 'calls' C64; ground the call site against the code.",
   "theme": "backbone",
   "drift_eligible": true
  }
 ],
 "themes": [
  "security",
  "rule",
  "dep-usage",
  "ownership",
  "persistence",
  "messaging",
  "lifecycle",
  "cadence",
  "description",
  "backbone"
 ],
 "theme_counts": {
  "security": 21,
  "rule": 224,
  "dep-usage": 17,
  "ownership": 56,
  "lifecycle": 3,
  "cadence": 11,
  "description": 31,
  "backbone": 77
 }
}
