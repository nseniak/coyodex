{"grounding": [
  {"claim": "C1 uses C30",
   "grounded": true,
   "evidence": "method.md:573",
   "note": "The method doc's pre-index step instructs running `.venv/bin/coyodex preindex --root <repo>`, a real invocation of the CLI dispatcher — 'uses' is the right weak verb for a doc-to-tool edge (it is not a code call)."},
  {"claim": "C3 uses C4",
   "grounded": true,
   "evidence": "tools/coyodex/model.py:529",
   "note": "`_remap_str_ids` calls `grammar.ID_TOKEN.sub(...)` on the grammar module's regex — a real use of C4 from C3's own code."},
  {"claim": "C5 reads C3",
   "grounded": true,
   "evidence": "tools/coyodex/json_schema.py:271",
   "note": "`generate_schema` passes C3's `ProjectModel` dataclass into `_ensure_def`, which introspects its fields to emit the schema — a genuine read of the model's shape."},
  {"claim": "C5 uses C4",
   "grounded": true,
   "evidence": "tools/coyodex/json_schema.py:81",
   "note": "The Dep `kind` schema entry inlines `grammar.DEP_KINDS` from C4."},
  {"claim": "C6 calls C3",
   "grounded": true,
   "evidence": "tools/coyodex/assemble.py:139",
   "note": "`load_fragment` calls `_build(data, ProjectModel, label)`, both imported from `coyodex.model` (C3)."},
  {"claim": "C6 calls C10",
   "grounded": true,
   "evidence": "tools/coyodex/assemble.py:103",
   "note": "`_derive_entity_edges` calls `unbacked_entity_steps` (validate_model.py:1645), which sits in C10's span — C10's own evidence cites 1684 and 1846, so the rulebook extends past C16's 552 anchor."},
  {"claim": "C6 calls C8",
   "grounded": true,
   "evidence": "tools/coyodex/assemble.py:456",
   "note": "assemble's main calls `apply_reconcile(model, rec, rec_stats)`, defined in reconcile.py:238 (C8)."},
  {"claim": "C6 calls C20",
   "grounded": true,
   "evidence": "tools/coyodex/assemble.py:475",
   "note": "Writes the markdown view via `model_to_markdown(model)` imported from `coyodex.views` (C20)."},
  {"claim": "C7 calls C6",
   "grounded": true,
   "evidence": "tools/coyodex/lint_fragment.py:238",
   "note": "The lint loop calls `load_fragment` (assemble.py:122); assemble.py is owned wholly by C6, so the parse-and-validate call really crosses into the assembler."},
  {"claim": "C7 calls C10",
   "grounded": true,
   "evidence": "tools/coyodex/lint_fragment.py:117",
   "note": "`lint_fragment_problems` calls `_check_anchor_format` (validate_model.py:1840), a C10 rulebook check (C10 evidence cites 1846)."},
  {"claim": "C7 calls C14",
   "grounded": true,
   "evidence": "tools/coyodex/lint_fragment.py:150",
   "note": "The `--repo` branch calls `check_anchor_existence_model` (validate_model.py:2046), inside C14's 1966-2295 range."},
  {"claim": "C7 uses C4",
   "grounded": true,
   "evidence": "tools/coyodex/lint_fragment.py:91",
   "note": "The actor-endpoint gate calls `grammar.is_role_id(k)` from C4; 'calls' would be sharper than 'uses'."},
  {"claim": "C8 calls C12",
   "grounded": true,
   "evidence": "tools/coyodex/fix.py:85",
   "note": "`fix apply-drift` calls `l2_worklist_model(m)` from audit_model.py (C12) to build the worklist it matches drift records against."},
  {"claim": "C8 calls C5",
   "grounded": true,
   "evidence": "tools/coyodex/anchor_drift.py:64",
   "note": "`_confirmed_drifts` calls `anchor_drift(...)` imported from `coyodex.anchors` (C5) — not the same-named local module."},
  {"claim": "C8 calls C14",
   "grounded": true,
   "evidence": "tools/coyodex/anchor_drift.py:110",
   "note": "`shape_findings` returns `check_operative_lines_model(m, roots)` (validate_model.py:2092), squarely in C14's range."},
  {"claim": "C8 calls C3",
   "grounded": true,
   "evidence": "tools/coyodex/fix.py:42",
   "note": "`_write` serializes through `to_canonical_json(m)` imported from `coyodex.model` (C3)."},
  {"claim": "C8 calls C11",
   "grounded": true,
   "evidence": "tools/coyodex/reconcile.py:227",
   "note": "`validate_reconcile` calls `check_hierarchy(hier_parents, defined)` from validate_analysis.py (C11)."},
  {"claim": "C9 calls C5",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:271",
   "note": "`py_symbols` calls `parse_python(...)` imported from `coyodex.pysrc`, one of C5's files."},
  {"claim": "C10 calls C16",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:522",
   "note": "`unclaimed_surface_components` (line 514, before C16's 552 anchor) calls the adjudication reader `_recorded_ids` at 552 and `_recorded_coverage_dirs` at 523 — direction and split both hold."},
  {"claim": "C10 uses C4",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:208",
   "note": "`_referenced_ids` (184-248, inside C10) calls `grammar.is_step_id` / `grammar.is_role_id` from C4."},
  {"claim": "C11 calls C5",
   "grounded": true,
   "evidence": "tools/coyodex/validate_analysis.py:322",
   "note": "`_resolve_source_file` calls `strip_anchor(source)` imported from `coyodex.anchors` (C5)."},
  {"claim": "C11 uses C4",
   "grounded": true,
   "evidence": "tools/coyodex/validate_analysis.py:66",
   "note": "The nesting-depth walk compares against `DEEP_NEST_WARN` imported from `coyodex.grammar` (C4) — a constant read, so 'uses' fits."},
  {"claim": "C12 calls C3",
   "grounded": true,
   "evidence": "tools/coyodex/audit_model.py:124",
   "note": "`_flow_component_ids` calls `expanded_flow_steps(m, f)` imported from `coyodex.model` (C3)."},
  {"claim": "C12 uses C4",
   "grounded": true,
   "evidence": "tools/coyodex/audit_model.py:372",
   "note": "The dep legend line calls `grammar.classify_dep(d.kind or '', d.type)`; 'calls' would be sharper than 'uses'."},
  {"claim": "C12 uses C5",
   "grounded": true,
   "evidence": "tools/coyodex/audit_model.py:56",
   "note": "`_anchor` matches with `_FILEREF`, which is `FILEREF` imported from `coyodex.anchors` (C5)."},
  {"claim": "C13 calls C3",
   "grounded": true,
   "evidence": "tools/coyodex/balance.py:193",
   "note": "balance's main calls `load_model(...)` imported from `coyodex.model` (C3)."},
  {"claim": "C14 calls C5",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2119",
   "note": "`check_operative_lines_model` calls `non_operative_reason(...)` imported from `coyodex.anchors` (C5)."},
  {"claim": "C14 calls C11",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2105",
   "note": "The same function calls `_resolve_source_file(anchor, roots)`, imported from validate_analysis.py (C11) at validate_model.py:61."},
  {"claim": "C14 calls C20",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2288",
   "note": "`_check_view_fresh` (2280-2291) compares against `model_to_markdown(m)` from `coyodex.views` (C20); the line sits in C14's 1966-2295 range, though C15's purpose text also claims view-freshness — C15 only calls this helper from 2483."},
  {"claim": "C15 calls C10",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2307",
   "note": "The orchestrator `validate_model` (2296) calls `_check_ids(m)` at validate_model.py:133, C10's own anchor."},
  {"claim": "C15 calls C14",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2364",
   "note": "The `--check-sources` branch calls `check_entity_sources_model(m, roots)` (2160), inside C14."},
  {"claim": "C15 calls C11",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2376",
   "note": "The orchestrator calls `check_hierarchy(parents, defined)` from validate_analysis.py (C11)."},
  {"claim": "C15 calls C13",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2415",
   "note": "The orchestrator calls `balance_lib.balance_warnings(m)`; balance_lib.py is one of C13's files."},
  {"claim": "C15 calls C3",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2532",
   "note": "The `coyodex validate` main calls `load_model(...)` imported from `coyodex.model` (C3)."},
  {"claim": "C16 calls C13",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:555",
   "note": "Anchor drifted by two lines: 557 is a local regex match; the real C13 call is `balance_lib.extras_bodies(m, heading)` at 555, inside `_recorded_ids` (C16's own function)."},
  {"claim": "C30 calls C9",
   "grounded": true,
   "evidence": "tools/coyodex/cli.py:81",
   "note": "The dispatcher's `preindex` branch returns `preindex.main(rest)` (preindex.py:379, C9)."},
  {"claim": "C30 calls C15",
   "grounded": true,
   "evidence": "tools/coyodex/cli.py:84",
   "note": "The `validate` branch returns `validate_model.main(_default_map(rest))`; `main` is at validate_model.py:2496, inside C15."},
  {"claim": "C30 calls C12",
   "grounded": true,
   "evidence": "tools/coyodex/cli.py:87",
   "note": "The `audit` branch returns `audit_model.main(...)` (audit_model.py:560, C12)."},
  {"claim": "C30 calls C20",
   "grounded": true,
   "evidence": "tools/coyodex/cli.py:90",
   "note": "The `render` branch returns `render.main(rest)`; viewer/render.py is one of C20's files."},
  {"claim": "C30 calls C24",
   "grounded": true,
   "evidence": "tools/coyodex/cli.py:93",
   "note": "The `serve` branch returns `serve.main(rest)` (viewer/serve.py:763, C24)."},
  {"claim": "C30 calls C6",
   "grounded": true,
   "evidence": "tools/coyodex/cli.py:96",
   "note": "The `assemble` branch returns `assemble.main(rest)` (assemble.py:343, C6)."}
]}
