{
 "grounding": [
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at tools/coyodex/viewer/viewer.js:13472 — Picks, for each kind of thing, the single screen that draws it.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:13472",
   "skeptic": "rule-4",
   "note": "I read selectTargetFor (viewer.js:13467-13483+): line 13472 is the `switch (n.kind)` that dispatches on an element's kind, and each case returns one view state (e.g. 'interface' -> {kind:'interfaces'}). Its own header comment says it answers 'which view draws `id`', so one kind resolves to exactly one screen."
  },
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at tools/coyodex/viewer/viewer.js:7647 — Reads that same choice to decide which tab a thing's own page sits under.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:7647",
   "skeptic": "rule-4",
   "note": "Line 7647 is `const t = selectTargetFor(id);` inside elementHomeView (7646-7649), whose comment says a details page has no tab of its own and belongs under the element's home view, with selectTargetFor as the single source of that answer. It then hands the state to topView on 7648."
  },
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at tools/coyodex/viewer/viewer.js:7651 — Sends a page about one thing to the tab that thing lives under.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:7651",
   "skeptic": "rule-4",
   "note": "Line 7651 is `if (kind === 'element') return id ? elementHomeView(id) : 'container';` at the head of topView (7650). A state of kind 'element' (a page about one thing) is routed to that element's home view, i.e. the tab it lives under; every other kind falls to the fixed mappings on 7652-7663."
  },
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at method/diagrams.md:54 — Tells the map builder that a link to a thing lands on that thing's home screen.",
   "grounded": true,
   "evidence": "method/diagrams.md:54",
   "skeptic": "rule-4",
   "note": "Line 54 reads '  links locate each element in its home view). A flow has ONE rendering of its step list:' — the tail of the sentence begun on line 53 about the T6 flow's element links. It states plainly that an element link lands on that element's home view."
  },
  {
   "claim": "Rule 'A label that would be true of nearly every card is left off.' is enforced at tools/coyodex/viewer/viewer.js:8219 — Leaves off the word saying who a feature serves when the answer is the ordinary one.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:8219",
   "skeptic": "rule-4",
   "note": "Line 8219 is `return (list.length === 1 && list[0] === 'user') ? [] : list;` in shownAudience. I opened both call sites, 748 (a card's Audience field) and 10326 (a feature card's audience pills): an audience of exactly ['user'] yields no pill, so the ordinary answer is left off. The comment at 685 names this as dropping 'user' from a feature card."
  },
  {
   "claim": "Rule 'A label that would be true of nearly every card is left off.' is enforced at tools/coyodex/viewer/viewer.js:721 — Gives a person on the customer's side no word saying whose side they are on.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:721",
   "skeptic": "rule-4",
   "note": "Line 721 is the final `return [];` of actorSidePills (704-722). A machine actor always gets a pill (714-717) and a human whose side is 'internal' gets the 'staff' pill (718-720); a human on any other side — the customer's side — falls through to 721 and gets no side pill at all."
  },
  {
   "claim": "Rule 'A screen leaves off a fact it already shows beside the same thing.' is enforced at tools/coyodex/viewer/viewer.js:730 — Drops a card's sentence when the sentence only repeats the card's own name.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:730",
   "skeptic": "rule-4",
   "note": "Line 730 is `if (desc.trim() === (n.name || '').trim()) desc = '';` in cardFacts (723-). The comment on 728-729 says a rule with no short name uses the whole statement as its title, so the description field then holds the same words; the line blanks the sentence so the card prints one copy, not two."
  },
  {
   "claim": "Rule 'A screen leaves off a fact it already shows beside the same thing.' is enforced at tools/coyodex/viewer/viewer.js:6915 — Drops the feature line when the path above the page already names that feature.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:6915",
   "skeptic": "rule-4",
   "note": "Line 6915 is `}) + (inTrail || sub ? '' : useCaseFeatureFootHtml(id));` on a use case's page. `inTrail` is set on 6905 as 'the breadcrumb chain contains a capability', so the feature foot line is appended only when the trail above the page does NOT already name that feature."
  },
  {
   "claim": "Rule 'A screen leaves off a fact it already shows beside the same thing.' is enforced at tools/coyodex/viewer/viewer.js:8345 — Skips a group heading that would only repeat the section heading above it.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:8345",
   "skeptic": "rule-4",
   "note": "Line 8345 is `if (groups.length < 2) return chips(ids);` in featChipGroupsHtml (8332-8352). Its comment on 8343-8344 says a single group is not a grouping, so the chips are drawn plain rather than under a heading that would repeat the section heading above."
  },
  {
   "claim": "Rule 'A box drops the type word wherever its colour and mark already say what kind of thing it is.' is enforced at tools/coyodex/viewer/viewer.js:987 — Prints the type word only for the box sizes that ask for it, and for every interface.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:987",
   "skeptic": "rule-4",
   "note": "Line 987 is `: (o.word === true || v.word || spec.k === 'interface') ? itemWord(spec) : '';` in itemBoxHtml. `v` is the variant record from ITEM_VARIANT (946-959), where full/map/card carry word:true and compact/tight/figure carry word:false — so the type word prints for the asking box sizes, plus unconditionally for an interface."
  },
  {
   "claim": "Rule 'A box drops the type word wherever its colour and mark already say what kind of thing it is.' is enforced at tools/coyodex/viewer/gen_viewer.py:3057 — Asks for the small box on a flow picture, which carries no type word.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/gen_viewer.py:3057",
   "skeptic": "rule-4",
   "note": "Line 3057 is `decls.append(f'  {token}[\"{_slot(kind, \"tight\", token)}\"]:::cy-{token}')` inside gen_flow_map_mermaid (starts 2999), which emits the 'flowchart LR' flow map at 3088-3092. It asks for the 'tight' box, and ITEM_VARIANT.tight in viewer.js:950 has word:false, so that box carries no type word."
  },
  {
   "claim": "Rule 'A box drops the type word wherever its colour and mark already say what kind of thing it is.' is enforced at method/diagrams.md:56 — Tells the map builder to colour each box on a flow picture by its kind.",
   "grounded": true,
   "evidence": "method/diagrams.md:56",
   "skeptic": "rule-4",
   "note": "Line 56 reads '  element, kind-coloured as everywhere else, entities and dependencies included, with no container' — the middle of the sentence 'one box per element, kind-coloured as everywhere else'. It states the colouring premise the rule rests on (colour says the kind); it does not itself state the word being dropped, which is done in code at viewer.js:987/950."
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyodex/viewer/viewer.js:15584 — Hides the decisions tab on a map that states no decision.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:15584",
   "skeptic": "rule-4",
   "note": "Line 15584 is `if (b.dataset.view === 'rules' && !HAS_RULES) { b.style.display = 'none'; return; }` in the loop over the view tab buttons (15573-15587). The Rules (business rules / decisions) tab is hidden when the map holds no rules; HAS_RULES comes from hasBusinessRules in gen_viewer.py:3416."
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyodex/viewer/viewer.js:15585 — Hides the interfaces tab on a map that records no interface.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:15585",
   "skeptic": "rule-4",
   "note": "Line 15585 is `if (b.dataset.view === 'interfaces' && !HAS_INTERFACES) { b.style.display = 'none'; return; }` in the same tab-gating loop. The Interfaces tab is hidden when the map records no interface; HAS_INTERFACES is fed by gen_viewer.py:3417."
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyodex/viewer/viewer.js:15595 — Drops a whole group of tabs once every tab inside it has been hidden.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:15595",
   "skeptic": "rule-4",
   "note": "Line 15595 is `if (!views.length) continue;` in the loop that builds the group tab row (15592-15603), running after the per-view gating above. groupViews(gid) returns the group's surviving views, so a group whose every view was hidden gets no button at all — the comment on 15588-15591 says exactly this."
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyodex/viewer/gen_viewer.py:3417 — Works out from the map whether there is any interface for a tab to show.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/gen_viewer.py:3417",
   "skeptic": "rule-4",
   "note": "Line 3417 is `hasInterfaces=bool(model is not None and model.interfaces),` in build_view_bundle (starts 3322). The flag is computed from the model's own interfaces list and is the HAS_INTERFACES the viewer's tab gate at viewer.js:15585 reads."
  },
  {
   "claim": "Rule 'Where the map records nothing, the screen says so instead of leaving empty space.' is enforced at tools/coyodex/viewer/viewer.js:8541 — Says a feature reaches no outside service, rather than showing an empty block.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:8541",
   "skeptic": "rule-4",
   "note": "Line 8541 is `: featEmpty('Not stated: no step of this feature\\u2019s walks is drawn at an outside service.'),` — the else arm of the 'What it reaches out to' section (8534-8543). An empty reachesOut list prints that sentence instead of nothing; the comment on 8531-8533 says the section states it rather than vanishing."
  },
  {
   "claim": "Rule 'Where the map records nothing, the screen says so instead of leaving empty space.' is enforced at tools/coyodex/viewer/viewer.js:12874 — Says no flow step reaches this decision, rather than showing an empty block.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:12874",
   "skeptic": "rule-4",
   "note": "Line 12874 is `: '<p class=\"empty\">No traced flow step reaches this rule.</p>';` on a rule's own page. When the rule records no steps (nSteps is 0, set on 12869) the section prints that sentence rather than an empty block; its two neighbours, sites (12872) and entities (12878), do the same."
  },
  {
   "claim": "Rule 'Where the map records nothing, the screen says so instead of leaving empty space.' is enforced at tools/coyodex/viewer/viewer.js:12757 — Says no flow comes through this interface, so no feature can be named here.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:12757",
   "skeptic": "rule-4",
   "note": "Line 12757 prints 'Not stated. No walk in this map comes through this interface, so nothing here can say which features use it.' when i.featuresUnknown is set (12756). The next line (12758-12759) shows the cards, or a different sentence for a known-but-empty list, so the block is never blank."
  },
  {
   "claim": "Rule 'A line between two things offers no code link, because the place it names is only one example.' is enforced at tools/coyodex/viewer/viewer.js:3052 — Builds a relation row as plain text, with no link to a line of code.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:3052",
   "skeptic": "rule-4",
   "note": "Line 3052 is `return '<div class=\"xpair\">' + esc(srcName) + ' → ' + esc(dstName) + ':</div>'` in arrowRowInner (3051-3054). The row is a plain div of escaped text with an optional why line — no anchor, no data-id, no click target. The comment on 3046-3050 states the reason: an arrow's `where` is only an example call site, so rows deliberately do not link to code."
  },
  {
   "claim": "Rule 'A line between two things offers no code link, because the place it names is only one example.' is enforced at tools/coyodex/viewer/viewer.js:2957 — Clears the code viewer when a relation line is picked, rather than pointing it somewhere.",
   "grounded": false,
   "evidence": "tools/coyodex/viewer/viewer.js:2947",
   "skeptic": "rule-4",
   "note": "The line does not do what the claim says. Line 2957 is `highlightTreePath(null);` and I read highlightTreePath (13778-13790): it clears the selected row in the FILE TREE, not the code viewer. showEdge's own comment on 2947-2950 says the opposite of the claim — 'the code viewer is left untouched'. The rule itself IS enforced by showEdge (2951-2957 sets no source link), so the anchor's function is right; the description should say it clears the file-tree path highlight so a previous selection's path cannot read as this arrow's location."
  },
  {
   "claim": "Rule 'A line between two things offers no code link, because the place it names is only one example.' is enforced at tools/coyodex/viewer/viewer.js:14163 — Marks a line of code from a flow step's own place, never from a relation line.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:14163",
   "skeptic": "rule-4",
   "note": "Line 14163 is `if (!st.where) return;` in stepsByPath (14158-14171), which builds the file->decoration index. The key is taken from each STEP's own `where` (14164-14167) and steps without one simply do not decorate; the comment on 14152-14156 says an arrow's `where` is only an example call site and must never stamp a pill on a line."
  },
  {
   "claim": "Rule 'A picture of one flow draws only the connections that flow itself makes.' is enforced at tools/coyodex/viewer/gen_viewer.py:3081 — Builds each arrow from the flow's own steps, grouped by the pair of things they join.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/gen_viewer.py:3081",
   "skeptic": "rule-4",
   "note": "Line 3081 is `pairs.setdefault((pid[str(st['src'])], box_of(st)), []).append(i + 1)` in gen_flow_map_mermaid. It iterates the flow's own `steps` (3080), keys one arrow per ordered src/dst pair and carries the step numbers; the arrows emitted on 3089-3090 come only from that dict, so no backbone edge can appear."
  },
  {
   "claim": "Rule 'A picture of one flow draws only the connections that flow itself makes.' is enforced at method/diagrams.md:58 — Tells the map builder that the arrows are the flow's steps, never the general connection list.",
   "grounded": true,
   "evidence": "method/diagrams.md:58",
   "skeptic": "rule-4",
   "note": "Line 58 reads '  each box names its area instead). The map's arrows are the flow's own steps, deduplicated per pair' and continues on 59 with 'never the backbone edge list — so it cannot draw a relationship this scenario does not exercise'. That is the instruction the claim states."
  },
  {
   "claim": "Rule 'Code shown beside the map is the version the map was built from, not whatever sits on disk now.' is enforced at tools/coyodex/viewer/serve.py:782 — Answers a file request at the map's own commit unless the address asks for another version.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/serve.py:782",
   "skeptic": "rule-4",
   "note": "Line 782 is `at = (query.get('at') or [proj.commit])[0]` in the /src handler. The commit defaults to the project's pinned map commit and is overridden only when the URL carries an `at=` parameter; the file is then read with git_blob_size/git blob at that commit (792+). The comment on 780-781 says the default stays the map's pin."
  },
  {
   "claim": "Rule 'Code shown beside the map is the version the map was built from, not whatever sits on disk now.' is enforced at tools/coyodex/viewer/serve.py:491 — Builds the file list from version history at the map's commit rather than from the folder on disk.",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/serve.py:491",
   "skeptic": "rule-4",
   "note": "Line 491 is `rels = sorted(git_ls_files(proj.repo_root, proj.commit))` in project_tree (482-494). The browser's file list is the git file set at the map's commit; the docstring on 483-487 says the only difference from the render-path tree is 'the file source (git vs a disk walk)'."
  },
  {
   "claim": "Rule 'Code shown beside the map is the version the map was built from, not whatever sits on disk now.' is enforced at method.md:3108 — The instructions tell the reader that data and source both come from history at the map's commit.",
   "grounded": true,
   "evidence": "method.md:3108",
   "skeptic": "rule-4",
   "note": "Line 3108 begins 'served from git at the map's commit).' — the close of the sentence started on 3106-3107 about the map server, where 'the diagram, file browser, and code viewer light up (data + source served from git at the map's commit)'. The instruction says both data and source come from history at the map's commit."
  },
  {
   "claim": "Rule 'Data the product writes only to read back crosses no interface, unless the map records an outside reader.' is enforced at method.md:511 — Keeps the product's own database, cache and queue off the outside edge.",
   "grounded": true,
   "evidence": "method.md:511",
   "skeptic": "rule-4",
   "note": "Line 511 reads 'itself**. Four lines close the gaps: (1) data the product produces only to read back is EXCLUDED' and the exclusion's examples follow on 512: '(its own database, cache, queue)'. So the T2b instruction keeps a read-back store off the outside edge."
  },
  {
   "claim": "Rule 'Data the product writes only to read back crosses no interface, unless the map records an outside reader.' is enforced at method.md:515 — Lets a reader the map records overturn that exclusion, so a store a person browses counts again.",
   "grounded": true,
   "evidence": "method.md:515",
   "skeptic": "rule-4",
   "note": "Line 515 reads 'consumer **the map records** beats the read-back exclusion — a store a person browses on their own' and finishes on 516 with 'IS a surface, a store only the product reads back is not'. That is the override the claim describes."
  },
  {
   "claim": "Rule 'Data the product writes only to read back crosses no interface, unless the map records an outside reader.' is enforced at method/model.md:460 — Repeats the same exclusion on the page that fixes every field a map may hold.",
   "grounded": true,
   "evidence": "method/model.md:460",
   "skeptic": "rule-4",
   "note": "Line 460 reads '**not generated by the product itself**. Excluded: data produced only to read back (its own' and continues on 461 with 'database, cache, queue)'. This is the `interfaces` (T2b) entry of the model spec, so the same exclusion is stated on the page that fixes the map's fields."
  },
  {
   "claim": "Rule 'A step names the service on the far side, never the proxy or the library that carries the call.' is enforced at method.md:516 — Names a reverse proxy and a log shipper as carriers, and the log store as the real edge.",
   "grounded": true,
   "evidence": "method.md:516",
   "skeptic": "rule-4",
   "note": "Line 516 reads 'IS a surface, a store only the product reads back is not. **Name the far side, never the pipe**: a' and line 517 names them: 'log shipper and a reverse proxy are pipes, the log store and the dashboard are the surfaces'. One sentence spanning 516-517; the anchor line carries the instruction itself."
  },
  {
   "claim": "Rule 'A step names the service on the far side, never the proxy or the library that carries the call.' is enforced at tools/coyodex/validate_model.py:2840 — Flags a story step that stops at a carrier standing in front of an interface.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2840",
   "skeptic": "rule-4",
   "note": "Line 2840 is `if side in dep_on_surface and not _excused(f.uc, 'migration'):` inside _check_interfaces (def at 2503). dep_on_surface (2806) is every dep that names an interface, so a flow step whose src or dst is such a dep is collected as owed_migrations and warned at 2876-2884: 'A step at the dep names the pipe; a step at the surface names the far side.' I checked the guard is live: _excused (2794-2803) is true only for an explicitly recorded UCn/SFn escape, and _check_interfaces is called at 6184 with its warnings extended into the run."
  },
  {
   "claim": "Rule 'A step names the service on the far side, never the proxy or the library that carries the call.' is enforced at tools/coyodex/validate_model.py:2851 — Flags the same shortcut inside shared steps, where one carrier is drawn in every story running them.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2851",
   "skeptic": "rule-4",
   "note": "Line 2851 is the same test applied to sub-flow steps: `if side in dep_on_surface and not _excused(sf.id, 'migration'):` in the `for sf in m.subflows` loop (2848-2854), feeding the same owed_migrations warning at 2876. The comment on 2842-2847 gives the reason — one unmigrated step in a shared sub-flow draws the dep in every flow that runs it."
  },
  {
   "claim": "Rule 'Each outside system names the interfaces it stands on, or records why it is none, never both.' is enforced at tools/coyodex/validate_model.py:2744 — Stops a map where an outside system neither claims an interface nor gives a reason.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2744",
   "skeptic": "rule-4",
   "note": "Lines 2744-2748: for each dep, when classify_dep puts it in DEP_KINDS_SYSTEM (an external system) and it has neither `interfaces` nor `not_an_interface`, the message goes to `problems`. Problems are blocking: _check_interfaces' problems are extended into the validator's problem list at 6184-6185."
  },
  {
   "claim": "Rule 'Each outside system names the interfaces it stands on, or records why it is none, never both.' is enforced at tools/coyodex/validate_model.py:2531 — Stops a system that claims an interface and also gives a reason for having none.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2531",
   "skeptic": "rule-4",
   "note": "Lines 2531-2533: `if d.interfaces and d.not_an_interface:` appends 'names an interface AND says why it is none — exactly one of the two' to `problems`. The comment on 2519-2522 says this dep-side check runs even on a map that records no interface at all, and problems block via 6185."
  },
  {
   "claim": "Rule 'Each outside system names the interfaces it stands on, or records why it is none, never both.' is enforced at method/model.md:530 — Demands the reason on every outside system that names no interface.",
   "grounded": true,
   "evidence": "method/model.md:530",
   "skeptic": "rule-4",
   "note": "Line 530 reads 'reason a dep is none, and is REQUIRED on every dep in the external group (`datastore` / `messaging`' — continuing '**`deps[].not_an_interface`** carries the' from 529 and '/ `service` / `platform`) that names no surface' on 531. So the spec demands the reason on every outside system naming no interface."
  },
  {
   "claim": "Rule 'An interface is labelled by what it is, never by what it is for.' is enforced at tools/coyodex/validate_model.py:2630 — Flags a label that answers what the interface is for, such as payments or analytics.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:2630",
   "skeptic": "rule-4",
   "note": "Line 2630 is `elif canon.lower() in grammar.INTERFACE_KIND_PURPOSE_WORDS and not escaped:` and its warning (2631-2636) says the kind 'says what the surface is FOR, not what SHAPE it is'. I read the vocabulary at grammar.py:466-468: payment, analytics, identity, observability, search, storage — so 'payment' and 'analytics' are literally in it."
  }
 ]
}