{
 "schema": "coyodex/theme-batch/v1",
 "theme": "rule",
 "claims": [
  {
   "claim": "Rule 'A front door is accounted for when a traced scenario reaches it, or a use case names it. A door nothing claims is reported, including one that starts itself with no caller. Writing a door off silences the report, and the map still counts it as a gap.' is enforced at tools/coyodex/validate_model.py:1313 — lists every outside-facing door that neither a traced scenario nor a use case reaches",
   "anchor": "tools/coyodex/validate_model.py:1313",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A front door is accounted for when a traced scenario reaches it, or a use case names it. A door nothing claims is reported, including one that starts itself with no caller. Writing a door off silences the report, and the map still counts it as a gap.' is enforced at tools/coyodex/validate_model.py:1335 — keeps a self-starting job in the same test, so having no caller is not an excuse",
   "anchor": "tools/coyodex/validate_model.py:1335",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A front door is accounted for when a traced scenario reaches it, or a use case names it. A door nothing claims is reported, including one that starts itself with no caller. Writing a door off silences the report, and the map still counts it as a gap.' is enforced at tools/coyodex/validate_model.py:1365 — drops a door the map has written off, so the report stays quiet about it",
   "anchor": "tools/coyodex/validate_model.py:1365",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A front door is accounted for when a traced scenario reaches it, or a use case names it. A door nothing claims is reported, including one that starts itself with no caller. Writing a door off silences the report, and the map still counts it as a gap.' is enforced at tools/coyodex/validate_model.py:1530 — counts the written-off doors out loud, so a silenced gap is still visible",
   "anchor": "tools/coyodex/validate_model.py:1530",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Every party that drives a feature states in its own words what it comes there to do. A blank line does not count as an answer. A line written for a party that drives nothing is reported too.' is enforced at tools/coyodex/validate_model.py:2971 — reports a driving party that has no line of its own",
   "anchor": "tools/coyodex/validate_model.py:2971",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Every party that drives a feature states in its own words what it comes there to do. A blank line does not count as an answer. A line written for a party that drives nothing is reported too.' is enforced at tools/coyodex/validate_model.py:2944 — blocks an empty line, which would count the party as covered while the arrow still falls back",
   "anchor": "tools/coyodex/validate_model.py:2944",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Every party that drives a feature states in its own words what it comes there to do. A blank line does not count as an answer. A line written for a party that drives nothing is reported too.' is enforced at tools/coyodex/validate_model.py:2981 — reports a line written for a party that drives none of this feature's work",
   "anchor": "tools/coyodex/validate_model.py:2981",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Every party that drives a feature states in its own words what it comes there to do. A blank line does not count as an answer. A line written for a party that drives nothing is reported too.' is enforced at tools/coyodex/validate_model.py:2930 — blocks these lines on a code area or a data area, because only a feature has parties coming to it",
   "anchor": "tools/coyodex/validate_model.py:2930",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'When one person wears several roles, the map records the link between those roles. A role that turns into another names the action where the change happens.' is enforced at tools/coyodex/validate_model.py:3017 — blocks a role change that names no action where the change happens",
   "anchor": "tools/coyodex/validate_model.py:3017",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'When one person wears several roles, the map records the link between those roles. A role that turns into another names the action where the change happens.' is enforced at tools/coyodex/validate_model.py:3014 — allows only two kinds of link, so a misspelled one cannot pass unseen",
   "anchor": "tools/coyodex/validate_model.py:3014",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'The map draws a kind of data only when the code defines it as a named type.' is enforced at tools/coyodex/validate_model.py:3754 — rejects a data card that points at no definition in the code",
   "anchor": "tools/coyodex/validate_model.py:3754",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'The map draws a kind of data only when the code defines it as a named type.' is enforced at tools/coyodex/validate_model.py:3832 — requires that pointer to be a real file location, so a reader can open the type",
   "anchor": "tools/coyodex/validate_model.py:3832",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'The map credits a part of the project with owning data only when that part's own code writes it.' is enforced at tools/coyodex/validate_model.py:4533 — counts only save and write links when deciding which part owns a kind of data",
   "anchor": "tools/coyodex/validate_model.py:4533",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'The map credits a part of the project with owning data only when that part's own code writes it.' is enforced at tools/coyodex/assemble.py:89 — turns an unclear touch into a read, so a filled-in link never grants ownership",
   "anchor": "tools/coyodex/assemble.py:89",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'The map credits a part of the project with owning data only when that part's own code writes it.' is enforced at tools/coyodex/assemble.py:128 — raises a filled-in link to ownership only when some step shows a real write",
   "anchor": "tools/coyodex/assemble.py:128",
   "detail": "In: Fragment merge (C2)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Data that lives inside another record, in the source, or only during a call needs no owner.' is enforced at tools/coyodex/grammar.py:295 — names the five ways of being stored that mean nothing in the project writes the data",
   "anchor": "tools/coyodex/grammar.py:295",
   "detail": "In: Map document (C1)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Data that lives inside another record, in the source, or only during a call needs no owner.' is enforced at tools/coyodex/validate_model.py:4551 — excuses data stored in one of those ways from the missing-owner report",
   "anchor": "tools/coyodex/validate_model.py:4551",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A save into a store must be explained by a named type that records the same store.' is enforced at tools/coyodex/validate_model.py:2747 — accepts the save when the saving part writes a named type kept in that store",
   "anchor": "tools/coyodex/validate_model.py:2747",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A save into a store must be explained by a named type that records the same store.' is enforced at tools/coyodex/validate_model.py:2748 — also accepts it when the part saves on behalf of the type's owner, one step away",
   "anchor": "tools/coyodex/validate_model.py:2748",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Where data lives must be recorded exactly enough for a reader to find it in the running system.' is enforced at tools/coyodex/validate_model.py:2795 — flags a compartment named with no store linked to it",
   "anchor": "tools/coyodex/validate_model.py:2795",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Where data lives must be recorded exactly enough for a reader to find it in the running system.' is enforced at tools/coyodex/validate_model.py:2817 — flags a compartment name that reads as a description, spotted by the space inside it",
   "anchor": "tools/coyodex/validate_model.py:2817",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A relation between two kinds of data is recorded on one side only, never twice.' is enforced at tools/coyodex/validate_model.py:3780 — rejects a pair where both kinds of data declare the same link",
   "anchor": "tools/coyodex/validate_model.py:3780",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A relation between two kinds of data is recorded on one side only, never twice.' is enforced at tools/coyodex/validate_model.py:3680 — rejects the same link written twice on one card",
   "anchor": "tools/coyodex/validate_model.py:3680",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A lifecycle whose states no line declares is kept as a guess rather than thrown away.' is enforced at tools/coyodex/validate_model.py:2658 — marks a lifecycle citing no declaring line as guessed instead of rejecting it",
   "anchor": "tools/coyodex/validate_model.py:2658",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A lifecycle whose states no line declares is kept as a guess rather than thrown away.' is enforced at tools/coyodex/model.py:322 — makes the declaring line optional, with nothing written meaning guessed",
   "anchor": "tools/coyodex/model.py:322",
   "detail": "In: Map document (C1)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:2548 — Blocks a record whose held-up, disproved and unsettled tallies do not sum to the number of statements checked.",
   "anchor": "tools/coyodex/validate_model.py:2548",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:2555 — Blocks a record claiming more statements were checked than the list ever held.",
   "anchor": "tools/coyodex/validate_model.py:2555",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:2530 — Blocks negative tallies, which can make a wrong record add up.",
   "anchor": "tools/coyodex/validate_model.py:2530",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:4427 — Sends those arithmetic failures to the blocking list, so a map cannot ship carrying them.",
   "anchor": "tools/coyodex/validate_model.py:4427",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:4423 — Sends every judgement about how much was checked to the warning list instead, so effort never blocks a map.",
   "anchor": "tools/coyodex/validate_model.py:4423",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:2413 — Warns when a map carries no testing record at all, so silence cannot read as a clean pass.",
   "anchor": "tools/coyodex/validate_model.py:2413",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A map is blocked when the numbers in its testing record contradict each other. Having tested very little only raises a warning.' is enforced at tools/coyodex/validate_model.py:2435 — Warns when under three statements in five held up, and asks which statements were checked first.",
   "anchor": "tools/coyodex/validate_model.py:2435",
   "detail": "In: Map validator (C13)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A check on one statement comes back held up, disproved, or impossible to settle from the code. A fourth answer is refused, never rounded into one of the three.' is enforced at tools/coyodex/grounding.py:157 — Refuses to write the record when any check carries a word outside the three answers.",
   "anchor": "tools/coyodex/grounding.py:157",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A check on one statement comes back held up, disproved, or impossible to settle from the code. A fourth answer is refused, never rounded into one of the three.' is enforced at tools/coyodex/grounding.py:767 — Runs the same refusal the moment a checker returns, where the fix is still cheap.",
   "anchor": "tools/coyodex/grounding.py:767",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A check on one statement comes back held up, disproved, or impossible to settle from the code. A fourth answer is refused, never rounded into one of the three.' is enforced at tools/coyodex/grounding.py:103 — Reaches the third answer only when a checker actually said the code cannot settle the statement.",
   "anchor": "tools/coyodex/grounding.py:103",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A statement counts as held up only when more than half of its checks agree. An even split is filed as unsettled and credited to neither side.' is enforced at tools/coyodex/grounding.py:99 — Requires a strict majority of the checks before a statement counts as held up.",
   "anchor": "tools/coyodex/grounding.py:99",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A statement counts as held up only when more than half of its checks agree. An even split is filed as unsettled and credited to neither side.' is enforced at tools/coyodex/grounding.py:107 — Files an even split as unsettled rather than giving it to the larger-looking side.",
   "anchor": "tools/coyodex/grounding.py:107",
   "detail": "In: Grounding record (C23)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  }
 ]
}
