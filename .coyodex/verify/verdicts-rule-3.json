{
 "grounding": [
  {
   "claim": "Rule 'A group holding exactly one thing is reported, because that level costs a click and adds no meaning.' is enforced at tools/coyodex/balance_lib.py:303 — reports a group whose single member is one box rather than another group",
   "grounded": true,
   "evidence": "tools/coyodex/balance_lib.py:303",
   "skeptic": "rule-3",
   "note": "Read _forest_warnings: line 303 is `if n == 1 and not kids[0].startswith((\"S\", \"SD\")):`, so a group with exactly one child appends the 'wraps a single {leaf}' warning, and the startswith test restricts it to a child that is NOT a subsystem/subdomain id, i.e. a leaf box."
  },
  {
   "claim": "Rule 'A group holding exactly one thing is reported, because that level costs a click and adds no meaning.' is enforced at tools/coyodex/validate_model.py:6317 — reports a group whose single member is another group of the same kind",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:6317",
   "skeptic": "rule-3",
   "note": "Read the 'Redundant nesting' block at 6311-6324: line 6317 opens `redundant = sorted(p for p, n in child_count.items() if n == 1 and ((_is_subsystem_id(p) and _is_subsystem_id(only_child[p])) or (p.startswith('SD') and only_child[p].startswith('SD'))))`, and 6322-6324 appends the warning. Exactly one child, and both parent and child are groups of the same kind."
  },
  {
   "claim": "Rule 'A crowded or thin screen is only ever advice. It never refuses a map or stops a build.' is enforced at method.md:1673 — states that screen density never refuses a map, because grouping is a free choice about the picture",
   "grounded": true,
   "evidence": "method.md:1673",
   "skeptic": "rule-3",
   "note": "Read method.md 1665-1676. Line 1673 reads 'safe: **balance never gates and only ever re-groups** — grouping is a free, view-only choice'. It states both halves of the claim: no gate, and grouping is a free choice about the picture."
  },
  {
   "claim": "Rule 'A crowded or thin screen is only ever advice. It never refuses a map or stops a build.' is enforced at tools/coyodex/validate_model.py:6328 — files every screen-density finding as advice rather than as a fault that refuses the map",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:6328",
   "skeptic": "rule-3",
   "note": "Line 6328 is `warnings.extend(balance_lib.balance_warnings(m))` — the density findings go into `warnings`, never `problems`. Checked the caller at 6680-6693: only a non-empty `problems` prints VALIDATION FAILED and returns 1, so a balance finding cannot refuse the map."
  },
  {
   "claim": "Rule 'A crowded or thin screen is only ever advice. It never refuses a map or stops a build.' is enforced at tools/coyodex/finalize.py:587 — records the density findings in the closing report without moving its verdict",
   "grounded": true,
   "evidence": "tools/coyodex/finalize.py:587",
   "skeptic": "rule-3",
   "note": "Line 587 returns `Leg(\"balance (informational)\", RAN, note=...)` with blocking and advisory left at their empty defaults. Checked the verdict computation at 685-697: it sums len(l.blocking) and len(l.advisory), so this leg contributes 0 to both and the verdict is unmoved; format_report (754-768) still prints the leg's note. Nuance: the note records the COUNT of density findings, not each finding's text."
  },
  {
   "claim": "Rule 'Regrouping may only move boxes between groups. It may never merge or split the boxes themselves.' is enforced at method.md:1675 — forbids any density finding from merging or splitting boxes to reach a number",
   "grounded": true,
   "evidence": "method.md:1675",
   "skeptic": "rule-3",
   "note": "Read method.md 1672-1676. Line 1675 ends the sentence with 'no balance finding may merge or split components to hit a number', and 1674 says the leaf decision is 'out of bounds for balance tooling'. That is the forbidding half of the rule."
  },
  {
   "claim": "Rule 'Regrouping may only move boxes between groups. It may never merge or split the boxes themselves.' is enforced at tools/coyodex/balance.py:107 — writes a split suggestion as a change of which group a box sits in, and nothing else",
   "grounded": true,
   "evidence": "tools/coyodex/balance.py:107",
   "skeptic": "rule-3",
   "note": "Read the proposal printer at 99-110. Line 107 is `key = \"parent\" if mem_id.startswith(\"S\") else \"subsystem\"` — the only two membership fields — and line 108 emits `{mem_id} → \"{key}\": \"{gid}\"`. The suggestion is a new group row plus a membership field on each existing member; nothing merges or splits a component."
  },
  {
   "claim": "Rule 'Regrouping may only move boxes between groups. It may never merge or split the boxes themselves.' is enforced at tools/coyodex/balance.py:109 — tells the reader applying a suggestion to add no member the suggestion did not list",
   "grounded": true,
   "evidence": "tools/coyodex/balance.py:109",
   "skeptic": "rule-3",
   "note": "Line 109 is `out.append(\"      Do not invent members not listed here. Apply via a Direct map change, then re-run validate → audit → render.\")` — the printed instruction telling whoever applies a suggestion to add no member the suggestion did not list."
  },
  {
   "claim": "Rule 'A group is never one end of an arrow, so moving a box into another group moves no arrow.' is enforced at method.md:3134 — says dissolving a level moves no arrow, because a group is never an end of one",
   "grounded": true,
   "evidence": "method.md:3134",
   "skeptic": "rule-3",
   "note": "Read the 'Drilling deeper' block at 3128-3140. Line 3134 reads 'row. Pure regrouping — no edge moves, since a subsystem is never an edge endpoint.' — the Flatten bullet, saying dissolving a level moves no arrow and giving the group-is-never-an-endpoint reason."
  },
  {
   "claim": "Rule 'A group is never one end of an arrow, so moving a box into another group moves no arrow.' is enforced at tools/coyodex/validate_model.py:5283 — refuses a map holding any arrow that starts or ends at a group",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:5283",
   "skeptic": "rule-3",
   "note": "Read _check_edges 5282-5287: line 5283 is `if _is_container_id(eid):` over both ends (`for side, eid in ((\"src\", e.src), (\"dst\", e.dst))`), appending to `problems`. _is_container_id (182-183) matches a subsystem or subdomain id, and a problem returns exit 1 at 6684, so such a map is refused."
  },
  {
   "claim": "Rule 'Grouping is worth doing only once a map passes about fifteen boxes. A smaller map stays flat.' is enforced at method.md:414 — makes groups optional, and recommends them only above about fifteen boxes",
   "grounded": true,
   "evidence": "method.md:414",
   "skeptic": "rule-3",
   "note": "Read method.md 411-422. Line 414 reads '- **Subsystems (S)** *(optional; recommended above ~15 components)*' — both halves: optional, and recommended only above about fifteen."
  },
  {
   "claim": "Rule 'Grouping is worth doing only once a map passes about fifteen boxes. A smaller map stays flat.' is enforced at tools/coyodex/balance_lib.py:280 — asks for groups only once an ungrouped map passes that count",
   "grounded": true,
   "evidence": "tools/coyodex/balance_lib.py:280",
   "skeptic": "rule-3",
   "note": "Line 280 is `if n_leaves > SUBSYSTEMS_RECOMMENDED_ABOVE and \"root\" not in skip:` inside the `if not groups_exist:` branch at 279, so the 'no groups' nudge fires only on an ungrouped map above the count. SUBSYSTEMS_RECOMMENDED_ABOVE is 15 at balance_lib.py:41."
  },
  {
   "claim": "Rule 'A map covers the files a project's version control accounts for, including new files it does not already skip.' is enforced at tools/coyodex/preindex_lib.py:198 — Asks version control for the files it already tracks.",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:198",
   "skeptic": "rule-3",
   "note": "Read _git_rels 188-209. Line 198 is `tracked = ls()`, and `ls` at 190-191 runs `git -C <root> ls-files -z` — the files version control already tracks."
  },
  {
   "claim": "Rule 'A map covers the files a project's version control accounts for, including new files it does not already skip.' is enforced at tools/coyodex/preindex_lib.py:201 — Adds files the author created but never registered, minus what version control skips.",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:201",
   "skeptic": "rule-3",
   "note": "Line 201 is `untracked = ls(\"--others\", \"--exclude-standard\") or []` — `--others` is the files git does not track, `--exclude-standard` subtracts everything git is told to ignore. 204-208 merges tracked and untracked into one list."
  },
  {
   "claim": "Rule 'A map covers the files a project's version control accounts for, including new files it does not already skip.' is enforced at tools/coyodex/preindex_lib.py:222 — The file list for a project starts from version control, not from the folder tree.",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:222",
   "skeptic": "rule-3",
   "note": "Read iter_source_files 212-250. Line 222 is `rels = _git_rels(root)`, and only when that returns None (no git) does line 225 fall back to `_walk_rels(root)`. So the file list starts from version control, and the folder walk is the fallback."
  },
  {
   "claim": "Rule 'Packaged output, copied-in libraries and lock files never count as a project's own code.' is enforced at tools/coyodex/preindex_lib.py:148 — Drops every file sitting under a known build, package or vendor folder.",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:148",
   "skeptic": "rule-3",
   "note": "Line 148 is `if any(part in DEFAULT_EXCLUDE_DIRS for part in rel.parts): return True` — any path component, so the whole subtree goes. DEFAULT_EXCLUDE_DIRS (104-109) holds node_modules, bower_components, vendor, third_party, dist, build, out, target, .next, .nuxt, .svelte-kit."
  },
  {
   "claim": "Rule 'Packaged output, copied-in libraries and lock files never count as a project's own code.' is enforced at tools/coyodex/preindex_lib.py:150 — Drops the lock files a package manager writes for itself.",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:150",
   "skeptic": "rule-3",
   "note": "Line 150 is `if rel.name in DEFAULT_EXCLUDE_NAMES: return True`. DEFAULT_EXCLUDE_NAMES (116-119) is package-lock.json, yarn.lock, pnpm-lock.yaml, poetry.lock, Pipfile.lock, Cargo.lock, composer.lock, go.sum (plus .DS_Store) — package-manager lock files."
  },
  {
   "claim": "Rule 'Packaged output, copied-in libraries and lock files never count as a project's own code.' is enforced at tools/coyodex/preindex_lib.py:257 — Keeps the same folders out when the project has no version control at all.",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:257",
   "skeptic": "rule-3",
   "note": "Line 257 is `dirnames[:] = [d for d in dirnames if d not in DEFAULT_EXCLUDE_DIRS]` inside _walk_rels, which iter_source_files reaches at line 225 only when _git_rels returned None (no git repo). Same DEFAULT_EXCLUDE_DIRS set the git path uses via _excluded at 148."
  },
  {
   "claim": "Rule 'A project can list committed code that its map must leave out.' is enforced at tools/coyodex/ignorefile.py:88 — Keeps the last pattern that matched, so a later line can put a file back.",
   "grounded": true,
   "evidence": "tools/coyodex/ignorefile.py:88",
   "skeptic": "rule-3",
   "note": "Read IgnoreSpec.match_index 78-89. The loop at 86-88 sets `hit = i` on every match with no break, so the LAST matching rule is what is returned. Together with the polarity read at 95, a later `!` line can put a file back that an earlier line removed."
  },
  {
   "claim": "Rule 'A project can list committed code that its map must leave out.' is enforced at tools/coyodex/ignorefile.py:95 — Answers whether a file is left out, from the pattern that decided it.",
   "grounded": true,
   "evidence": "tools/coyodex/ignorefile.py:95",
   "skeptic": "rule-3",
   "note": "Read IgnoreSpec.match 91-95. Line 95 is `return i is not None and not self.rules[i][0]` — `i` comes from match_index (the deciding rule) and `rules[i][0]` is that rule's `negated` flag, so the answer is read off the pattern that decided, not off any other match."
  },
  {
   "claim": "Rule 'A project can list committed code that its map must leave out.' is enforced at tools/coyodex/preindex_lib.py:245 — Removes the file from the set a build will read.",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:245",
   "skeptic": "rule-3",
   "note": "Read the walk loop 235-248. Line 245 is `if not ignore.rules[idx][0]:   # a positive rule decided -> the file leaves the tree`, and 246-247 do `ignored += 1; continue`, skipping the `files.append(root / rel)` at 248. So the file never enters the set returned in WalkResult."
  },
  {
   "claim": "Rule 'Every run reports what a project's own skip list removed, one pattern at a time.' is enforced at tools/coyodex/validate_model.py:6288 — Says the tree was narrowed on every check, not only the thorough one.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:6288",
   "skeptic": "rule-3",
   "note": "Line 6288 `warnings.extend(ignore_disclosure(...))` sits inside `if walk_root is not None:` (6284) at function-body indentation, ABOVE and outside the `if check_coverage:` block at 6292. Confirmed nothing else gates it: the only other guards in validate_model above are 6163 and `if check_sources:` at 6256, and the CLI at 6641 always passes model_path, so walk_root exists on a plain `validate` run too."
  },
  {
   "claim": "Rule 'Every run reports what a project's own skip list removed, one pattern at a time.' is enforced at tools/coyodex/scope.py:133 — Prints one line per pattern in the briefing a person reads before a build.",
   "grounded": true,
   "evidence": "tools/coyodex/scope.py:133",
   "skeptic": "rule-3",
   "note": "Line 133 is `out += [f\"    {line}\" for line in rep.per_rule]`, one output line per rule, under the `{rep.removed} file(s) removed by .coyodex/.ignore:` header at 132. scope.py's docstring (1-19) says this is the up-front briefing a human reads before the build."
  },
  {
   "claim": "Rule 'Every run reports what a project's own skip list removed, one pattern at a time.' is enforced at tools/coyodex/ignorefile.py:129 — Builds the line saying how many files each pattern removed.",
   "grounded": true,
   "evidence": "tools/coyodex/ignorefile.py:129",
   "skeptic": "rule-3",
   "note": "Read ignore_report 114-136. Line 129 opens `per_rule = tuple(` and 130 formats `f\"{'!' if neg else ''}{pat} ({'put back' if neg else 'removed'} {c} file(s))\"` per rule — the per-pattern line carrying its own file count."
  },
  {
   "claim": "Rule 'Every run reports what a project's own skip list removed, one pattern at a time.' is enforced at tools/coyodex/validate_analysis.py:336 — Names the patterns that removed nothing, which read as coverage the author never got.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_analysis.py:336",
   "skeptic": "rule-3",
   "note": "Read ignore_disclosure 320-343. Line 336 is `if rep.unused:` and 337-340 appends the message naming them, ending '… either way it reads as coverage the author never got.' — the claim's wording is that sentence."
  },
  {
   "claim": "Rule 'One box covers about one folder of code, at most ten files or three thousand lines.' is enforced at tools/coyodex/preindex_lib.py:599 — A folder inside both limits becomes one box and is not opened further.",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:599",
   "skeptic": "rule-3",
   "note": "Read `build` in expected_components 595-625. Line 599 is `if tf <= file_cap and tl <= loc_cap:` and 600 returns `DirExpectation(path, tf, tl, 1, [])` — expected 1, empty children, so the recursion at 616 is never reached for that folder. Caps default to GRANULARITY_FILE_CAP=10 and GRANULARITY_LOC_CAP=3000 (521-522, 564-565)."
  },
  {
   "claim": "Rule 'One box covers about one folder of code, at most ten files or three thousand lines.' is enforced at tools/coyodex/preindex_lib.py:615 — One oversized folder holding no subfolders splits into several boxes.",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:615",
   "skeptic": "rule-3",
   "note": "Line 614 is `if not substantial:` (no subdir big enough to be its own unit) and line 615 returns `DirExpectation(path, tf, tl, _ceil_units(tf, tl, file_cap, loc_cap), [])`. _ceil_units (558-561) is `max(1, ceil(files/file_cap), ceil(loc/loc_cap))`, so an oversized flat folder yields more than one expected box."
  },
  {
   "claim": "Rule 'One box covers about one folder of code, at most ten files or three thousand lines.' is enforced at method.md:1591 — States the size one box should cover, in the instructions a build follows.",
   "grounded": true,
   "evidence": "method.md:1591",
   "skeptic": "rule-3",
   "note": "Read method.md 1590-1596. The rule paragraph 'Component granularity — the leaf rule' continues on 1591 as 'module-/folder-/deployable-sized unit — roughly a directory of **≤ ~10 source files / ≤ ~3 kLOC** with one purpose.' That is the size, stated in the instructions a build follows."
  },
  {
   "claim": "Rule 'Findings about how coarsely a map is drawn are advice, because deliberate abstraction is allowed.' is enforced at tools/coyodex/validate_model.py:6299 — Files a folded-away module as advice rather than as a failure.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:6299",
   "skeptic": "rule-3",
   "note": "Line 6299 is `warnings.extend(compression_coverage_from_refs(refs, walk_root, cov_dirs))`. Read compression_coverage_from_refs (validate_analysis.py 150-188): its findings are the 'Compression: <dir>/ holds N sibling source subdirs … folded into ~one box' messages, and they go into `warnings`, never `problems`, so they never fail the run (6680)."
  },
  {
   "claim": "Rule 'Findings about how coarsely a map is drawn are advice, because deliberate abstraction is allowed.' is enforced at tools/coyodex/validate_model.py:6308 — Files the box-count nudge as advice rather than as a failure.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:6308",
   "skeptic": "rule-3",
   "note": "Line 6308 is `warnings.extend(granularity_advisory(len(m.components), walk_root))` — the component-count-vs-E nudge, appended to `warnings`. Only `problems` prints VALIDATION FAILED and returns 1 (6680-6684), so the nudge is advice."
  },
  {
   "claim": "Rule 'Findings about how coarsely a map is drawn are advice, because deliberate abstraction is allowed.' is enforced at tools/coyodex/validate_model.py:6680 — The run fails on real errors only, so advice never stops it.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:6680",
   "skeptic": "rule-3",
   "note": "Read main's tail 6658-6693. Line 6680 is `if problems:` — the only branch that prints 'VALIDATION FAILED' and returns 1. Warnings are printed at 6664-6667 under the header 'VALIDATION WARNINGS (non-blocking):' and change no return value; the JSON path at 6657 is the same rule (`return 1 if problems else 0`)."
  },
  {
   "claim": "Rule 'A map can record a deliberate coarse decision, and the advice about that decision then stops.' is enforced at tools/coyodex/validate_model.py:6293 — Reads the folders a map records as deliberately drawn coarsely.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:6293",
   "skeptic": "rule-3",
   "note": "Line 6293 is `cov_dirs = frozenset(_recorded_coverage_dirs(m))  # 'Coverage exceptions': conscious coarse-fold` — it reads the dirs the MAP itself records as a deliberate coarse fold, and passes them into the coverage checks at 6299, 6302 and 6309."
  },
  {
   "claim": "Rule 'A map can record a deliberate coarse decision, and the advice about that decision then stops.' is enforced at tools/coyodex/validate_analysis.py:171 — Skips a folder that sits at or under one the map recorded.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_analysis.py:171",
   "skeptic": "rule-3",
   "note": "Read compression_coverage_from_refs 165-182. Line 171 is `return any(dpath == d or dpath.startswith(d + \"/\") for d in skip_dirs)` inside `recorded()`, and line 178 uses it as `or recorded(dpath)` to `continue` — so a folder AT or UNDER a recorded dir is skipped and raises no finding."
  },
  {
   "claim": "Rule 'A map can record a deliberate coarse decision, and the advice about that decision then stops.' is enforced at tools/coyodex/validate_model.py:6307 — Skips the box-count nudge when the map records that altitude decision.",
   "grounded": true,
   "evidence": "tools/coyodex/validate_model.py:6307",
   "skeptic": "rule-3",
   "note": "Line 6307 is `if \"granularity\" not in balance_lib._exceptions(m):`, guarding the granularity_advisory call on 6308. The comment on 6305-6306 states the intent: the literal `granularity` under 'Balance exceptions' records the operator's altitude decision and silences the nudge."
  },
  {
   "claim": "Rule 'The map a build replaces is never an input to the new one.' is enforced at method/dispatch.md:135 — Tells a build not to open the map it is replacing.",
   "grounded": true,
   "evidence": "method/dispatch.md:135",
   "skeptic": "rule-3",
   "note": "Read method/dispatch.md 135-142. Line 135 is '**Do not open a previous map while building.**', and 136-139 extends it to the one git still has and to `.coyodex/dev-rebuilds/`, with the reason that a build reading the map it replaces is no longer independent of it."
  },
  {
   "claim": "Rule 'The map a build replaces is never an input to the new one.' is enforced at tools/coyodex/scope.py:116 — Warns before a build when a stray map sits among the files to be read.",
   "grounded": true,
   "evidence": "tools/coyodex/scope.py:116",
   "skeptic": "rule-3",
   "note": "Read scope.py 100-125. Lines 107-115 collect the folders holding a project-map.json/.md inside the analysis set and drop the legitimate `.coyodex` home; line 116 `if strays:` emits the WARNING at 117-125 saying those folders 'will be read as SOURCE'. scope is the pre-build briefing (docstring, 1-19)."
  },
  {
   "claim": "Rule 'The map a build replaces is never an input to the new one.' is enforced at tools/coyodex/preindex_lib.py:108 — Keeps the folder holding a map out of the files a build reads.",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:108",
   "skeptic": "rule-3",
   "note": "Line 108 is the DEFAULT_EXCLUDE_DIRS entry line `\".gradle\", \".idea\", \".vscode\", \"coverage\", \".coyodex\", \".coyodex-eval\",` — `.coyodex` is the folder the map lives in, and _excluded (148) drops any path with that component, in both the git and the os.walk modes (257)."
  }
 ]
}
