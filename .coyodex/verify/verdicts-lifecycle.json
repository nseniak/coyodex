{
 "grounding": [
  {
   "claim": "E45 (Leg) has states [ran, failed]",
   "grounded": true,
   "evidence": "tools/coyodex/finalize.py:65",
   "skeptic": "lifecycle",
   "note": "Read tools/coyodex/finalize.py:65-66, where the two module constants RAN = \"ran\" and FAILED = \"failed\" are declared, and the Leg dataclass at line 70 stores one of them in `status` (its `ran` property at line 82 tests `self.status == RAN`). Grepped every Leg(...) construction in the file: each passes either RAN or FAILED and no third status value exists, so the declared set is exactly [ran, failed] and no transitions are claimed."
  },
  {
   "claim": "E46 (FinalizeReport) has states [BLOCKED, INCOMPLETE, ADVISORIES, CLEAN]",
   "grounded": true,
   "evidence": "tools/coyodex/finalize.py:487",
   "skeptic": "lifecycle",
   "note": "Read build_report in tools/coyodex/finalize.py:482-498: the if/elif chain at lines 487-494 assigns verdict = \"BLOCKED\" / \"INCOMPLETE\" / \"ADVISORIES\" / \"CLEAN\" and there is no other branch, so this dispatch block declares the whole set (the field comment at line 90 repeats it, but the dispatch is the declaration). The anchor line 488 is inside that block; all four names appear literally, none is invented."
  },
  {
   "claim": "E55 (DeltaReport) has states [PASS, DRIFT, REGRESSED]",
   "grounded": true,
   "evidence": "eval/tools/coyodex_eval/compare.py:38",
   "skeptic": "lifecycle",
   "note": "Read eval/tools/coyodex_eval/compare.py:38-41: PASS = \"PASS\", DRIFT = \"DRIFT\", REGRESSED = \"REGRESSED\" are declared as constants and immediately enumerated again in _EXIT = {PASS: 0, REGRESSED: 1, DRIFT: 2}. The verdict dispatch at lines 503-509 assigns exactly those three to DeltaReport.verdict, and _EXIT[report.verdict] at line 670 would KeyError on any fourth value, so the set is exactly these three."
  }
 ]
}
