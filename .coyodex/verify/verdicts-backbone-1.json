{
 "grounding": [
  {
   "claim": "C3 reads C1",
   "grounded": true,
   "evidence": "tools/coyodex/json_schema.py:505",
   "skeptic": "backbone-1",
   "note": "Opened json_schema.py. Line 505 is `_ensure_def(ProjectModel, defs)` inside generate_schema; ProjectModel is imported from coyodex.model at line 26 (C1's only file) and the whole schema is reflected off that dataclass tree. The schema document reads the map model."
  },
  {
   "claim": "C3 reads C2",
   "grounded": true,
   "evidence": "tools/coyodex/json_schema.py:206",
   "skeptic": "backbone-1",
   "note": "Opened json_schema.py:206 — `(\"Dep\", \"kind\"): {\"enum\": [*grammar.DEP_KINDS, None], ...}`, reading a constant out of `coyodex.grammar` (imported line 26). grammar.py is C2's anchor file, so the schema document reads the shared grammar."
  },
  {
   "claim": "C20 calls C24",
   "grounded": true,
   "evidence": "tools/coyodex/cli.py:177",
   "skeptic": "backbone-1",
   "note": "Opened cli.py `_dispatch`. Line 175-177: `if cmd == \"dump\": from coyodex import dump; return dump.main(rest)`. dump.py is C24's anchor file, so the command line calls the map reader directly."
  },
  {
   "claim": "C24 calls C4",
   "grounded": true,
   "evidence": "tools/coyodex/dump.py:298",
   "skeptic": "backbone-1",
   "note": "Opened dump.py:293-301. Line 297 imports `load_map_or_fragment` from coyodex.assemble and line 298 calls it: `m, _present = load_map_or_fragment(path)`. assemble.py is C4's only file."
  },
  {
   "claim": "C24 reads C1",
   "grounded": true,
   "evidence": "tools/coyodex/context.py:196",
   "skeptic": "backbone-1",
   "note": "Opened context.py:196 — `model = load_model(resolve_map_path(ns.map).read_text(...))`; both names are imported from coyodex.model at line 36. context.py is one of C24's files, so the map reader reads the map model."
  },
  {
   "claim": "C46 calls C2",
   "grounded": true,
   "evidence": "tools/coyodex/ignorefile.py:87",
   "skeptic": "backbone-1",
   "note": "Opened ignorefile.py:87 — `if matches(pattern, rel):` inside IgnoreSpec.match_index; `matches` is imported at line 54 from coyodex.pathmatch. I dumped C2's record: pathmatch.py is one of its files, so this call does land in the shared grammar."
  },
  {
   "claim": "C45 calls C46",
   "grounded": true,
   "evidence": "tools/coyodex/preindex_lib.py:227",
   "skeptic": "backbone-1",
   "note": "Opened preindex_lib.py:227 — `ignore = load_ignore(root)` inside the source-file walk; imported at line 27 from coyodex.ignorefile (C46's only file). preindex_lib.py is one of C45's files."
  },
  {
   "claim": "C26 calls C46",
   "grounded": true,
   "evidence": "tools/coyodex/scope.py:131",
   "skeptic": "backbone-1",
   "note": "Opened scope.py:131 — `rep = ignore_report(walk.ignore, walk.ignore_hits)`; `ignore_report` is imported at line 28 from coyodex.ignorefile. scope.py is C26's anchor file."
  },
  {
   "claim": "C84 calls C46",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/filetree.py:260",
   "skeptic": "backbone-1",
   "note": "Opened filetree.py:260 — `rep = ignore_report(walk.ignore, walk.ignore_hits)` inside build_file_tree; imported at line 39 from coyodex.ignorefile. filetree.py is C84's only file."
  },
  {
   "claim": "C6 calls C46",
   "grounded": true,
   "evidence": "tools/coyodex/validate_analysis.py:321",
   "skeptic": "backbone-1",
   "note": "Opened validate_analysis.py:321 — `spec0 = load_ignore(root)`; imported at line 28 from coyodex.ignorefile (line 328 also calls ignore_report). validate_analysis.py is one of C6's files."
  },
  {
   "claim": "C48 reads C1",
   "grounded": true,
   "evidence": "tools/coyodex/balance_lib.py:156",
   "skeptic": "backbone-1",
   "note": "Opened balance_lib.py:148-165. Line 156 is `for grp in group_forests(m):`, walking the ProjectModel `m`; both `group_forests` and `ProjectModel` are imported from coyodex.model at line 32. balance_lib.py is one of C48's files."
  },
  {
   "claim": "C48 calls C2",
   "grounded": true,
   "evidence": "tools/coyodex/balance_lib.py:238",
   "skeptic": "backbone-1",
   "note": "Opened balance_lib.py:238 — `for body in extras_bodies(m, _EXCEPTIONS_HEADING):`. `extras_bodies` is not local: line 206 binds it as `extras_bodies = records.extras_bodies`, defined at records.py:214, and records.py is one of C2's files (checked via the C2 record)."
  },
  {
   "claim": "C20 calls C48",
   "grounded": true,
   "evidence": "tools/coyodex/cli.py:183",
   "skeptic": "backbone-1",
   "note": "Opened cli.py `_dispatch`. Lines 181-183: `if cmd == \"balance\": from coyodex import balance; return balance.main(rest)`. balance.py is C48's anchor file."
  },
  {
   "claim": "C25 calls C48",
   "grounded": true,
   "evidence": "tools/coyodex/finalize.py:125",
   "skeptic": "backbone-1",
   "note": "Opened finalize.py:123-125 — the leg runner's `elif name == \"balance\": from coyodex import balance; code = balance.main(argv)`, reached from `_balance_leg` (line 581 passes leg name \"balance\"). finalize.py is one of C25's files."
  },
  {
   "claim": "C101 calls C48",
   "grounded": true,
   "evidence": "eval/tools/coyodex_eval/profile.py:383",
   "skeptic": "backbone-1",
   "note": "Opened profile.py:383 — `root_fanout, max_fanout, in_band_pct, depth = balance_lib.fanout_summary(m)`; balance_lib is imported at line 28. balance_lib.py is one of C48's files, profile.py is C101's anchor file."
  },
  {
   "claim": "C71 calls C62",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:15120",
   "skeptic": "backbone-1",
   "note": "Opened viewer.js:15112-15131 (`sbBuildStatic`, inside C71's region which starts at 15119). Line 15120 wires each search row to `selectFromTree(nid)`, defined at viewer.js:13572 — inside C62's line region (13096 up to C69 at 13894) and doing exactly C62's stated job of selecting/centring the box. Attribution here is by the map's line partition of viewer.js, since C62 lists no files."
  },
  {
   "claim": "C71 calls C83",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:15224",
   "skeptic": "backbone-1",
   "note": "Opened viewer.js:15222-15228 (`sbEnsureSymbols`): line 15224 is `fetch(API_BASE + 'symbols', {cache:'no-store'})`. I then opened serve.py and found the matching handler at line 772-775 (`if rest == [\"symbols\"]: return self._json({\"symbols\": project_symbols(proj), ...})`). serve.py is C83's anchor file."
  },
  {
   "claim": "C72 calls C83",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:15794",
   "skeptic": "backbone-1",
   "note": "Opened viewer.js:15785-15798 (`loadImpact`, inside C72's region from 15650): line 15794 is `await fetch(API_BASE + 'impact?' + qs.toString(), ...)`. serve.py:802 handles `rest == [\"impact\"]`, so the far side is the map server."
  },
  {
   "claim": "C72 calls C62",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.js:15716",
   "skeptic": "backbone-1",
   "note": "Opened viewer.js:15705-15717 — the impact row's open handler ends `if (GRAPH.nodes[id]) selectFromTree(id);` at line 15716. selectFromTree is defined at viewer.js:13572, inside C62's line region (13096-13893)."
  },
  {
   "claim": "C70 loads C73",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/viewer.html:12",
   "skeptic": "backbone-1",
   "note": "Opened viewer.html:1-20. Line 12 is `<link rel=\"stylesheet\" href=\"/static/viewer.css\">`. viewer.html is one of C70's files and viewer.css is C73's only file, so the shell does load the stylesheet."
  },
  {
   "claim": "C83 serves C73",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/serve.py:851",
   "skeptic": "backbone-1",
   "note": "Opened serve.py:846-851 (`_static`): it looks the requested name up in `_STATIC_FILES` and returns `self._send_file(_FRONTEND_DIR / name, ctype)`. I read the whitelist at lines 59-62 — it holds exactly viewer.js and viewer.css, so line 851 is where viewer.css (C73) is sent."
  },
  {
   "claim": "C83 calls C84",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/serve.py:492",
   "skeptic": "backbone-1",
   "note": "Opened serve.py:482-494 (`project_tree`): line 492 calls `build_tree(rels, node_path_index(graph), resolved_path_index(graph), ...)`, all three imported at line 45 from coyodex.viewer.filetree — C84's only file, and build_tree is C84's own anchor (filetree.py:214)."
  },
  {
   "claim": "C84 calls C45",
   "grounded": true,
   "evidence": "tools/coyodex/viewer/filetree.py:253",
   "skeptic": "backbone-1",
   "note": "Opened filetree.py:248-254: line 248 lazily imports `iter_source_files` from coyodex.preindex_lib and line 253 calls it (`walk = iter_source_files(root)`). preindex_lib.py is one of C45's files."
  },
  {
   "claim": "C121 cites C122",
   "grounded": true,
   "evidence": "method.md:489",
   "skeptic": "backbone-1",
   "note": "Opened method.md:486-492. Line 489 reads 'This is the rule T2b already states — \"the pipeline that builds and tests the product is not a product interface\"'. It sits inside C121's region (414, the Level-0 inventory tables T1-T4, up to 505) and names T2b, whose section starts at method.md:506 — C122's anchor."
  },
  {
   "claim": "C125 cites C122",
   "grounded": true,
   "evidence": "method.md:1465",
   "skeptic": "backbone-1",
   "note": "Opened method.md:1462-1470, inside C125's region (starts 1332). Line 1465 reads 'group T2 + T4 into T2b interfaces (the surfaces only — their inputs T2 and T4 both exist by here...)', naming the T2b section at method.md:506 = C122."
  },
  {
   "claim": "C125 cites C123",
   "grounded": true,
   "evidence": "method.md:1467",
   "skeptic": "backbone-1",
   "note": "Opened method.md:1467 — 'trace T6 + edge list, dooring each flow as it is written'. I checked the T-headings: T5 is at method.md:879 (C123's anchor) and T6 at 937, both inside C123's region, which ends where C124 starts at 1068. So the T6 reference points into C123."
  },
  {
   "claim": "C125 cites C132",
   "grounded": true,
   "evidence": "method.md:1333",
   "skeptic": "backbone-1",
   "note": "Opened method.md:1330-1336. Lines 1332-1333: 'The six rules live in ONE file, [method/templates/writing-rules.md](method/templates/writing-rules.md)'. I confirmed method/templates/writing-rules.md exists on disk and is listed in C132's files."
  },
  {
   "claim": "C123 cites C129",
   "grounded": true,
   "evidence": "method.md:884",
   "skeptic": "backbone-1",
   "note": "Opened method.md:881-884, inside the T5 Domain model block (C123's anchor is 879). Line 884 reads 'Full spec: [domain cards](method/domain-cards.md)'. The link is not method/model.md, but I dumped C129's record and method/domain-cards.md is one of its files; the file exists on disk."
  },
  {
   "claim": "C124 cites C129",
   "grounded": true,
   "evidence": "method.md:1321",
   "skeptic": "backbone-1",
   "note": "Opened method.md:1318-1324, inside C124's region (1068-1331). Lines 1320-1321: 'Format it as a bare `path:line` anchor (never a markdown link — see [the map model](method/model.md)'s Anchor formats)'. method/model.md is C129's anchor file."
  },
  {
   "claim": "C121 cites C129",
   "grounded": true,
   "evidence": "method.md:505",
   "skeptic": "backbone-1",
   "note": "Opened method.md:502-505, the messaging-catalog bullet inside C121's region (414-505). Line 505 ends 'the edges claim — see [the map model](method/model.md))'. method/model.md exists and is C129's anchor file."
  },
  {
   "claim": "C127 cites C129",
   "grounded": true,
   "evidence": "method.md:2205",
   "skeptic": "backbone-1",
   "note": "Opened method.md:2202-2208, inside C127's region (starts 2152). Line 2204-2205 reads 'Those seven keys are the WHOLE authored surface — the full field semantics are in `method/model.md`.' — a citation of C129's file."
  },
  {
   "claim": "C128 cites C129",
   "grounded": true,
   "evidence": "method.md:2834",
   "skeptic": "backbone-1",
   "note": "Opened method.md:2831-2837, inside C128's region (starts 2651). Line 2834 reads 'The committed source of truth is `.coyodex/project-map.json` ([the map model](method/model.md))'. method/model.md is C129's anchor file."
  },
  {
   "claim": "C126 cites C132",
   "grounded": true,
   "evidence": "method.md:2113",
   "skeptic": "backbone-1",
   "note": "Opened method.md:2110-2116, inside C126's region (1728 up to C127 at 2152). Lines 2112-2113: 'The copyable contract is [method/templates/trace-contract.md](method/templates/trace-contract.md)'. The file exists and is one of C132's files."
  },
  {
   "claim": "C128 cites C132",
   "grounded": true,
   "evidence": "method.md:2781",
   "skeptic": "backbone-1",
   "note": "Opened method.md:2780-2784. Line 2781 is '[method/templates/harvest-contract.md](method/templates/harvest-contract.md)' and the file exists (one of C132's). Line 2781 falls after C128's anchor at 2651 with no later method.md component anchor, so by the map's line partition this text is C128's; the citation itself is real either way."
  },
  {
   "claim": "C138 cites C132",
   "grounded": true,
   "evidence": "CONTRIBUTING.md:48",
   "skeptic": "backbone-1",
   "note": "Opened CONTRIBUTING.md:46-51. Line 48 reads '`method.md`, the `method/` docs and the templates in `method/templates/` are not documentation. They are the prompt an agent reads on every build'. method/templates/ is C132's own source ref."
  },
  {
   "claim": "C136 cites C133",
   "grounded": true,
   "evidence": "eval/retro/method.md:281",
   "skeptic": "backbone-1",
   "note": "Opened eval/retro/method.md:274-286. Line 281 is the command `git -C COYODEX_HOME diff --diff-filter=AM --name-only <old>..<new> -- method/retro-checks/`, and the following lines explain that added files there are the pending checks. method/retro-checks/ exists and is C133's source ref; eval/retro/method.md is C136's anchor file."
  },
  {
   "claim": "C135 cites C100",
   "grounded": true,
   "evidence": "eval/method.md:215",
   "skeptic": "backbone-1",
   "note": "Opened eval/method.md:208-218. Line 215 invokes `COYODEX_HOME/.venv/bin/coyodex-eval hash <baseline map>`. I then opened eval/tools/coyodex_eval/cli.py and found the `hash` verb dispatched at line 76-78, so the cited command line is C100."
  },
  {
   "claim": "C138 cites C137",
   "grounded": true,
   "evidence": "CONTRIBUTING.md:40",
   "skeptic": "backbone-1",
   "note": "Opened CONTRIBUTING.md:30-44. Line 40 reads '**`README.md`** — user-facing overview and the install / usage steps.', an entry in the repo-layout list naming C137's anchor file README.md."
  }
 ]
}