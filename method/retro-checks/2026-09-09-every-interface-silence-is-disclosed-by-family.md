# Every 'Interface exceptions' silence is disclosed, by family

Change (2026-09-09): `validate` ends the interface checks with one line naming, per check family,
every surface id a recorded `Interface exceptions` line silenced (no `facing`; `kind`; a minted
`kind`; a person-shaped surface nobody is drawn at; a person at a machine-shaped surface; OUR
surface that sends to nobody; reached by no use case; `theirs` with no evidence; ways in that no
use case names), and a second line naming recorded ids that silence nothing. On the 2026-09-08
mcpolis build one heading forgave 35 keys across five families and `validate` disclosed 8 of them
(retro row 15).

Escalation: none on its own.

## Checks

1. expect: on the next build's final `validate`, every keyed id under the heading appears either in
   the by-family line or in the silence-nothing line.
   regression sign: a recorded id in the map's extras that neither line names.

2. expect: the silence-nothing line, when present, is acted on — the id dropped or a `<why>`
   saying it is kept knowingly.
   regression sign: the same stale ids on two consecutive builds.
