{
 "grounding": [
  {
   "claim": "Rule 'An advisory the operator decides to live with is silenced only by a reason recorded in the map.' is enforced at method.md:1640 — forbids rewording the map to dodge an advisory, and asks for a recorded reason instead",
   "grounded": true,
   "evidence": "method.md:1640",
   "skeptic": "rule-6",
   "note": "Read method.md:1636-1641. The paragraph lists every literal that records an exception, and line 1640 closes it with 'Never reword prose to dodge a heuristic — record the exception instead' — the reword is forbidden and the recorded exception is named as the remedy."
  },
  {
   "claim": "Rule 'An advisory the operator decides to live with is silenced only by a reason recorded in the map.' is enforced at tools/coyomap/records.py:324 — matches a finding against the recorded reasons, so only a recorded one is passed over",
   "grounded": true,
   "evidence": "tools/coyomap/records.py:324",
   "skeptic": "rule-6",
   "note": "Read records_key at tools/coyomap/records.py:308-324. Line 324 tests every recorded line for a '<key>:' prefix, so a finding is passed over only when some recorded line is keyed to that exact finding; the docstring states why it is prefix-and-colon rather than a substring test."
  },
  {
   "claim": "Rule 'A recorded reason is refused unless it names a finding the checks really read.' is enforced at tools/coyomap/record.py:268 — refuses a reason filed under a heading no check ever reads",
   "grounded": true,
   "evidence": "tools/coyomap/record.py:268",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/record.py:267-270 together with _resolve_heading at 78-87. An unrecognised heading comes back with a complaint ('is not a heading any check reads, so a line under it silences nothing'), and line 268 prints it as ERROR and returns 2 before the write at 318."
  },
  {
   "claim": "Rule 'A recorded reason is refused unless it names a finding the checks really read.' is enforced at tools/coyomap/record.py:309 — refuses a reason that answers no finding, and writes nothing at all",
   "grounded": true,
   "evidence": "tools/coyomap/record.py:309",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/record.py:303-318. malformed_records is asked about the lines THIS call added; line 309 gates the refusal, 312-314 print 'REFUSED ... Nothing was written', and 315 returns before the single path.write_text at 318, so the file is genuinely untouched."
  },
  {
   "claim": "Rule 'A recorded reason is refused unless it names a finding the checks really read.' is enforced at tools/coyomap/records.py:243 — reads nothing from a reason listing several things when one of them is not a finding",
   "grounded": true,
   "evidence": "tools/coyomap/records.py:243",
   "skeptic": "rule-6",
   "note": "Read keys_on_line at tools/coyomap/records.py:228-245. When a line's leading comma list holds more than one token, line 243 requires EVERY token to match the family's strict key pattern and returns [] otherwise, so the whole line records nothing rather than partly reading."
  },
  {
   "claim": "Rule 'Every advisory a recorded reason silenced is still counted out loud in the build's report.' is enforced at tools/coyomap/validate_model.py:720 — adds a line saying how many advisories the recorded reasons silenced, and on which elements",
   "grounded": true,
   "evidence": "tools/coyomap/validate_model.py:720",
   "skeptic": "rule-6",
   "note": "Read _granularity_warnings at tools/coyomap/validate_model.py:713-726. Each suppressed finding is collected as '<id> (<signal>)' at 716, and line 720 appends a warning carrying the count and the list of ids and signals that a recorded id silenced."
  },
  {
   "claim": "Rule 'Every advisory a recorded reason silenced is still counted out loud in the build's report.' is enforced at tools/coyomap/validate_analysis.py:329 — names on every build the code a written exclusion removed from every check",
   "grounded": true,
   "evidence": "tools/coyomap/validate_analysis.py:329",
   "skeptic": "rule-6",
   "note": "Read ignore_disclosure at tools/coyomap/validate_analysis.py:289-343. Line 329 opens the disclosure: how many files the ignore file removed, and each pattern with what it removed (from ignore_report), plus the statement that those files are out of every coverage check. It names each PATTERN and a file count, not the individual filenames; the pass is unconditional."
  },
  {
   "claim": "Rule 'Every advisory a recorded reason silenced is still counted out loud in the build's report.' is enforced at tools/coyomap/records.py:286 — reports a reason that failed to parse instead of dropping it in silence",
   "grounded": true,
   "evidence": "tools/coyomap/records.py:286",
   "skeptic": "rule-6",
   "note": "Read malformed_records at tools/coyomap/records.py:259-287. A line that opens like a record of this family but parses to no key is appended at 286 and returned to the callers, instead of being skipped silently."
  },
  {
   "claim": "Rule 'Every advisory a recorded reason silenced is still counted out loud in the build's report.' is enforced at tools/coyomap/anchor_drift.py:199 — collects a line that tries to be a reason and answers nothing, so the build reports it",
   "grounded": true,
   "evidence": "tools/coyomap/anchor_drift.py:199",
   "skeptic": "rule-6",
   "note": "Read drift_exceptions at tools/coyomap/anchor_drift.py:194-200. A line matching the ATTEMPT pattern but not the record pattern is collected at 199; apply_drift_exceptions turns that list into a reported note at 234-237, so it reaches the build's output."
  },
  {
   "claim": "Rule 'Every advisory a recorded reason silenced is still counted out loud in the build's report.' is enforced at tests/test_method_contract.py:671 — keeps the announcement of a silence out of reach of any recorded reason",
   "grounded": true,
   "evidence": "tests/test_method_contract.py:671",
   "skeptic": "rule-6",
   "note": "Read tests/test_method_contract.py:665-678 with the test at 711-719. Line 671 is the KNOWN_NO_ESCAPE entry for the line that discloses how many externally-activated ways in a record suppressed, and its stated reason is 'the finding IS the disclosure of a record; silencing it restores the silence it fixes'. The entry is the recorded decision that this disclosure carries no escape, and it is why the escape test tolerates a message naming none."
  },
  {
   "claim": "Rule 'A finding whose only honest answer is a fix cannot be silenced by a recorded reason.' is enforced at tests/test_method_contract.py:531 — holds the findings no recorded reason may silence, each beside the reason it cannot",
   "grounded": true,
   "evidence": "tests/test_method_contract.py:531",
   "skeptic": "rule-6",
   "note": "Read tests/test_method_contract.py:526-678. KNOWN_NO_ESCAPE opens at 531 as a dict mapping each advisory's message prefix to the reason it needs no recordable escape, and the comment at 528-530 states that an entry is a claim that the finding is always fixable where it fires."
  },
  {
   "claim": "Rule 'A finding whose only honest answer is a fix cannot be silenced by a recorded reason.' is enforced at method.md:2093 — refuses any way to record a use case as deliberately left untold",
   "grounded": true,
   "evidence": "method.md:2093",
   "skeptic": "rule-6",
   "note": "Read method.md:2085-2096. Line 2093 states there is 'deliberately NO \"recorded as deliberately untraced\" escape' for the no-flow advisory, because an untraced use case is a claim with nothing behind it and the two honest remedies are to trace it or drop it."
  },
  {
   "claim": "Rule 'A finding whose only honest answer is a fix cannot be silenced by a recorded reason.' is enforced at tools/coyomap/anchor_drift.py:437 — tells an operator that a recorded reason cannot answer a finding about a code link's shape",
   "grounded": true,
   "evidence": "tools/coyomap/anchor_drift.py:437",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/anchor_drift.py:410-445. On the shape-only pass, line 437 fires when any drift exception is recorded, and the message at 438-444 tells the operator those lines silence nothing here and that a shape-only finding has no recorded escape by design — fix the anchor instead."
  },
  {
   "claim": "Rule 'An advisory must say where the operator records the reason for living with it.' is enforced at tests/test_method_contract.py:716 — fails when an advisory leaves the reader nowhere to record the decision",
   "grounded": true,
   "evidence": "tests/test_method_contract.py:716",
   "skeptic": "rule-6",
   "note": "Read tests/test_method_contract.py:711-719 with _has_escape (681-684) and _allowlisted (687-688). Every advisory whose message names no recordable heading and is not allowlisted becomes an orphan, and the assert at 716 fails and lists each one as NO-ESCAPE or UNNAMED."
  },
  {
   "claim": "Rule 'An advisory must say where the operator records the reason for living with it.' is enforced at tools/coyomap/validate_model.py:6676 — prints beside the advisories the command that records a reason the checks will find",
   "grounded": true,
   "evidence": "tools/coyomap/validate_model.py:6676",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/validate_model.py:6664-6679. After printing the warnings, line 6675 checks whether any of them names an extras heading and line 6676 prints the `coyomap record` command that writes such a line, saying it shape-checks the line before writing."
  },
  {
   "claim": "Rule 'Only files git accounts for are analysed, so ignored and generated trees stay out of the map.' is enforced at tools/coyomap/preindex_lib.py:198 — Takes the starting file list from everything git tracks in the project.",
   "grounded": true,
   "evidence": "tools/coyomap/preindex_lib.py:198",
   "skeptic": "rule-6",
   "note": "Read _git_rels at tools/coyomap/preindex_lib.py:173-209. Line 198 is `tracked = ls()`, which runs `git ls-files -z` in the repo root; that tracked list is the start of the file set, and a None result at 199-200 is what falls back to a plain walk."
  },
  {
   "claim": "Rule 'Only files git accounts for are analysed, so ignored and generated trees stay out of the map.' is enforced at tools/coyomap/preindex_lib.py:201 — Adds files someone created but never added yet, and leaves every ignored path to git.",
   "grounded": true,
   "evidence": "tools/coyomap/preindex_lib.py:201",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/preindex_lib.py:201 in _git_rels. It runs `git ls-files --others --exclude-standard`, which adds files created but never added, while leaving every ignore rule to git itself (nested .gitignore, negations, global excludes) — the docstring at 173-187 says so."
  },
  {
   "claim": "Rule 'Only files git accounts for are analysed, so ignored and generated trees stay out of the map.' is enforced at tools/coyomap/preindex_lib.py:148 — Drops dependency folders, build output and caches by name, whether or not git is there to ask.",
   "grounded": true,
   "evidence": "tools/coyomap/preindex_lib.py:148",
   "skeptic": "rule-6",
   "note": "Read _excluded at tools/coyomap/preindex_lib.py:147-152 with DEFAULT_EXCLUDE_DIRS at 104-109 (node_modules, vendor, dist, build, out, target, __pycache__, .venv, caches). Line 148 drops any path holding one of those names, and iter_source_files applies _excluded at 236 to BOTH the git list and the non-git walk."
  },
  {
   "claim": "Rule 'A project's own exclusion patterns are named on every run, each beside the number of files it removed.' is enforced at tools/coyomap/validate_model.py:6288 — Names the patterns on every check run, including the cheap pass a lead runs most often.",
   "grounded": true,
   "evidence": "tools/coyomap/validate_model.py:6288",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/validate_model.py:6279-6291. The ignore_disclosure call sits outside the `if check_coverage:` block at 6292 and runs whenever a walk root is known, so the cheap `validate --check-sources` pass emits it too; the callee (validate_analysis.py:301-308) states that requirement, and it names each pattern with the file count it decided."
  },
  {
   "claim": "Rule 'A project's own exclusion patterns are named on every run, each beside the number of files it removed.' is enforced at tools/coyomap/ignorefile.py:133 — Collects each pattern that removed nothing, so a typo cannot read as coverage the map never got.",
   "grounded": true,
   "evidence": "tools/coyomap/ignorefile.py:133",
   "skeptic": "rule-6",
   "note": "Read ignore_report at tools/coyomap/ignorefile.py:114-136. Line 133 builds `unused` from every rule whose deciding-match count is zero, covering positive and `!` rules alike; the disclosure surfaces it as 'pattern(s) that decided nothing on this tree'."
  },
  {
   "claim": "Rule 'A project's own exclusion patterns are named on every run, each beside the number of files it removed.' is enforced at tools/coyomap/scope.py:131 — Puts the same per pattern account in the briefing, printed before a build reads any code.",
   "grounded": true,
   "evidence": "tools/coyomap/scope.py:131",
   "skeptic": "rule-6",
   "note": "Read scope_report at tools/coyomap/scope.py:72-136 with the module docstring at 2-19 (this is the up-front briefing, printed before the build). Line 131 calls the same ignore_report the validator uses, and 132-136 print the per-pattern lines plus the patterns that removed nothing."
  },
  {
   "claim": "Rule 'A build never opens the map it is replacing, so the new map cannot quietly copy the old one.' is enforced at method/dispatch.md:135 — Bans opening the map being replaced, including the one version history still holds and one already filed away.",
   "grounded": true,
   "evidence": "method/dispatch.md:135",
   "skeptic": "rule-6",
   "note": "Read method/dispatch.md:135-142. The paragraph opens 'Do not open a previous map while building' and names both the copy git still holds and one filed under .coyomap/dev-rebuilds/, with the reason that a build reading the map it replaces is no longer independent of it."
  },
  {
   "claim": "Rule 'A build never opens the map it is replacing, so the new map cannot quietly copy the old one.' is enforced at tools/coyomap/scope.py:116 — Warns when a copy of a map sits among the files that are about to be read as code.",
   "grounded": true,
   "evidence": "tools/coyomap/scope.py:116",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/scope.py:106-125. The briefing scans the files the walk will analyse for a project-map.json / .md, and line 116 fires the WARNING naming each folder that holds one and will be read as source."
  },
  {
   "claim": "Rule 'A map is built again from scratch only when the person asks, because a rebuild discards the accepted map.' is enforced at method/dispatch.md:187 — Sends an existing map to a change report, and makes a fresh build need a spoken request and a confirmation.",
   "grounded": true,
   "evidence": "method/dispatch.md:187",
   "skeptic": "rule-6",
   "note": "Read method/dispatch.md:187-189: a rebuild happens 'only when the user explicitly asks', with a warning that it overwrites the baseline and discards curation, and a confirmation required. The routing half of the claim is the same section's default at 144 and 166-168, which sends an existing baseline to Analyze — the change report."
  },
  {
   "claim": "Rule 'A map is built again from scratch only when the person asks, because a rebuild discards the accepted map.' is enforced at method.md:2831 — Says a build writes a new map over any existing one, so nothing of the older map is kept.",
   "grounded": true,
   "evidence": "method.md:2831",
   "skeptic": "rule-6",
   "note": "Read method.md:2831-2835. The line states that Build writes a new baseline and overwrites any existing .coyomap/ map, so the mode is only for a first map or a confirmed rebuild."
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at tools/coyomap/fix.py:136 — Warns that a correction made to the finished map disappears the next time the pieces are merged.",
   "grounded": true,
   "evidence": "tools/coyomap/fix.py:136",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/fix.py:132-138. When the edited file was the assembled map, line 136 prints that a later `assemble` rebuilds it from the fragments and this edit is lost, and tells the operator to run `fix` only after the last assemble."
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at tools/coyomap/fix.py:470 — Refuses a removal aimed at one piece, because the pieces beside that one cannot be seen from there.",
   "grounded": true,
   "evidence": "tools/coyomap/fix.py:470",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/fix.py:469-471 with _refuse_fragment_drop at 141-160. A drop-edge aimed at a fragment returns 2 after an ERROR explaining that the flow steps riding the edge may live in a sibling fragment this file cannot see, so the drop would be undone by the next assemble."
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at tools/coyomap/assemble.py:922 — Applies the recorded assignments after every merge, which is what makes those assignments last.",
   "grounded": true,
   "evidence": "tools/coyomap/assemble.py:922",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/assemble.py:896-940. The reconcile block at 922 runs after merge_fragments (897) and after the entity-edge derive (914), and apply_reconcile at 939 applies the directives, so a recorded assignment is re-applied on each assemble. It is conditional on --reconcile being passed, which is what the method prescribes."
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at method.md:2742 — States the split: what must survive a rebuild goes in the assignment file, never in the finished map.",
   "grounded": true,
   "evidence": "method.md:2742",
   "skeptic": "rule-6",
   "note": "Read method.md:2735-2747. Line 2742 gives the one rule: an assignment or drop that must survive a rebuild goes in the reconcile file, and only a terminal anchor fix after the last assemble goes through `fix` on the finished map."
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at tools/coyomap/validate_model.py:6134 — Compares the committed readable copy with a freshly generated one, and reports any difference.",
   "grounded": true,
   "evidence": "tools/coyomap/validate_model.py:6134",
   "skeptic": "rule-6",
   "note": "Read _check_view_fresh at tools/coyomap/validate_model.py:6126-6137. Line 6134 compares the committed project-map.md against model_to_markdown(m) and returns a problem naming it as a generated file when they differ."
  },
  {
   "claim": "Rule 'A correction only lasts where the build reads from, because every file the build produces is written again from scratch.' is enforced at method/dispatch.md:194 — States that the readable copy is generated from the map and never edited by hand.",
   "grounded": true,
   "evidence": "method/dispatch.md:194",
   "skeptic": "rule-6",
   "note": "Read method/dispatch.md:193-195. It states the committed .coyomap/project-map.md is a generated view of the map and is never hand-edited, while the diagram is served live and is not committed."
  },
  {
   "claim": "Rule 'A warning the person decides to accept is silenced only by a note written under a heading the checks read.' is enforced at tools/coyomap/record.py:85 — Refuses a heading no check reads, because a note filed under such a heading silences nothing.",
   "grounded": true,
   "evidence": "tools/coyomap/record.py:85",
   "skeptic": "rule-6",
   "note": "Read _resolve_heading at tools/coyomap/record.py:78-87, then its call sites (the only two: record.py:267 and assemble.py:451). Line 85 returns the complaint '...is not a heading any check reads, so a line under it silences nothing', and the caller at record.py:267-270 turns it into an ERROR and exit code 2, so the record is refused. assemble.py:451 only canonicalises and ignores the complaint, which is a different job."
  },
  {
   "claim": "Rule 'A warning the person decides to accept is silenced only by a note written under a heading the checks read.' is enforced at tools/coyomap/finalize.py:635 — Drops each file named by a recorded note out of the advisory the finish check would otherwise raise.",
   "grounded": true,
   "evidence": "tools/coyomap/finalize.py:635",
   "skeptic": "rule-6",
   "note": "Read tools/coyomap/finalize.py:613-654. The recorded keys under 'Access baseline exceptions' are read at 634, and line 635 removes each recorded file from the lost list, so it drops out of the advisory — while 636-654 keep it in a disclosure line and in the count."
  },
  {
   "claim": "Rule 'A warning the person decides to accept is silenced only by a note written under a heading the checks read.' is enforced at method/dispatch.md:110 — Names the heading the finish check reads, after twenty notes were filed where nothing could find them.",
   "grounded": true,
   "evidence": "method/dispatch.md:110",
   "skeptic": "rule-6",
   "note": "Read method/dispatch.md:110-115. It names the 'Access baseline exceptions' heading, says finalize READS it so the path leaves the advisory, and records that the earlier wording pointed at 'Audit exceptions', where one live build wrote twenty records nothing could read."
  },
  {
   "claim": "Rule 'A map names the version of the code it describes. That name is marked when the code on disk was never committed.' is enforced at tools/coyomap/provenance.py:191 — appends the mark to the version name when anything is uncommitted",
   "grounded": true,
   "evidence": "tools/coyomap/provenance.py:191",
   "skeptic": "rule-6",
   "note": "Read pin_sha at tools/coyomap/provenance.py:177-191. Line 191 returns '<short-sha>-dirty' when dirty_paths finds anything uncommitted, and the plain sha otherwise."
  },
  {
   "claim": "Rule 'A map names the version of the code it describes. That name is marked when the code on disk was never committed.' is enforced at tools/coyomap/provenance.py:164 — asks version control what is uncommitted, ignoring the map's own folder",
   "grounded": true,
   "evidence": "tools/coyomap/provenance.py:164",
   "skeptic": "rule-6",
   "note": "Read dirty_paths at tools/coyomap/provenance.py:157-165 with _OURS at 139. Line 164 runs `git status --porcelain -- .` with a pathspec excluding .coyomap and .coyomap-eval, so the map's own folder never makes the tree read as dirty."
  },
  {
   "claim": "Rule 'A map names the version of the code it describes. That name is marked when the code on disk was never committed.' is enforced at method/change-impact.md:129 — tells the agent to offer the choice, and to mark the pin only if the person declines to commit",
   "grounded": true,
   "evidence": "method/change-impact.md:129",
   "skeptic": "rule-6",
   "note": "Read method/change-impact.md:125-129 with the pin gate it points at, method.md:2857-2869. Line 129 says to give the user the A/B choice and record the pin '-dirty' only if they pick B, and B is defined in method.md as proceeding without committing."
  }
 ]
}
