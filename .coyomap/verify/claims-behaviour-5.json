{
 "schema": "coyomap/theme-batch/v1",
 "theme": "behaviour",
 "claims": [
  {
   "claim": "UC5 step 11: I3 → C61 [in] — pass the feature's address to the cards",
   "anchor": "tools/coyomap/viewer/viewer.js:13016",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 12: C61 → C65 — open that feature's own page",
   "anchor": "tools/coyomap/viewer/viewer.js:1401",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 13: C65 → C66 — list the feature's use cases and the people who drive them",
   "anchor": "tools/coyomap/viewer/viewer.js:8719",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 14: R2 → I3 — drill from one use case into its numbered steps",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 15: I3 → C62 [in] — pass the use case's address to the canvas",
   "anchor": "tools/coyomap/viewer/viewer.js:6503",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 16: C62 → I3 [out] — draw the use case's flow, one box per step",
   "anchor": "tools/coyomap/viewer/viewer.js:13104",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 17: I3 → R2 — show every step of that feature, in order",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 18: R2 → I3 — click one box on the picture",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 19: I3 → C63 [in] — pass the box click to the click rules",
   "anchor": "tools/coyomap/viewer/viewer.js:5895",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 20: C63 → C64 — show that box's card in the info pane",
   "anchor": "tools/coyomap/viewer/viewer.js:4158",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 21: C64 → I3 [out] — put the box's card beside the picture",
   "anchor": "tools/coyomap/viewer/viewer.js:2881",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC5 step 22: I3 → R2 — show one box's name, type, sentence and code link",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 2: R2 → I3 — open one box's own page",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 3: I3 → C60 [in] — pass the element's address to the page",
   "anchor": "tools/coyomap/viewer/viewer.js:13011",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 4: C60 → E40 [in] — read that box's name, file and line",
   "anchor": "tools/coyomap/viewer/viewer.js:2818",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 5: C60 → I3 [out] — render the box's page with its code link",
   "anchor": "tools/coyomap/viewer/viewer.js:2837",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 6: I3 → R2 — show everything the map holds about that one box",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 7: R2 → I3 — pick the code link on the box",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 8: I3 → C69 [in] — pass the picked code link to the source column",
   "anchor": "tools/coyomap/viewer/viewer.js:223",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 9: C69 → C83 — fetch that file's text at the map's own commit",
   "anchor": "tools/coyomap/viewer/viewer.js:14537",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 10: C83 → C69 — return the file exactly as the pinned commit holds it",
   "anchor": "tools/coyomap/viewer/serve.py:797",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 11: C69 → I3 [out] — show the file, scrolled to the box's line",
   "anchor": "tools/coyomap/viewer/viewer.js:14396",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 12: I3 → R2 — show the code behind the box, beside the map",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 13: R2 → I3 — press the open button on the file's header",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 14: I3 → C69 [in] — pass the open request to the source column",
   "anchor": "tools/coyomap/viewer/viewer.js:13930",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 15: C69 → I9 [out] — open the file at that line in the reader's own editor",
   "anchor": "tools/coyomap/viewer/viewer.js:14925",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 16: I9 → R2 — put the reader in their editor, at the mapped line",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 17: C69 → I10 [out] — open the file on the code host at the pinned commit",
   "anchor": "tools/coyomap/viewer/viewer.js:14928",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "UC6 step 18: I10 → R2 — show the file on the web, pinned to the map's commit",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "SF1 step 1: C134 → C130 — read the dispatcher end to end",
   "anchor": "skill/coyomap/SKILL.md:27",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "SF1 step 2: C130 → C20 — run every coyomap command from the clone's own command line",
   "anchor": "method/dispatch.md:4",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "SF1 step 3: C20 → C26 — print the build briefing before anything else runs",
   "anchor": "tools/coyomap/cli.py:180",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "SF1 step 4: C26 → I8 [in] — read the project's own files to see what the build will cover",
   "anchor": "tools/coyomap/scope.py:74",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "SF1 step 5: C26 → C46 — drop every path the project's own ignore list names",
   "anchor": "tools/coyomap/scope.py:131",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "SF1 step 6: C26 → I1 [out] — print what will be read and what the map will be pinned to",
   "anchor": "tools/coyomap/scope.py:172",
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  },
  {
   "claim": "SF1 step 7: I1 → R1 — show the person the briefing before any work starts",
   "anchor": null,
   "detail": null,
   "why_risky": "the walk a reader follows — a step phrase is read as what the code does, and nothing else checks it against the line it names."
  }
 ]
}
