{
 "schema": "coyomap/theme-batch/v1",
 "theme": "rule",
 "claims": [
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at tools/coyomap/viewer/viewer.js:13472 — Picks, for each kind of thing, the single screen that draws it.",
   "anchor": "tools/coyomap/viewer/viewer.js:13472",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at tools/coyomap/viewer/viewer.js:7647 — Reads that same choice to decide which tab a thing's own page sits under.",
   "anchor": "tools/coyomap/viewer/viewer.js:7647",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at tools/coyomap/viewer/viewer.js:7651 — Sends a page about one thing to the tab that thing lives under.",
   "anchor": "tools/coyomap/viewer/viewer.js:7651",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Every thing the map records belongs to exactly one screen.' is enforced at method/diagrams.md:54 — Tells the map builder that a link to a thing lands on that thing's home screen.",
   "anchor": "method/diagrams.md:54",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A label that would be true of nearly every card is left off.' is enforced at tools/coyomap/viewer/viewer.js:8219 — Leaves off the word saying who a feature serves when the answer is the ordinary one.",
   "anchor": "tools/coyomap/viewer/viewer.js:8219",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A label that would be true of nearly every card is left off.' is enforced at tools/coyomap/viewer/viewer.js:721 — Gives a person on the customer's side no word saying whose side they are on.",
   "anchor": "tools/coyomap/viewer/viewer.js:721",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A screen leaves off a fact it already shows beside the same thing.' is enforced at tools/coyomap/viewer/viewer.js:730 — Drops a card's sentence when the sentence only repeats the card's own name.",
   "anchor": "tools/coyomap/viewer/viewer.js:730",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A screen leaves off a fact it already shows beside the same thing.' is enforced at tools/coyomap/viewer/viewer.js:6915 — Drops the feature line when the path above the page already names that feature.",
   "anchor": "tools/coyomap/viewer/viewer.js:6915",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A screen leaves off a fact it already shows beside the same thing.' is enforced at tools/coyomap/viewer/viewer.js:8345 — Skips a group heading that would only repeat the section heading above it.",
   "anchor": "tools/coyomap/viewer/viewer.js:8345",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A box drops the type word wherever its colour and mark already say what kind of thing it is.' is enforced at tools/coyomap/viewer/viewer.js:987 — Prints the type word only for the box sizes that ask for it, and for every interface.",
   "anchor": "tools/coyomap/viewer/viewer.js:987",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A box drops the type word wherever its colour and mark already say what kind of thing it is.' is enforced at tools/coyomap/viewer/gen_viewer.py:3057 — Asks for the small box on a flow picture, which carries no type word.",
   "anchor": "tools/coyomap/viewer/gen_viewer.py:3057",
   "detail": "In: Diagram builder (C82)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A box drops the type word wherever its colour and mark already say what kind of thing it is.' is enforced at method/diagrams.md:56 — Tells the map builder to colour each box on a flow picture by its kind.",
   "anchor": "method/diagrams.md:56",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyomap/viewer/viewer.js:15584 — Hides the decisions tab on a map that states no decision.",
   "anchor": "tools/coyomap/viewer/viewer.js:15584",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyomap/viewer/viewer.js:15585 — Hides the interfaces tab on a map that records no interface.",
   "anchor": "tools/coyomap/viewer/viewer.js:15585",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyomap/viewer/viewer.js:15595 — Drops a whole group of tabs once every tab inside it has been hidden.",
   "anchor": "tools/coyomap/viewer/viewer.js:15595",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A screen is offered only when the map holds something to draw on it.' is enforced at tools/coyomap/viewer/gen_viewer.py:3417 — Works out from the map whether there is any interface for a tab to show.",
   "anchor": "tools/coyomap/viewer/gen_viewer.py:3417",
   "detail": "In: Diagram builder (C82)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Where the map records nothing, the screen says so instead of leaving empty space.' is enforced at tools/coyomap/viewer/viewer.js:8541 — Says a feature reaches no outside service, rather than showing an empty block.",
   "anchor": "tools/coyomap/viewer/viewer.js:8541",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Where the map records nothing, the screen says so instead of leaving empty space.' is enforced at tools/coyomap/viewer/viewer.js:12874 — Says no flow step reaches this decision, rather than showing an empty block.",
   "anchor": "tools/coyomap/viewer/viewer.js:12874",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Where the map records nothing, the screen says so instead of leaving empty space.' is enforced at tools/coyomap/viewer/viewer.js:12757 — Says no flow comes through this interface, so no feature can be named here.",
   "anchor": "tools/coyomap/viewer/viewer.js:12757",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A line between two things offers no code link, because the place it names is only one example.' is enforced at tools/coyomap/viewer/viewer.js:3052 — Builds a relation row as plain text, with no link to a line of code.",
   "anchor": "tools/coyomap/viewer/viewer.js:3052",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A line between two things offers no code link, because the place it names is only one example.' is enforced at tools/coyomap/viewer/viewer.js:2957 — Clears the code viewer when a relation line is picked, rather than pointing it somewhere.",
   "anchor": "tools/coyomap/viewer/viewer.js:2957",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A line between two things offers no code link, because the place it names is only one example.' is enforced at tools/coyomap/viewer/viewer.js:14163 — Marks a line of code from a flow step's own place, never from a relation line.",
   "anchor": "tools/coyomap/viewer/viewer.js:14163",
   "detail": "In: Info pane (C64), Viewer shell (C70)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A picture of one flow draws only the connections that flow itself makes.' is enforced at tools/coyomap/viewer/gen_viewer.py:3081 — Builds each arrow from the flow's own steps, grouped by the pair of things they join.",
   "anchor": "tools/coyomap/viewer/gen_viewer.py:3081",
   "detail": "In: Diagram builder (C82)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A picture of one flow draws only the connections that flow itself makes.' is enforced at method/diagrams.md:58 — Tells the map builder that the arrows are the flow's steps, never the general connection list.",
   "anchor": "method/diagrams.md:58",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Code shown beside the map is the version the map was built from, not whatever sits on disk now.' is enforced at tools/coyomap/viewer/serve.py:782 — Answers a file request at the map's own commit unless the address asks for another version.",
   "anchor": "tools/coyomap/viewer/serve.py:782",
   "detail": "In: Map server (C83)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Code shown beside the map is the version the map was built from, not whatever sits on disk now.' is enforced at tools/coyomap/viewer/serve.py:491 — Builds the file list from version history at the map's commit rather than from the folder on disk.",
   "anchor": "tools/coyomap/viewer/serve.py:491",
   "detail": "In: Map server (C83)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Code shown beside the map is the version the map was built from, not whatever sits on disk now.' is enforced at method.md:3108 — The instructions tell the reader that data and source both come from history at the map's commit.",
   "anchor": "method.md:3108",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Data the product writes only to read back crosses no interface, unless the map records an outside reader.' is enforced at method.md:511 — Keeps the product's own database, cache and queue off the outside edge.",
   "anchor": "method.md:511",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Data the product writes only to read back crosses no interface, unless the map records an outside reader.' is enforced at method.md:515 — Lets a reader the map records overturn that exclusion, so a store a person browses counts again.",
   "anchor": "method.md:515",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Data the product writes only to read back crosses no interface, unless the map records an outside reader.' is enforced at method/model.md:460 — Repeats the same exclusion on the page that fixes every field a map may hold.",
   "anchor": "method/model.md:460",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A step names the service on the far side, never the proxy or the library that carries the call.' is enforced at method.md:516 — Names a reverse proxy and a log shipper as carriers, and the log store as the real edge.",
   "anchor": "method.md:516",
   "detail": "In: Product description instructions (C120), Code inventory instructions (C121), Outside edge instructions (C122), Data and step instructions (C123), Operations and decision instructions (C124), Cross-cutting build instructions (C125), Fan-out phase instructions (C126), Verification phase instructions (C127), Closing sequence instructions (C128)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A step names the service on the far side, never the proxy or the library that carries the call.' is enforced at tools/coyomap/validate_model.py:2840 — Flags a story step that stops at a carrier standing in front of an interface.",
   "anchor": "tools/coyomap/validate_model.py:2840",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'A step names the service on the far side, never the proxy or the library that carries the call.' is enforced at tools/coyomap/validate_model.py:2851 — Flags the same shortcut inside shared steps, where one carrier is drawn in every story running them.",
   "anchor": "tools/coyomap/validate_model.py:2851",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Each outside system names the interfaces it stands on, or records why it is none, never both.' is enforced at tools/coyomap/validate_model.py:2744 — Stops a map where an outside system neither claims an interface nor gives a reason.",
   "anchor": "tools/coyomap/validate_model.py:2744",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Each outside system names the interfaces it stands on, or records why it is none, never both.' is enforced at tools/coyomap/validate_model.py:2531 — Stops a system that claims an interface and also gives a reason for having none.",
   "anchor": "tools/coyomap/validate_model.py:2531",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'Each outside system names the interfaces it stands on, or records why it is none, never both.' is enforced at method/model.md:530 — Demands the reason on every outside system that names no interface.",
   "anchor": "method/model.md:530",
   "detail": "In: Map model spec (C129)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  },
  {
   "claim": "Rule 'An interface is labelled by what it is, never by what it is for.' is enforced at tools/coyomap/validate_model.py:2630 — Flags a label that answers what the interface is for, such as payments or analytics.",
   "anchor": "tools/coyomap/validate_model.py:2630",
   "detail": "In: Shape checks (C6), Story checks (C7), Rule checks (C8), Wiring checks (C9), Data checks (C10)",
   "why_risky": "a product DECISION and the line that enforces it — a false claim here reads as a fact about the business, and the map's decision layer is the part a reader trusts most."
  }
 ]
}
