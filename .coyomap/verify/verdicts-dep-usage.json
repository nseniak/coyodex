{"grounding": [
  {"claim": "C27 queries D90",
   "grounded": true,
   "evidence": "tools/coyomap/provenance.py:125",
   "skeptic": "dep-usage",
   "note": "Opened the anchor: `git_value` runs `subprocess.run([\"git\", \"-C\", str(repo), *args], ...)` at line 125, with GIT_TERMINAL_PROMPT=0 and a 5s timeout, and returns git's stdout. `stamp` calls it at line 218 for `show -s --format=%cs HEAD`, so the build log really does ask the git program for a value."},

  {"claim": "C27 reads D95",
   "grounded": true,
   "evidence": "tools/coyomap/provenance.py:209",
   "skeptic": "dep-usage",
   "note": "Line 209 is `sid = session_id or os.environ.get(SESSION_ENV)` and SESSION_ENV is defined at line 29 as \"CLAUDE_CODE_SESSION_ID\"; the error text at 211-212 says it is \"present inside a Claude Code session\". The value is produced by the coding-agent harness, not by this product, so data does cross in from the far side."},

  {"claim": "C27 writes D91",
   "grounded": true,
   "evidence": "tools/coyomap/provenance.py:232",
   "skeptic": "dep-usage",
   "note": "Line 232 is `path.write_text(prov.to_json(), encoding=\"utf-8\")`, where `path` is built at line 219 as `coyomap_dir / PROVENANCE_NAME` under the repo. A plain file write to local disk."},

  {"claim": "C49 queries D90",
   "grounded": true,
   "evidence": "tools/coyomap/impact_git.py:50",
   "skeptic": "dep-usage",
   "note": "Line 50 in `_git` is `proc = subprocess.run([\"git\", \"-C\", str(repo), *args], capture_output=True, timeout=60)`, returning git's stdout and raising ImpactError on an unexpected exit code. Change impact runs the git program directly."},

  {"claim": "C50 writes D91",
   "grounded": true,
   "evidence": "tools/map_backup.py:391",
   "skeptic": "dep-usage",
   "note": "Line 391 is `shutil.copytree(coyomap_dir, dest / \"map\", symlinks=True)`, after `dest.mkdir(parents=True, exist_ok=False)` at 387. The archive step copies map files into a new directory on local disk."},

  {"claim": "C69 requests D161",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:14100",
   "skeptic": "dep-usage",
   "note": "Line 14100 is `document.head.appendChild(s)` where `s.src = HLJS_JS` (line 14097); HLJS_JS is defined at 14083 as 'https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js', and the stylesheet at 14085 is the same host. cdnjs.cloudflare.com is the host that serves the file, so it is the far side, not a pipe."},

  {"claim": "C83 calls D92",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/serve.py:895",
   "skeptic": "dep-usage",
   "note": "Line 895 is `webbrowser.open(url)`, guarded by `if open_browser:` at 894, with url = `http://127.0.0.1:{port}/` from line 886. The server launches the desktop browser on the machine."},

  {"claim": "C70 calls D93",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:14925",
   "skeptic": "dep-usage",
   "note": "Line 14925 is `if (uri) { fireUri(uri); return; }` with `uri = editorUri(n.file, n.line)` at 14924. `editorUri` (14868-14881) fills a per-editor scheme template from OPEN_TARGETS (14795-14808: vscode://, cursor://, idea://, zed:// ...) and only returns a URI whose scheme is in ALLOWED_OPEN_SCHEMES (14811-14815). `fireUri` (14905-14909) clicks a hidden <a> with that href, which hands the file and line to the desktop editor."},

  {"claim": "C70 requests D94",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:14928",
   "skeptic": "dep-usage",
   "note": "Line 14928 is `if (gh) { window.open(gh, '_blank', 'noopener'); return; }`, with `gh = ghUrl(n.file, n.line)` at 14927. `ghUrl` (14897-14903) builds `<repo>/blob|tree/<ref>/<path>` from `ghRepo()`, whose default comes from `gh_repo_url` in tools/coyomap/viewer/gen_viewer.py:119-127, which returns None unless the origin remote matches github.com and otherwise yields 'https://github.com/<owner>/<repo>'. So the opened URL is a GitHub blob URL."},

  {"claim": "C70 requests D96",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.html:19",
   "skeptic": "dep-usage",
   "note": "Line 19 is `<script src=\"https://cdn.jsdelivr.net/npm/mermaid@11.15.0/dist/mermaid.min.js\"` with an SRI integrity hash on 20 and crossorigin on 21; line 16 loads svg-pan-zoom from the same host. The viewer page fetches both libraries from cdn.jsdelivr.net, which is the serving host itself."},

  {"claim": "C105 queries D90",
   "grounded": true,
   "evidence": "eval/tools/coyomap_eval/arrows.py:184",
   "skeptic": "dep-usage",
   "note": "Line 184 is `proc = subprocess.run([\"git\", \"-C\", str(repo), \"diff\", \"--name-only\", a, b], ...)` and lines 188-191 read its returncode and stdout to list changed source files. The lost-arrows check asks the git program whether source moved between the two pins."},

  {"claim": "C113 reads D91",
   "grounded": true,
   "evidence": "eval/tools/coyomap_eval/mutate.py:112",
   "skeptic": "dep-usage",
   "note": "Line 112 is `lines = src.read_text(encoding=\"utf-8\", errors=\"replace\").splitlines()`, with `src = repo / path` at 109 and an `is_file()` guard at 110. The drifted-anchor mutation reads the real source file off local disk to pick a line that exists."},

  {"claim": "C107 reads D105",
   "grounded": true,
   "evidence": "eval/tools/coyomap_eval/transcript.py:328",
   "skeptic": "dep-usage",
   "note": "Line 328 is `with Path(path).open(encoding=\"utf-8\") as fh:` and 329-340 parse each line as JSON and keep records whose `type` is assistant or user. The module docstring at transcript.py:2 names the input as a Claude Code build transcript at `~/.claude/projects/<slug>/*.jsonl`, so the far side is the agent harness's own chat log, not this product's output."},

  {"claim": "C108 reads D105",
   "grounded": true,
   "evidence": "eval/tools/coyomap_eval/cost.py:272",
   "skeptic": "dep-usage",
   "note": "Line 272 is `for path in sorted(directory.glob(\"agent-*.jsonl\")):` over `subagent_dir(session)` = `<session-dir>/<session-id>/subagents` (lines 260-264); the loop then reads `<name>.meta.json` at 277 and the transcript itself via `read_turns(path, ...)` at 283. Those are the coding agent's sub-agent chat logs."},

  {"claim": "C110 reads D105",
   "grounded": true,
   "evidence": "eval/tools/coyomap_eval/retro_precheck.py:216",
   "skeptic": "dep-usage",
   "note": "Line 216 is `for f in tdir.glob(\"*.jsonl\"):` where `tdir = transcript_dir(repo)` (line 212), and `transcript_dir` returns `Path.home() / \".claude\" / \"projects\" / project_slug(repo)` (lines 58-59). The loop reads each file's mtime at 220 and, per the comment at 223-225, its last conversation record. It is reading the agent harness's chat logs."},

  {"claim": "C111 queries D90",
   "grounded": true,
   "evidence": "eval/tools/coyomap_eval/ledger.py:87",
   "skeptic": "dep-usage",
   "note": "Line 87 is `r = subprocess.run([\"git\", \"-C\", str(repo), \"merge-base\", \"--is-ancestor\", sha, \"HEAD\"], ...)` and line 91 returns `r.returncode == 0`. The ledger check asks the git program whether a row's commit is reachable from HEAD."},

  {"claim": "C85 persists D91",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/recents.py:44",
   "skeptic": "dep-usage",
   "note": "Line 44 is `self.path.write_text(json.dumps({\"folders\": self.folders}, indent=2), encoding=\"utf-8\")` inside `save`, after `self.path.parent.mkdir(parents=True, exist_ok=True)` at 43, with an OSError fallback printing to stderr at 45-46. `reload` at 34 reads the same file back. The remembered-projects list is kept in a JSON file on local disk."}
]}
