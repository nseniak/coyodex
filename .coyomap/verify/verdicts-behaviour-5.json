{
 "grounding": [
  {
   "claim": "UC5 step 11: I3 → C61 [in] — pass the feature's address to the cards",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:13016",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:13013-13019: inside `if (s.kind === 'capability')` the router hands the screen address's feature id and actor to `renderUseCases({ cap: s.cap, actor: s.act })`, which builds that feature's card page. The feature's address really does travel from the viewer into the card-building path. Two notes on attribution: `renderUseCases` is defined at viewer.js:8591, the same function this batch's next-but-one claim attributes to C66, and the actual click hand-off from a card into the cards module is bindElementCards at viewer.js:1439-1440."
  },
  {
   "claim": "UC5 step 12: C61 → C65 — open that feature's own page",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:1401",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:1397-1412 (`drillInto`, inside the Element cards block that starts at 1264). Line 1401 is `case 'capability': return go({ kind: 'capability', cap: id });` — a feature card's drill calls `go` (viewer.js:5109, the screen-address function) with that feature's own page state. Exactly the C61 -> C65 hand-off the step describes."
  },
  {
   "claim": "UC5 step 13: C65 → C66 — list the feature's use cases and the people who drive them",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:8719",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:8591-8726 (`renderUseCases`). Line 8719 writes the assembled page into `diagram`; the `board`/`sections` it writes are built at 8698-8702, where each section is headed by `<h3 class=\"uc-actor\">` (the driving actor) and holds `elementCardGridHtml(ids, per)` — one card per use case. So the page really does list the feature's use cases grouped by the people who drive them."
  },
  {
   "claim": "UC5 step 14: R2 → I3 — drill from one use case into its numbered steps",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:1402",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map, so I checked the action exists. Read viewer.js:1397-1412: a use-case card's drill is `case 'usecase': return go({ kind: 'usecase', uc: id })`, and viewer.js:5725 (`isFlowState`) makes `kind === 'usecase'` a flow screen whose picture is that use case's own steps (viewer.js:6503 -> flowMermaidFor). The reader can drill from a use case into its steps."
  },
  {
   "claim": "UC5 step 15: I3 → C62 [in] — pass the use case's address to the canvas",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:6503",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:6489-6509 (`mermaidFor`). Line 6503 is `if (isFlowState(s)) return flowMermaidFor(flowIdOf(s));` — the screen address `s` is reduced to a use-case (or shared sub-flow) id at viewer.js:5728 and used to pick that flow's drawing (viewer.js:5709-5711). The use case's address is what selects what the canvas draws."
  },
  {
   "claim": "UC5 step 16: C62 → I3 [out] — draw the use case's flow, one box per step",
   "grounded": false,
   "evidence": "tools/coyomap/viewer/gen_viewer.py:3000",
   "skeptic": "behaviour-5",
   "note": "The hand-off is real but the phrase is not. viewer.js:13104 (`diagram.innerHTML = svg;`) does put the rendered flow picture on screen. But the picture is generated by `gen_flow_map_mermaid` (gen_viewer.py:2999), whose own contract at line 3000-3001 reads 'a box per touched element (component / dependency / record / the driving actor) and one arrow per ordered pair, labelled with the step numbers that ride it'. I read the generator body at gen_viewer.py:3030-3069: boxes are declared once per element/actor/shared-sub-flow token (`ensure`, 3036-3058), and the step numbers go on the ARROW labels (`_flow_map_arrow_label`, 2988-2996). It is not one box per step."
  },
  {
   "claim": "UC5 step 17: I3 → R2 — show every step of that feature, in order",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/gen_viewer.py:2996",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Read `_flow_map_arrow_label` (gen_viewer.py:2988-2996): it returns every step number riding a pair, joined in order, and its docstring says every number is listed and never a truncated head, so no step of the flow is missing from the drawing. The order is carried by those 1-based numbers. Caveat for the lead: what is drawn is one USE CASE's steps, not the whole feature's."
  },
  {
   "claim": "UC5 step 18: R2 → I3 — click one box on the picture",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:5887",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Read `bindFlowMap` (viewer.js:5878-5910): line 5887 binds every drawn box on the flow picture with `bindNodes(scene, (id, el, ev) => {...})`, and 5897-5910 register the actor boxes by hand so they are clickable too. Every box on the picture takes a click."
  },
  {
   "claim": "UC5 step 19: I3 → C63 [in] — pass the box click to the click rules",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:5895",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:5878-5896. Inside `bindFlowMap`'s node callback, after the drill and name-click cases, line 5895 is `selectNodeFromCanvas(el, id, ev);` — the box click is handed to the shared click rules. That function sits in the per-state binding section the map anchors C63 to (viewer.js:5346)."
  },
  {
   "claim": "UC5 step 20: C63 → C64 — show that box's card in the info pane",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:4158",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:4142-4159. Line 4158 is `pickSelBox(mainScene, nodeDesc(mainScene, el, id), el, e)`. The descriptor it passes is built at 4144-4146, whose `show` is `() => showNodeDetailSynced(id)` — which reaches `showNode(id)` (viewer.js:2897-2899) and fills the info pane. So the click rules are what cause the box's card to appear."
  },
  {
   "claim": "UC5 step 21: C64 → I3 [out] — put the box's card beside the picture",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:2881",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:2876-2892 (`showNode`). Line 2881 is `panel.innerHTML = `<div class=\"pane-card\">${elementCardHtml(id, { bare: true })}</div>`` — the box's card is written into the info pane, the same card design a list uses (elementCardHtml, viewer.js:1264)."
  },
  {
   "claim": "UC5 step 22: I3 → R2 — show one box's name, type, sentence and code link",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:2881",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Read `elementCardHtml` (viewer.js:1264-1320) and `cardFacts` (viewer.js:723-756): the pane card carries the name, the type pill and the one sentence. IT CARRIES NO CODE LINK — `itemSpecOf` (viewer.js:1081-1110) adds only a `Stored` fact, and the comment at viewer.js:2807-2808 says the source ref is deliberately kept out of the panel. The file and line still reach the reader, through the source column: `showNode` calls `syncTreeToNode` (viewer.js:2891), which mirrors the node's source into the code viewer (viewer.js:13775), and on a diagram that column is the other half of the view (viewer.js:13931-13932). True on that reading; the lead may want the phrase to say the code shows BESIDE the card, not on it."
  },
  {
   "claim": "UC6 step 2: R2 → I3 — open one box's own page",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:1410",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Read `drillInto` (viewer.js:1397-1412): the default case, which covers a component, a record and a dependency, is `return go({ kind: 'element', id })` — the element's own details page. Clicking such a box's card opens its own page."
  },
  {
   "claim": "UC6 step 3: I3 → C60 [in] — pass the element's address to the page",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:13011",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:13009-13012: `if (s.kind === 'element') { renderElementDetails(s.id); ... }` — the screen address's element id is handed to the page renderer. (Attribution note for the lead: the renderer is at viewer.js:2817, nowhere near the map-loading fetch at viewer.js:172 that C60 is anchored to.)"
  },
  {
   "claim": "UC6 step 4: C60 → E40 [in] — read that box's name, file and line",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:2818",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:2817-2841 (`renderElementDetails`). Line 2818 is `const n = GRAPH.nodes[id];`. The stored box is the `Node` dataclass at tools/coyomap/viewer/build_graph.py:28-35, whose fields are `id, kind, name, file, line, fields` — so the name, the file and the line are exactly what this read returns."
  },
  {
   "claim": "UC6 step 5: C60 → I3 [out] — render the box's page with its code link",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:2837",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:2837-2838: the page is written as the hero plus `nodeDetailBodyHtml(id, true)`. I then read `nodeDetailBodyHtml` (viewer.js:2788-2812): its rows render any field value that is a bare code anchor through `srcCell` (2806), and `triggeredByHtml` (2599-2611) gives every entry point a `srcCell` link — `srcCell` (8157-8168) emits the `.srclink` button. So the page does carry code links. Worth flagging: the element's OWN anchor is deliberately not printed there (comment at 2807-2808); it is mirrored into the code column instead."
  },
  {
   "claim": "UC6 step 6: I3 → R2 — show everything the map holds about that one box",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:2837",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Read the comment and body at viewer.js:2813-2841: 'Everything the map holds about one element, as a PAGE', built from the hero plus `nodeDetailBodyHtml` (every field, plus the variants, run-by, owned-by, persisted-in, access, used-in, decides and triggered-by blocks) and the impact section. This is the page the step's outcome names."
  },
  {
   "claim": "UC6 step 7: R2 → I3 — pick the code link on the box",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:8163",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Read `srcCell` (viewer.js:8157-8168): an in-repo anchor becomes `<button type=\"button\" class=\"src srclink\" data-where=...>` showing the file name and line, and viewer.js:8154-8155 says the delegated container listener reads it on click. That button is the code link the reader picks on an element's page."
  },
  {
   "claim": "UC6 step 8: I3 → C69 [in] — pass the picked code link to the source column",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:223",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:213-224: one delegated click listener on the panel host and the diagram finds the closest `.srclink`, turns its `data-where` into a file and line (`whereNode`, 14859) and calls `openInCodeViewer(wn.file, wn.line)` at line 223. `openInCodeViewer` (14591-14606) opens the source column and shows the file. Exactly the step."
  },
  {
   "claim": "UC6 step 9: C69 → C83 — fetch that file's text at the map's own commit",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:14537",
   "skeptic": "behaviour-5",
   "note": "Read `loadCode` (viewer.js:14513-14560). The URL is built at 14531-14534 as `API_BASE + 'src?path=' + encodeURIComponent(path)`, and line 14537 is `r = await fetch(url, { cache: 'no-store' })` — the source column asking the map server for the file. The commit pinning is the server's side of it (serve.py:782)."
  },
  {
   "claim": "UC6 step 10: C83 → C69 — return the file exactly as the pinned commit holds it",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/serve.py:797",
   "skeptic": "behaviour-5",
   "note": "Read serve.py:776-801 (the `src` route). Line 782 defaults the commit to `proj.commit`, the map's own pin; 792-796 size-check the blob; line 797 is `blob = git_show(proj.repo_root, at, path)` and 801 returns it as plain text. So the file comes back as that commit holds it, not as the working tree holds it. The only way to read another version is an explicit `at=` parameter, which the impact explorer passes."
  },
  {
   "claim": "UC6 step 11: C69 → I3 [out] — show the file, scrolled to the box's line",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:14396",
   "skeptic": "behaviour-5",
   "note": "Read `renderCode` (viewer.js:14372-14404). Line 14396 writes the numbered line table into `cvscroll`, and 14398 immediately calls `markLine(cvLine)` — the line the box asked for, which arrived through `openInCodeViewer` -> `syncCodeView` -> `loadCode` (14520 sets `cvLine`). File shown, scrolled to the box's line."
  },
  {
   "claim": "UC6 step 12: I3 → R2 — show the code behind the box, beside the map",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:14396",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Same read as above: viewer.js:14390-14398 builds the file's lines into the code pane and marks the requested one. viewer.js:13931-13932 says that pane is the source column, which on a diagram is the view's other half and on a card page opens over it — so the code shows beside the map."
  },
  {
   "claim": "UC6 step 13: R2 → I3 — press the open button on the file's header",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:13930",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Read viewer.js:13929-13930: `cvopen` is described in the code as the '↗ opens the shown file in the external editor / on GitHub' control in the code viewer's header, and it is wired to a click listener. The button the step names exists and is live."
  },
  {
   "claim": "UC6 step 14: I3 → C69 [in] — pass the open request to the source column",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:13930",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:13929-13930: `cvopen.addEventListener('click', () => { if (cvPath) openSource({ file: cvPath, line: cvLine }); })`. The header's open button hands the currently shown file and line to the source column's own `openSource` (14914)."
  },
  {
   "claim": "UC6 step 15: C69 → I9 [out] — open the file at that line in the reader's own editor",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:14925",
   "skeptic": "behaviour-5",
   "note": "Read `doOpenSource` (viewer.js:14921-14930). When the chosen target is not GitHub, line 14924 builds `editorUri(n.file, n.line)` and line 14925 fires it (`fireUri`, 14905-14909, clicks a hidden anchor carrying the scheme URI). That is the hand-off to the reader's own editor."
  },
  {
   "claim": "UC6 step 16: I9 → R2 — put the reader in their editor, at the mapped line",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:14879",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Read `editorUri` (viewer.js:14868-14881): line 14879 fills the chosen editor's URI template with `{abspath}`, `{path}`, `{line}` (the mapped line, or 1) and `{col}`, and 14880 only returns it if its scheme is allowed. A directory ref has the line/column suffix stripped (14878). So the editor is opened at the mapped line."
  },
  {
   "claim": "UC6 step 17: C69 → I10 [out] — open the file on the code host at the pinned commit",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:14928",
   "skeptic": "behaviour-5",
   "note": "Read viewer.js:14926-14928: after the editor branch, `const gh = ghUrl(n.file, n.line);` and line 14928 is `if (gh) { window.open(gh, '_blank', 'noopener'); return; }`. `ghUrl` (14897-14904) builds `<repo>/blob/<ghRef()>/<path>#L<line>`, and `ghRef` (14891-14895) reduces the map's own `GH_COMMIT` to a resolvable ref — so the code host is opened at the pinned commit."
  },
  {
   "claim": "UC6 step 18: I10 → R2 — show the file on the web, pinned to the map's commit",
   "grounded": true,
   "evidence": "tools/coyomap/viewer/viewer.js:14903",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Read `ghUrl` (viewer.js:14896-14904): line 14899 refuses to build anything without the map's commit, and line 14903 assembles `repo + '/blob/' + ghRef() + '/' + rel + '#L' + line`. The reader lands on the file on the web at the map's commit, at the mapped line."
  },
  {
   "claim": "SF1 step 1: C134 → C130 — read the dispatcher end to end",
   "grounded": true,
   "evidence": "skill/coyomap/SKILL.md:27",
   "skeptic": "behaviour-5",
   "note": "Read skill/coyomap/SKILL.md:16-29. Line 27 reads 'Read `__COYOMAP_HOME__/method/dispatch.md` and follow it end to end', and 27-29 add that it is the entry point naming every other doc and tool. The skill pointer really does send the agent to the dispatcher, whole."
  },
  {
   "claim": "SF1 step 2: C130 → C20 — run every coyomap command from the clone's own command line",
   "grounded": true,
   "evidence": "method/dispatch.md:4",
   "skeptic": "behaviour-5",
   "note": "Read method/dispatch.md:1-10. Lines 4-5 say 'The tooling is the `coyomap` CLI, installed into this clone's venv (`.venv/bin/coyomap`; source under `tools/coyomap/`)', and 8-10 repeat that every `.venv/bin/coyomap` path is under the coyomap clone and must be run with that absolute prefix, not from the repo being mapped."
  },
  {
   "claim": "SF1 step 3: C20 → C26 — print the build briefing before anything else runs",
   "grounded": true,
   "evidence": "tools/coyomap/cli.py:180",
   "skeptic": "behaviour-5",
   "note": "Read cli.py:150-200 (`_dispatch`). Lines 178-180 route the `scope` command to `scope.main(rest)`, and `scope.main` (scope.py:166-173) prints the briefing. The 'before anything else runs' half is not in this line: it is stated in method/dispatch.md:12-20 ('Step 0 — brief the user BEFORE doing anything else ... Run this immediately')."
  },
  {
   "claim": "SF1 step 4: C26 → I8 [in] — read the project's own files to see what the build will cover",
   "grounded": true,
   "evidence": "tools/coyomap/scope.py:74",
   "skeptic": "behaviour-5",
   "note": "Read `scope_report` (scope.py:72-92). Line 74 is `walk = iter_source_files(root)`, and preindex_lib.py:212-250 shows that walk listing the project's own files (git-tracked plus untracked not ignored, else a plain disk walk). The counts printed at 89-91 come straight from it, so the briefing's coverage really is read off the project's files."
  },
  {
   "claim": "SF1 step 5: C26 → C46 — drop every path the project's own ignore list names",
   "grounded": true,
   "evidence": "tools/coyomap/scope.py:131",
   "skeptic": "behaviour-5",
   "note": "Read scope.py:127-136. Line 131 is `rep = ignore_report(walk.ignore, walk.ignore_hits)` — a call into the ignore-list module (ignorefile.py, which C46 is anchored to at line 178) — and 132-136 print how many files each pattern removed plus a warning for patterns that removed nothing, which matches the step's own note. Precision point for the lead: this line REPORTS the removal. The removal itself happens in the shared walk that line 74 calls, at tools/coyomap/preindex_lib.py:242-246 (`ignore.match_index(...)`, then 'the file leaves the tree'), using the same ignore spec loaded at preindex_lib.py:227."
  },
  {
   "claim": "SF1 step 6: C26 → I1 [out] — print what will be read and what the map will be pinned to",
   "grounded": true,
   "evidence": "tools/coyomap/scope.py:172",
   "skeptic": "behaviour-5",
   "note": "Read `main` (scope.py:166-173). Line 172 is `print(\"\\n\".join(scope_report(Path(args.repo).resolve())))`, and `scope_report` builds exactly two sections: 'What coyomap will analyze' (scope.py:75) and 'What the map will be pinned to' (scope.py:139). Printed to stdout, which is the command line."
  },
  {
   "claim": "SF1 step 7: I1 → R1 — show the person the briefing before any work starts",
   "grounded": true,
   "evidence": "method/dispatch.md:15",
   "skeptic": "behaviour-5",
   "note": "No anchor in the map. Read method/dispatch.md:12-20: 'Step 0 — brief the user BEFORE doing anything else ... The first thing the user sees must be what coyomap is about to read ... Run this immediately — before choosing a mode, before reading the repo', followed by the `coyomap scope --repo <repo>` command. Paired with scope.py:172 printing to stdout, the person does get the briefing before any work starts."
  }
 ]
}
