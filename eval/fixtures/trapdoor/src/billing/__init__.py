"""Billing: what a tenant owes for the tickets it raised.

A NESTED area on purpose. `pricing/` and `ledger/` are two sub-areas under one parent, so a
map of this tree has a subsystem that contains subsystems, and the viewer has something to
drill into. Everything above this file in the fixture is one level deep.
"""
