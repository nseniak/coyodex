# Dependencies for the coyodex structural pre-index (tools/preindex.py) ONLY.
#
# Deliberate, scoped exception to the "Python side is stdlib-only" rule
# (see internal/docs/design-notes.md): the pre-index needs polyglot symbol/import
# extraction for non-Python languages. Python itself uses stdlib `ast`.
#
# The core gate — validate_analysis.py and the viewer (build_graph/render/gen_viewer) —
# imports NONE of this and stays dependency-free. Installed by `make deps` / `make install`.

tree-sitter>=0.21
tree-sitter-language-pack>=0.2
